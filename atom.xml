<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Daniel Shih</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://isdaniel.github.io/</id>
  <link href="https://isdaniel.github.io/" rel="alternate"/>
  <link href="https://isdaniel.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Daniel Shih</rights>
  <subtitle>好點子沒價值，有價值的是把好點子實現</subtitle>
  <title>石頭的coding之路</title>
  <updated>2026-08-05T10:10:26.494Z</updated>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="SQLite" scheme="https://isdaniel.github.io/categories/SQLite/"/>
    <category term="SystemDesign" scheme="https://isdaniel.github.io/categories/SQLite/SystemDesign/"/>
    <category term="Database" scheme="https://isdaniel.github.io/tags/Database/"/>
    <category term="SystemDesign" scheme="https://isdaniel.github.io/tags/SystemDesign/"/>
    <category term="WAL" scheme="https://isdaniel.github.io/tags/WAL/"/>
    <category term="Storage" scheme="https://isdaniel.github.io/tags/Storage/"/>
    <category term="SQLite" scheme="https://isdaniel.github.io/tags/SQLite/"/>
    <content>
      <![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>這篇是寫一個 SQLite WAL parser 時的完整驗證記錄。目標很單純：把 <code>-wal</code> 檔案正確解析成 frame 與 commit 邊界。</p><p>底下所有數字都是實際跑出來的，環境是 SQLite 3.37.2 &#x2F; Linux x86-64，對照的原始碼是 <code>libsqlite3-sys</code> bundled 的 amalgamation。凡是跟文件衝突的地方，一律以 C 原始碼為準。</p><p>如果對 PostgreSQL 那一套 WAL 的先寫 log、後寫 dirty block 的模型不熟，可以先讀我之前寫的 <a href="https://isdaniel.github.io/postgresql-wal-introduce/">PostgreSQL WAL (Write-Ahead Logging) 機制</a>，後面第八節會把兩者的設計取捨拿來對照。</p><hr><h2 id="一、WAL-的實體格式"><a href="#一、WAL-的實體格式" class="headerlink" title="一、WAL 的實體格式"></a>一、WAL 的實體格式</h2><p>先把地基打好。一個 WAL 檔就是 32 bytes 的檔頭，後面接一串固定大小的 frame。</p><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">offset 0        32                          568         1104</span><br><span class="line">  +<span class="params">-------------</span>+<span class="params">---------------------------</span>+<span class="params">-----------</span>+<span class="params">---</span></span><br><span class="line">  | WAL header  | frame 1                   | frame 2   | <span class="string">...</span></span><br><span class="line">  |  32 bytes   | 24 header + 512 payload   |           |</span><br><span class="line">  +<span class="params">-------------</span>+<span class="params">---------------------------</span>+<span class="params">-----------</span>+<span class="params">---</span></span><br></pre></td></tr></table></figure><h3 id="檔頭-32-bytes，全部-big-endian"><a href="#檔頭-32-bytes，全部-big-endian" class="headerlink" title="檔頭 32 bytes，全部 big-endian"></a>檔頭 32 bytes，全部 big-endian</h3><p>拿一個真實的檔案來看：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">00</span>: <span class="number">37</span> <span class="number">7</span>f <span class="number">06</span> <span class="number">82</span>  <span class="number">00</span> <span class="number">2</span>d e2 <span class="number">18</span></span><br><span class="line"><span class="attribute">08</span>: <span class="number">00</span> <span class="number">00</span> <span class="number">02</span> <span class="number">00</span>  <span class="number">00</span> <span class="number">00</span> <span class="number">00</span> <span class="number">00</span></span><br><span class="line"><span class="attribute">16</span>: bf b5 a7 <span class="number">01</span>  <span class="number">58</span> <span class="number">54</span> <span class="number">6</span>f <span class="number">3</span>d</span><br><span class="line"><span class="attribute">24</span>: d6 <span class="number">72</span> b8 d2  cb bb e4 cf</span><br></pre></td></tr></table></figure><table><thead><tr><th align="right">offset</th><th>值</th><th>意義</th></tr></thead><tbody><tr><td align="right">0</td><td><code>0x377f0682</code></td><td>magic。最低位元決定 checksum 內容用哪種位元組序</td></tr><tr><td align="right">4</td><td><code>3007000</code></td><td>format version，自 2010 年凍結至今</td></tr><tr><td align="right">8</td><td><code>512</code></td><td>page size</td></tr><tr><td align="right">12</td><td><code>0</code></td><td>checkpoint sequence</td></tr><tr><td align="right">16</td><td><code>0xbfb5a701</code></td><td>salt-1</td></tr><tr><td align="right">20</td><td><code>0x58546f3d</code></td><td>salt-2</td></tr><tr><td align="right">24</td><td><code>0xd672b8d2</code></td><td>checksum-1，涵蓋前 24 bytes</td></tr><tr><td align="right">28</td><td><code>0xcbbbe4cf</code></td><td>checksum-2</td></tr></tbody></table><h3 id="Frame-header-24-bytes"><a href="#Frame-header-24-bytes" class="headerlink" title="Frame header 24 bytes"></a>Frame header 24 bytes</h3><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">frame<span class="number"> 1 </span>@ offset 32:</span><br><span class="line">  +00:<span class="number"> 00 </span>00<span class="number"> 00 </span>01 <span class="number"> 00 </span>00<span class="number"> 00 </span>00    pgno=1, commit=0</span><br><span class="line">  +08: bf b5 a7<span class="number"> 01 </span><span class="number"> 58 </span>54 6f 3d    salt，必須與檔頭逐位元組相同</span><br><span class="line">  +16:<span class="number"> 33 </span>ea<span class="number"> 45 </span>86  d2<span class="number"> 72 </span>63<span class="number"> 05 </span>   checksum</span><br></pre></td></tr></table></figure><p><code>db size after commit</code> 那個欄位是交易邊界的唯一訊號：<code>0</code> 表示交易還沒結束，非 <code>0</code> 表示這個 frame 提交了一筆交易，提交後資料庫共這麼多頁。</p><h3 id="Frame-自己不帶長度"><a href="#Frame-自己不帶長度" class="headerlink" title="Frame 自己不帶長度"></a>Frame 自己不帶長度</h3><p>這點值得單獨講，因為它決定了整個解析流程的順序。</p><p>frame header 是固定 24 bytes，六個欄位全部有名有姓，沒有一個是長度。整個檔案的切法是<strong>推導</strong>出來的：</p><ol><li>讀檔頭。它固定 32 bytes，可以無條件讀，這是唯一的立足點</li><li>從 offset 8 取 <code>page_size</code></li><li><code>stride = 24 + page_size</code></li><li>frame <code>i</code>（1-based）的 offset &#x3D; <code>32 + (i-1) * stride</code></li></ol><p>換句話說，<strong>整個 framing 由檔頭裡的一個 u32 決定</strong>。用兩個不同 page size 的真實檔案對照：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">page_size</span> = <span class="number">512</span>：  offset <span class="number">8</span> 讀到 <span class="number">512</span>     stride <span class="number">536</span>    檔案 <span class="number">1640</span> = <span class="number">32</span> + <span class="number">3</span>×<span class="number">536</span></span><br><span class="line"><span class="attribute">page_size</span> = <span class="number">4096</span>： offset <span class="number">8</span> 讀到 <span class="number">4096</span>    stride <span class="number">4120</span>   檔案 <span class="number">12392</span> = <span class="number">32</span> + <span class="number">3</span>×<span class="number">4120</span></span><br></pre></td></tr></table></figure><p>同樣的結構、同樣 3 個 frame，只因為那 4 個 bytes 不同，整個檔案的切法就變了。</p><p>這也是為什麼 page size 必須在任何東西用到它之前先驗證。一個被竄改的 page size 會讓其後每一個 offset 都算錯，而且錯得很安靜——你會在錯誤的位置讀到看似合理的位元組。所以驗證順序是規範性的：長度 → magic → version → page size → checksum。</p><p>順帶一提，<code>(len - 32) % stride == 0</code> 是個免費的自我檢查。有餘數就代表尾端有殘缺的 slot。</p><h3 id="Checksum-演算法"><a href="#Checksum-演算法" class="headerlink" title="Checksum 演算法"></a>Checksum 演算法</h3><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">s0</span>, <span class="built_in">s1</span> = seed</span><br><span class="line">每 <span class="number">8</span> <span class="keyword">bytes </span>取兩個 u32 (x, y)：</span><br><span class="line">    <span class="built_in">s0</span> = <span class="built_in">s0</span> + x + <span class="built_in">s1</span></span><br><span class="line">    <span class="built_in">s1</span> = <span class="built_in">s1</span> + y + <span class="built_in">s0</span>        （皆為 wrapping）</span><br></pre></td></tr></table></figure><p>seed 與涵蓋範圍：</p><table><thead><tr><th>對象</th><th>seed</th><th>涵蓋</th></tr></thead><tbody><tr><td>WAL header</td><td><code>(0, 0)</code></td><td>檔頭前 <strong>24</strong> bytes</td></tr><tr><td>Frame 1</td><td>檔頭的 checksum</td><td>frame header 前 <strong>8</strong> bytes + 整個 payload</td></tr><tr><td>Frame k</td><td>frame k−1 的 checksum</td><td>同上</td></tr></tbody></table><p><code>magic &amp; 1</code> 決定被 checksum 的<strong>內容</strong>如何解讀成 32-bit word。但存進檔案的 checksum <strong>值</strong>永遠是 big-endian，跟其他欄位一樣。這兩件事很容易混為一談，而且混淆之後寫出來的程式會在小端序機器上「剛好能動」。</p><p>實際驗證一條 chain：</p><figure class="highlight maxima"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">frame   seed（來源）                   算出              檔案裡存的</span><br><span class="line">  <span class="number">1</span>     (c0780944, 3f17b724) header    (de3ecc9d, ...)   同左  OK</span><br><span class="line">  <span class="number">2</span>     (de3ecc9d, 5d2c46e0) frame <span class="number">1</span>   (<span class="number">68b39233</span>, ...)   同左  OK</span><br><span class="line">  <span class="number">3</span>     (<span class="number">68b39233</span>, 281b0f4c) frame <span class="number">2</span>   (161fdd33, ...)   同左  OK</span><br><span class="line">  <span class="number">4</span>     (161fdd33, a303ab26) frame <span class="number">3</span>   (f7d14d86, ...)   同左  OK</span><br></pre></td></tr></table></figure><h2 id="二、最重要的一件事：檔案大小不等於有效-frame-數"><a href="#二、最重要的一件事：檔案大小不等於有效-frame-數" class="headerlink" title="二、最重要的一件事：檔案大小不等於有效 frame 數"></a>二、最重要的一件事：檔案大小不等於有效 frame 數</h2><p>這是整個專案的核心風險，也是我花最多時間驗證的一點。</p><p>SQLite 的 WAL 是<strong>原地重用</strong>的。checkpoint 把內容 backfill 進主資料庫之後，下一次寫入會 reset WAL 並從 frame 1 重新開始寫——但上一代的 frame 位元組還躺在檔案裡，唯一的區別只有 salt 對不上。</p><p>實測：寫 5 個 frame，跑 <code>PRAGMA wal_checkpoint(PASSIVE)</code>，再寫 1 筆。</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">檔案仍是 20632 bytes（看起來有 5 個 frame slot），但：</span><br><span class="line"></span><br><span class="line">  slot 1: <span class="attribute">salt</span>=0x2fe926a3/0xf3e4c752 -&gt; VALID</span><br><span class="line">  slot 2: <span class="attribute">salt</span>=0x2fe926a2/0x834e83a0 -&gt; STALE-SALT   上一代殘留</span><br><span class="line">  &gt;&gt;&gt; 有效 frame 數 = 1，檔案卻暗示 5</span><br></pre></td></tr></table></figure><p>用 <code>(len - 32) / stride</code> 算 frame 數的實作，會把上一代的資料當成新交易複製出去。</p><h3 id="完整走一遍-INSERT-→-UPDATE-→-CHECKPOINT-→-INSERT"><a href="#完整走一遍-INSERT-→-UPDATE-→-CHECKPOINT-→-INSERT" class="headerlink" title="完整走一遍 INSERT → UPDATE → CHECKPOINT → INSERT"></a>完整走一遍 INSERT → UPDATE → CHECKPOINT → INSERT</h3><p>把每一步的檔案狀態攤開來看，會比講抽象規則清楚得多。用 <code>page_size=512</code>，關掉 autocheckpoint。</p><p><strong>步驟 0，<code>CREATE TABLE</code>：</strong></p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">-wal <span class="number"> 1104 </span>bytes =<span class="number"> 32 </span>+ 2×536  <span class="number"> 2 </span>個 frame</span><br><span class="line">.db   <span class="number"> 512 </span>bytes                主檔還是空的</span><br><span class="line"></span><br><span class="line"> slot  pgno  commit  內容</span><br><span class="line">  <span class="number"> 1 </span>  <span class="number"> 1 </span>     -     SQLite format, CREATE TABLE users...</span><br><span class="line">  <span class="number"> 2 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    (空的 table 根頁)</span><br></pre></td></tr></table></figure><p>主檔一個 byte 都沒動。資料現在只存在於 WAL 裡，但 <code>SELECT</code> 讀得到，因為 SQLite 讀取時會先查 WAL。</p><p><strong>步驟 1，<code>INSERT alice</code>：</strong></p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">-wal <span class="number"> 1640 </span>bytes (+536)   <span class="number"> 3 </span>個 frame</span><br><span class="line">.db   <span class="number"> 512 </span>bytes           還是沒動</span><br><span class="line"></span><br><span class="line">  <span class="number"> 3 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    [&#x27;alice&#x27;]     page<span class="number"> 2 </span>的新版本</span><br></pre></td></tr></table></figure><p>page 2 現在在 WAL 裡有兩份。SQLite 沒有回頭去改 slot 2，而是在後面加了 slot 3。</p><p><strong>步驟 2，<code>UPDATE alice → ALICE_UPDATED</code>：</strong></p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">-wal <span class="number"> 2176 </span>bytes (+536)   <span class="number"> 4 </span>個 frame</span><br><span class="line">.db   <span class="number"> 512 </span>bytes</span><br><span class="line"></span><br><span class="line">  <span class="number"> 2 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    (空)                 第<span class="number"> 1 </span>版</span><br><span class="line">  <span class="number"> 3 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    [&#x27;alice&#x27;]            第<span class="number"> 2 </span>版</span><br><span class="line">  <span class="number"> 4 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    [&#x27;ALICE_UPDATED&#x27;]    第<span class="number"> 3 </span>版</span><br></pre></td></tr></table></figure><p>這是最關鍵的一步。UPDATE 沒有修改 slot 3 裡的 alice，它把<strong>整個 page 2 的新樣子</strong>寫成一個新 frame。page 2 現在有三個版本同時躺在檔案裡。</p><p>這也解釋了為什麼 WAL 會長得很快：你在一個大頁上改一個 byte，WAL 就多 512 bytes（或 4096）。WAL 記的是頁，不是列。</p><p><strong>步驟 3，<code>CHECKPOINT(PASSIVE)</code>，回傳 <code>(0, 4, 4)</code>：</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">-wal</span>  <span class="number">2176 </span><span class="string">bytes</span>  <span class="string">完全沒變。長度、salt、ckptseq、4</span> <span class="string">個</span> <span class="string">frame</span> <span class="string">全都還在且全都有效</span></span><br><span class="line"><span class="string">.db</span>   <span class="number">1024 </span><span class="string">bytes</span>  <span class="string">從</span> <span class="number">512</span> <span class="string">變成</span> <span class="number">1024</span></span><br></pre></td></tr></table></figure><p>checkpoint 做的唯一一件事是把 WAL 裡每個頁的最新版本抄進 <code>.db</code>。<strong>WAL 一個 byte 都沒有被動。</strong> 沒有清空、沒有截短、salt 沒變。</p><p>這是最多人搞錯的地方。checkpoint 不是「清 WAL」，是「同步到主檔」。</p><p><strong>步驟 4，<code>INSERT bob</code>，這一步才 reset：</strong></p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">-wal <span class="number"> 2176 </span>bytes  長度還是一樣，沒有截短</span><br><span class="line">salt  (0x8f3d1cd5, ...) → (0x8f3d1cd6, ...)</span><br><span class="line">ckptseq <span class="number"> 0 </span>→ 1</span><br><span class="line"></span><br><span class="line"> slot  pgno  commit  狀態          內容</span><br><span class="line">  <span class="number"> 1 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    有效          [&#x27;bob&#x27;, &#x27;ALICE_UPDATED&#x27;]   被覆寫</span><br><span class="line">  <span class="number"> 2 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    上一代殘留     (空)</span><br><span class="line">  <span class="number"> 3 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    （已停）       [&#x27;alice&#x27;]</span><br><span class="line">  <span class="number"> 4 </span>  <span class="number"> 2 </span>    <span class="number"> 2 </span>    （已停）       [&#x27;ALICE_UPDATED&#x27;]</span><br></pre></td></tr></table></figure><h3 id="這個坑咬人的方式"><a href="#這個坑咬人的方式" class="headerlink" title="這個坑咬人的方式"></a>這個坑咬人的方式</h3><p>假設你在步驟 4 的時間點寫一個備份工具去讀這個檔案。你算 <code>(2176 - 32) / 536 = 4</code>，拿到 4 個 frame，依序套用到 page 2：</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">套用 slot<span class="number"> 1 </span>→ page<span class="number"> 2 </span>= &#123;bob, ALICE_UPDATED&#125;</span><br><span class="line">套用 slot<span class="number"> 2 </span>→ page<span class="number"> 2 </span>= &#123;&#125;</span><br><span class="line">套用 slot<span class="number"> 3 </span>→ page<span class="number"> 2 </span>= &#123;alice&#125;</span><br><span class="line">套用 slot<span class="number"> 4 </span>→ page<span class="number"> 2 </span>= &#123;ALICE_UPDATED&#125;     最終結果</span><br></pre></td></tr></table></figure><p>備份出來的資料庫：有 ALICE_UPDATED，沒有 bob。 bob 是剛剛才寫進去的，不見了。而 slot 3 的 <code>alice</code> 是兩代以前、已經被 UPDATE 掉、又已經被 checkpoint 進主檔的資料，它從墳墓裡爬出來影響了結果。 沒有錯誤訊息、沒有 panic，<code>PRAGMA integrity_check</code> 也會回 <code>ok</code>，因為產出的確實是一個結構合法的資料庫。</p><hr><h2 id="三、salt-到底什麼時候變"><a href="#三、salt-到底什麼時候變" class="headerlink" title="三、salt 到底什麼時候變"></a>三、salt 到底什麼時候變</h2><p>這是文件錯得最嚴重的地方。</p><p><code>fileformat2.html</code> §4.1 的表格寫：</p><blockquote><p>Salt-1: random integer <strong>incremented with each checkpoint</strong></p></blockquote><p>但同一頁後面又寫：</p><blockquote><p>Upon WAL <strong>reset</strong>, the WAL header salt-1 value is incremented and the salt-2 value is randomized.</p></blockquote><p><strong>後者才對。</strong> 實測各個 checkpoint 模式：</p><table><thead><tr><th>動作</th><th align="right">檔案大小</th><th align="right">ckptseq</th><th>salt-1</th></tr></thead><tbody><tr><td>寫 5 frames</td><td align="right">20632</td><td align="right">0</td><td><code>0x752f09f4</code></td></tr><tr><td><code>PASSIVE</code></td><td align="right">20632</td><td align="right">0</td><td><code>0x752f09f4</code>（沒變）</td></tr><tr><td>再寫 1 筆</td><td align="right">20632</td><td align="right"><strong>1</strong></td><td><code>0x752f09f5</code>（這時才變）</td></tr><tr><td><code>RESTART</code></td><td align="right">20632</td><td align="right">1</td><td><code>0x752f09f5</code></td></tr><tr><td>再寫 1 筆</td><td align="right">20632</td><td align="right"><strong>2</strong></td><td><code>0x752f09f6</code></td></tr><tr><td><code>TRUNCATE</code></td><td align="right"><strong>0</strong></td><td align="right">n&#x2F;a</td><td>n&#x2F;a</td></tr><tr><td>再寫 1 筆</td><td align="right">4152</td><td align="right"><strong>3</strong></td><td><code>0x752f09f7</code></td></tr></tbody></table><p><code>PASSIVE</code>、<code>FULL</code>、<code>RESTART</code> 都不改 salt。只有 <code>TRUNCATE</code> 是例外——它在 checkpoint 內部就呼叫 <code>walRestartHdr()</code>，再把檔案截為 0。</p><h3 id="真正改-salt-的路徑"><a href="#真正改-salt-的路徑" class="headerlink" title="真正改 salt 的路徑"></a>真正改 salt 的路徑</h3><figure class="highlight scss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">walRestartHdr</span>()  ←──┬── <span class="built_in">walRestartLog</span>()  ←── <span class="built_in">sqlite3WalFrames</span>()   寫入路徑</span><br><span class="line">                    │</span><br><span class="line">                    └── <span class="built_in">walCheckpoint</span>()  ← 只有 TRUNCATE 模式</span><br></pre></td></tr></table></figure><p><code>walRestartLog()</code> 在 <code>sqlite3WalFrames()</code> 開頭被呼叫，也就是每次要寫 frame 之前。它的完整條件：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">walRestartLog</span><span class="params">(Wal *pWal)</span>&#123;</span><br><span class="line">  <span class="keyword">if</span>( pWal-&gt;readLock==<span class="number">0</span> )&#123;                                   <span class="comment">// 條件 1</span></span><br><span class="line">    <span class="keyword">volatile</span> WalCkptInfo *pInfo = walCkptInfo(pWal);</span><br><span class="line">    assert( pInfo-&gt;nBackfill==pWal-&gt;hdr.mxFrame );           <span class="comment">// 這只是 assert</span></span><br><span class="line">    <span class="keyword">if</span>( pInfo-&gt;nBackfill&gt;<span class="number">0</span> )&#123;                                <span class="comment">// 條件 2</span></span><br><span class="line">      u32 salt1;</span><br><span class="line">      sqlite3_randomness(<span class="number">4</span>, &amp;salt1);</span><br><span class="line">      rc = walLockExclusive(pWal, WAL_READ_LOCK(<span class="number">1</span>), WAL_NREADER<span class="number">-1</span>);  <span class="comment">// 條件 3</span></span><br><span class="line">      <span class="keyword">if</span>( rc==SQLITE_OK )&#123;</span><br><span class="line">        walRestartHdr(pWal, salt1);     <span class="comment">// 這裡才改 salt</span></span><br><span class="line">        walUnlockExclusive(...);</span><br><span class="line">      &#125;<span class="keyword">else</span> <span class="keyword">if</span>( rc!=SQLITE_BUSY )&#123;</span><br><span class="line">        <span class="keyword">return</span> rc;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">// BUSY → 什麼都不做，靜默跳過</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>注意 <code>nBackfill == mxFrame</code> 是個 <code>assert</code>，不是 <code>if</code>。真正被測試的是 <code>readLock == 0</code>。</p><p><strong>條件 1</strong> 的語意要往回追一層。reader 什麼時候會拿到 read slot 0？<code>walTryBeginRead</code> 第 69312 行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>( !useWal &amp;&amp; AtomicLoad(&amp;pInfo-&gt;nBackfill)==pWal-&gt;hdr.mxFrame )&#123;</span><br><span class="line">    <span class="comment">/* The WAL has been completely backfilled (or it is empty).</span></span><br><span class="line"><span class="comment">    ** and can be safely ignored. */</span></span><br><span class="line">    rc = walLockShared(pWal, WAL_READ_LOCK(<span class="number">0</span>));</span><br><span class="line">    ...</span><br><span class="line">    pWal-&gt;readLock = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>slot 0 的語意（原始碼註解 66562）：</p><blockquote><p>Readers holding WAL_READ_LOCK(0) always <strong>ignore the entire WAL and read all content directly from the database</strong>.</p></blockquote><p>所以 <code>readLock == 0</code> 的意思是「我開始交易時 WAL 已經全部進主檔了，我根本不看 WAL」。既然如此，寫入時就可以安全地從頭覆寫。那個 assert 是<strong>推論</strong>出來的，不是直接測試的。</p><p><strong>條件 3</strong> 拿不到就回 <code>SQLITE_BUSY</code>，而程式碼靜默跳過 restart——寫入照常進行，只是變成 append 而不是覆寫。這是一道 race 防護。</p><h3 id="salt-不是單調-counter"><a href="#salt-不是單調-counter" class="headerlink" title="salt 不是單調 counter"></a>salt 不是單調 counter</h3><p>在同一個 WAL 檔延續期間，salt-1 確實是遞增 1。但<strong>跨 WAL 檔就不是</strong>。</p><p><code>wal.c:4096</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>( pWal-&gt;nCkpt==<span class="number">0</span> ) sqlite3_randomness(<span class="number">8</span>, pWal-&gt;hdr.aSalt);</span><br></pre></td></tr></table></figure><p><code>nCkpt == 0</code> 時<strong>兩個 salt 都重新隨機</strong>，ckptseq 寫 0。實測連續三代 open → write → close（每次乾淨關閉，SQLite 會刪掉 <code>-wal</code>）：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">gen0: <span class="attribute">salt1</span>=0xab6fca0c <span class="attribute">salt2</span>=0x2d2d4733 <span class="attribute">ckptseq</span>=0</span><br><span class="line">gen1: <span class="attribute">salt1</span>=0x85f533be <span class="attribute">salt2</span>=0xa8174adb <span class="attribute">ckptseq</span>=0  <span class="attribute">delta</span>=0xda856992</span><br><span class="line">gen2: <span class="attribute">salt1</span>=0x0220132f <span class="attribute">salt2</span>=0xe197c9da <span class="attribute">ckptseq</span>=0  <span class="attribute">delta</span>=0x7c2adf71</span><br></pre></td></tr></table></figure><p>delta 是亂數，不是 +1。ckptseq 全是 0。</p><p>所以偵測「複製位置失效」的唯一安全方法是<strong>相等性測試</strong>，而且要比對<strong>兩個</strong> salt：</p><figure class="highlight erlang-repl"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(salt1, salt2) != 上次記錄的 (salt1, salt2)   ⇒ 換代</span><br></pre></td></tr></table></figure><p>任何算術或單調性假設都是錯的。</p><h3 id="salt-為什麼存在"><a href="#salt-為什麼存在" class="headerlink" title="salt 為什麼存在"></a>salt 為什麼存在</h3><p><code>wal.c:95-98</code> 講得很清楚，而且理由跟我原本想的不同：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">** After each checkpoint, the salt<span class="number">-1</span> value is incremented and the salt<span class="number">-2</span></span><br><span class="line">** value is randomized.  This prevents old and new frames in the WAL from</span><br><span class="line">** being considered valid at the same time and being checkpointing together</span><br><span class="line">** following a crash.</span><br></pre></td></tr></table></figure><p>「following a crash」才是重點。</p><p>想像沒有 salt：WAL 有 100 個 frame，checkpoint 完，新的寫入從 frame 1 開始覆蓋，寫到 frame 30 時斷電。重開機後 frame 1<del>30 是新的、frame 31</del>100 是舊的，而且它們的 checksum chain 各自都是自洽的。</p><p>recovery 要怎麼知道該停在 30？checksum chain 在 30→31 的交界會斷，但那跟「frame 31 是個 torn write」長得一模一樣。更糟的是如果新舊資料剛好接得上，它會把兩個世代的 frame 混在一起 checkpoint 進主檔。</p><p>salt 讓這件事變成不可能。</p><hr><h2 id="四、自動-checkpoint-的真實時機"><a href="#四、自動-checkpoint-的真實時機" class="headerlink" title="四、自動 checkpoint 的真實時機"></a>四、自動 checkpoint 的真實時機</h2><p>文件說：「automatically checkpoint whenever a COMMIT occurs that causes the WAL file to be 1000 pages or more in size」。這句話對，但程式碼講得更多。</p><h3 id="完整呼叫鏈"><a href="#完整呼叫鏈" class="headerlink" title="完整呼叫鏈"></a>完整呼叫鏈</h3><p><strong>① <code>walFrames()</code>，只有 commit 才記錄：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>( isCommit )&#123;</span><br><span class="line">    walIndexWriteHdr(pWal);</span><br><span class="line">    pWal-&gt;iCallback = iFrame;     <span class="comment">// commit 後 WAL 裡的 frame 總數</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>交易中間寫的 frame 不會設 <code>iCallback</code>。</p><p><strong>② <code>doWalCallbacks()</code>，在交易提交完成之後：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">** This function is called after a transaction has been committed. It</span></span><br><span class="line"><span class="comment">** invokes callbacks registered with sqlite3_wal_hook() as required.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">doWalCallbacks</span><span class="params">(sqlite3 *db)</span>&#123;</span><br><span class="line">  <span class="keyword">for</span>(i=<span class="number">0</span>; i&lt;db-&gt;nDb; i++)&#123;</span><br><span class="line">      nEntry = sqlite3PagerWalCallback(sqlite3BtreePager(pBt));</span><br><span class="line">      <span class="keyword">if</span>( nEntry&gt;<span class="number">0</span> &amp;&amp; db-&gt;xWalCallback &amp;&amp; rc==SQLITE_OK )&#123;</span><br><span class="line">          rc = db-&gt;xWalCallback(db-&gt;pWalArg, db, db-&gt;aDb[i].zDbSName, nEntry);</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>③ <code>sqlite3WalCallback()</code>，讀取後歸零：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ret = pWal-&gt;iCallback;</span><br><span class="line">pWal-&gt;iCallback = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>每次 commit 只觸發一次。</p><p><strong>④ <code>sqlite3WalDefaultHook()</code>，真正的判斷：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>( nFrame &gt;= SQLITE_PTR_TO_INT(pClientData) )&#123;</span><br><span class="line">    sqlite3BeginBenignMalloc();</span><br><span class="line">    sqlite3_wal_checkpoint(db, zDb);</span><br><span class="line">    sqlite3EndBenignMalloc();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="實測"><a href="#實測" class="headerlink" title="實測"></a>實測</h3><p>用預設值（<code>page_size=512</code>，不動任何 pragma），逐筆 INSERT：</p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"> 第幾筆   -wal bytes   slot數  有效frame  ckptseq   salt-1       .db bytes</span></span><br><span class="line"><span class="section">--------------------------------------------------------------------------</span></span><br><span class="line"><span class="code">     1        1640        3        3        0    0x4f233a94       512</span></span><br><span class="line"><span class="code">     2        2176        4        4        0    0x4f233a94       512</span></span><br><span class="line"><span class="code">   773      536032     1000     1000        0    0x4f233a94     58880</span></span><br><span class="line"><span class="code">   774      536032     1000        1        1    0x4f233a95     58880</span></span><br><span class="line"><span class="code">   775      536032     1000        2        1    0x4f233a95     58880</span></span><br></pre></td></tr></table></figure><p><code>536032 = 32 + 1000 × 536</code>，剛好 1000 個 frame，門檻分毫不差。</p><p>第 773 筆 checkpoint 觸發，<code>.db</code> 從 512 跳到 58880，但 <code>-wal</code> 完全沒變。第 774 筆才 reset。之後檔案永遠停在 536032 bytes。</p><p>換算成預設的 <code>page_size=4096</code>，門檻是 <code>32 + 1000 × 4120 ≈ 4.1 MB</code>。</p><h3 id="程式碼講出來、文件沒說的"><a href="#程式碼講出來、文件沒說的" class="headerlink" title="程式碼講出來、文件沒說的"></a>程式碼講出來、文件沒說的</h3><p><strong>autocheckpoint 根本不是內建機制，它就是一個 WAL hook。</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">SQLITE_API <span class="type">int</span> <span class="title function_">sqlite3_wal_autocheckpoint</span><span class="params">(sqlite3 *db, <span class="type">int</span> nFrame)</span>&#123;</span><br><span class="line">  <span class="keyword">if</span>( nFrame&gt;<span class="number">0</span> )&#123;</span><br><span class="line">    sqlite3_wal_hook(db, sqlite3WalDefaultHook, SQLITE_INT_TO_PTR(nFrame));</span><br><span class="line">  &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    sqlite3_wal_hook(db, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>註解明說：「registering a callback using <code>sqlite3_wal_hook()</code> <strong>disables the automatic checkpoint mechanism</strong> configured by this function」。</p><p>這對做備份工具的人是陷阱。你只要自己註冊一個 <code>sqlite3_wal_hook</code>（例如想在 commit 時被通知），就等於把 autocheckpoint 整個關掉了。兩者共用同一個 slot。</p><p><strong>checkpoint 的回傳值被完全丟棄。</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sqlite3_wal_checkpoint(db, zDb);      <span class="comment">// 回傳值沒有被接</span></span><br></pre></td></tr></table></figure><p>外面再包一層 <code>BeginBenignMalloc</code> &#x2F; <code>EndBenignMalloc</code>，連 checkpoint 過程中的記憶體配置失敗都標記為良性。autocheckpoint 是 best-effort，失敗是設計的一部分。這解釋了後面 starvation 為什麼完全沒有錯誤訊息。</p><p><strong>巨大交易完全不會被中途 checkpoint。</strong> 因為 <code>iCallback</code> 只在 <code>isCommit</code> 時設定。一個寫 5000 頁的交易，從頭到尾不會觸發任何 checkpoint。</p><h3 id="完整的自動-checkpoint-時機"><a href="#完整的自動-checkpoint-時機" class="headerlink" title="完整的自動 checkpoint 時機"></a>完整的自動 checkpoint 時機</h3><p>只有兩個：</p><table><thead><tr><th>時機</th><th>條件</th><th>模式</th><th>失敗會怎樣</th></tr></thead><tbody><tr><td>每次 COMMIT 之後</td><td>commit 後 frame 總數 <code>&gt;= 1000</code></td><td>PASSIVE</td><td>靜默忽略</td></tr><tr><td>最後一個連線關閉</td><td>無條件</td><td>PASSIVE</td><td>失敗則 <code>-wal</code> 留著</td></tr></tbody></table><p>沒有計時器、沒有背景執行緒。關閉連線那條路是唯一會讓 <code>-wal</code> 消失的自動路徑——它會 checkpoint 然後刪掉 <code>-wal</code> 與 <code>-shm</code>，除非設了 <code>SQLITE_FCNTL_PERSIST_WAL</code>。</p><hr><h2 id="五、Checkpoint-starvation"><a href="#五、Checkpoint-starvation" class="headerlink" title="五、Checkpoint starvation"></a>五、Checkpoint starvation</h2><p>只要有連線持有長時間讀取交易，就會進入這個狀態。這是我花最多力氣量測的部分。</p><h3 id="現象"><a href="#現象" class="headerlink" title="現象"></a>現象</h3><p>要複現這個狀態，有個前提得先講：<strong>光下 <code>BEGIN</code> 是沒用的</strong>。SQLite 的 <code>BEGIN</code> 是 deferred，read mark 要等<strong>第一次實際讀取</strong>才建立。必須 <code>BEGIN</code> 之後再跑一個 <code>SELECT</code>，那個交易才真的卡住 checkpoint。</p><p>開一個這樣的長時間讀取交易，然後持續寫入，autocheckpoint 用預設值：</p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"> 寫入筆數   -wal bytes   有效frame   ckptseq   salt-1        .db bytes</span></span><br><span class="line"><span class="section">--------------------------------------------------------------------------</span></span><br><span class="line"><span class="code">     500       346824        647        0    0x94ddde1d         512</span></span><br><span class="line"><span class="code">    1000       697904       1302        0    0x94ddde1d         512</span></span><br><span class="line"><span class="code">    2000      1407568       2626        0    0x94ddde1d         512</span></span><br><span class="line"><span class="code">    4000      2829040       5278        0    0x94ddde1d         512</span></span><br></pre></td></tr></table></figure><p>WAL 無界成長到 2.8 MB，salt 從頭到尾沒變，ckptseq 還是 0，<code>.db</code> 停在 512 bytes。</p><p>手動下 PASSIVE checkpoint：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">PRAGMA</span> wal_checkpoint(PASSIVE)  →  (<span class="number">0</span>, <span class="number">5278</span>, <span class="number">3</span>)</span><br></pre></td></tr></table></figure><p><code>busy = 0</code>。SQLite 認為這次「成功」了。</p><h3 id="autocheckpoint-到底有沒有在跑"><a href="#autocheckpoint-到底有沒有在跑" class="headerlink" title="autocheckpoint 到底有沒有在跑"></a>autocheckpoint 到底有沒有在跑</h3><p>這裡我犯了一個錯誤，值得記下來。我一開始寫「autocheckpoint 每次 commit 都有觸發，只是 backfill 不動」——但那是從程式碼推的，不是量測的。觀察到的現象（WAL 成長、salt 不變、<code>.db</code> 不變）同時符合「有跑但無效」和「根本沒跑」兩種解釋。</p><p>要分辨，得找一個只有「有跑」才會動的指標。<code>-shm</code> 的 <code>nBackfill</code> 就是。</p><p>A&#x2F;B 測試，兩組除了 <code>wal_autocheckpoint</code> 之外完全一樣：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">A 組（autocheckpoint = 1000）：</span><br><span class="line">   寫 3000 筆後：<span class="attribute">mxFrame</span>=3958  <span class="attribute">nBackfill</span>=3     <span class="attribute">-wal</span>=2,121,520</span><br><span class="line">B 組（autocheckpoint = 0）：</span><br><span class="line">   寫 3000 筆後：<span class="attribute">mxFrame</span>=3958  <span class="attribute">nBackfill</span>=0     <span class="attribute">-wal</span>=2,121,520</span><br></pre></td></tr></table></figure><p>兩組 WAL 逐位元組相同，只有 <code>nBackfill</code> 不同。checkpoint 確實跑了，並把 backfill 推到 reader 的 read mark 就停住。</p><p>但這只證明「跑過」，不證明「持續跑」。再做一個測試：讓 read mark 分階段往前移。</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">階段   reader 建立時 mxFrame   寫<span class="number"> 400 </span>筆後 nBackfill   .db bytes</span><br><span class="line"> <span class="number"> 1 </span>            <span class="number"> 3 </span>                   <span class="number"> 0 </span>                 512</span><br><span class="line"> <span class="number"> 2 </span>          <span class="number"> 518 </span>                 <span class="number"> 518 </span>              30,208</span><br><span class="line"> <span class="number"> 3 </span>         <span class="number"> 1038 </span>                <span class="number"> 1038 </span>              60,416</span><br><span class="line"> <span class="number"> 4 </span>         <span class="number"> 1564 </span>                <span class="number"> 1564 </span>              90,112</span><br></pre></td></tr></table></figure><p><code>nBackfill</code> 每一階段都精確追上當時 reader 的位置。只跑一次的話會凍在 518。</p><p>階段 1 的 <code>nBackfill = 0</code> 也是對的——那時 WAL 還沒到 1000 frame 門檻。</p><h3 id="為什麼-salt-不變"><a href="#為什麼-salt-不變" class="headerlink" title="為什麼 salt 不變"></a>為什麼 salt 不變</h3><p>真正的原因是 <code>walRestartLog</code> 的條件 1 失敗：</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">readLock</span> <span class="operator">=</span><span class="operator">=</span> <span class="number">0</span>  需要  nBackfill <span class="operator">=</span><span class="operator">=</span> mxFrame</span><br><span class="line">                     <span class="number">3</span>        ≠  <span class="number">5278</span></span><br><span class="line">→ 沒有任何 reader 能拿到 slot <span class="number">0</span></span><br><span class="line">→ 沒有任何 writer 的 readLock 會是 <span class="number">0</span></span><br><span class="line">→ walRestartHdr() 永遠不被呼叫</span><br></pre></td></tr></table></figure><p>所以精確的說法是「<code>nBackfill</code> 追不上 <code>mxFrame</code>」，不是「backfill 完全沒動」。</p><h3 id="為什麼-db-大小也不變"><a href="#為什麼-db-大小也不變" class="headerlink" title="為什麼 .db 大小也不變"></a>為什麼 <code>.db</code> 大小也不變</h3><p>這個比較繞。<code>nBackfill = 3</code> 代表 frame 1~3 已被 backfill，而那三個 frame 是 page 1、2、2。按理說寫入 page 2 應該讓 <code>.db</code> 從 1 頁變成 2 頁。</p><p>實際沒有。原因在 <code>walCheckpoint</code> 的複製迴圈（第 68492 行）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>( iFrame&lt;=nBackfill || iFrame&gt;mxSafeFrame || iDbpage&gt;mxPage )&#123;</span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>iterator 對每個 page 回報的是<strong>整個 WAL 裡最新的那個 frame</strong>。page 2 的最新 frame 是 1966，<code>1966 &gt; mxSafeFrame(3)</code>，被跳過。只有 page 1（最新 frame &#x3D; 1）被真的複製，而 page 1 本來就存在，檔案不需要成長。</p><p><code>nBackfill</code> 卻仍被設成 <code>mxSafeFrame = 3</code>。</p><h3 id="卡法有兩種"><a href="#卡法有兩種" class="headerlink" title="卡法有兩種"></a>卡法有兩種</h3><p>比較兩次實驗：</p><table><thead><tr><th>reader 建立時機</th><th>reader 佔的 slot</th><th><code>nBackfill</code> 停在</th></tr></thead><tbody><tr><td>WAL <strong>沒</strong>完全 backfill 時</td><td>slot 1~4</td><td>reader 的 mark</td></tr><tr><td>WAL <strong>已</strong>完全 backfill 時</td><td><strong>slot 0</strong></td><td><strong>0</strong>，完全不動</td></tr></tbody></table><p>原因在 <code>walCheckpoint</code> 第 68451 行——backfill 的前置條件是拿到 slot 0 的<strong>獨佔</strong>鎖：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>( pIter</span><br><span class="line"> &amp;&amp; (rc = walBusyLock(pWal,xBusy,pBusyArg,WAL_READ_LOCK(<span class="number">0</span>),<span class="number">1</span>))==SQLITE_OK</span><br><span class="line">)&#123;</span><br></pre></td></tr></table></figure><p>而 reader 在 <code>nBackfill == mxFrame</code> 時會拿 slot 0 的<strong>共享</strong>鎖。於是：</p><ul><li>reader 佔 <strong>slot 0</strong>：checkpointer 拿不到獨佔鎖，backfill 完全癱瘓</li><li>reader 佔 <strong>slot 1~4</strong>：backfill 可以推到那個 reader 的 mark</li></ul><p>諷刺的是，在資料庫最健康的時候（WAL 剛清空）開一個長交易，反而會造成最嚴重的癱瘓。</p><h3 id="怎麼診斷"><a href="#怎麼診斷" class="headerlink" title="怎麼診斷"></a>怎麼診斷</h3><p>兩個可靠的方法，兩個會騙你的。</p><p><strong>可靠一：<code>PRAGMA wal_checkpoint(PASSIVE)</code> 的回傳值。</strong></p><p>從原始碼確認（第 70577-70579 行）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>( pnLog ) *pnLog = (<span class="type">int</span>)pWal-&gt;hdr.mxFrame;</span><br><span class="line"><span class="keyword">if</span>( pnCkpt ) *pnCkpt = (<span class="type">int</span>)(walCkptInfo(pWal)-&gt;nBackfill);</span><br></pre></td></tr></table></figure><p>第三個數字是<strong>累計水位</strong>，不是「這次搬了幾個」。判讀：</p><table><thead><tr><th>條件</th><th>意義</th></tr></thead><tbody><tr><td><code>busy == 1</code></td><td>連 checkpoint 鎖都拿不到</td></tr><tr><td><code>nLog == nCkpt</code></td><td>完全追上，下次寫入就會 reset</td></tr><tr><td><code>nLog &gt; nCkpt</code></td><td>卡住，落後 <code>nLog - nCkpt</code> 個 frame</td></tr></tbody></table><p><strong>可靠二：直接讀 <code>-shm</code>，無副作用。</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">b = <span class="built_in">open</span>(path + <span class="string">&#x27;-shm&#x27;</span>, <span class="string">&#x27;rb&#x27;</span>).read(<span class="number">136</span>)</span><br><span class="line">mxFrame   = struct.unpack(<span class="string">&#x27;=I&#x27;</span>, b[<span class="number">16</span>:<span class="number">20</span>])[<span class="number">0</span>]</span><br><span class="line">nBackfill = struct.unpack(<span class="string">&#x27;=I&#x27;</span>, b[<span class="number">96</span>:<span class="number">100</span>])[<span class="number">0</span>]</span><br></pre></td></tr></table></figure><p>注意是 native byte order，不是 big-endian。<code>-shm</code> 是機器位元序的。</p><p><strong>不可靠一：<code>.db</code> 檔案大小。</strong> 實測「健康」和「卡死」兩個狀態都是 23,040 bytes，完全看不出差別。</p><p><strong>不可靠二：salt &#x2F; ckptseq。</strong> 更糟：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">健康:  <span class="attribute">salt-1</span>=0x473f6df2  <span class="attribute">ckptseq</span>=0</span><br><span class="line">卡死:  <span class="attribute">salt-1</span>=0x473f6df3  <span class="attribute">ckptseq</span>=1      salt 變了</span><br></pre></td></tr></table></figure><p>salt 在卡死狀態下反而變了，因為那個變化是前一個健康階段留下的，跟當下完全無關。拿 salt 當「checkpoint 有沒有在運作」的指標，這裡就會誤判成健康。</p><p>還有一個限制值得說清楚：從 <code>-shm</code> 的 <code>aReadMark[]</code> <strong>值</strong>是看不出誰卡住的。checkpointer 自己會去改 <code>aReadMark[1]</code>（第 68434 行），所以看到某個 slot 有值，不代表有 reader 坐在上面。值和鎖是兩回事。要真的找出兇手，得去試著取那些鎖。</p><hr><h2 id="六、其他容易錯的細節"><a href="#六、其他容易錯的細節" class="headerlink" title="六、其他容易錯的細節"></a>六、其他容易錯的細節</h2><h3 id="Page-size-有三種編碼"><a href="#Page-size-有三種編碼" class="headerlink" title="Page size 有三種編碼"></a>Page size 有三種編碼</h3><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">page_size</span>    WAL檔頭(u32)   DB檔頭(u16)   -shm szPage(u16)</span><br><span class="line"><span class="attribute">512</span>          <span class="number">512</span>            <span class="number">512</span>           <span class="number">512</span></span><br><span class="line"><span class="attribute">4096</span>         <span class="number">4096</span>           <span class="number">4096</span>          <span class="number">4096</span></span><br><span class="line"><span class="attribute">65536</span>        <span class="number">65536</span>          <span class="number">1</span>             <span class="number">1</span></span><br></pre></td></tr></table></figure><p>WAL 檔頭照實存 65536。DB 檔頭因為只有 2 bytes，用 <code>1</code> 當特例。 第三種最陰險，它不是特例值而是位元打包：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">編碼: (u16)((szPage &amp; <span class="number">0xff00</span>) | (szPage &gt;&gt; <span class="number">16</span>))       wal.c:<span class="number">1519</span>, :<span class="number">4249</span></span><br><span class="line">解碼: (szPage &amp; <span class="number">0xfe00</span>) + ((szPage &amp; <span class="number">0x0001</span>) &lt;&lt; <span class="number">16</span>)   wal.c:<span class="number">2132</span>, :<span class="number">2633</span></span><br></pre></td></tr></table></figure><p><code>wal.c:317</code> 的註解說「szPage 可以是 512 到 32768 之間的任何 2 的冪」，講的正是存進去的形式。</p><h3 id="Sector-padding，以及為什麼-API-叫-commits-而不是-transactions"><a href="#Sector-padding，以及為什麼-API-叫-commits-而不是-transactions" class="headerlink" title="Sector padding，以及為什麼 API 叫 commits() 而不是 transactions()"></a>Sector padding，以及為什麼 API 叫 commits() 而不是 transactions()</h3><p><code>walFrames()</code> 只在兩個條件同時成立時寫 padding frame：</p><ol><li><code>isCommit &amp;&amp; WAL_SYNC_FLAGS(sync_flags) != 0</code>，即 <code>synchronous=FULL</code></li><li><code>pWal-&gt;padToSectorBoundary</code>，只有主 DB 檔<strong>不</strong>帶 <code>SQLITE_IOCAP_POWERSAFE_OVERWRITE</code> 時才為 1</li></ol><p>unix VFS 的 psow 預設為 1，所以 Linux 上預設永遠不會 padding。這也是我一開始試不出來的原因——得用 URI <code>file:test.db?psow=0</code> 才行。</p><p>實測一個 <code>CREATE TABLE</code>（page_size 512，synchronous&#x3D;FULL）：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">psow</span>=<span class="number">1</span>: <span class="number">2</span> frames → <span class="number">1</span> 個 commit group</span><br><span class="line"><span class="attribute">psow</span>=<span class="number">0</span>: <span class="number">8</span> frames → <span class="number">7</span> 個 commit group</span><br></pre></td></tr></table></figure><p>padding frame 複製前一個 frame 的 pgno、payload、salt <strong>以及 commit 標記</strong>，只有 checksum 不同（chain 有前進）。所以它是完全合法的 commit frame，四項有效性判定全過。</p><p>結論：「這個 WAL 裡有幾個 SQL 交易」不是可從位元組回復的量。可以回復的是 commit 邊界。所以 API 應該叫 <code>commits()</code>，而下游去重的 key 必須是 <code>(page_no, payload)</code>，不能比對 raw frame bytes。</p><h3 id="Reserved-bytes-不影響-frame-大小"><a href="#Reserved-bytes-不影響-frame-大小" class="headerlink" title="Reserved bytes 不影響 frame 大小"></a>Reserved bytes 不影響 frame 大小</h3><p><code>pagerWalFrames()</code> 傳給 WAL 層的是 <code>pPager-&gt;pageSize</code>，不是 <code>usableSize</code>。所以 frame payload 恆等於 page_size。</p><p>驗證這件事的時候踩了一個坑：<strong>SQLite 沒有 <code>PRAGMA reserve_bytes</code></strong>。這個 pragma 不存在於任何版本，而 SQLite 會靜默忽略無法識別的 pragma——這才是它「看起來像 no-op」的原因。正確的 API 是 <code>sqlite3_file_control</code> 配 <code>SQLITE_FCNTL_RESERVE_BYTES</code>，opcode <strong>38</strong>。</p><p>用對之後實測：設 reserved&#x3D;32、page_size&#x3D;4096，DB header offset 20 讀到 32，而 WAL 長度仍是 12392 bytes，不管有沒有 reserved 都一樣。</p><hr><h2 id="七、為什麼-SQLite-要這樣設計"><a href="#七、為什麼-SQLite-要這樣設計" class="headerlink" title="七、為什麼 SQLite 要這樣設計"></a>七、為什麼 SQLite 要這樣設計</h2><p>被問到一個好問題：為什麼不像 PostgreSQL 一直往前寫，而要回頭重用同一個檔案還得靠 salt 分辨？</p><p>先修正一個前提：<strong>PostgreSQL 也回收 WAL 檔案。</strong></p><blockquote><p>When old WAL segment files are no longer needed, they are removed or <strong>recycled (that is, renamed to become future segments in the numbered sequence)</strong>.</p></blockquote><p>所以差別不是「一直寫 vs 重用」，而是<strong>重用的粒度與可辨識性</strong>。</p><table><thead><tr><th></th><th>PostgreSQL</th><th>SQLite</th></tr></thead><tbody><tr><td>重用單位</td><td>整個 16MB segment 檔</td><td>同一個檔案裡的同一個 offset</td></tr><tr><td>重用手法</td><td>改名</td><td>原地覆寫</td></tr><tr><td>世代如何辨識</td><td>檔名本身就是位置，LSN 全域單調</td><td>檔名和 offset 都沒變，需要 salt</td></tr><tr><td>誰負責清理</td><td>checkpointer 背景行程</td><td>沒有任何背景行程</td></tr></tbody></table><p>PostgreSQL 的 rename 本身就攜帶了「這是新世代」的資訊。SQLite 覆寫進同一個 offset，檔案系統層面看不出差異，salt 是被迫補上的標記。</p><h3 id="SQLite-自己給的理由"><a href="#SQLite-自己給的理由" class="headerlink" title="SQLite 自己給的理由"></a>SQLite 自己給的理由</h3><p><code>wal.html</code> 直說：</p><blockquote><p>The checkpoint does not normally truncate the WAL file… Instead, it merely causes SQLite to start overwriting the WAL file from the beginning. <strong>This is done because it is normally faster to overwrite an existing file than to append.</strong></p></blockquote><p>append 要跟檔案系統要新的 block、更新 inode 大小、可能要 fsync 目錄。覆寫既有 block 全都省掉。對一個常跑在手機、嵌入式裝置、便宜 SD 卡上的資料庫，這不是小事。</p><h3 id="為什麼不能學-PostgreSQL"><a href="#為什麼不能學-PostgreSQL" class="headerlink" title="為什麼不能學 PostgreSQL"></a>為什麼不能學 PostgreSQL</h3><p>三個結構性原因。</p><p><strong>沒有背景行程。</strong> PostgreSQL 有 checkpointer process。SQLite 是函式庫，checkpoint 是由「剛好某個連線」順手做的。如果改成 segment 檔，誰來刪舊的？程式當掉沒清乾淨怎麼辦？你的 <code>app.db</code> 旁邊會慢慢堆出上千個 segment，而且沒有任何行程有責任收拾。</p><p><strong>PostgreSQL 保留 WAL 是因為它需要。</strong> archiving、streaming replication、PITR、WAL summarization——這些 SQLite 一個都沒有。對 SQLite 來說，checkpoint 完成的那一刻，WAL 裡的內容就是純粹的冗餘。</p><p><strong>「單一檔案」是 SQLite 的核心賣點。</strong> WAL 模式已經多了兩個暫時性檔案，再加上一串數量不定的 segment，這個賣點就沒了。</p><h3 id="這樣設計是不是更容易誤解"><a href="#這樣設計是不是更容易誤解" class="headerlink" title="這樣設計是不是更容易誤解"></a>這樣設計是不是更容易誤解</h3><p>對外部讀取者來說，確實危險。而且危險的形式最糟：「用檔案大小算 frame 數」是最自然的直覺，錯了不會有錯誤訊息，產出的資料庫結構完全合法。</p><p>但對 SQLite 自己來說一點都不容易誤解，因為它從來不靠檔案大小判斷——它讀 <code>-shm</code> 裡的 <code>mxFrame</code>。salt 檢查只是 recovery 時的保險。</p><p>這是一個成本轉嫁的設計：SQLite 省下背景行程、多檔案管理、目錄 fsync，代價是外部工具必須理解 salt 語意。而 SQLite 從來沒有官方支援過「外部工具直接讀 <code>-wal</code>」這個使用情境。</p><p>公平地說，SQLite 也把該講的都講了。<code>wal.c:29-32</code> 白紙黑字：</p><blockquote><p>Checksums and counters attached to each frame are used to determine which frames within the WAL are valid and which are <strong>leftovers from prior checkpoints</strong>.</p></blockquote><h2 id="小結"><a href="#小結" class="headerlink" title="小結"></a>小結</h2><p>寫這個 parser 最大的收穫不是格式細節，而是一個很硬的教訓：面對 SQLite 的 WAL，<strong>可信度排序是程式碼 &gt; 實測 &gt; 文件，而且連程式碼的註解都會錯</strong>。整理成幾條可以帶走的原則：</p><table><thead><tr><th>主題</th><th>結論</th></tr></thead><tbody><tr><td>有效 frame 數</td><td><strong>不能</strong>用 <code>(len - 32) / stride</code> 算，必須逐 slot 驗 salt</td></tr><tr><td>salt 何時變</td><td>只有 WAL reset 或 <code>TRUNCATE</code> 才變，checkpoint 不變</td></tr><tr><td>salt 判別</td><td>用相等性測試，且要比對 <strong>兩個</strong> salt，不能假設單調遞增</td></tr><tr><td>autocheckpoint</td><td>只是一個 WAL hook，自己註冊 hook 就會關掉它</td></tr><tr><td>checkpoint starvation</td><td>用 <code>PRAGMA wal_checkpoint</code> 回傳值或 <code>-shm</code> 的 <code>nBackfill</code> 診斷，不能看檔案大小或 salt</td></tr><tr><td>commit vs transaction</td><td>位元組只能回復 commit 邊界，去重 key 必須是 <code>(page_no, payload)</code></td></tr><tr><td>判定順序</td><td>長度 → salt → <code>page_no != 0</code> → checksum，順序是規範性的</td></tr></tbody></table><p>如果你也在寫任何直接讀 <code>-wal</code> 的工具（備份、CDC、replication），這幾條踩過的坑希望能幫你少繞一圈。</p><hr><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a target="_blank" rel="noopener" href="https://sqlite.org/fileformat2.html">SQLite Database File Format §4</a> — WAL 檔案格式、checksum 演算法、checkpoint &#x2F; reader 演算法</li><li><a target="_blank" rel="noopener" href="https://sqlite.org/walformat.html">SQLite WAL-Mode File Format</a> — <code>-shm</code> 格式、鎖定協定、recovery</li><li><a target="_blank" rel="noopener" href="https://sqlite.org/wal.html">Write-Ahead Logging</a> — 設計取捨、autocheckpoint、避免 WAL 過大</li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/wal-configuration.html">PostgreSQL WAL Configuration</a> — segment 回收與保留條件</li><li>相關文章：<a href="https://isdaniel.github.io/postgresql-wal-introduce/">PostgreSQL WAL (Write-Ahead Logging) 機制</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/sqlite-wal-parser-verification-notes/">https://isdaniel.github.io/sqlite-wal-parser-verification-notes/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/sqlite-wal-parser-verification-notes/</id>
    <link href="https://isdaniel.github.io/sqlite-wal-parser-verification-notes/"/>
    <published>2026-08-05T13:30:00.000Z</published>
    <summary>把 SQLite -wal 檔正確解析成 frame 與 commit 邊界的驗證記錄，並揭露官方文件在 salt、checksum、checkpoint 時機等關鍵處的錯誤</summary>
    <title>寫一個 SQLite WAL Parser</title>
    <updated>2026-08-05T10:10:26.494Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/PostgreSQL/"/>
    <category term="Performance-Tuning" scheme="https://isdaniel.github.io/categories/PostgreSQL/Performance-Tuning/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Performance-Tuning" scheme="https://isdaniel.github.io/tags/Performance-Tuning/"/>
    <category term="Troubleshooting" scheme="https://isdaniel.github.io/tags/Troubleshooting/"/>
    <category term="pg_stat_statements" scheme="https://isdaniel.github.io/tags/pg-stat-statements/"/>
    <category term="EXPLAIN" scheme="https://isdaniel.github.io/tags/EXPLAIN/"/>
    <content>
      <![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>當 PostgreSQL 效能變差時，最常見的錯誤是「憑感覺」直接跳到某個 SQL 去猜哪裡慢。真正有效率的排查方式是 <strong>由上而下 (top-down)</strong>：先看作業系統與實例層級的指標（哪個資源、哪個時間點被打爆），再看 <strong>wait event</strong>（backend 到底卡在哪一類瓶頸），最後才進到 <strong>query 層級</strong> 去針對元凶做 <code>EXPLAIN</code>。</p><p>其他相關基礎可以搭配我之前寫的文章一起看：</p><ul><li><a href="https://isdaniel.github.io/postgresql-cost-factor-tuning/">postgresql 執行計畫重要因子 (成本因子調教)</a></li><li><a href="https://isdaniel.github.io/postgresql-autovacuum/">Postgresql AutoVacuum 介紹</a></li><li><a href="https://isdaniel.github.io/postgresql-wal-introduce/">PostgreSQL WAL 介紹</a></li></ul><h2 id="前置作業：先把觀測能力打開"><a href="#前置作業：先把觀測能力打開" class="headerlink" title="前置作業：先把觀測能力打開"></a>前置作業：先把觀測能力打開</h2><p>在做任何 query 層級的診斷之前，下面這些參數必須先就位，否則很多統計表根本沒資料。</p><ul><li><strong><code>pg_stat_statements</code></strong>：要先加進 <code>shared_preload_libraries</code>、<strong>重啟</strong> DB（分配共享記憶體，reload 不夠），再在目標 DB 執行 <code>CREATE EXTENSION pg_stat_statements;</code>。</li><li><strong><code>track_activities = on</code></strong>（預設開）：<code>pg_stat_activity</code> 才會回報 <code>state</code>、<code>query</code>、<code>wait_event</code>。</li><li><strong><code>track_counts = on</code></strong>（預設開）：<code>pg_stat_user_*</code>、<code>pg_statio_*</code>、<code>pg_stat_database</code> 的計數器才會累積，否則「未使用索引」、「cache hit ratio」查出來都是空的。</li><li><strong><code>track_io_timing = on</code></strong>（預設關）：<code>pg_stat_statements</code> 與 <code>EXPLAIN (BUFFERS)</code> 的 <strong>I&#x2F;O 時間</strong>欄位才有值（block 數量欄位一直都有）。這個有額外開銷，正式環境開之前先壓測。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- postgresql.conf</span></span><br><span class="line">shared_preload_libraries <span class="operator">=</span> <span class="string">&#x27;pg_stat_statements&#x27;</span></span><br><span class="line">track_io_timing <span class="operator">=</span> <span class="keyword">on</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 重啟後在目標 DB 執行</span></span><br><span class="line"><span class="keyword">CREATE</span> EXTENSION IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> pg_stat_statements;</span><br></pre></td></tr></table></figure><h2 id="排查方法論：三層由上而下定位"><a href="#排查方法論：三層由上而下定位" class="headerlink" title="排查方法論：三層由上而下定位"></a>排查方法論：三層由上而下定位</h2><table><thead><tr><th>層級</th><th>問「什麼」</th><th>工具</th></tr></thead><tbody><tr><td>Tier 1 — OS &#x2F; 實例指標</td><td><strong>何時</strong>、<strong>哪個資源</strong>被打爆</td><td>CPU% &#x2F; IOPS &#x2F; Throughput、<code>top</code>、<code>iostat</code>、<code>vmstat</code>、<code>pg_stat_database</code></td></tr><tr><td>Tier 2 — Wait event</td><td>backend <strong>卡在哪一類瓶頸</strong></td><td><code>pg_stat_activity.wait_event_type</code></td></tr><tr><td>Tier 3 — Query 層級</td><td><strong>哪一條</strong> SQL 是元凶</td><td><code>pg_stat_statements</code>、<code>EXPLAIN (ANALYZE, BUFFERS)</code></td></tr></tbody></table><p>Tier 2 是很多人忽略但最關鍵的一層。判讀 <code>wait_event_type</code> 先看「類別」：</p><table><thead><tr><th><code>wait_event_type</code></th><th>代表的瓶頸類別</th></tr></thead><tbody><tr><td><code>NULL</code> 且 <code>state=&#39;active&#39;</code></td><td><strong>On-CPU</strong> — backend 正在真的跑（CPU-bound）。PostgreSQL 沒有一個叫 <code>&#39;CPU&#39;</code> 的 wait type，on-CPU 是靠「active + 沒有 wait event」推論出來的</td></tr><tr><td><code>Lock</code></td><td>重量級鎖競爭（例如 <code>wait_event=&#39;relation&#39;</code> 卡在 table lock）</td></tr><tr><td><code>LWLock</code></td><td>共享記憶體結構競爭（例如 <code>ProcArray</code>）</td></tr><tr><td><code>IO</code> &#x2F; <code>BufferPin</code></td><td>儲存 &#x2F; buffer 瓶頸</td></tr><tr><td><code>Client</code></td><td>在等應用程式 &#x2F; 網路 — <strong>不是</strong> DB 的瓶頸</td></tr><tr><td><code>IPC</code></td><td>在等另一個 backend</td></tr><tr><td><code>Activity</code> &#x2F; <code>Timeout</code></td><td>設計上就在閒置的背景程序 — <strong>不是</strong>使用者查詢問題</td></tr></tbody></table><blockquote><p><strong>重要觀念</strong>：<code>wait_event</code> 是 <strong>瞬時取樣 (point-in-time)</strong>，不是累積時間。單看一次快照只知道「此刻」誰在等。要把時間歸因到 CPU &#x2F; IO &#x2F; Lock，必須 <strong>反覆取樣</strong>（例如每秒一次）或用擴充套件。</p></blockquote><p>下面這條快照查詢會把所有 client backend 依「瓶頸類別」分桶，是排查第一步最好用的一條：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    <span class="keyword">CASE</span></span><br><span class="line">        <span class="keyword">WHEN</span> wait_event_type <span class="keyword">IS</span> <span class="keyword">NULL</span>                            <span class="keyword">THEN</span> <span class="string">&#x27;CPU/Running (no wait)&#x27;</span></span><br><span class="line">        <span class="keyword">WHEN</span> wait_event_type <span class="keyword">IN</span> (<span class="string">&#x27;Lock&#x27;</span>, <span class="string">&#x27;LWLock&#x27;</span>, <span class="string">&#x27;BufferPin&#x27;</span>) <span class="keyword">THEN</span> <span class="string">&#x27;Lock/Contention&#x27;</span></span><br><span class="line">        <span class="keyword">WHEN</span> wait_event_type <span class="operator">=</span> <span class="string">&#x27;IO&#x27;</span>                             <span class="keyword">THEN</span> <span class="string">&#x27;IO&#x27;</span></span><br><span class="line">        <span class="keyword">ELSE</span> <span class="string">&#x27;Wait: &#x27;</span> <span class="operator">||</span> wait_event_type</span><br><span class="line">    <span class="keyword">END</span>                                            <span class="keyword">AS</span> bottleneck_class,</span><br><span class="line">    <span class="built_in">coalesce</span>(wait_event, <span class="string">&#x27;(none)&#x27;</span>)                 <span class="keyword">AS</span> wait_event,</span><br><span class="line">    <span class="built_in">count</span>(<span class="operator">*</span>)                                       <span class="keyword">AS</span> backends,</span><br><span class="line">    string_agg(<span class="keyword">DISTINCT</span> state, <span class="string">&#x27;, &#x27;</span>)               <span class="keyword">AS</span> states</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> backend_type <span class="operator">=</span> <span class="string">&#x27;client backend&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> pid <span class="operator">&lt;&gt;</span> pg_backend_pid()</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> <span class="number">1</span>, <span class="number">2</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> backends <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>判讀：如果 <code>CPU/Running</code> 這桶 backend 數量爆多 → 往 <a href="#%E9%AB%98-CPU-%E6%8E%92%E6%9F%A5">高 CPU</a> 走；<code>Lock/Contention</code> 多 → 往 <a href="#%E9%8E%96%E8%88%87-Blocking-%E6%8E%92%E6%9F%A5">鎖與 blocking</a> 走；<code>IO</code> 多 → 往 <a href="#Cache-hit-ratio-%E8%88%87-I-O-%E6%8E%92%E6%9F%A5">cache hit 與 I&#x2F;O</a> 走。</p><h2 id="高-CPU-排查"><a href="#高-CPU-排查" class="headerlink" title="高 CPU 排查"></a>高 CPU 排查</h2><p>持續高 CPU 常見成因：(a) 單條很貴的查詢、(b) 便宜但呼叫超頻繁的查詢、(c) long transaction &#x2F; idle-in-transaction、(d) 連線數過多、(e) 統計資訊過期或表膨脹逼出爛執行計畫。</p><h3 id="找「每次執行最貴」的查詢（mean-exec-time）"><a href="#找「每次執行最貴」的查詢（mean-exec-time）" class="headerlink" title="找「每次執行最貴」的查詢（mean_exec_time）"></a>找「每次執行最貴」的查詢（mean_exec_time）</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> userid::regrole, dbid, query, mean_exec_time</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_statements</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> mean_exec_time <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">5</span>;</span><br></pre></td></tr></table></figure><h3 id="找「總成本最高」的查詢（total-exec-time）"><a href="#找「總成本最高」的查詢（total-exec-time）" class="headerlink" title="找「總成本最高」的查詢（total_exec_time）"></a>找「總成本最高」的查詢（total_exec_time）</h3><p>這個抓的是「單次便宜、但被呼叫幾萬次」那種累積殺手：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> userid::regrole, dbid, total_exec_time, query</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_statements</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> total_exec_time <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">5</span>;</span><br></pre></td></tr></table></figure><blockquote><p><strong>版本雷點</strong>：PG13 以後請用 <code>mean_exec_time</code> &#x2F; <code>total_exec_time</code>。舊名 <code>mean_time</code> &#x2F; <code>total_time</code> 在 PG13 被 <strong>移除</strong>（拆成 <code>*_exec_time</code> + <code>*_plan_time</code>），所以 Postgres 12 那組用 <code>mean_time</code> &#x2F; <code>total_time</code> 的查詢在 v13+ 會直接報錯。</p></blockquote><h3 id="總時間-每條的-cache-命中率"><a href="#總時間-每條的-cache-命中率" class="headerlink" title="總時間 + 每條的 cache 命中率"></a>總時間 + 每條的 cache 命中率</h3><p>一次看出「哪條 SQL 花最多時間、而且 cache 命中率差」：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> query,</span><br><span class="line">       calls,</span><br><span class="line">       total_exec_time,</span><br><span class="line">       <span class="keyword">rows</span>,</span><br><span class="line">       <span class="number">100.0</span> <span class="operator">*</span> shared_blks_hit <span class="operator">/</span> <span class="built_in">nullif</span>(shared_blks_hit <span class="operator">+</span> shared_blks_read, <span class="number">0</span>) <span class="keyword">AS</span> hit_percent</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_statements</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> total_exec_time <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">5</span>;</span><br></pre></td></tr></table></figure><p><code>hit_percent</code> 偏低代表這條查詢一直在讀 disk（cache miss），通常是缺索引或工作集超過 <code>shared_buffers</code>。</p><h3 id="找「穩定慢」的查詢（過濾掉一次性偶發）"><a href="#找「穩定慢」的查詢（過濾掉一次性偶發）" class="headerlink" title="找「穩定慢」的查詢（過濾掉一次性偶發）"></a>找「穩定慢」的查詢（過濾掉一次性偶發）</h3><p><code>calls &gt; 50</code> 濾掉偶發，看平均 &#x2F; 最大 &#x2F; 標準差：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> query,</span><br><span class="line">       calls,</span><br><span class="line">       mean_exec_time,</span><br><span class="line">       max_exec_time,</span><br><span class="line">       stddev_exec_time,</span><br><span class="line">       <span class="keyword">rows</span>::<span class="type">numeric</span> <span class="operator">/</span> <span class="built_in">nullif</span>(calls, <span class="number">0</span>) <span class="keyword">AS</span> mean_rows</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_statements</span><br><span class="line"><span class="keyword">WHERE</span> calls <span class="operator">&gt;</span> <span class="number">50</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> mean_exec_time <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><h3 id="看「當下正在跑」的查詢與它在等什麼"><a href="#看「當下正在跑」的查詢與它在等什麼" class="headerlink" title="看「當下正在跑」的查詢與它在等什麼"></a>看「當下正在跑」的查詢與它在等什麼</h3><p><code>state=&#39;active&#39;</code> 且 <code>wait_event_type IS NULL</code> 的那一列，就是你正在燒 CPU 的查詢：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid,</span><br><span class="line">       now() <span class="operator">-</span> query_start <span class="keyword">AS</span> query_duration,</span><br><span class="line">       now() <span class="operator">-</span> xact_start  <span class="keyword">AS</span> xact_duration,</span><br><span class="line">       state,</span><br><span class="line">       wait_event_type,</span><br><span class="line">       wait_event,</span><br><span class="line">       query</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> state <span class="operator">=</span> <span class="string">&#x27;active&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> pid <span class="operator">&lt;&gt;</span> pg_backend_pid()</span><br><span class="line">  <span class="keyword">AND</span> backend_type <span class="operator">=</span> <span class="string">&#x27;client backend&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> query_start <span class="keyword">ASC</span>;</span><br></pre></td></tr></table></figure><blockquote><p>想只看長時間跑的，加上 <code>AND now() - query_start &gt; interval &#39;5 seconds&#39;</code>。</p></blockquote><h3 id="揪出跑超過-5-秒的-long-running-query，並抓它的-EXPLAIN"><a href="#揪出跑超過-5-秒的-long-running-query，並抓它的-EXPLAIN" class="headerlink" title="揪出跑超過 5 秒的 long-running query，並抓它的 EXPLAIN"></a>揪出跑超過 5 秒的 long-running query，並抓它的 EXPLAIN</h3><p>排查慢查詢最實戰的一步：先用 <code>pg_stat_activity</code> 撈出 <strong>已經跑超過 5 秒</strong> 的查詢（門檻自己調），拿到它的 <code>pid</code> 與完整 SQL，再對那條 SQL 跑 <code>EXPLAIN</code> 看它為什麼慢。</p><p><strong>Step 1：撈出跑超過 5 秒、當下 active 的查詢</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid,</span><br><span class="line">       now() <span class="operator">-</span> query_start           <span class="keyword">AS</span> running_for,   <span class="comment">-- 這條查詢已經跑多久</span></span><br><span class="line">       state,</span><br><span class="line">       wait_event_type,</span><br><span class="line">       wait_event,</span><br><span class="line">       query</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> state <span class="operator">=</span> <span class="string">&#x27;active&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> backend_type <span class="operator">=</span> <span class="string">&#x27;client backend&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> pid <span class="operator">&lt;&gt;</span> pg_backend_pid()</span><br><span class="line">  <span class="keyword">AND</span> now() <span class="operator">-</span> query_start <span class="operator">&gt;</span> <span class="type">interval</span> <span class="string">&#x27;5 seconds&#x27;</span>        <span class="comment">-- 只看跑超過 5 秒的</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> running_for <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><p>判讀：<code>running_for</code> 由大到小排，最上面那條就是「跑最久還沒結束」的元凶。搭配 <code>wait_event_type</code> 判斷它卡在什麼——<code>NULL</code> 代表正在燒 CPU（多半是缺索引 &#x2F; 爛計畫）；<code>Lock</code> 代表被別人擋住（跳到<a href="#%E9%8E%96%E8%88%87-Blocking-%E6%8E%92%E6%9F%A5">鎖與 Blocking</a>）；<code>IO</code> 代表在等磁碟。</p><blockquote><p><code>query</code> 欄位預設會被 <code>track_activity_query_size</code>（預設 1024 bytes）截斷，長 SQL 會看不全，必要時可調大這個參數（需重啟）。</p></blockquote><p><strong>Step 2：對這條慢查詢跑 EXPLAIN</strong></p><p>從 Step 1 拿到完整 <code>query</code> 後，把它貼到 <code>EXPLAIN</code> 裡分析。先用 <strong>不帶 <code>ANALYZE</code></strong> 的版本（只看預估計畫、不會真的執行，安全）：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN (VERBOSE, SETTINGS) <span class="operator">&lt;</span>把上面查到的 query 貼進來<span class="operator">&gt;</span>;</span><br></pre></td></tr></table></figure><p>如果這條查詢可以安全地再跑一次（純 <code>SELECT</code>、或你能接受它實際執行），再用 <code>ANALYZE</code> 拿到真實執行時間與 buffer 命中狀況，這才看得出「預估 vs 實際」的落差：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN (ANALYZE, BUFFERS, VERBOSE) <span class="operator">&lt;</span>同一條 query<span class="operator">&gt;</span>;</span><br></pre></td></tr></table></figure><blockquote><p><strong>對 <code>INSERT</code> &#x2F; <code>UPDATE</code> &#x2F; <code>DELETE</code> 用 <code>ANALYZE</code> 會真的改到資料</strong>，一定要包在交易裡（見<a href="#%E6%85%A2%E6%9F%A5%E8%A9%A2%E5%88%86%E6%9E%90%EF%BC%9A%E8%AE%80%E6%87%82-EXPLAIN-ANALYZE-BUFFERS">慢查詢分析</a>）：<code>BEGIN; EXPLAIN (ANALYZE, BUFFERS) &lt;DML&gt;; ROLLBACK;</code></p></blockquote><p><strong>Step 3（可選）：確定是元凶且影響線上，就先取消它</strong></p><p>用 Step 1 查到的 <code>pid</code> 取消它（優先 <code>pg_cancel_backend</code>，只取消查詢、保留連線）：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pg_cancel_backend(<span class="number">12345</span>);   <span class="comment">-- 換成 Step 1 查到的 pid</span></span><br></pre></td></tr></table></figure><h3 id="找-long-transaction-idle-in-transaction"><a href="#找-long-transaction-idle-in-transaction" class="headerlink" title="找 long transaction &#x2F; idle-in-transaction"></a>找 long transaction &#x2F; idle-in-transaction</h3><p>這類 session 會一直卡住 CPU、持鎖、並釘住 xmin horizon 讓 VACUUM 無法回收（延伸閱讀：<a href="https://isdaniel.github.io/postgresql-autovacuum/">Postgresql AutoVacuum 介紹</a>）：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid, usename, datname, state, wait_event_type,</span><br><span class="line">       now() <span class="operator">-</span> xact_start  <span class="keyword">AS</span> xact_duration,</span><br><span class="line">       now() <span class="operator">-</span> query_start <span class="keyword">AS</span> query_duration,</span><br><span class="line">       query</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> pid <span class="operator">&lt;&gt;</span> pg_backend_pid()</span><br><span class="line">  <span class="keyword">AND</span> backend_type <span class="operator">=</span> <span class="string">&#x27;client backend&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> state <span class="keyword">IN</span> (<span class="string">&#x27;idle in transaction&#x27;</span>, <span class="string">&#x27;active&#x27;</span>)</span><br><span class="line">  <span class="keyword">AND</span> xact_start <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> xact_duration <span class="keyword">DESC</span> <span class="keyword">NULLS LAST</span>;</span><br></pre></td></tr></table></figure><h3 id="看連線數分佈（連線太多-CPU-記憶體雙重負擔）"><a href="#看連線數分佈（連線太多-CPU-記憶體雙重負擔）" class="headerlink" title="看連線數分佈（連線太多 &#x3D; CPU + 記憶體雙重負擔）"></a>看連線數分佈（連線太多 &#x3D; CPU + 記憶體雙重負擔）</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> state, <span class="built_in">count</span>(<span class="operator">*</span>)</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> pid <span class="operator">&lt;&gt;</span> pg_backend_pid()</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> state</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> state <span class="keyword">ASC</span>;</span><br></pre></td></tr></table></figure><blockquote><p>背景輔助程序的 <code>state</code> 是 <code>NULL</code>，會多出一列，屬正常現象。</p></blockquote><h3 id="高-CPU-的處置"><a href="#高-CPU-的處置" class="headerlink" title="高 CPU 的處置"></a>高 CPU 的處置</h3><ul><li><strong>貴的查詢</strong>：拿 <code>EXPLAIN (ANALYZE, BUFFERS)</code> 分析（見<a href="#%E6%85%A2%E6%9F%A5%E8%A9%A2%E5%88%86%E6%9E%90%EF%BC%9A%E8%AE%80%E6%87%82-EXPLAIN-ANALYZE-BUFFERS">慢查詢分析</a>），補索引 &#x2F; 更新統計 &#x2F; 改寫 SQL。</li><li><strong>連線太多、短連線頻繁</strong>：前面架 <strong>PgBouncer</strong> 連線池（transaction 模式），不要一味調高 <code>max_connections</code>。</li><li><strong>已知某個 PID 是元凶要處理</strong>：優先用 <code>pg_cancel_backend</code>（送 SIGINT，只取消當前查詢、保留 session），不行再用 <code>pg_terminate_backend</code>（送 SIGTERM，直接砍掉整個 backend 並 rollback）：</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pg_cancel_backend(<span class="number">12345</span>);     <span class="comment">-- 溫和：取消當前查詢</span></span><br><span class="line"><span class="keyword">SELECT</span> pg_terminate_backend(<span class="number">12345</span>);  <span class="comment">-- 強硬：砍掉整個 session（會 rollback 進行中的工作）</span></span><br></pre></td></tr></table></figure><h2 id="高記憶體排查"><a href="#高記憶體排查" class="headerlink" title="高記憶體排查"></a>高記憶體排查</h2><blockquote><p>高記憶體不像高 CPU 有那麼多現成的診斷 SQL 可以排名——它主要是「參數配置」問題（<code>work_mem</code>、<code>max_connections</code> 的乘法效應），SQL 層面能看到徵兆的只有 <code>EXPLAIN (ANALYZE, BUFFERS)</code> 裡的 sort&#x2F;hash 溢出。所以本節重點在「哪些參數會吃記憶體、怎麼避免 OOM」。</p></blockquote><h3 id="work-mem-溢出陷阱（最大的記憶體風險）"><a href="#work-mem-溢出陷阱（最大的記憶體風險）" class="headerlink" title="work_mem 溢出陷阱（最大的記憶體風險）"></a><code>work_mem</code> 溢出陷阱（最大的記憶體風險）</h3><p><code>work_mem</code> 是 <strong>每個 sort &#x2F; hash 運算、每條查詢、每個連線</strong> 各自分配的，最壞情況會相乘：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">最壞情況記憶體 = work_mem × (單條查詢的 sort/hash 運算數) × 併發連線數</span><br></pre></td></tr></table></figure><p>預設 4 MB。會吃 <code>work_mem</code> 的：<code>ORDER BY</code>、<code>DISTINCT</code>、merge join（sort）；hash join、hash aggregate、<code>IN</code> 子查詢（hash）。在 <code>max_connections</code> 很大時把 <code>work_mem</code> 全域調高，很容易乘出 OOM。<strong>正確做法是針對特定重查詢在 session &#x2F; role 層級調高</strong>，而不是全域。</p><blockquote><p><code>hash_mem_multiplier</code>（預設 2.0）會再把 hash 類運算的記憶體上限放大成 <code>work_mem × 2.0</code>。</p></blockquote><h3 id="連線太多-→-用-PgBouncer-而不是調高-max-connections"><a href="#連線太多-→-用-PgBouncer-而不是調高-max-connections" class="headerlink" title="連線太多 → 用 PgBouncer 而不是調高 max_connections"></a>連線太多 → 用 PgBouncer 而不是調高 max_connections</h3><p>PostgreSQL 是 <strong>process-per-connection</strong> 模型，每個 backend 都可能吃掉 <code>work_mem</code> &#x2F; <code>temp_buffers</code>，所以 <code>max_connections</code> 是直接的記憶體乘數。要服務大量 client，正確做法是前面架 <strong>PgBouncer</strong>（transaction pooling），讓後端只用少量連線就能撐住上萬個 client 連線。</p><p>使用 PgBouncer 時要注意 pool 模式與 prepared statement 的相容性：</p><ul><li><strong>statement pool 模式與 prepared statement 不相容</strong>。</li><li><strong>transaction 模式</strong> 只有在 <code>max_prepared_statements &gt; 0</code>（預設 0）時才支援 protocol-level 的 prepared statement。</li><li>若是用託管服務（managed service）內建的 PgBouncer，通常會另外開一個埠（常見是 <strong>6432</strong>），且不一定所有規格層都支援，實際以你的服務規格為準。</li></ul><h3 id="唯一能看到記憶體徵兆的查詢：EXPLAIN-BUFFERS"><a href="#唯一能看到記憶體徵兆的查詢：EXPLAIN-BUFFERS" class="headerlink" title="唯一能看到記憶體徵兆的查詢：EXPLAIN + BUFFERS"></a>唯一能看到記憶體徵兆的查詢：EXPLAIN + BUFFERS</h3><p>單純 <code>EXPLAIN ANALYZE</code> 看不到記憶體，要加 <code>BUFFERS</code>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN (ANALYZE, VERBOSE, BUFFERS) <span class="operator">&lt;</span>your_query<span class="operator">&gt;</span>;</span><br></pre></td></tr></table></figure><p>判讀重點在 sort 節點：</p><ul><li><code>Sort Method: quicksort  Memory: NNNkB</code> → 塞得進 <code>work_mem</code>，很好。</li><li><code>Sort Method: external merge  Disk: NNNkB</code> → <strong>溢出到硬碟</strong>，代表 <code>work_mem</code> 不夠。</li></ul><blockquote><p><strong>版本雷點</strong>：專門顯示 planner 記憶體的 <code>EXPLAIN (MEMORY)</code> 是 <strong>PG17 才有</strong>，v13–16 不能用。但每個 Sort &#x2F; Hash 節點的記憶體用量，<code>ANALYZE</code> 本來就會印出來。</p></blockquote><h3 id="相關記憶體參數"><a href="#相關記憶體參數" class="headerlink" title="相關記憶體參數"></a>相關記憶體參數</h3><table><thead><tr><th>參數</th><th>預設</th><th>說明</th></tr></thead><tbody><tr><td><code>maintenance_work_mem</code></td><td>64 MB</td><td>VACUUM &#x2F; CREATE INDEX &#x2F; ADD FK 用；可設得比 <code>work_mem</code> 大。但它是每個 autovacuum worker 的上限 → autovacuum 實際吃 <code>maintenance_work_mem × autovacuum_max_workers</code></td></tr><tr><td><code>autovacuum_work_mem</code></td><td>-1</td><td><code>-1</code> &#x3D; 繼承 <code>maintenance_work_mem</code>；<code>maintenance_work_mem</code> 設很大時，設一個明確較小值來壓 autovacuum 併發記憶體</td></tr><tr><td><code>shared_buffers</code></td><td>128 MB</td><td>全域單一分配（非每連線）；建議 ~25% RAM，別超過 ~40%；<strong>改了要重啟</strong></td></tr><tr><td><code>temp_buffers</code></td><td>8 MB</td><td>每個 session 給暫存表用；屬每連線記憶體</td></tr></tbody></table><h2 id="慢查詢分析：讀懂-EXPLAIN-ANALYZE-BUFFERS"><a href="#慢查詢分析：讀懂-EXPLAIN-ANALYZE-BUFFERS" class="headerlink" title="慢查詢分析：讀懂 EXPLAIN (ANALYZE, BUFFERS)"></a>慢查詢分析：讀懂 EXPLAIN (ANALYZE, BUFFERS)</h2><p>推薦的完整寫法：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN (ANALYZE, BUFFERS, VERBOSE, SETTINGS) <span class="operator">&lt;</span>query<span class="operator">&gt;</span>;</span><br></pre></td></tr></table></figure><ul><li>有寫入時再加 <code>WAL</code>（需搭配 <code>ANALYZE</code>）。</li><li><code>SETTINGS</code> 只印出「和預設值不同」的 planner GUC（空的代表全預設）。</li></ul><h3 id="怎麼讀輸出"><a href="#怎麼讀輸出" class="headerlink" title="怎麼讀輸出"></a>怎麼讀輸出</h3><ul><li><strong>預估值</strong>：<code>(cost=STARTUP..TOTAL rows=N width=W)</code> — cost 是 <strong>無單位</strong> 的 planner 估算值（以 <code>seq_page_cost=1.0</code> 為基準），<strong>永遠不要拿 cost 去跟實際 ms 比</strong>。想深入理解 cost 怎麼算出來，可看我的 <a href="https://isdaniel.github.io/postgresql-cost-factor-tuning/">成本因子調教</a>。</li><li><strong>實際值（ANALYZE）</strong>：<code>(actual time=STARTUP..TOTAL rows=N loops=L)</code> — 真實毫秒與執行次數。</li><li><strong>BUFFERS</strong>：<code>shared hit</code> &#x3D; 命中 cache（省了讀取）、<code>read</code> &#x3D; 從 disk 讀（miss）、<code>dirtied</code> &#x3D; 這次改髒的 block、<code>written</code> &#x3D; 被這個 backend 逐出的髒 block。</li></ul><h3 id="爛執行計畫的訊號（診斷清單）"><a href="#爛執行計畫的訊號（診斷清單）" class="headerlink" title="爛執行計畫的訊號（診斷清單）"></a>爛執行計畫的訊號（診斷清單）</h3><ol><li><strong>預估 rows vs 實際 rows 差 10～1000 倍以上</strong> — <em>最重要的訊號</em>。代表 planner 的選擇性統計爛了，通常導致選錯 join 型態（該用 hash join 卻跑 nested loop）。<strong>修法</strong>：<code>ANALYZE</code>、調高 <code>default_statistics_target</code>（或對特定欄位 <code>ALTER TABLE ... ALTER COLUMN ... SET STATISTICS</code>）、建 extended statistics。</li><li><strong><code>loops &gt; 1</code></strong> — 印出來的 <code>actual time</code> 與 <code>rows</code> 是 <strong>每次執行的平均值</strong>，要乘上 <code>loops</code> 才是真正總量。一個看起來很便宜、卻被跑一萬次的內層節點，可能才是真正的瓶頸（超級常見的誤判）。</li><li><strong><code>Sort Method: external merge  Disk: NkB</code></strong>（或 Hash 節點分成多個 batch）→ 溢出到 disk → <strong>調高 <code>work_mem</code></strong>。</li><li><strong>大表上出現 <code>Seq Scan</code> 且 <code>Rows Removed by Filter</code> 很大</strong> → 缺索引 &#x2F; 索引沒被用到。（但小表的 Seq Scan 是正確的，要看表大小 + 過濾掉的行數判斷，不是看到節點名就下結論。）</li><li><strong>BUFFERS 裡 <code>read</code> 遠大於 <code>hit</code></strong> → cache 太冷或工作集超過 <code>shared_buffers</code>。</li></ol><h3 id="對-DML-做-EXPLAIN-ANALYZE-一定要包在交易裡"><a href="#對-DML-做-EXPLAIN-ANALYZE-一定要包在交易裡" class="headerlink" title="對 DML 做 EXPLAIN ANALYZE 一定要包在交易裡"></a>對 DML 做 EXPLAIN ANALYZE 一定要包在交易裡</h3><p><code>EXPLAIN ANALYZE</code> 會 <strong>真的執行</strong> 語句（含 INSERT&#x2F;UPDATE&#x2F;DELETE&#x2F;DDL 的副作用！），所以分析寫入語句時務必包起來：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">BEGIN</span>;</span><br><span class="line">EXPLAIN (ANALYZE, BUFFERS, WAL) <span class="keyword">DELETE</span> <span class="keyword">FROM</span> tenk1 <span class="keyword">WHERE</span> unique1 <span class="operator">&lt;</span> <span class="number">100</span>;</span><br><span class="line"><span class="keyword">ROLLBACK</span>;</span><br></pre></td></tr></table></figure><h3 id="auto-explain：抓那些「當下重現不了」的慢查詢"><a href="#auto-explain：抓那些「當下重現不了」的慢查詢" class="headerlink" title="auto_explain：抓那些「當下重現不了」的慢查詢"></a>auto_explain：抓那些「當下重現不了」的慢查詢</h3><p>正式環境裡有些慢查詢你無法在 psql 手動重現，這時用 <code>auto_explain</code> 自動把超過門檻的查詢計畫寫進 log：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># postgresql.conf</span></span><br><span class="line">shared_preload_libraries = <span class="string">&#x27;auto_explain&#x27;</span></span><br><span class="line">auto_explain.log_min_duration = 0      <span class="comment"># 記錄的最小執行時間 (ms)，0 = 全記錄</span></span><br><span class="line">auto_explain.log_analyze = <span class="literal">true</span>        <span class="comment"># 記錄 EXPLAIN ANALYZE (false 只會是 ANALYZE)</span></span><br><span class="line">auto_explain.log_buffers = <span class="literal">true</span>        <span class="comment"># 需要 log_analyze = true 才有用</span></span><br><span class="line">auto_explain.log_timing = <span class="literal">true</span></span><br><span class="line">auto_explain.log_nested_statements = <span class="literal">true</span></span><br></pre></td></tr></table></figure><blockquote><p>正式環境請 <strong>不要</strong> 開 <code>log_statement</code> &#x2F; <code>log_duration</code> 去記全部語句，那會把 log 灌爆；用 <code>auto_explain</code> 針對慢查詢才對。</p></blockquote><h2 id="鎖與-Blocking-排查"><a href="#鎖與-Blocking-排查" class="headerlink" title="鎖與 Blocking 排查"></a>鎖與 Blocking 排查</h2><p>當 <a href="#%E6%8E%92%E6%9F%A5%E6%96%B9%E6%B3%95%E8%AB%96%EF%BC%9A%E4%B8%89%E5%B1%A4%E7%94%B1%E4%B8%8A%E8%80%8C%E4%B8%8B%E5%AE%9A%E4%BD%8D">方法論</a> 那條快照查詢顯示 <code>Lock/Contention</code> 一堆時，就進來這一節。關於各種 lock 模式的互斥對照，可以搭配我的 <a href="https://isdaniel.github.io/dblock-1/">dblock 系列</a> 一起看。</p><h3 id="快速看誰被鎖住"><a href="#快速看誰被鎖住" class="headerlink" title="快速看誰被鎖住"></a>快速看誰被鎖住</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid, wait_event_type, wait_event</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> wait_event_type <span class="operator">=</span> <span class="string">&#x27;Lock&#x27;</span>;</span><br></pre></td></tr></table></figure><h3 id="Blocking-樹：被鎖的-backend-↔-鎖住它的-backend"><a href="#Blocking-樹：被鎖的-backend-↔-鎖住它的-backend" class="headerlink" title="Blocking 樹：被鎖的 backend ↔ 鎖住它的 backend"></a>Blocking 樹：被鎖的 backend ↔ 鎖住它的 backend</h3><p><code>pg_blocking_pids()</code>（PG9.6+）是關鍵，直接把「誰被誰擋住」攤開：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> blocked.pid              <span class="keyword">AS</span> blocked_pid,</span><br><span class="line">       blocked.usename          <span class="keyword">AS</span> blocked_user,</span><br><span class="line">       blocked.query            <span class="keyword">AS</span> blocked_query,</span><br><span class="line">       blocking.pid             <span class="keyword">AS</span> blocking_pid,</span><br><span class="line">       blocking.usename         <span class="keyword">AS</span> blocking_user,</span><br><span class="line">       blocking.state           <span class="keyword">AS</span> blocking_state,</span><br><span class="line">       blocking.query           <span class="keyword">AS</span> blocking_query,</span><br><span class="line">       now() <span class="operator">-</span> blocking.xact_start <span class="keyword">AS</span> blocking_xact_age</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity <span class="keyword">AS</span> blocked</span><br><span class="line"><span class="keyword">JOIN</span> <span class="keyword">LATERAL</span> <span class="built_in">unnest</span>(pg_blocking_pids(blocked.pid)) <span class="keyword">AS</span> b(blocking_pid) <span class="keyword">ON</span> <span class="literal">true</span></span><br><span class="line"><span class="keyword">JOIN</span> pg_stat_activity <span class="keyword">AS</span> blocking <span class="keyword">ON</span> blocking.pid <span class="operator">=</span> b.blocking_pid</span><br><span class="line"><span class="keyword">WHERE</span> blocked.wait_event_type <span class="operator">=</span> <span class="string">&#x27;Lock&#x27;</span>;</span><br></pre></td></tr></table></figure><h3 id="直接看-pg-locks（過濾未授予的鎖-正在等的）"><a href="#直接看-pg-locks（過濾未授予的鎖-正在等的）" class="headerlink" title="直接看 pg_locks（過濾未授予的鎖 &#x3D; 正在等的）"></a>直接看 pg_locks（過濾未授予的鎖 &#x3D; 正在等的）</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> l.pid, l.locktype, l.mode, l.granted,</span><br><span class="line">       l.relation::regclass <span class="keyword">AS</span> relation,</span><br><span class="line">       a.state, a.query</span><br><span class="line"><span class="keyword">FROM</span> pg_locks l</span><br><span class="line"><span class="keyword">JOIN</span> pg_stat_activity a <span class="keyword">ON</span> a.pid <span class="operator">=</span> l.pid</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">NOT</span> l.granted</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> l.pid;</span><br></pre></td></tr></table></figure><h3 id="揪出-idle-in-transaction-元凶"><a href="#揪出-idle-in-transaction-元凶" class="headerlink" title="揪出 idle-in-transaction 元凶"></a>揪出 idle-in-transaction 元凶</h3><p>這類 session 開著交易不 commit，會持鎖 + 釘住 xmin horizon 擋住 VACUUM 清理，<code>backend_xmin</code> 越舊問題越大：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid,</span><br><span class="line">       state,</span><br><span class="line">       now() <span class="operator">-</span> xact_start   <span class="keyword">AS</span> xact_age,</span><br><span class="line">       now() <span class="operator">-</span> state_change <span class="keyword">AS</span> idle_duration,</span><br><span class="line">       wait_event_type,</span><br><span class="line">       wait_event,</span><br><span class="line">       backend_xmin,</span><br><span class="line">       query <span class="keyword">AS</span> last_query</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_activity</span><br><span class="line"><span class="keyword">WHERE</span> state <span class="keyword">IN</span> (<span class="string">&#x27;idle in transaction&#x27;</span>, <span class="string">&#x27;idle in transaction (aborted)&#x27;</span>)</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> xact_start <span class="keyword">ASC</span>;</span><br></pre></td></tr></table></figure><p><strong>處置</strong>：設 <code>idle_in_transaction_session_timeout</code>（例如 <code>&#39;5min&#39;</code>，預設 0 &#x3D; 停用）自動終止這類 session；或對特定 PID 用 <code>pg_terminate_backend(pid)</code>。另外 <code>deadlock_timeout</code>（預設 1s）控制 backend 等多久才觸發死鎖偵測，也決定 <code>log_lock_waits = on</code> 何時記錄 Lock 等待。</p><h2 id="Cache-hit-ratio-與-I-O-排查"><a href="#Cache-hit-ratio-與-I-O-排查" class="headerlink" title="Cache hit ratio 與 I&#x2F;O 排查"></a>Cache hit ratio 與 I&#x2F;O 排查</h2><blockquote><p>這裡的 “read” 指「不在 <code>shared_buffers</code>」，它仍可能命中 OS page cache，所以 ratio 沒到 0.99 不代表一定有物理磁碟 I&#x2F;O。健康的 OLTP 通常 &gt; 0.99。</p></blockquote><blockquote><p><strong>注意整數除法陷阱</strong>：社群常見的 cache-hit 片段常直接 <code>bigint / bigint</code>，這是 <strong>整數除法</strong>，結果會被截成 <code>0</code>。下面全部都有 <code>::numeric</code> 轉型。</p></blockquote><h3 id="資料庫層級-cache-命中率"><a href="#資料庫層級-cache-命中率" class="headerlink" title="資料庫層級 cache 命中率"></a>資料庫層級 cache 命中率</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    datname,</span><br><span class="line">    blks_read,</span><br><span class="line">    blks_hit,</span><br><span class="line">    round(blks_hit::<span class="type">numeric</span> <span class="operator">/</span> <span class="built_in">nullif</span>(blks_hit <span class="operator">+</span> blks_read, <span class="number">0</span>), <span class="number">4</span>) <span class="keyword">AS</span> cache_hit_ratio</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_database</span><br><span class="line"><span class="keyword">WHERE</span> datname <span class="operator">=</span> current_database();</span><br></pre></td></tr></table></figure><h3 id="Heap-Index-命中率"><a href="#Heap-Index-命中率" class="headerlink" title="Heap &#x2F; Index 命中率"></a>Heap &#x2F; Index 命中率</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Heap (資料表本體)</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    <span class="built_in">sum</span>(heap_blks_read) <span class="keyword">AS</span> heap_read,</span><br><span class="line">    <span class="built_in">sum</span>(heap_blks_hit)  <span class="keyword">AS</span> heap_hit,</span><br><span class="line">    <span class="built_in">sum</span>(heap_blks_hit)::<span class="type">numeric</span></span><br><span class="line">        <span class="operator">/</span> <span class="built_in">nullif</span>(<span class="built_in">sum</span>(heap_blks_hit) <span class="operator">+</span> <span class="built_in">sum</span>(heap_blks_read), <span class="number">0</span>) <span class="keyword">AS</span> ratio</span><br><span class="line"><span class="keyword">FROM</span> pg_statio_user_tables;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Index</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    <span class="built_in">sum</span>(idx_blks_read) <span class="keyword">AS</span> idx_read,</span><br><span class="line">    <span class="built_in">sum</span>(idx_blks_hit)  <span class="keyword">AS</span> idx_hit,</span><br><span class="line">    <span class="built_in">sum</span>(idx_blks_hit)::<span class="type">numeric</span></span><br><span class="line">      <span class="operator">/</span> <span class="built_in">nullif</span>(<span class="built_in">sum</span>(idx_blks_hit) <span class="operator">+</span> <span class="built_in">sum</span>(idx_blks_read), <span class="number">0</span>) <span class="keyword">AS</span> ratio</span><br><span class="line"><span class="keyword">FROM</span> pg_statio_user_indexes;</span><br></pre></td></tr></table></figure><p>命中率長期偏低 → 工作集超過 <code>shared_buffers</code> → 調高 <code>shared_buffers</code>（~25% RAM）並把 <code>effective_cache_size</code> 設實際一點，讓 planner 願意選 index scan。</p><h3 id="逐條查詢的-I-O-元凶"><a href="#逐條查詢的-I-O-元凶" class="headerlink" title="逐條查詢的 I&#x2F;O 元凶"></a>逐條查詢的 I&#x2F;O 元凶</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> query, calls, shared_blks_read, shared_blks_hit,</span><br><span class="line">       shared_blks_dirtied, shared_blks_written,</span><br><span class="line">       temp_blks_read, temp_blks_written</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_statements</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> shared_blks_read <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p><code>shared_blks_read</code> 高 &#x3D; cache miss 多；<code>temp_blks_*</code> 高 &#x3D; <code>work_mem</code> 溢出到 disk。</p><blockquote><p><strong>版本雷點</strong>：想用 I&#x2F;O <strong>時間</strong> 排序（需 <code>track_io_timing = on</code>），欄位名稱依版本不同：v13–16 是 <code>blk_read_time</code> &#x2F; <code>blk_write_time</code>；PG17+ 改名成 <code>shared_blk_read_time</code> &#x2F; <code>shared_blk_write_time</code>。v13–16 請用前者：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> query, calls, blk_read_time, blk_write_time,</span><br><span class="line">       blk_read_time <span class="operator">+</span> blk_write_time <span class="keyword">AS</span> total_io_time</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_statements</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> total_io_time <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure></blockquote><h3 id="找沒被用到的索引（浪費寫入與記憶體）"><a href="#找沒被用到的索引（浪費寫入與記憶體）" class="headerlink" title="找沒被用到的索引（浪費寫入與記憶體）"></a>找沒被用到的索引（浪費寫入與記憶體）</h3><p>沒用到的索引會拖慢寫入、佔記憶體、還會排擠 cache。<code>idx_scan = 0</code> 就是候選：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    n.nspname                                      <span class="keyword">AS</span> schemaname,</span><br><span class="line">    c.relname                                      <span class="keyword">AS</span> tablename,</span><br><span class="line">    c.reltuples::<span class="type">bigint</span>                            <span class="keyword">AS</span> num_rows,</span><br><span class="line">    pg_size_pretty(pg_relation_size(c.oid))        <span class="keyword">AS</span> table_size,</span><br><span class="line">    psai.indexrelname                              <span class="keyword">AS</span> index_name,</span><br><span class="line">    pg_size_pretty(pg_relation_size(i.indexrelid)) <span class="keyword">AS</span> index_size,</span><br><span class="line">    <span class="keyword">CASE</span> <span class="keyword">WHEN</span> i.indisunique <span class="keyword">THEN</span> <span class="string">&#x27;Y&#x27;</span> <span class="keyword">ELSE</span> <span class="string">&#x27;N&#x27;</span> <span class="keyword">END</span>  <span class="keyword">AS</span> &quot;unique&quot;,</span><br><span class="line">    psai.idx_scan                                  <span class="keyword">AS</span> number_of_scans</span><br><span class="line"><span class="keyword">FROM</span> pg_class c</span><br><span class="line">    <span class="keyword">JOIN</span> pg_namespace n <span class="keyword">ON</span> n.oid <span class="operator">=</span> c.relnamespace</span><br><span class="line">    <span class="keyword">JOIN</span> pg_index i <span class="keyword">ON</span> c.oid <span class="operator">=</span> i.indrelid</span><br><span class="line">    <span class="keyword">JOIN</span> pg_stat_all_indexes psai <span class="keyword">ON</span> i.indexrelid <span class="operator">=</span> psai.indexrelid</span><br><span class="line"><span class="keyword">WHERE</span> c.relkind <span class="keyword">IN</span> (<span class="string">&#x27;r&#x27;</span>, <span class="string">&#x27;p&#x27;</span>)</span><br><span class="line">  <span class="keyword">AND</span> n.nspname <span class="keyword">NOT</span> <span class="keyword">IN</span> (<span class="string">&#x27;pg_catalog&#x27;</span>, <span class="string">&#x27;information_schema&#x27;</span>)</span><br><span class="line">  <span class="comment">-- AND psai.idx_scan = 0        -- 取消註解可只看「完全沒用到」的索引</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> <span class="number">1</span>, <span class="number">2</span>;</span><br></pre></td></tr></table></figure><blockquote><p><strong>刪索引前務必注意</strong>：這些統計是從上次 <code>pg_stat_reset</code> 起才累積的；replica 上的 <code>idx_scan</code> 是分開計數的（主庫沒用到的索引，可能在讀取副本上有在用）；<strong>絕對不要</strong>因此刪掉 unique &#x2F; PK &#x2F; 約束用的索引。</p></blockquote><h2 id="表膨脹與-Autovacuum-排查"><a href="#表膨脹與-Autovacuum-排查" class="headerlink" title="表膨脹與 Autovacuum 排查"></a>表膨脹與 Autovacuum 排查</h2><p>統計資訊過期與 dead tuple 膨脹會拖爛執行計畫、抬高 CPU。原理與觸發時機我在 <a href="https://isdaniel.github.io/postgresql-autovacuum/">Postgresql AutoVacuum 介紹</a> 有完整說明，這裡給排查用的查詢。</p><h3 id="Dead-tuple-膨脹排名"><a href="#Dead-tuple-膨脹排名" class="headerlink" title="Dead tuple &#x2F; 膨脹排名"></a>Dead tuple &#x2F; 膨脹排名</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    schemaname,</span><br><span class="line">    relname,</span><br><span class="line">    n_live_tup,</span><br><span class="line">    n_dead_tup,</span><br><span class="line">    <span class="keyword">CASE</span> <span class="keyword">WHEN</span> n_live_tup <span class="operator">&gt;</span> <span class="number">0</span></span><br><span class="line">         <span class="keyword">THEN</span> round(n_dead_tup::<span class="type">numeric</span> <span class="operator">/</span> n_live_tup, <span class="number">4</span>)</span><br><span class="line">         <span class="keyword">ELSE</span> <span class="keyword">NULL</span></span><br><span class="line">    <span class="keyword">END</span> <span class="keyword">AS</span> dead_tuple_ratio,</span><br><span class="line">    last_vacuum,</span><br><span class="line">    last_autovacuum,</span><br><span class="line">    last_analyze,</span><br><span class="line">    last_autoanalyze</span><br><span class="line"><span class="keyword">FROM</span> pg_stat_user_tables</span><br><span class="line"><span class="keyword">WHERE</span> n_dead_tup <span class="operator">&gt;</span> <span class="number">0</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> n_dead_tup <span class="keyword">DESC</span>, dead_tuple_ratio <span class="keyword">DESC</span> <span class="keyword">NULLS LAST</span>;</span><br></pre></td></tr></table></figure><p>判讀：<code>n_dead_tup</code> 大、<code>dead_tuple_ratio</code> 高，且 <code>last_autovacuum</code> 很舊或空 → autovacuum 在這張熱表上沒跟上。</p><p><strong>短期手動修</strong>（語法所有版本通用）：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">VACUUM ANALYZE <span class="operator">&lt;</span><span class="keyword">table</span><span class="operator">&gt;</span>;</span><br></pre></td></tr></table></figure><p><strong>Autovacuum 關鍵參數</strong>：</p><table><thead><tr><th>參數</th><th>預設</th><th>建議</th></tr></thead><tbody><tr><td><code>autovacuum</code></td><td>on</td><td>保持開啟</td></tr><tr><td><code>autovacuum_vacuum_scale_factor</code></td><td>0.2</td><td>大熱表調到 ~0.05–0.1（固定 20% 對大表太粗，資料量越大越難觸發）</td></tr><tr><td><code>autovacuum_max_workers</code></td><td>3</td><td>會乘上 autovacuum 總記憶體（見 <code>maintenance_work_mem</code>）</td></tr><tr><td><code>autovacuum_vacuum_cost_delay</code> &#x2F; <code>_cost_limit</code></td><td>2ms &#x2F; -1(→200)</td><td>寫入量大時降低 delay &#x2F; 提高 limit，讓 autovacuum 跟得上</td></tr></tbody></table><blockquote><p>也可以 <strong>針對單張表</strong> 設定閾值，例如 <code>ALTER TABLE t SET (autovacuum_vacuum_scale_factor = 0.02);</code>，細節見 autovacuum 那篇。</p></blockquote><h2 id="關鍵設定參數速查表"><a href="#關鍵設定參數速查表" class="headerlink" title="關鍵設定參數速查表"></a>關鍵設定參數速查表</h2><p>下表預設值為 PostgreSQL 上游預設；若跑在託管服務或雲端，實際值通常會依機器規格（vCore &#x2F; 記憶體）自動調整，以你環境查到的 <code>SHOW &lt;參數&gt;;</code> 為準。</p><table><thead><tr><th>參數</th><th>預設</th><th>作用 &#x2F; 影響</th><th>建議</th><th>需重啟</th></tr></thead><tbody><tr><td><code>shared_buffers</code></td><td>128 MB</td><td>全域 buffer cache（單一分配），決定 cache-hit ratio</td><td>~25% RAM，別超過 ~40%</td><td><strong>是</strong></td></tr><tr><td><code>work_mem</code></td><td>4 MB</td><td><strong>每 sort&#x2F;hash 運算、每查詢、每連線</strong> 溢出前的記憶體</td><td>最壞 &#x3D; <code>work_mem × ops × connections</code>；針對重查詢在 session&#x2F;role 層調高，別全域</td><td>否</td></tr><tr><td><code>hash_mem_multiplier</code></td><td>2.0</td><td>hash 類運算的 <code>work_mem</code> 倍數</td><td>調高可減少 hash 溢出，但也放大 work_mem 風險</td><td>否</td></tr><tr><td><code>effective_cache_size</code></td><td>4 GB</td><td><strong>純 planner 提示</strong>（不分配任何記憶體），影響是否偏好 index scan</td><td>專用機設 ~50–75% RAM</td><td>否</td></tr><tr><td><code>maintenance_work_mem</code></td><td>64 MB</td><td>VACUUM &#x2F; CREATE INDEX &#x2F; ADD FK</td><td>可比 <code>work_mem</code> 大；乘上 <code>autovacuum_max_workers</code> 才是 autovacuum 總量</td><td>否</td></tr><tr><td><code>max_connections</code></td><td>100</td><td>最大併發連線，<strong>直接的記憶體乘數</strong></td><td>別靠調高它服務大量 client，前面架 <strong>PgBouncer</strong></td><td><strong>是</strong></td></tr><tr><td><code>random_page_cost</code></td><td>4.0</td><td>成本模型：隨機讀一頁的相對成本（相對 <code>seq_page_cost=1.0</code>）</td><td>SSD &#x2F; cache 熱的工作負載調到 ~1.1，讓 planner 偏好 index scan</td><td>否 (reload)</td></tr><tr><td><code>default_statistics_target</code></td><td>100</td><td><code>ANALYZE</code> 收集的欄位統計解析度</td><td>預估與實際 rows 差很多時調高並重新 <code>ANALYZE</code></td><td>否</td></tr><tr><td><code>track_io_timing</code></td><td>off</td><td>開啟 block I&#x2F;O <strong>時間</strong> 統計</td><td>要分辨 CPU-bound vs I&#x2F;O-bound 時開；有開銷</td><td>否 (reload)</td></tr><tr><td><code>idle_in_transaction_session_timeout</code></td><td>0 (關)</td><td>自動終止卡在 idle-in-transaction 的 session</td><td>設 <code>&#39;5min&#39;</code> 避免它持鎖 &#x2F; 釘住 xmin 擋 VACUUM</td><td>否</td></tr></tbody></table><blockquote><p><code>pg_stat_statements.max</code>（預設 5000）：超過時會淘汰最少執行的項目，長尾查詢會悄悄消失，改它要 <strong>重啟</strong>；<code>.track = all</code> 可看到 PL&#x2F;pgSQL 函式內部的查詢；<code>.track_planning</code>（預設 off）開了才有 plan-time 欄位（有額外開銷）。</p></blockquote><h2 id="小結"><a href="#小結" class="headerlink" title="小結"></a>小結</h2><p>整套排查手冊的核心就一句話：<strong>由上而下，先分類再定位</strong>。</p><ol><li><strong>Tier 1</strong>：看 OS &#x2F; 實例指標，確定是哪個資源、哪個時間點。</li><li><strong>Tier 2</strong>：用 <code>pg_stat_activity</code> 的 <code>wait_event_type</code> 把 backend 分桶（CPU &#x2F; Lock &#x2F; IO），決定往哪個方向查——這一步最省時間，卻最常被跳過。</li><li><strong>Tier 3</strong>：用 <code>pg_stat_statements</code> 排名找元凶，再用 <code>EXPLAIN (ANALYZE, BUFFERS)</code> 針對單條查詢驗證。</li></ol><p>幾個最容易踩的雷再提醒一次：</p><ul><li><code>wait_event</code> 是 <strong>瞬時取樣</strong>，要看趨勢得反覆取樣。</li><li><code>EXPLAIN ANALYZE</code> 讀 <code>loops</code> 時記得 <strong>actual time &#x2F; rows 是每次平均</strong>，要乘 <code>loops</code>。</li><li><code>EXPLAIN ANALYZE</code> 對 DML <strong>會真的執行</strong>，一定包 <code>BEGIN ... ROLLBACK</code>。</li><li>PG13 之後 <code>pg_stat_statements</code> 用 <code>total_exec_time</code>（不是 <code>total_time</code>）；<code>pg_wait_events</code>、<code>EXPLAIN (MEMORY)</code> 是 PG17 才有。</li><li>調 <code>work_mem</code> 前先想清楚 <strong>× ops × connections</strong> 的乘法，避免 OOM。</li></ul><p>把這幾條查詢存成書籤，下次線上出事時就能照著症狀一路查下去，而不是憑感覺瞎猜。</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a target="_blank" rel="noopener" href="https://learn.microsoft.com/en-us/azure/postgresql/troubleshoot/how-to-high-cpu-utilization?tabs=mean-postgres13,total-postgres13">Troubleshoot high CPU utilization (Microsoft Learn)</a></li><li><a target="_blank" rel="noopener" href="https://learn.microsoft.com/zh-tw/azure/postgresql/troubleshoot/how-to-high-memory-utilization">Troubleshoot high memory utilization (Microsoft Learn)</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/pgstatstatements.html">PostgreSQL Docs — pg_stat_statements</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/monitoring-stats.html">PostgreSQL Docs — Monitoring Statistics (pg_stat_activity &#x2F; wait events)</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/using-explain.html">PostgreSQL Docs — Using EXPLAIN</a></li><li><a target="_blank" rel="noopener" href="https://wiki.postgresql.org/wiki/Index_Maintenance">PostgreSQL Wiki — Index Maintenance</a></li><li><a target="_blank" rel="noopener" href="https://wiki.postgresql.org/wiki/Lock_Monitoring">PostgreSQL Wiki — Lock Monitoring</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/postgresql-performance-troubleshooting-playbook/">https://isdaniel.github.io/postgresql-performance-troubleshooting-playbook/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/postgresql-performance-troubleshooting-playbook/</id>
    <link href="https://isdaniel.github.io/postgresql-performance-troubleshooting-playbook/"/>
    <published>2026-07-31T13:30:00.000Z</published>
    <summary>一套可實作的 PostgreSQL 效能排查：用 wait event、pg_stat_statements、EXPLAIN 由上而下定位高 CPU、高記憶體、慢查詢、鎖等待與表膨脹問題，每個症狀都附上可直接執行的 SQL。</summary>
    <title>PostgreSQL 效能調校實戰：從高 CPU、高記憶體到慢查詢的排查</title>
    <updated>2026-08-05T10:10:26.492Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/pgrx/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="SystemDesign" scheme="https://isdaniel.github.io/tags/SystemDesign/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/tags/pgrx/"/>
    <category term="Datum" scheme="https://isdaniel.github.io/tags/Datum/"/>
    <category term="FFI" scheme="https://isdaniel.github.io/tags/FFI/"/>
    <content>
      <![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>PostgreSQL 是 C 寫的，它不認識 Rust。但 <a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx</a> 卻能讓你只寫三行 Rust，就得到一個和原生 C extension 同等地位的 SQL 函式。這中間到底發生了什麼？</p><p>本文用一個最簡單的 <code>#[pg_extern]</code> 函式當主角，一路追到 pgrx 最底層的 ABI trait，把「Rust 型別 ↔ PostgreSQL Datum」的完整轉換機制講清楚。讀完你會理解 pgrx 的四個核心 trait（<code>FromDatum</code> &#x2F; <code>IntoDatum</code> &#x2F; <code>ArgAbi</code> &#x2F; <code>RetAbi</code>）各自的職責，以及巨集生成的 wrapper 是怎麼把它們串起來的。</p><blockquote><p>對照的原始碼版本：<strong>pgrx 0.19.1</strong>。行號可能隨版本變動，但結構穩定。</p><p>這篇是我先前 <a href="https://isdaniel.github.io/pgrx-postgresql-extension-mechanism-deep-dive/">《深入 pgrx 機制：用 Rust 撰寫 PostgreSQL Extension 的底層原理與實戰》</a> 的延伸：前一篇談的是「Rust 怎麼跟 C 寫的 PostgreSQL 互通」（ABI、Magic Block、bindgen），這篇則聚焦「一個值怎麼在函式邊界進出」。</p></blockquote><hr><h2 id="一、心智模型：Datum-是一個「萬用信封」"><a href="#一、心智模型：Datum-是一個「萬用信封」" class="headerlink" title="一、心智模型：Datum 是一個「萬用信封」"></a>一、心智模型：Datum 是一個「萬用信封」</h2><p>PostgreSQL 在函式邊界傳遞的唯一貨幣是 <strong>Datum</strong>——可以想成一個「8 bytes 的萬用信封」：</p><ul><li>型別很小（<code>int4</code>）：信封裡直接裝著值。</li><li>型別很大（<code>text</code>、<code>array</code>、<code>jsonb</code>）：信封裡裝的是一個<strong>指標</strong>，指向記憶體某處真正的資料。</li></ul><p>PG 呼叫你的函式時，遞給你一疊信封（包在 <code>FunctionCallInfo</code> 裡）；你算完後，也要把答案塞回一個信封還給它。</p><p><strong>pgrx 做的全部事情</strong>，就是幫你自動完成兩個方向的信封拆裝：</p><ol><li><strong>拆信封</strong>：Datum → 好用的 Rust 型別（<code>&amp;str</code>）</li><li><strong>裝信封</strong>：Rust 答案（<code>String</code>）→ Datum</li></ol><p>你寫的業務邏輯夾在中間，乾乾淨淨完全不碰信封。這就是 pgrx 的價值。</p><hr><h2 id="二、主角登場"><a href="#二、主角登場" class="headerlink" title="二、主角登場"></a>二、主角登場</h2><p>以 <code>pgrx-examples/strings/src/lib.rs</code> 裡最單純的函式為例：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_extern]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">to_lowercase</span>(input: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> <span class="type">String</span> &#123;</span><br><span class="line">    input.<span class="title function_ invoke__">to_lowercase</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 SQL 端：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> to_lowercase(<span class="string">&#x27;HELLO&#x27;</span>);   <span class="comment">-- =&gt; &#x27;hello&#x27;</span></span><br></pre></td></tr></table></figure><p>它剛好同時用到「參數拆信封」與「回傳裝信封」兩個方向，是完美的教材。整條呼叫鏈的全景：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">psql: <span class="keyword">SELECT</span> to_lowercase(<span class="string">&#x27;HELLO&#x27;</span>);</span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">① PostgreSQL 執行器</span><br><span class="line">   準備好 FunctionCallInfo（那疊信封），呼叫 C ABI 函式指標</span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">② to_lowercase_wrapper()   ← #[pg_extern] 巨集「自動生成」的那層</span><br><span class="line">   - 從 fcinfo 拿出第 <span class="number">0</span> 個 Datum</span><br><span class="line">   - 【拆信封】Datum ──► &amp;str</span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">③ 你手寫的 to_lowercase(<span class="keyword">input</span>: &amp;str) -&gt; String</span><br><span class="line">        <span class="keyword">input</span>.to_lowercase()              （純 Rust，沒 PG 的事）</span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">② 回到 <span class="keyword">wrapper</span></span><br><span class="line">   - 【裝信封】String ──► Datum</span><br><span class="line">   - <span class="keyword">return</span> Datum 給 PostgreSQL</span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">① PostgreSQL 拿到結果 → <span class="string">&#x27;hello&#x27;</span></span><br></pre></td></tr></table></figure><p>三個角色：<strong>① PG 執行器</strong>（別人的）、<strong>② wrapper</strong>（巨集生的，你看不到但很重要）、<strong>③ 你的函式</strong>。以下逐段拆解。</p><hr><h2 id="三、-pg-extern-巨集生成了什麼"><a href="#三、-pg-extern-巨集生成了什麼" class="headerlink" title="三、#[pg_extern] 巨集生成了什麼"></a>三、<code>#[pg_extern]</code> 巨集生成了什麼</h2><p>你只寫了 3 行，<code>#[pg_extern]</code>（一個 proc-macro）在<strong>編譯時</strong>幫你多生成一個「翻譯官」wrapper 函式。為什麼需要它？因為 PG 只會呼叫「長得像 C 函式、吃 <code>fcinfo</code> 吐 <code>Datum</code>」的東西，你的 <code>fn(&amp;str) -&gt; String</code> 簽名 PG 根本沒辦法直接呼叫。</p><p>真實生成的 wrapper 核心（<code>pgrx-sql-entity-graph/src/pg_extern/mod.rs</code>，已簡化排版）：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">_internal_wrapper</span>&lt;<span class="string">&#x27;fcx&gt;(fcinfo: &amp;mut FcInfo&lt;&#x27;</span>fcx&gt;) <span class="punctuation">-&gt;</span> Datum&lt;<span class="string">&#x27;fcx&gt; &#123;</span></span><br><span class="line"><span class="string">    unsafe &#123;</span></span><br><span class="line"><span class="string">        // 問回傳型別：這次該「跑函式」還是「恢復 SRF 狀態」？</span></span><br><span class="line"><span class="string">        let call_flow = &lt;RetTy as RetAbi&gt;::check_and_prepare(fcinfo);</span></span><br><span class="line"><span class="string">        let result = match call_flow &#123;</span></span><br><span class="line"><span class="string">            CallCx::WrappedFn(mcx) =&gt; &#123;</span></span><br><span class="line"><span class="string">                let mut mcx = PgMemoryContexts::For(mcx);</span></span><br><span class="line"><span class="string">                let _args = &amp;mut fcinfo.args();</span></span><br><span class="line"><span class="string">                let call_result = mcx.switch_to(|_| &#123;</span></span><br><span class="line"><span class="string">                    // ← 逐參數拆信封（見第六節）</span></span><br><span class="line"><span class="string">                    // ← 呼叫你的函式</span></span><br><span class="line"><span class="string">                &#125;);</span></span><br><span class="line"><span class="string">                RetAbi::to_ret(call_result)</span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string">            CallCx::RestoreCx =&gt; &lt;RetTy as RetAbi&gt;::ret_from_fcx(fcinfo),</span></span><br><span class="line"><span class="string">        &#125;;</span></span><br><span class="line"><span class="string">        &lt;RetTy as RetAbi&gt;::box_ret_in(fcinfo, result) // ← 裝信封（見第四節）</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>動手驗證</strong>：<code>cargo install cargo-expand</code>，在 <code>pgrx-examples/strings/</code> 跑 <code>cargo expand</code>，就能親眼看到這個 wrapper 被生出來。這是理解 pgrx 最有價值的一步。</p></blockquote><p>除了生 wrapper，<code>#[pg_extern]</code> 還記錄「這個函式的 SQL 長相」，之後 <code>cargo pgrx schema</code> 用它產生 <code>CREATE FUNCTION to_lowercase(text) RETURNS text ...</code>。這部分屬於 <code>pgrx-sql-entity-graph</code>，本文不展開。</p><hr><h2 id="四、拆信封：FromDatum"><a href="#四、拆信封：FromDatum" class="headerlink" title="四、拆信封：FromDatum"></a>四、拆信封：<code>FromDatum</code></h2><p><code>FromDatum</code>（<code>pgrx/src/datum/from.rs</code>）是「值轉換」層，核心一個方法：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">trait</span> <span class="title class_">FromDatum</span>: <span class="built_in">Sized</span> &#123;</span><br><span class="line">    <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">from_polymorphic_datum</span>(</span><br><span class="line">        datum: pg_sys::Datum,   <span class="comment">// 信封</span></span><br><span class="line">        is_null: <span class="type">bool</span>,          <span class="comment">// 是不是 SQL NULL</span></span><br><span class="line">        typoid: pg_sys::Oid,    <span class="comment">// 型別 OID（多型才需要）</span></span><br><span class="line">    ) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;<span class="keyword">Self</span>&gt;;          <span class="comment">// 拆出來的 Rust 值；NULL → None</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>「給我一個信封 + 它是不是 NULL，我還你 <code>Option&lt;你的型別&gt;</code>。」<code>None</code> 就代表 SQL 的 <code>NULL</code>——這是 pgrx 處理 NULL 的統一手法。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">impl</span>&lt;<span class="string">&#x27;a&gt; FromDatum for &amp;&#x27;</span>a <span class="type">str</span> &#123;</span><br><span class="line">    <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">from_polymorphic_datum</span>(datum, is_null, _) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;&amp;<span class="string">&#x27;a str&gt; &#123;</span></span><br><span class="line"><span class="string">        if is_null || datum.is_null() &#123;</span></span><br><span class="line"><span class="string">            None                              // NULL → None</span></span><br><span class="line"><span class="string">        &#125; else &#123;</span></span><br><span class="line"><span class="string">            // text 是變長型別(varlena)，可能被壓縮或搬到 TOAST 表，</span></span><br><span class="line"><span class="string">            // pg_detoast_datum_packed 把它「解壓 / 拉回來」</span></span><br><span class="line"><span class="string">            let varlena = pg_sys::pg_detoast_datum_packed(datum.cast_mut_ptr());</span></span><br><span class="line"><span class="string">            // 把 varlena 內容當 UTF-8 包成 &amp;str（不複製，直接借用 PG 的記憶體）</span></span><br><span class="line"><span class="string">            Some(convert_varlena_to_str_memoized(varlena))</span></span><br><span class="line"><span class="string">        &#125;</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure><p>這裡藏著三個必懂的 PostgreSQL 機制：</p><ol><li><strong>varlena</strong>：<code>text</code> &#x2F; <code>bytea</code> &#x2F; <code>array</code> 這種變長型別，記憶體佈局是「4-byte 長度標頭 + 資料」。信封裡的指標指向的就是這個結構。</li><li><strong>TOAST &#x2F; detoast</strong>：字串很大時 PG 會壓縮、甚至存到另一張表。所以不能直接讀信封裡的指標，必須先 <code>detoast</code> 還原。這就是為什麼 <code>int4</code> 的 <code>FromDatum</code> 一行搞定，而 <code>&amp;str</code> 要多這一步。（想深入 varlena &#x2F; TOAST 的記憶體佈局，可參考我另一篇 <a href="https://isdaniel.github.io/postgresql-varlena-toast-deep-dive/">《PostgreSQL Varlena 與 TOAST 機制深度解析》</a>。）</li><li><strong>零複製借用</strong>：回傳的 <code>&amp;&#39;a str</code> 直接指向 PG 記憶體，沒有 copy，所以帶生命週期 <code>&#39;a</code>。快，但不能讓它活過那塊記憶體——這也是 pgrx 型別到處都是 lifetime 的原因。</li></ol><blockquote><p><strong>借用 vs 複製</strong>：同檔案下方的 <code>impl FromDatum for String</code> 會<strong>複製一份</strong>到 Rust 自己管理的記憶體。<code>&amp;str</code> &#x3D; 借 PG 記憶體，快但短命；<code>String</code> &#x3D; 複製一份，慢但你自己擁有。這個取捨貫穿整個 pgrx。</p></blockquote><hr><h2 id="五、裝信封：IntoDatum-→-BoxRet-→-RetAbi"><a href="#五、裝信封：IntoDatum-→-BoxRet-→-RetAbi" class="headerlink" title="五、裝信封：IntoDatum → BoxRet → RetAbi"></a>五、裝信封：<code>IntoDatum</code> → <code>BoxRet</code> → <code>RetAbi</code></h2><p>回傳方向比想像中多一層。<strong>你的 <code>IntoDatum</code> 不是被 wrapper 直接呼叫的</strong>，中間隔著 <code>BoxRet</code> &#x2F; <code>RetAbi</code>。</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">你的函式 <span class="keyword">return</span> String</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">① RetAbi      ← 回傳位置的「總 ABI 協定」（處理單值 / 多列 SRF 所有情況）</span><br><span class="line">      │  String 走最單純那條，靠 blanket impl 轉包</span><br><span class="line">      ▼</span><br><span class="line">② BoxRet      ← 「把單一 Rust 值裝成 Datum」的簡化協定</span><br><span class="line">      │  String 的 BoxRet 實作 = 呼叫你的 into_datum()</span><br><span class="line">      ▼</span><br><span class="line">③ IntoDatum   ← 你實現的這個，純粹做「值 → Datum」轉換</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">   <span class="keyword">Option</span>&lt;Datum&gt;   （<span class="keyword">Some</span>=有值, <span class="keyword">None</span>=<span class="keyword">SQL</span> <span class="keyword">NULL</span>）</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">④ return_raw_datum / return_null   ← 把結果 + <span class="keyword">null</span> 旗標寫回 fcinfo</span><br><span class="line">      │</span><br><span class="line">      ▼</span><br><span class="line">   交還 pg_sys::Datum 給 PostgreSQL</span><br></pre></td></tr></table></figure><h3 id="5-1-IntoDatum：純值轉換"><a href="#5-1-IntoDatum：純值轉換" class="headerlink" title="5.1 IntoDatum：純值轉換"></a>5.1 <code>IntoDatum</code>：純值轉換</h3><p><code>String</code> 的實作（<code>datum/into.rs:262</code> 的巨集）：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">impl</span> <span class="title class_">IntoDatum</span> <span class="keyword">for</span> <span class="title class_">String</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">into_datum</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;pg_sys::Datum&gt; &#123;</span><br><span class="line">        <span class="keyword">self</span>.<span class="title function_ invoke__">as_bytes</span>().<span class="title function_ invoke__">into_datum</span>()          <span class="comment">// 轉包給 &amp;[u8] 的實作</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">type_oid</span>() <span class="punctuation">-&gt;</span> pg_sys::Oid &#123; pg_sys::TEXTOID &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>真正幹活的 <code>&amp;[u8]::into_datum</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">into_datum</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;pg_sys::Datum&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">len</span> = <span class="keyword">self</span>.<span class="title function_ invoke__">len</span>() + VARHDRSZ;          <span class="comment">// 資料長度 + 4 bytes 標頭</span></span><br><span class="line">    <span class="comment">// ★ 用 palloc，不是 Rust 的 malloc！</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">varlena</span> = pg_sys::<span class="title function_ invoke__">palloc</span>(len) <span class="keyword">as</span> *<span class="keyword">mut</span> pg_sys::varlena;</span><br><span class="line">    <span class="title function_ invoke__">set_varsize_4b</span>(varlena, len);             <span class="comment">// 把長度寫進 varlena 標頭</span></span><br><span class="line">    std::ptr::<span class="title function_ invoke__">copy_nonoverlapping</span>(<span class="keyword">self</span>.<span class="title function_ invoke__">as_ptr</span>(), 資料區, <span class="keyword">self</span>.<span class="title function_ invoke__">len</span>());</span><br><span class="line">    <span class="title function_ invoke__">Some</span>(pg_sys::Datum::<span class="title function_ invoke__">from</span>(varlena))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>最關鍵的 PostgreSQL 機制：<code>palloc</code> &#x2F; MemoryContext。</strong></p><blockquote><p>你回傳給 PG 的東西，<strong>必須用 <code>palloc</code> 分配</strong>，不能用 Rust 的 <code>Box</code> &#x2F; <code>malloc</code>。因為 PG 用它自己的「記憶體上下文（MemoryContext）」管生命週期——查詢結束時整包釋放。若你用 Rust heap 給它，PG 不知道怎麼回收，就會洩漏或 crash。</p><p><strong>進 PG 的東西走 palloc；純 Rust 內部運算走 Rust heap。</strong> 這是貫穿全 pgrx 的第二個取捨。</p></blockquote><h3 id="5-2-BoxRet：把-IntoDatum-接上函式邊界"><a href="#5-2-BoxRet：把-IntoDatum-接上函式邊界" class="headerlink" title="5.2 BoxRet：把 IntoDatum 接上函式邊界"></a>5.2 <code>BoxRet</code>：把 <code>IntoDatum</code> 接上函式邊界</h3><p><code>IntoDatum</code> 是通用轉換，不知道 fcinfo 存在。但「從 PG 函式回傳」還要處理 NULL 旗標。pgrx 用一個 macro 幫一票有 <code>IntoDatum</code> 的型別自動生出 <code>BoxRet</code>：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">macro_rules!</span> impl_repackage_into_datum &#123;</span><br><span class="line">    ($($boxable:ty),*) =&gt; &#123;</span><br><span class="line">        $(  <span class="keyword">unsafe</span> <span class="keyword">impl</span> <span class="title class_">BoxRet</span> <span class="keyword">for</span> $boxable &#123;</span><br><span class="line">                <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">box_into</span>&lt;<span class="string">&#x27;fcx&gt;(self, fcinfo: &amp;mut FcInfo&lt;&#x27;</span>fcx&gt;) <span class="punctuation">-&gt;</span> Datum&lt;<span class="string">&#x27;fcx&gt; &#123;</span></span><br><span class="line"><span class="string">                    match self.into_datum() &#123;          // ★★ 呼叫你的 IntoDatum ★★</span></span><br><span class="line"><span class="string">                        Some(datum) =&gt; unsafe &#123; fcinfo.return_raw_datum(datum) &#125;,</span></span><br><span class="line"><span class="string">                        None        =&gt; fcinfo.return_null(),</span></span><br><span class="line"><span class="string">                    &#125;</span></span><br><span class="line"><span class="string">                &#125;</span></span><br><span class="line"><span class="string">          &#125;)*</span></span><br><span class="line"><span class="string">    &#125;;</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string">impl_repackage_into_datum! &#123; String, CString, Vec&lt;u8&gt;, char, Json, JsonB, Uuid, ... &#125;</span></span><br></pre></td></tr></table></figure><p><strong>這行 <code>match self.into_datum()</code> 就是「協同」發生的地方。</strong></p><blockquote><p>實務重點：如果你為一個新型別實作了 <code>IntoDatum</code>，它<strong>還不能直接當回傳值</strong>，因為缺 <code>BoxRet</code>。最省事的方式就是把型別加進這個 <code>impl_repackage_into_datum!</code> 清單。</p></blockquote><h3 id="5-3-NULL-是旗標，不是值"><a href="#5-3-NULL-是旗標，不是值" class="headerlink" title="5.3 NULL 是旗標，不是值"></a>5.3 NULL 是旗標，不是值</h3><p><code>box_into</code> 把 <code>Option&lt;Datum&gt;</code> 翻譯成 PG 機制：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Some 路徑（callconv.rs:743）</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">return_raw_datum</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>, datum) <span class="punctuation">-&gt;</span> Datum&lt;<span class="string">&#x27;fcx&gt; &#123;</span></span><br><span class="line"><span class="string">    *self.set_return_is_null() = false;   // 告訴 PG：這次回傳「不是」NULL</span></span><br><span class="line"><span class="string">    mem::transmute(datum)</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string">// None 路徑（callconv.rs:723）</span></span><br><span class="line"><span class="string">pub fn return_null(&amp;mut self) -&gt; Datum&lt;&#x27;</span>fcx&gt; &#123;</span><br><span class="line">    <span class="keyword">unsafe</span> &#123; *<span class="keyword">self</span>.<span class="title function_ invoke__">set_return_is_null</span>() = <span class="literal">true</span> &#125;;   <span class="comment">// 告訴 PG：這次回傳「是」NULL</span></span><br><span class="line">    Datum::<span class="title function_ invoke__">null</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>PostgreSQL 判斷回傳是不是 NULL，<strong>不看 Datum 內容</strong>，而是看 <code>fcinfo-&gt;isnull</code> 這個獨立布林欄位。所以「回傳 NULL」&#x3D; 設 <code>isnull = true</code>，Datum 給什麼都無所謂。這就是為什麼不能讓 <code>into_datum</code> 直接對接 PG，中間一定要有 <code>box_into</code> 這個「同時管 Datum 和 isnull」的協調者。</p></blockquote><hr><h2 id="六、RetAbi-到底扛了什麼髒活：SRF"><a href="#六、RetAbi-到底扛了什麼髒活：SRF" class="headerlink" title="六、RetAbi 到底扛了什麼髒活：SRF"></a>六、<code>RetAbi</code> 到底扛了什麼髒活：SRF</h2><p><code>RetAbi</code> 比 <code>BoxRet</code> 複雜得多，而它的複雜度 <strong>90% 來自 SRF（Set-Returning Function，回傳多列的函式）</strong>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> split_table(<span class="string">&#x27;a,b,c&#x27;</span>, <span class="string">&#x27;,&#x27;</span>);   <span class="comment">-- 回傳 3 列</span></span><br></pre></td></tr></table></figure><p>對應的 Rust：<code>fn split_table(...) -&gt; TableIterator&lt;...&gt;</code>。</p><h3 id="6-1-PostgreSQL-的-SRF-協定：Value-Per-Call"><a href="#6-1-PostgreSQL-的-SRF-協定：Value-Per-Call" class="headerlink" title="6.1 PostgreSQL 的 SRF 協定：Value-Per-Call"></a>6.1 PostgreSQL 的 SRF 協定：Value-Per-Call</h3><p>PG <strong>不支援</strong>「你一次 return 一個 Vec，我收下」。它用「<strong>每列呼叫一次</strong>」協定：</p><blockquote><p>PG：「給我第一列。」→ 函式被呼叫，回傳 row 1，然後<strong>整個函式結束、堆疊清空</strong>。<br>PG：「給我下一列。」→ <strong>同一個函式又從頭被呼叫一次</strong>，回傳 row 2。<br>…重複 N 次…<br>你：「沒了。」→ 回傳 done 訊號，PG 停止。</p></blockquote><p>問題：函式每次從頭跑，區域變數全沒了，「我上次吐到第幾列」的狀態存哪？</p><p>PG 的答案：給你一塊<strong>跨呼叫存活的記憶體</strong>（<code>FuncCallContext</code> + <code>multi_call_memory_ctx</code>），叫你把 iterator 塞進去；下次呼叫再撈回來續跑。<strong><code>RetAbi</code> 的全部髒活就是管這個生命週期。</strong></p><h3 id="6-2-RetAbi-每個方法對應的髒活"><a href="#6-2-RetAbi-每個方法對應的髒活" class="headerlink" title="6.2 RetAbi 每個方法對應的髒活"></a>6.2 <code>RetAbi</code> 每個方法對應的髒活</h3><p>對照 <code>TableIterator</code> 的實作 ：</p><table><thead><tr><th>方法</th><th>髒活</th></tr></thead><tbody><tr><td><code>check_fcinfo_and_prepare</code></td><td>問 PG「這是第幾次呼叫？」首次 → 開跨呼叫記憶體、回 <code>WrappedFn</code>；續集 → 回 <code>RestoreCx</code></td></tr><tr><td><code>to_ret</code></td><td>把使用者的 iterator 轉成內部 <code>Step</code> 狀態機，順便先 <code>next()</code> 一次確認空不空</td></tr><tr><td><code>move_into_fcinfo_fcx</code></td><td><strong>把 iterator 存進跨呼叫記憶體</strong>（<code>leak_and_drop_on_delete</code>：搬進 PG 記憶體並登記「這塊被刪時要 drop 這個 Rust 物件」）</td></tr><tr><td><code>ret_from_fcinfo_fcx</code></td><td><strong>續集時把 iterator 撈回來</strong>，<code>next()</code> 抽下一列</td></tr><tr><td><code>box_ret_in_fcinfo</code></td><td>把這一列裝成 Datum，並 <code>srf_return_next</code> 撥「還有下一列」旗標</td></tr><tr><td><code>finish_call_fcinfo</code></td><td><code>srf_return_done</code> 撥「結束」旗標，PG 才停止呼叫並清記憶體（觸發 Rust drop）</td></tr></tbody></table><p><code>CallCx</code> 這個 enum 就是給 wrapper 的方向盤：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">enum</span> <span class="title class_">CallCx</span> &#123;</span><br><span class="line">    RestoreCx,                        <span class="comment">// 續集：跳過使用者函式，去撈狀態</span></span><br><span class="line">    <span class="title function_ invoke__">WrappedFn</span>(pg_sys::MemoryContext), <span class="comment">// 首次：切到這個記憶體、跑使用者函式</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="6-3-兩條生命週期並排看"><a href="#6-3-兩條生命週期並排看" class="headerlink" title="6.3 兩條生命週期並排看"></a>6.3 兩條生命週期並排看</h3><table><thead><tr><th>階段</th><th><code>String</code>（單值，走 <code>BoxRet</code>）</th><th><code>TableIterator</code>（SRF，走完整 <code>RetAbi</code>）</th></tr></thead><tbody><tr><td>PG 呼叫次數</td><td><strong>1 次</strong></td><td><strong>N+1 次</strong>（N 列 + 1 次收尾）</td></tr><tr><td>跑使用者函式</td><td>每次都跑</td><td><strong>只有第一次跑</strong>，之後不跑</td></tr><tr><td>狀態保存</td><td>不需要</td><td><code>move_into_fcinfo_fcx</code> 存 iterator</td></tr><tr><td>狀態恢復</td><td>不需要</td><td><code>ret_from_fcinfo_fcx</code> 撈 iterator</td></tr><tr><td>每次回傳</td><td><code>box_into</code> → 一個 Datum</td><td><code>box_ret_in_fcinfo</code> → 一列 + <code>srf_return_next</code></td></tr><tr><td>收尾</td><td>無</td><td><code>finish_call_fcinfo</code> → <code>srf_return_done</code></td></tr></tbody></table><p>這就是為什麼 <code>String</code> 只需簡化版 <code>BoxRet</code>，而 <code>TableIterator</code> 需要完整 <code>RetAbi</code>。<code>BoxRet</code> 本質上是「把 <code>RetAbi</code> 除 <code>box_into</code> 外的方法全給空實作」的那條 blanket impl。</p><blockquote><p><strong>最硬的髒活</strong>是「Rust 物件跨 C 函式呼叫存活 + 正確 Drop」。<code>leak_and_drop_on_delete</code> 把 iterator 搬進 PG 記憶體並掛上解構子——這是手寫 C extension 最容易洩漏 &#x2F; crash 的地方，<code>RetAbi</code> 全包了。</p></blockquote><h3 id="6-4-附帶：Result-的回傳"><a href="#6-4-附帶：Result-的回傳" class="headerlink" title="6.4 附帶：Result&lt;T, E&gt; 的回傳"></a>6.4 附帶：<code>Result&lt;T, E&gt;</code> 的回傳</h3><p>pgrx 讓你能寫 <code>-&gt; Result&lt;String, MyError&gt;</code>。它的 <code>to_ret</code> 裡呼叫 <code>unwrap_or_report</code>：若是 <code>Err</code>，轉成 PostgreSQL 的 <code>ereport</code> 錯誤（SQL 端看到錯誤並中止交易），否則解包 <code>Ok</code> 值繼續走正常回傳。<strong>所以 pgrx 函式回傳 <code>Result</code> 的 <code>Err</code> &#x3D; PG 端的 ERROR。</strong></p><hr><h2 id="七、參數方向：與回傳對稱的-ArgAbi"><a href="#七、參數方向：與回傳對稱的-ArgAbi" class="headerlink" title="七、參數方向：與回傳對稱的 ArgAbi"></a>七、參數方向：與回傳對稱的 <code>ArgAbi</code></h2><p>回頭看第三節 wrapper 裡的參數拆解。參數方向有一套跟回傳完全對稱的 ABI trait，叫 <code>ArgAbi</code>。</p><h3 id="7-1-巨集怎麼生「拆信封」語句"><a href="#7-1-巨集怎麼生「拆信封」語句" class="headerlink" title="7.1 巨集怎麼生「拆信封」語句"></a>7.1 巨集怎麼生「拆信封」語句</h3><p>wrapper 生成邏輯：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// ① 每個參數的綁定名：input → input_（加底線避開名稱衝突，是個 workaround）</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">arg_pats</span> = args.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">map</span>(|v| format_ident!(<span class="string">&quot;&#123;&#125;_&quot;</span>, &amp;v.pat)).<span class="title function_ invoke__">collect</span>();</span><br><span class="line"><span class="comment">// ② 參數迭代器變數名：_args</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">args_ident</span> = Ident::<span class="title function_ invoke__">new</span>(<span class="string">&quot;_args&quot;</span>, ...);</span><br><span class="line"><span class="comment">// ③ 每個參數生一句「掏出下一個、解箱、失敗就 panic」</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">arg_fetches</span> = arg_pats.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">map</span>(|pat| quote!&#123;</span><br><span class="line">    <span class="keyword">let</span> #pat = #args_ident.<span class="title function_ invoke__">next_arg_unchecked</span>()</span><br><span class="line">        .<span class="title function_ invoke__">unwrap_or_else</span>(|| <span class="built_in">panic!</span>(<span class="string">&quot;unboxing &#123;&#125; argument failed&quot;</span>, <span class="built_in">stringify!</span>(#pat)));</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p><code>quote!</code> 的重複語法只有一條規則：</p><figure class="highlight leaf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">#</span><span class="params">(#變數)</span>*      → 逐元素展開，之間「沒有分隔符」</span><br><span class="line"><span class="punctuation">#</span><span class="params">(#變數)</span>,*     → 逐元素展開，之間用「逗號」分隔</span><br></pre></td></tr></table></figure><p>對 <code>substring(input: &amp;str, start: i32, end: i32)</code>，逐參數拆解會展開成三句 <code>let</code>，再展開成 <code>substring(input_, start_, end_)</code>：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">input_</span> = _args.<span class="title function_ invoke__">next_arg_unchecked</span>().<span class="title function_ invoke__">unwrap_or_else</span>(|| <span class="built_in">panic!</span>(...));</span><br><span class="line"><span class="keyword">let</span> <span class="variable">start_</span> = _args.<span class="title function_ invoke__">next_arg_unchecked</span>().<span class="title function_ invoke__">unwrap_or_else</span>(|| <span class="built_in">panic!</span>(...));</span><br><span class="line"><span class="keyword">let</span> <span class="variable">end_</span>   = _args.<span class="title function_ invoke__">next_arg_unchecked</span>().<span class="title function_ invoke__">unwrap_or_else</span>(|| <span class="built_in">panic!</span>(...));</span><br><span class="line"><span class="title function_ invoke__">substring</span>(input_, start_, end_)</span><br></pre></td></tr></table></figure><blockquote><p><strong>為什麼需要這道拆解？</strong> wrapper 從 PG 拿到的是 <code>fcinfo</code>（一坨裸信封），不是打好型別的 Rust 值。你的函式要 <code>&amp;str</code> 不是 <code>Datum</code>。這道「拆信封」工序就是：每個參數生一句 <code>let</code>，把第 i 個裸 Datum 掏出來、解箱成正確型別的綁定。</p><p><strong>漂亮的細節</strong>：<code>next_arg_unchecked()</code> 沒寫 <code>::&lt;&amp;str&gt;</code>。型別靠 Rust 從後面 <code>to_lowercase(input_)</code> 的簽名<strong>反推</strong>回來——巨集完全不用知道參數型別，交給編譯器。這就是這句能對所有型別通用的原因。</p></blockquote><h3 id="7-2-ArgAbi-→-FromDatum-的接線"><a href="#7-2-ArgAbi-→-FromDatum-的接線" class="headerlink" title="7.2 ArgAbi → FromDatum 的接線"></a>7.2 <code>ArgAbi</code> → <code>FromDatum</code> 的接線</h3><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">_args.next_arg_unchecked::&lt;&amp;<span class="type">str</span>&gt;()</span><br><span class="line">        │  callconv.rs:<span class="number">940</span></span><br><span class="line">        ▼</span><br><span class="line">Args::<span class="title function_ invoke__">next</span>() → 取出第 i 個 <span class="title function_ invoke__">Arg</span>(fcinfo, i, &amp;NullableDatum)  <span class="comment">// &#123;value: Datum, isnull&#125;</span></span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">&lt;&amp;<span class="type">str</span> <span class="keyword">as</span> ArgAbi&gt;::<span class="title function_ invoke__">unbox_arg_unchecked</span>(arg)   <span class="comment">// ArgAbi = 參數位置的 ABI 協定</span></span><br><span class="line">        │  （&amp;<span class="type">str</span> 由 argue_from_datum! 巨集生成，callconv.rs:<span class="number">253</span>）</span><br><span class="line">        ▼</span><br><span class="line">arg.unbox_arg_using_from_datum::&lt;&amp;<span class="type">str</span>&gt;()      <span class="comment">// callconv.rs:887</span></span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">&lt;&amp;<span class="type">str</span> <span class="keyword">as</span> FromDatum&gt;::<span class="title function_ invoke__">from_datum</span>(datum, isnull)  <span class="comment">// 第四節那個 detoast + 借用</span></span><br><span class="line">        │</span><br><span class="line">        ▼</span><br><span class="line">      &amp;<span class="type">str</span>（借用 PG 記憶體）</span><br></pre></td></tr></table></figure><p>橋接點 <code>unbox_arg_using_from_datum</code>：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">unbox_arg_using_from_datum</span>&lt;T: FromDatum&gt;(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> T::GET_TYPOID &#123;</span><br><span class="line">        T::<span class="title function_ invoke__">from_polymorphic_datum</span>(<span class="keyword">self</span>.<span class="number">2</span>.value, <span class="keyword">self</span>.<span class="title function_ invoke__">is_null</span>(), <span class="keyword">self</span>.<span class="title function_ invoke__">raw_oid</span>())  <span class="comment">// 多型</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        T::<span class="title function_ invoke__">from_datum</span>(<span class="keyword">self</span>.<span class="number">2</span>.value, <span class="keyword">self</span>.<span class="title function_ invoke__">is_null</span>())     <span class="comment">// 一般：detoast + 借用</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>Args</code> 就是「把 PG 那疊信封做成 Rust 迭代器」，<code>next_arg_unchecked</code> 每叫一次吐一個 <code>Arg</code>，交給 <code>ArgAbi</code> 解箱。</p><hr><h2 id="八、收束：pgrx-呼叫慣例的四格大圖"><a href="#八、收束：pgrx-呼叫慣例的四格大圖" class="headerlink" title="八、收束：pgrx 呼叫慣例的四格大圖"></a>八、收束：pgrx 呼叫慣例的四格大圖</h2><p>這是理解 pgrx 的最終地圖，值得背下來：</p><table><thead><tr><th></th><th>值轉換層（通用、到處用）</th><th>ABI 層（只在函式邊界、管 fcinfo）</th></tr></thead><tbody><tr><td><strong>參數方向（進）</strong></td><td><code>FromDatum::from_datum</code> 拆信封</td><td><code>ArgAbi::unbox_arg_unchecked</code></td></tr><tr><td><strong>回傳方向（出）</strong></td><td><code>IntoDatum::into_datum</code> 裝信封</td><td><code>BoxRet::box_into</code> &#x2F; <code>RetAbi</code>（含 SRF）</td></tr></tbody></table><p>兩個關鍵洞察：</p><ol><li><p><strong>內圈（<code>FromDatum</code> &#x2F; <code>IntoDatum</code>）是純資料轉換</strong>，跟 fcinfo 無關，所以 SPI 傳參、建 array、composite type 也都在用它們——通用。<strong>外圈（<code>ArgAbi</code> &#x2F; <code>BoxRet</code>）是函式邊界專屬的 ABI</strong>，負責 fcinfo 髒活（null 旗標、參數迭代、pass-by-ref&#x2F;value、SRF 多列、生命週期），再把值轉換轉包給內圈。</p></li><li><p><strong>唯一不對稱的地方是 <code>RetAbi</code></strong>：因為 PG「回傳多列」比「吃多個參數」複雜得多——參數永遠一次給齊，回傳卻可能分 N 次。所以進的方向只有 <code>ArgAbi</code> 一個 trait，出的方向卻有 <code>BoxRet</code> + <code>RetAbi</code> 兩個。</p></li></ol><hr><h2 id="九、給想貢獻-pgrx-的人：實務清單"><a href="#九、給想貢獻-pgrx-的人：實務清單" class="headerlink" title="九、給想貢獻 pgrx 的人：實務清單"></a>九、給想貢獻 pgrx 的人：實務清單</h2><ul><li>想讓一個新型別能當<strong>參數</strong>：實作 <code>FromDatum</code>，並確保有對應的 <code>ArgAbi</code>（多數走 <code>argue_from_datum!</code> 或 borrow-based 的 blanket impl）。</li><li>想讓一個新型別能當<strong>回傳值</strong>：實作 <code>IntoDatum</code>，並把它掛上 <code>BoxRet</code>（最省事：加進 <code>impl_repackage_into_datum!</code> 清單）。光有 <code>IntoDatum</code> 不夠。</li><li>回傳多列：實作 <code>Iterator</code> 並用 <code>SetOfIterator</code> &#x2F; <code>TableIterator</code> 包起來，<code>RetAbi</code> 的 SRF 生命週期它們已經幫你處理好。</li><li>測試一定要用 <code>#[pg_test]</code>（在真實 PG 裡跑），純 <code>#[test]</code> 碰不到 PG 內部。</li><li>動 unsafe 前先讀 repo 根目錄的 <code>SAFETY.md</code>，PR review 主要卡在這裡。</li></ul><hr><h2 id="小結"><a href="#小結" class="headerlink" title="小結"></a>小結</h2><p>pgrx 看似魔法，拆開後其實只是四個 trait 分工合作：<strong>內圈的 <code>FromDatum</code> &#x2F; <code>IntoDatum</code> 負責純值轉換，外圈的 <code>ArgAbi</code> &#x2F; <code>RetAbi</code> 負責函式邊界的 ABI 髒活</strong>，中間靠 <code>#[pg_extern]</code> 巨集生成的 wrapper 串起來。</p><p>理解這條「一個值如何進出 PostgreSQL」的鏈路後，你不但能看懂 pgrx 生成的程式碼，也能自己為新型別擴充參數與回傳支援——甚至讀懂手寫 C extension 為什麼那麼容易在記憶體管理上翻車。</p><hr><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a href="https://isdaniel.github.io/pgrx-postgresql-extension-mechanism-deep-dive/">深入 pgrx 機制：用 Rust 撰寫 PostgreSQL Extension 的底層原理與實戰</a></li><li><a href="https://isdaniel.github.io/postgresql-varlena-toast-deep-dive/">PostgreSQL Varlena 與 TOAST 機制深度解析</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/xfunc-c.html#XFUNC-C-V1-CALLING">PostgreSQL: Version 1 Calling Conventions</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pgrx-calling-convention-deep-dive/">https://isdaniel.github.io/pgrx-calling-convention-deep-dive/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pgrx-calling-convention-deep-dive/</id>
    <link href="https://isdaniel.github.io/pgrx-calling-convention-deep-dive/"/>
    <published>2026-07-03T13:15:30.000Z</published>
    <summary>用一個最簡單的 #[pg_extern] 函式當主角，追到 pgrx 最底層的 ABI trait，把 Rust 型別與 PostgreSQL Datum 之間的完整轉換機制講清楚。</summary>
    <title>深入 pgrx 呼叫慣例：一個 Rust 值如何進出 PostgreSQL</title>
    <updated>2026-08-05T10:10:26.491Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/PostgreSQL/"/>
    <category term="SystemDesign" scheme="https://isdaniel.github.io/categories/PostgreSQL/SystemDesign/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Performance-Tuning" scheme="https://isdaniel.github.io/tags/Performance-Tuning/"/>
    <category term="SystemDesign" scheme="https://isdaniel.github.io/tags/SystemDesign/"/>
    <category term="Storage" scheme="https://isdaniel.github.io/tags/Storage/"/>
    <category term="TOAST" scheme="https://isdaniel.github.io/tags/TOAST/"/>
    <category term="Varlena" scheme="https://isdaniel.github.io/tags/Varlena/"/>
    <content>
      <![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>PostgreSQL 的單一資料頁面 (Page) 預設只有 <strong>8 KB</strong>,但實務上我們經常會塞進 JSON、長文字、圖片 base64、甚至幾十 MB 的 bytea。這些「比一個頁面還大」的欄位,為什麼可以正常運作?背後的功臣就是 <strong>Varlena</strong>(變長度資料結構)與 <strong>TOAST</strong>(The Oversized-Attribute Storage Technique)。</p><p>這篇文章會把整個機制從 header bit-layout、切塊、compression、索引,一路講到 detoast 的執行流程,並補上原始 summary 中沒提到、但實務上非常重要的觀念:<strong>storage strategy</strong>、<strong>inline compression</strong>、<strong>TOAST threshold</strong>、<strong>expanded datum</strong>,以及如何用 SQL 直接觀察 TOAST 表。</p><hr><h2 id="一、為什麼需要-Varlena"><a href="#一、為什麼需要-Varlena" class="headerlink" title="一、為什麼需要 Varlena?"></a>一、為什麼需要 Varlena?</h2><p>C 語言的字串靠 <code>\0</code> 結尾,要知道長度就得 <code>strlen()</code> 一個一個 byte 數過去 — O(n) 而且資料中不能出現 <code>\0</code>。資料庫顯然不能這樣搞,所以 PostgreSQL 設計了一個通用的「變長度資料」結構,叫做 <strong>Varlena (Variable-length Attribute)</strong>。</p><p>所有變長度型別(<code>TEXT</code>、<code>VARCHAR</code>、<code>BYTEA</code>、<code>JSON</code>、<code>JSONB</code>、<code>NUMERIC</code>、<code>ARRAY</code> 等)在磁碟與記憶體中都是 Varlena。Varlena 的核心思想很簡單:<strong>把長度資訊放在資料最前面</strong>,讀取時看一眼 header 就知道整段資料有多長,O(1) 完成。</p><p>但只是「加個長度 header」還不夠 — 如果一個 30 byte 的小字串要硬塞 4 byte 的長度欄位,空間浪費太嚴重(超過 13%)。所以 PostgreSQL 為 Varlena 設計了<strong>三種 header 格式</strong>,根據資料大小自動切換,完全對開發者透明。</p><hr><h2 id="二、Varlena-的三種-Header-格式"><a href="#二、Varlena-的三種-Header-格式" class="headerlink" title="二、Varlena 的三種 Header 格式"></a>二、Varlena 的三種 Header 格式</h2><p>定義位於 <code>src/include/varatt.h</code>,核心結構大致如下:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span>                       <span class="comment">/* Normal varlena (4-byte length) */</span></span><br><span class="line">        uint32 va_header;</span><br><span class="line">        <span class="type">char</span>   va_data[FLEXIBLE_ARRAY_MEMBER];</span><br><span class="line">    &#125; va_4byte;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span>                       <span class="comment">/* Compressed-in-line format */</span></span><br><span class="line">        uint32 va_header;</span><br><span class="line">        uint32 va_tcinfo;          <span class="comment">/* raw size + compression method */</span></span><br><span class="line">        <span class="type">char</span>   va_data[FLEXIBLE_ARRAY_MEMBER];</span><br><span class="line">    &#125; va_compressed;</span><br><span class="line">&#125; varattrib_4b;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    uint8  va_header;              <span class="comment">/* 1-byte short header */</span></span><br><span class="line">    <span class="type">char</span>   va_data[FLEXIBLE_ARRAY_MEMBER];</span><br><span class="line">&#125; varattrib_1b;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span>                   <span class="comment">/* TOASTed, has external storage */</span></span><br><span class="line">    uint8  va_header;              <span class="comment">/* Always 0x01 */</span></span><br><span class="line">    uint8  va_tag;                 <span class="comment">/* Type of external data */</span></span><br><span class="line">    <span class="type">char</span>   va_data[FLEXIBLE_ARRAY_MEMBER];</span><br><span class="line">&#125; varattrib_1b_e;</span><br></pre></td></tr></table></figure><h3 id="三種格式的判別"><a href="#三種格式的判別" class="headerlink" title="三種格式的判別"></a>三種格式的判別</h3><p>PostgreSQL 用 header 的<strong>第一個 byte</strong> 做 tag dispatch:</p><table><thead><tr><th>第一 byte 的最低 2 bits</th><th>格式</th><th>用途</th></tr></thead><tbody><tr><td><code>00</code></td><td><code>varattrib_4b</code> (uncompressed)</td><td>大資料 (≥ 127 bytes、未 compressed)</td></tr><tr><td><code>10</code></td><td><code>varattrib_4b</code> (compressed)</td><td>大資料、行內 compressed</td></tr><tr><td><code>xxxxxxx1</code> (最低 bit &#x3D; 1, header ≠ 0x01)</td><td><code>varattrib_1b</code> (short)</td><td>小資料 (≤ 126 bytes)</td></tr><tr><td><code>0x01</code> (整個 byte)</td><td><code>varattrib_1b_e</code> (external)</td><td>已搬到 TOAST 表</td></tr></tbody></table><blockquote><p>注意:實作上是檢查最低位 bit 來分流,short header 用 1 bit 當 tag,其餘 7 bits 存長度;4-byte header 用 2 bits 當 tag,30 bits 存長度。原始 summary 提到的「2 bits 身分證」即指此處。</p></blockquote><h3 id="1-varattrib-1b-—-小資料的省空間設計"><a href="#1-varattrib-1b-—-小資料的省空間設計" class="headerlink" title="1. varattrib_1b — 小資料的省空間設計"></a>1. <code>varattrib_1b</code> — 小資料的省空間設計</h3><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">7</span> bits 長度   |  <span class="number">1</span> bit <span class="keyword">tag</span></span><br><span class="line"><span class="title">┌──────────────┬─────┐</span></span><br><span class="line"><span class="title">│ length</span> (≤<span class="number">126</span>)│  <span class="number">1</span>  │</span><br><span class="line">└──────────────┴─────┘</span><br><span class="line"> + data ...</span><br></pre></td></tr></table></figure><ul><li>header 只有 <strong>1 byte</strong>,7 bits 可表示長度。</li><li>「最低 bit &#x3D; 1」是其身分標記。</li><li>適合小於 127 bytes 的字串(實務上閥值是 <strong>126 bytes</strong>,因為 header 自己佔 1 byte)。</li></ul><h3 id="2-varattrib-4b-—-一般大資料-含-inline-compression"><a href="#2-varattrib-4b-—-一般大資料-含-inline-compression" class="headerlink" title="2. varattrib_4b — 一般大資料 (含 inline compression)"></a>2. <code>varattrib_4b</code> — 一般大資料 (含 inline compression)</h3><figure class="highlight oxygene"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"> <span class="number">30</span> bits 長度    | <span class="number">2</span> bits tag</span><br><span class="line">┌─────────────────┬─────┐</span><br><span class="line">│  length (≤<span class="number">1</span>GB)  │ <span class="number">00</span>  │   ← uncompressed</span><br><span class="line">└─────────────────┴─────┘</span><br><span class="line">┌─────────────────┬─────┐</span><br><span class="line">│  length         │ <span class="number">10</span>  │   ← compressed (header 後面多 <span class="number">4</span> bytes 描述原始大小 + compression <span class="keyword">method</span>)</span><br><span class="line">└─────────────────┴─────┘</span><br></pre></td></tr></table></figure><ul><li>header 4 bytes,可表示最大 <strong>1 GB</strong> (2^30,這也是 PostgreSQL 單一欄位的硬上限)。</li><li>如果是 compressed 格式,header 後面會多一個 4-byte <code>va_tcinfo</code>,其中:<ul><li>高 2 bits 是 <strong>compression method</strong> (<code>PGLZ</code> &#x3D; 00 &#x2F; <code>LZ4</code> &#x3D; 01)。</li><li>低 30 bits 是 <strong>原始未 compressed 大小</strong>(decompress 時需要)。</li></ul></li></ul><h3 id="3-varattrib-1b-e-—-External-pointer-TOAST-pointer"><a href="#3-varattrib-1b-e-—-External-pointer-TOAST-pointer" class="headerlink" title="3. varattrib_1b_e — External pointer (TOAST pointer)"></a>3. <code>varattrib_1b_e</code> — External pointer (TOAST pointer)</h3><figure class="highlight autohotkey"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">┌────────┬────────┬──────── ... ────────┐</span><br><span class="line">│  <span class="number">0</span>x01  │ v<span class="built_in">a_tag</span> │  pointer payload    │</span><br><span class="line">└────────┴────────┴─────────────────────┘</span><br></pre></td></tr></table></figure><p>第一 byte 固定 <code>0x01</code>(7 bits 全 0、最低 bit 為 1),是個特例標記:「我不是真資料,我是個指針」。第二 byte <code>va_tag</code> 表示後面 payload 的種類,常見有:</p><table><thead><tr><th>va_tag</th><th>結構</th><th>說明</th></tr></thead><tbody><tr><td><code>VARTAG_ONDISK</code> (18)</td><td><code>varatt_external</code></td><td>一般 on-disk TOAST 指針(最常見)</td></tr><tr><td><code>VARTAG_INDIRECT</code> (1)</td><td><code>varatt_indirect</code></td><td>記憶體中的轉址指針</td></tr><tr><td><code>VARTAG_EXPANDED_RO/RW</code> (2&#x2F;3)</td><td><code>varatt_expanded</code></td><td>展開後的可變物件(用於 array、JSONB 加速)</td></tr></tbody></table><hr><h2 id="三、TOAST-觸發條件與-Storage-Strategy"><a href="#三、TOAST-觸發條件與-Storage-Strategy" class="headerlink" title="三、TOAST 觸發條件與 Storage Strategy"></a>三、TOAST 觸發條件與 Storage Strategy</h2><p>很多人以為「資料只要大就會 TOAST」,實際上 PostgreSQL 是看<strong>整個 tuple 大小</strong>,而且每個欄位的處理方式由「<strong>storage strategy</strong>」決定 — 這是 summary 中沒提到、但對效能調校極重要的觀念。</p><h3 id="觸發閥值"><a href="#觸發閥值" class="headerlink" title="觸發閥值"></a>觸發閥值</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TOAST_TUPLE_THRESHOLD   2032   <span class="comment">/* TOAST_TUPLE_TARGET, ~2KB */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TOAST_TUPLE_TARGET      2032</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TOAST_MAX_CHUNK_SIZE    1996   <span class="comment">/* 每塊約 2KB */</span></span></span><br></pre></td></tr></table></figure><p>當一個 tuple 寫入時,只要它的大小 <strong>超過 <code>TOAST_TUPLE_THRESHOLD</code> (約 2KB,即 page 的 1&#x2F;4)</strong>,PostgreSQL 就會挑出 tuple 中標記為 compressible &#x2F; externalizable 的欄位,依以下順序處理直到 tuple 縮小到 target 以下:</p><ol><li>對最大的欄位嘗試 <strong>compression</strong>(若 storage 允許)。</li><li>若還是太大,把最大的欄位整個 <strong>移到 external storage</strong>。</li><li>重複 1–2 直到 tuple 達標。</li></ol><h3 id="四種-Storage-Strategy"><a href="#四種-Storage-Strategy" class="headerlink" title="四種 Storage Strategy"></a>四種 Storage Strategy</h3><p>每個變長度欄位都有一個 storage 屬性,可用 <code>ALTER TABLE ... ALTER COLUMN ... SET STORAGE ...</code> 調整:</p><table><thead><tr><th>Strategy</th><th>Compress</th><th>External</th><th>典型用途</th></tr></thead><tbody><tr><td><code>PLAIN</code></td><td>No</td><td>No</td><td>定長型別預設,不走 TOAST(資料必須能塞進 page)</td></tr><tr><td><code>EXTENDED</code></td><td>Yes</td><td>Yes</td><td><strong>變長度型別預設</strong>,先 compress,壓完仍太大才移 external</td></tr><tr><td><code>EXTERNAL</code></td><td>No</td><td>Yes</td><td>不 compress,直接 external(犧牲空間換取 substring&#x2F;length 速度)</td></tr><tr><td><code>MAIN</code></td><td>Yes</td><td>Prefer inline</td><td>先 compress、盡量留在 main table,壓完還是太大才 external</td></tr></tbody></table><p>實務情境:</p><ul><li><strong>大量 substring 操作</strong>(如全文檢索片段)→ 設為 <code>EXTERNAL</code>,避免每次都要 decompress 整段。</li><li><strong>整欄一次讀全部</strong>(如 LOB)→ 預設 <code>EXTENDED</code> 最划算。</li><li><strong>小到中型欄位但偶爾很大</strong>(如 JSONB)→ 可考慮 <code>MAIN</code>,讓 hot data 留在 main table 減少 TOAST 查表。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 觀察與調整</span></span><br><span class="line"><span class="keyword">SELECT</span> attname, attstorage <span class="keyword">FROM</span> pg_attribute</span><br><span class="line"><span class="keyword">WHERE</span> attrelid <span class="operator">=</span> <span class="string">&#x27;my_table&#x27;</span>::regclass <span class="keyword">AND</span> attnum <span class="operator">&gt;</span> <span class="number">0</span>;</span><br><span class="line"><span class="comment">-- attstorage: p = PLAIN, e = EXTERNAL, x = EXTENDED, m = MAIN</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER TABLE</span> my_table <span class="keyword">ALTER</span> <span class="keyword">COLUMN</span> payload <span class="keyword">SET</span> STORAGE <span class="keyword">EXTERNAL</span>;</span><br></pre></td></tr></table></figure><hr><h2 id="四、External-Pointer-的真實結構-varatt-external"><a href="#四、External-Pointer-的真實結構-varatt-external" class="headerlink" title="四、External Pointer 的真實結構 (varatt_external)"></a>四、External Pointer 的真實結構 (<code>varatt_external</code>)</h2><p>當欄位真的被移到 external storage 後,主表留下的不是 16 bytes,而是 <strong>18 bytes</strong> 的 <code>varatt_external</code> 結構(再加 2 bytes 的 <code>varattrib_1b_e</code> header,總共 20 bytes):</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">varatt_external</span> &#123;</span></span><br><span class="line">    int32   va_rawsize;      <span class="comment">/* 含 header 的原始大小 */</span></span><br><span class="line">    uint32  va_extinfo;      <span class="comment">/* compressed 大小 (30 bits) + compression method (2 bits) */</span></span><br><span class="line">    Oid     va_valueid;      <span class="comment">/* 此筆資料在 TOAST 表中的 ID */</span></span><br><span class="line">    Oid     va_toastrelid;   <span class="comment">/* 對應的 TOAST 表 OID */</span></span><br><span class="line">&#125; varatt_external;           <span class="comment">/* sizeof = 18 bytes,實際存放時要對齊 */</span></span><br></pre></td></tr></table></figure><blockquote><p>注意:PostgreSQL 13 之後 <code>va_extinfo</code> 把 compression method 的 2 bits 塞進去 (因為新增了 LZ4),這也是原始 summary 寫「16 bytes」需要修正之處。</p></blockquote><h3 id="4-bytes-Oid-的容量"><a href="#4-bytes-Oid-的容量" class="headerlink" title="4 bytes (Oid) 的容量"></a>4 bytes (Oid) 的容量</h3><p><code>va_valueid</code> 是 4 bytes 的 <code>Oid</code> (unsigned 32 bit),理論名額是 2^32 ≈ <strong>42 億</strong>。配合 <code>chunk_seq</code> 的 4-byte 序號,單一欄位的理論上限遠超 1 GB — 但 PostgreSQL 在程式碼層硬性限制單一變長度欄位 ≤ 1 GB (來自 <code>varattrib_4b</code> 的 30-bit 長度欄位)。</p><hr><h2 id="五、TOAST-表長什麼樣"><a href="#五、TOAST-表長什麼樣" class="headerlink" title="五、TOAST 表長什麼樣?"></a>五、TOAST 表長什麼樣?</h2><p>每個有變長度欄位的 user table,PostgreSQL 會<strong>自動</strong>幫它建立一張對應的 TOAST 表,位於 <code>pg_toast</code> schema:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.relname        <span class="keyword">AS</span> main_table,</span><br><span class="line">    t.relname        <span class="keyword">AS</span> toast_table,</span><br><span class="line">    pg_size_pretty(pg_relation_size(c.reltoastrelid)) <span class="keyword">AS</span> toast_size,</span><br><span class="line">    pg_size_pretty(pg_relation_size(c.oid))           <span class="keyword">AS</span> main_size</span><br><span class="line"><span class="keyword">FROM</span> pg_class c</span><br><span class="line"><span class="keyword">JOIN</span> pg_class t <span class="keyword">ON</span> t.oid <span class="operator">=</span> c.reltoastrelid</span><br><span class="line"><span class="keyword">WHERE</span> c.relname <span class="operator">=</span> <span class="string">&#x27;my_table&#x27;</span>;</span><br></pre></td></tr></table></figure><p>TOAST 表的 schema 永遠是這三欄:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> pg_toast.pg_toast_<span class="operator">&lt;</span>oid<span class="operator">&gt;</span> (</span><br><span class="line">    chunk_id   oid    <span class="keyword">NOT NULL</span>,   <span class="comment">-- 對應 va_valueid</span></span><br><span class="line">    chunk_seq  <span class="type">int</span>    <span class="keyword">NOT NULL</span>,   <span class="comment">-- 0, 1, 2, ... 切塊序號</span></span><br><span class="line">    chunk_data bytea  <span class="keyword">NOT NULL</span>    <span class="comment">-- 約 2KB 的資料碎塊</span></span><br><span class="line">);</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">UNIQUE</span> INDEX pg_toast_<span class="operator">&lt;</span>oid<span class="operator">&gt;</span>_index</span><br><span class="line">    <span class="keyword">ON</span> pg_toast.pg_toast_<span class="operator">&lt;</span>oid<span class="operator">&gt;</span> (chunk_id, chunk_seq);</span><br></pre></td></tr></table></figure><p>關鍵點:</p><ul><li>每塊 <code>chunk_data</code> 大小是 <code>TOAST_MAX_CHUNK_SIZE ≈ 1996 bytes</code>,讓 4 塊剛好塞滿一個 8KB page (再扣掉 tuple header)。</li><li><code>(chunk_id, chunk_seq)</code> 上有 <strong>unique B-Tree index</strong>,這就是高速撈塊的關鍵。</li></ul><hr><h2 id="六、Detoast-流程-資料是怎麼被讀回來的"><a href="#六、Detoast-流程-資料是怎麼被讀回來的" class="headerlink" title="六、Detoast 流程:資料是怎麼被讀回來的"></a>六、Detoast 流程:資料是怎麼被讀回來的</h2><p>當你執行 <code>SELECT payload FROM my_table WHERE id = 1</code>,且 payload 是 128 MB 的 external 資料,引擎做的事情大致是:</p><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">主表 tuple</span><br><span class="line">┌──────────────────────────────────────────┐</span><br><span class="line">│ id=1 │ <span class="string">...</span>其他欄位<span class="string">...</span> │ varattrib_1b_e   │</span><br><span class="line">└──────────────────────────────┬───────────┘</span><br><span class="line">                               │ <span class="params">(va_toastrelid, <span class="attr">va_valueid</span>=9527)</span></span><br><span class="line">                               ▼</span><br><span class="line">                    pg_toast.pg_toast_xxxx</span><br><span class="line">                    ┌──────────────────────────────┐</span><br><span class="line">                    │ B-Tree <span class="params">(chunk_id, chunk_seq)</span> │</span><br><span class="line">                    └──────┬───────────────────────┘</span><br><span class="line">                           │ Index Range Scan from <span class="params">(9527, 0)</span></span><br><span class="line">                           ▼</span><br><span class="line">┌──────────┬───────────┬────────────┐</span><br><span class="line">│ chunk_id │ chunk_seq │ chunk_data │</span><br><span class="line">├──────────┼───────────┼────────────┤</span><br><span class="line">│   9527   │     0     │ [≈2KB]     │  ──┐</span><br><span class="line">│   9527   │     1     │ [≈2KB]     │    │</span><br><span class="line">│    <span class="string">...</span>   │    <span class="string">...</span>    │   <span class="string">...</span>      │    │ 依序串接</span><br><span class="line">│   9527   │  65535    │ [≈2KB]     │    │</span><br><span class="line">└──────────┴───────────┴────────────┘    ▼</span><br><span class="line">                                    [拼接 + decompress pglz/lz4]</span><br><span class="line">                                          │</span><br><span class="line">                                          ▼</span><br><span class="line">                                   還原 128MB 原始 bytes</span><br></pre></td></tr></table></figure><h3 id="三個關鍵設計"><a href="#三個關鍵設計" class="headerlink" title="三個關鍵設計"></a>三個關鍵設計</h3><p><strong>1. B-Tree 一次定位</strong><br>拿著 <code>va_valueid</code> 在 unique index 上做 <code>=</code> 查詢,O(log n) 直達 <code>chunk_seq=0</code>。</p><p><strong>2. Index Range Scan,不是逐塊查</strong><br>找到第一塊後,沿著 B-Tree leaf 的 sibling pointer 連續往後讀(因為 <code>(chunk_id, chunk_seq)</code> 排序),直到 <code>chunk_id</code> 變化才停。底層通常是順序 I&#x2F;O,不是隨機 I&#x2F;O。</p><p><strong>3. Lazy detoast</strong><br>這點 summary 沒提:<strong>只有真的要用到欄位內容時才會 detoast</strong>。例如:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id <span class="keyword">FROM</span> my_table <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="comment">-- 即使 payload 是 1GB,根本不會碰 TOAST 表</span></span><br></pre></td></tr></table></figure><p>這就是為什麼「主表 SELECT * 很快」的真正原因 — 不只是因為指針小,而是因為<strong>指針從不主動被展開</strong>。Detoast 只發生在運算子真的需要 bytes 的那一刻 (例如 <code>length(payload)</code> 對 uncompressed 的 EXTERNAL 是免費的,但對 EXTENDED 就得 decompress)。</p><hr><h2 id="七、Inline-Compression-vs-External"><a href="#七、Inline-Compression-vs-External" class="headerlink" title="七、Inline Compression vs External"></a>七、Inline Compression vs External</h2><p>很多人混淆「compression」與「external」,其實它們是<strong>兩個獨立的步驟</strong>:</p><table><thead><tr><th>狀態</th><th>header 是?</th><th>在哪?</th><th>觸發條件</th></tr></thead><tbody><tr><td>純 inline</td><td><code>varattrib_4b</code> (tag&#x3D;00)</td><td>主表 tuple</td><td>小 tuple,完全不動</td></tr><tr><td>Inline compressed</td><td><code>varattrib_4b</code> (tag&#x3D;10)</td><td>主表 tuple</td><td>tuple 超過 threshold,compress 完仍能塞下</td></tr><tr><td>External uncompressed</td><td><code>varattrib_1b_e</code></td><td>TOAST 表</td><td>storage &#x3D; EXTERNAL,或 EXTENDED 但壓不下去</td></tr><tr><td>External compressed</td><td><code>varattrib_1b_e</code></td><td>TOAST 表</td><td>EXTENDED &#x2F; MAIN,compress 後仍超過 target</td></tr></tbody></table><p>注意:<strong>External 的資料本身也可以是 compressed 的</strong>。<code>varatt_external</code> 裡的 <code>va_extinfo</code> 同時記錄 compressed 大小與 compression method,讓 detoast 時知道要不要 decompress。</p><h3 id="Compression-Algorithm-pglz-vs-lz4"><a href="#Compression-Algorithm-pglz-vs-lz4" class="headerlink" title="Compression Algorithm:pglz vs lz4"></a>Compression Algorithm:pglz vs lz4</h3><p>PostgreSQL 14 之後支援兩種 compression algorithm,可在 column 層級指定:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> my_table <span class="keyword">ALTER</span> <span class="keyword">COLUMN</span> payload <span class="keyword">SET</span> COMPRESSION lz4;</span><br></pre></td></tr></table></figure><table><thead><tr><th>特性</th><th>pglz (預設)</th><th>lz4</th></tr></thead><tbody><tr><td>Compression speed</td><td>較慢</td><td><strong>約 4–5 倍快</strong></td></tr><tr><td>Decompression speed</td><td>較慢</td><td><strong>約 10 倍快</strong></td></tr><tr><td>Compression ratio</td><td>略好 (≈ 5–10%)</td><td>略差</td></tr><tr><td>需要編譯選項</td><td>內建</td><td><code>--with-lz4</code></td></tr></tbody></table><p>對於寫多讀多、且 CPU 比 I&#x2F;O 緊的場景,LZ4 幾乎是無腦選擇。只有極度在意 compression ratio(冷資料、空間敏感)才該留著 pglz。</p><hr><h2 id="八、實用-SQL-觀察-TOAST-的真實樣貌"><a href="#八、實用-SQL-觀察-TOAST-的真實樣貌" class="headerlink" title="八、實用 SQL:觀察 TOAST 的真實樣貌"></a>八、實用 SQL:觀察 TOAST 的真實樣貌</h2><h3 id="1-看一張表的-TOAST-表佔用"><a href="#1-看一張表的-TOAST-表佔用" class="headerlink" title="1. 看一張表的 TOAST 表佔用"></a>1. 看一張表的 TOAST 表佔用</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    relname,</span><br><span class="line">    pg_size_pretty(pg_table_size(oid))   <span class="keyword">AS</span> table_size,</span><br><span class="line">    pg_size_pretty(pg_relation_size(oid)) <span class="keyword">AS</span> heap_size,</span><br><span class="line">    pg_size_pretty(pg_relation_size(reltoastrelid)) <span class="keyword">AS</span> toast_size,</span><br><span class="line">    pg_size_pretty(pg_indexes_size(oid)) <span class="keyword">AS</span> index_size</span><br><span class="line"><span class="keyword">FROM</span> pg_class</span><br><span class="line"><span class="keyword">WHERE</span> relkind <span class="operator">=</span> <span class="string">&#x27;r&#x27;</span> <span class="keyword">AND</span> reltoastrelid <span class="operator">&lt;&gt;</span> <span class="number">0</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> pg_table_size(oid) <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">10</span>;</span><br></pre></td></tr></table></figure><h3 id="2-直接戳-TOAST-表-進階-需-superuser"><a href="#2-直接戳-TOAST-表-進階-需-superuser" class="headerlink" title="2. 直接戳 TOAST 表 (進階,需 superuser)"></a>2. 直接戳 TOAST 表 (進階,需 superuser)</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 看某個 chunk_id 被切成幾塊</span></span><br><span class="line"><span class="keyword">SELECT</span> chunk_id, <span class="built_in">count</span>(<span class="operator">*</span>) <span class="keyword">AS</span> chunks, <span class="built_in">sum</span>(<span class="keyword">octet_length</span>(chunk_data)) <span class="keyword">AS</span> bytes</span><br><span class="line"><span class="keyword">FROM</span> pg_toast.pg_toast_16384</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> chunk_id</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> bytes <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">5</span>;</span><br></pre></td></tr></table></figure><h3 id="3-判斷某筆資料有沒有被-TOAST"><a href="#3-判斷某筆資料有沒有被-TOAST" class="headerlink" title="3. 判斷某筆資料有沒有被 TOAST"></a>3. 判斷某筆資料有沒有被 TOAST</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    id,</span><br><span class="line">    pg_column_size(payload)         <span class="keyword">AS</span> stored_size,    <span class="comment">-- 含 header、compressed 後</span></span><br><span class="line">    <span class="keyword">octet_length</span>(payload)           <span class="keyword">AS</span> logical_size,   <span class="comment">-- 邏輯長度</span></span><br><span class="line">    pg_column_compression(payload)  <span class="keyword">AS</span> compression     <span class="comment">-- &#x27;pglz&#x27; / &#x27;lz4&#x27; / NULL</span></span><br><span class="line"><span class="keyword">FROM</span> my_table <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>如果 <code>stored_size &lt;&lt; logical_size</code>,代表有 compression;如果 <code>stored_size</code> 只有十幾 bytes 但 <code>logical_size</code> 很大,就是 external 了。</p><hr><h2 id="九、Expanded-Datum-summary-沒提到的記憶體加速機制"><a href="#九、Expanded-Datum-summary-沒提到的記憶體加速機制" class="headerlink" title="九、Expanded Datum:summary 沒提到的記憶體加速機制"></a>九、Expanded Datum:summary 沒提到的記憶體加速機制</h2><p>當你在 PL&#x2F;pgSQL 裡反覆對一個 array 或 JSONB 做修改,每次都「decompress → 修改 → 重新 compress」會很慘。於是 PostgreSQL 9.5 引入了 <strong>expanded datum</strong> (<code>VARTAG_EXPANDED_RW</code>):</p><ul><li>第一次使用時,把資料攤平成一個記憶體中可直接操作的物件 (e.g., array 變成真的 C array)。</li><li>在 query 執行期間,所有修改都直接在這個展開物件上完成。</li><li>寫回磁碟時才重新序列化、compress、TOAST。</li></ul><p>這對 array 的 <code>array_append</code>、JSONB 的 <code>jsonb_set</code> 等操作幫助巨大。Expanded datum 也是 <code>varattrib_1b_e</code> 的一種特例 — 你會看到一個「指針」,但它指向的不是 TOAST 表,而是記憶體中的展開物件。</p><hr><h2 id="十、設計哲學總結"><a href="#十、設計哲學總結" class="headerlink" title="十、設計哲學總結"></a>十、設計哲學總結</h2><p>PostgreSQL 的 Varlena &#x2F; TOAST 設計可以用三句話概括:</p><ol><li><strong>小資料零成本</strong> — 短 header 讓 99% 的字串只多付 1 byte 的代價。</li><li><strong>大資料對 OLTP 友善</strong> — 主表只留 20 bytes 指針,讓 <code>SELECT id FROM ...</code> 等不需要該欄位的查詢完全不受影響。</li><li><strong>Lazy &amp; Composable</strong> — compression、external storage、expansion 三個正交機制,可根據 workload 用 storage strategy 與 compression method 自由組合。</li></ol><p>當你下次看到 PostgreSQL 一張表的 size 是「1 MB 主表 + 800 MB TOAST + 100 MB index」時,你會知道那是正常的:它不是 bug,而是讓主表保持精瘦、把肥肉外掛出去的精密設計。</p><hr><h2 id="小結"><a href="#小結" class="headerlink" title="小結"></a>小結</h2><table><thead><tr><th>元件</th><th>角色</th></tr></thead><tbody><tr><td><code>varattrib_1b</code></td><td>小字串的省空間 header (1 byte)</td></tr><tr><td><code>varattrib_4b</code></td><td>一般 &#x2F; compressed 資料的 header (4 bytes)</td></tr><tr><td><code>varattrib_1b_e</code></td><td>external &#x2F; expanded 的 tag</td></tr><tr><td><code>varatt_external</code></td><td>主表中的 18 byte TOAST 指針</td></tr><tr><td><code>pg_toast.*</code></td><td>切塊存放的 side table</td></tr><tr><td><code>(chunk_id, chunk_seq)</code> B-Tree</td><td>高速回組碎塊的索引</td></tr><tr><td>Storage strategy</td><td>控制是否 compress &#x2F; external</td></tr><tr><td>Compression method</td><td>pglz vs lz4 的權衡</td></tr><tr><td>Expanded datum</td><td>記憶體層的加速</td></tr></tbody></table><p>理解這套設計後,你不只能在出問題時看懂 <code>pg_column_size</code>、<code>pg_relation_size</code> 的真正含義,還能透過調整 storage 與 compression 為自己的 workload 榨出最後一滴效能。</p><hr><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li>PostgreSQL 原始碼 <code>src/include/varatt.h</code>、<code>src/backend/access/common/toast_internals.c</code></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/storage-toast.html">PostgreSQL Docs — TOAST</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/storage-page-layout.html">PostgreSQL Docs — Storage Page Layout</a></li><li>相關文章: <a href="https://isdaniel.github.io/pgrx-postgresql-extension-mechanism-deep-dive/">深入 pgrx 機制:用 Rust 撰寫 PostgreSQL Extension 的底層原理與實戰</a></li><li>相關文章: <a href="https://isdaniel.github.io/postgresql-wal-introduce/">PostgreSQL WAL (Write-Ahead Logging) 機制介紹</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/postgresql-varlena-toast-deep-dive/">https://isdaniel.github.io/postgresql-varlena-toast-deep-dive/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/postgresql-varlena-toast-deep-dive/</id>
    <link href="https://isdaniel.github.io/postgresql-varlena-toast-deep-dive/"/>
    <published>2026-06-16T06:30:00.000Z</published>
    <summary>從 Varlena 三種 header 格式到 TOAST 切塊、compression、B-Tree 索引、storage strategy 與 detoast 流程,完整拆解 PostgreSQL 如何優雅地塞下 1GB 大欄位</summary>
    <title>深入 PostgreSQL 變長度資料儲存：Varlena 與 TOAST 機制完整解析</title>
    <updated>2026-08-05T10:10:26.493Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/pgrx/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="SystemDesign" scheme="https://isdaniel.github.io/tags/SystemDesign/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/tags/pgrx/"/>
    <category term="Extension" scheme="https://isdaniel.github.io/tags/Extension/"/>
    <category term="FFI" scheme="https://isdaniel.github.io/tags/FFI/"/>
    <content>
      <![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>PostgreSQL 是用 C 撰寫的資料庫,它的 extension 系統設計上就是「載入 shared library 並呼叫其中的 C 函數」。長期以來,如果你想擴充 PostgreSQL — 寫一個自訂的 SQL function、Aggregate、Index、FDW、甚至 Background Worker — 都得用 C。但 C 開發體驗痛苦:手動管理記憶體、容易踩到 segfault、缺乏現代化的測試框架。</p><p><strong><a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx</a></strong> 改變了這個局面。它讓 Rust 開發者可以在不犧牲效能的前提下,享受型別安全、ownership、RAII 等好處,寫出與原生 C extension 同等地位的 PostgreSQL extension。</p><p>但 pgrx 究竟是怎麼讓 Rust 與 C 寫的 PostgreSQL 「完美合作」?這篇文章會帶你深入到最底層,並透過我自己研究的 <a target="_blank" rel="noopener" href="https://github.com/isdaniel/timescale-extension-utils-rs"><code>timescale-extension-utils-rs</code></a> 這個輕量級實作(它可以視為一個「迷你 pgrx」),把每一個關鍵機制拆給你看 — 你會看到 pgrx 在背後到底幫你做了多少事。</p><hr><h2 id="一、為什麼-Rust-能跟-C-寫的-PostgreSQL-互通"><a href="#一、為什麼-Rust-能跟-C-寫的-PostgreSQL-互通" class="headerlink" title="一、為什麼 Rust 能跟 C 寫的 PostgreSQL 互通"></a>一、為什麼 Rust 能跟 C 寫的 PostgreSQL 互通</h2><p>PostgreSQL extension 的兼容性不是「語言」問題,而是「ABI」問題, PostgreSQL 載入 extension 的流程很單純:</p><ol><li>使用 <code>dlopen()</code> 載入 <code>.so</code> &#x2F; <code>.dll</code> 檔。</li><li>用 <code>dlsym()</code> 查找特定符號(例如 <code>add_integers</code>、<code>pg_finfo_add_integers</code>、<code>Pg_magic_func</code>)。</li><li>拿到 function pointer 後直接呼叫。</li></ol><p>只要產出的二進位:</p><ul><li>符合 <strong>C ABI</strong>(stack frame、register passing、name mangling 關閉)。</li><li>內含 PostgreSQL 規定的 <strong>Magic Block</strong>(讓 PG 確認版本相容)。</li><li>函數使用 <strong>Version 1 Calling Convention</strong>(固定簽名 <code>Datum func(FunctionCallInfo)</code>)。</li></ul><p>那麼這個 extension 就跟 C 寫的沒兩樣。Rust 透過 <code>extern &quot;C&quot;</code>、<code>#[no_mangle]</code>、<code>#[repr(C)]</code> 三大武器完美滿足這些條件。</p><hr><h2 id="二、第一塊拼圖-bindgen-自動產生-FFI-綁定"><a href="#二、第一塊拼圖-bindgen-自動產生-FFI-綁定" class="headerlink" title="二、第一塊拼圖:bindgen 自動產生 FFI 綁定"></a>二、第一塊拼圖:bindgen 自動產生 FFI 綁定</h2><p>要在 Rust 呼叫 PostgreSQL 內部的 C 函數,你需要對應的 Rust 宣告。手寫上萬個 struct 和 function declaration 是不切實際的。</p><p>pgrx 和 timescale-extension-utils-rs 都使用 <strong>bindgen</strong> 在 <code>build.rs</code> 階段自動解析 PostgreSQL 的 C header,產生 Rust 的 <code>unsafe extern &quot;C&quot;</code> 宣告。</p><p>來看 <code>postgres-headers-rs/build.rs</code> 中的關鍵段落:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">pg_include</span> = <span class="title function_ invoke__">include_dir</span>(&amp;pg_config)</span><br><span class="line">    .<span class="title function_ invoke__">expect</span>(<span class="string">&quot;Could not find postgres install&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">bindings</span> = bindgen::Builder::<span class="title function_ invoke__">default</span>()</span><br><span class="line">    .<span class="title function_ invoke__">clang_arg</span>(<span class="built_in">format!</span>(<span class="string">&quot;-I&#123;&#125;&quot;</span>, pg_include))</span><br><span class="line">    .<span class="title function_ invoke__">header</span>(<span class="string">&quot;wrapper.h&quot;</span>)</span><br><span class="line">    .<span class="title function_ invoke__">generate</span>()</span><br><span class="line">    .<span class="title function_ invoke__">expect</span>(<span class="string">&quot;Unable to generate bindings&quot;</span>);</span><br><span class="line"></span><br><span class="line">bindings.<span class="title function_ invoke__">write_to_file</span>(out_path).<span class="title function_ invoke__">expect</span>(<span class="string">&quot;Couldn&#x27;t write bindings!&quot;</span>);</span><br></pre></td></tr></table></figure><p>而 <code>wrapper.h</code> 是個簡單的 C header,把所有需要暴露給 Rust 的 PostgreSQL header <code>#include</code> 進來:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;postgres.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;executor/spi.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;foreign/fdwapi.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;utils/builtins.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;utils/palloc.h&quot;</span></span></span><br><span class="line"><span class="comment">// ... 等等</span></span><br></pre></td></tr></table></figure><p>bindgen 會幫你產生類似這樣的 Rust 程式碼(輸出到 <code>OUT_DIR/generated.rs</code>):</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[repr(C)]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">FunctionCallInfoBaseData</span> &#123;</span><br><span class="line">    <span class="keyword">pub</span> flinfo: *<span class="keyword">mut</span> FmgrInfo,</span><br><span class="line">    <span class="keyword">pub</span> context: *<span class="keyword">mut</span> Node,</span><br><span class="line">    <span class="keyword">pub</span> resultinfo: *<span class="keyword">mut</span> Node,</span><br><span class="line">    <span class="keyword">pub</span> fncollation: Oid,</span><br><span class="line">    <span class="keyword">pub</span> isnull: <span class="type">bool</span>,</span><br><span class="line">    <span class="keyword">pub</span> nargs: c_short,</span><br><span class="line">    <span class="keyword">pub</span> args: __IncompleteArrayField&lt;NullableDatum&gt;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">palloc</span>(size: Size) <span class="punctuation">-&gt;</span> *<span class="keyword">mut</span> c_void;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">pfree</span>(pointer: *<span class="keyword">mut</span> c_void);</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">pg_re_throw</span>() <span class="punctuation">-&gt;</span> !;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">static</span> <span class="keyword">mut</span> CurrentMemoryContext: MemoryContext;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">static</span> <span class="keyword">mut</span> PG_exception_stack: *<span class="keyword">mut</span> sigjmp_buf;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong><code>#[repr(C)]</code></strong> 是這裡的關鍵 — 它強制 Rust struct 的 memory layout 完全比照 C struct,這樣當 PG 把 <code>FunctionCallInfo</code> 的 pointer 傳進來時,Rust 可以正確讀取每個欄位。</p><hr><h2 id="三、第二塊拼圖-Magic-Block-—-PG-載入時的握手協議"><a href="#三、第二塊拼圖-Magic-Block-—-PG-載入時的握手協議" class="headerlink" title="三、第二塊拼圖:Magic Block — PG 載入時的握手協議"></a>三、第二塊拼圖:Magic Block — PG 載入時的握手協議</h2><p>PostgreSQL 載入 shared library 時,<strong>第一件事</strong>就是去找 <code>Pg_magic_func()</code> 這個符號,呼叫它取得一個結構,比對裡面的版本資訊。版本不符就直接拒絕載入,避免 ABI 不相容造成的 crash。</p><p>在 C extension 裡,通常一行 <code>PG_MODULE_MAGIC;</code> 就解決。在 Rust 中我們得手動產生這個函數。看 <code>timescale-extension-utils-rs</code> 怎麼用 macro 包起來:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[macro_export]</span></span><br><span class="line"><span class="built_in">macro_rules!</span> pg_module &#123;</span><br><span class="line">    ($version:expr) =&gt; &#123;</span><br><span class="line">        <span class="meta">#[no_mangle]</span></span><br><span class="line">        <span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">Pg_magic_func</span>() <span class="punctuation">-&gt;</span> &amp;<span class="string">&#x27;static pg_sys::Pg_magic_struct &#123;</span></span><br><span class="line"><span class="string">            const MY_MAGIC: pg_sys::Pg_magic_struct = pg_sys::Pg_magic_struct &#123;</span></span><br><span class="line"><span class="string">                len: std::mem::size_of::&lt;pg_sys::Pg_magic_struct&gt;() as c_int,</span></span><br><span class="line"><span class="string">                version: $version,</span></span><br><span class="line"><span class="string">                funcmaxargs: pg_sys::FUNC_MAX_ARGS as c_int,</span></span><br><span class="line"><span class="string">                indexmaxkeys: pg_sys::INDEX_MAX_KEYS as c_int,</span></span><br><span class="line"><span class="string">                namedatalen: pg_sys::NAMEDATALEN as c_int,</span></span><br><span class="line"><span class="string">                float8byval: pg_sys::USE_FLOAT8_BYVAL as c_int,</span></span><br><span class="line"><span class="string">            &#125;;</span></span><br><span class="line"><span class="string">            &amp;MY_MAGIC</span></span><br><span class="line"><span class="string">        &#125;</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure><p>使用時只要一行:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">use</span> timescale_extension_utils::*;</span><br><span class="line">pg_module!(<span class="number">1400</span>);  <span class="comment">// 對應 PostgreSQL 14</span></span><br></pre></td></tr></table></figure><p>幾個重點:</p><ul><li><strong><code>#[no_mangle]</code></strong>:Rust 預設會對函數名做 name mangling(產生像 <code>_ZN3foo3barE</code> 這種符號),加了這個屬性才會保留原名,讓 <code>dlsym(&quot;Pg_magic_func&quot;)</code> 找得到。</li><li><strong><code>pub extern &quot;C&quot;</code></strong>:強制使用 C ABI。</li><li><strong><code>&amp;&#39;static</code></strong>:回傳一個指向 static 區段的 pointer,不會被釋放。</li></ul><p>pgrx 有對應的 <code>pg_module_magic!()</code> macro,功能完全一樣。</p><hr><h2 id="四、第三塊拼圖-V1-Calling-Convention-的橋接"><a href="#四、第三塊拼圖-V1-Calling-Convention-的橋接" class="headerlink" title="四、第三塊拼圖:V1 Calling Convention 的橋接"></a>四、第三塊拼圖:V1 Calling Convention 的橋接</h2><p>PostgreSQL 的 SQL function 在 C 層全部長這個樣子:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Datum <span class="title function_">my_func</span><span class="params">(PG_FUNCTION_ARGS)</span>;   <span class="comment">// 展開後簽名:Datum my_func(FunctionCallInfo fcinfo)</span></span><br><span class="line">PG_FUNCTION_INFO_V1(my_func);      <span class="comment">// 註冊 V1 metadata</span></span><br></pre></td></tr></table></figure><p>兩個關鍵元素:</p><ol><li><strong>每個 function 都必須有一個 <code>pg_finfo_xxx()</code> 函數</strong>回傳 <code>Pg_finfo_record { api_version: 1 }</code>,告訴 PG 「我用 V1 慣例」。</li><li><strong>函數簽名固定</strong>:接 <code>FunctionCallInfo</code>,回傳 <code>Datum</code>。參數從 <code>fcinfo-&gt;args[i]</code> 取出,return 值是個 <code>Datum</code>。</li></ol><h3 id="Rust-端的對應-pg-fn-macro-展開"><a href="#Rust-端的對應-pg-fn-macro-展開" class="headerlink" title="Rust 端的對應:pg_fn! macro 展開"></a>Rust 端的對應:<code>pg_fn!</code> macro 展開</h3><p>來看 <code>timescale-extension-utils-rs</code> 怎麼把它包成易用的 API。先看使用者程式碼(<code>example-extension/src/lib.rs</code>):</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">pg_fn! &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">add_integers</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">        a + b</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>從使用者角度看,這就是個普通的 Rust 函數。但展開後,macro 產生了<strong>兩個</strong> <code>extern &quot;C&quot;</code> 函數:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1) V1 metadata 函數</span></span><br><span class="line"><span class="meta">#[no_mangle]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">pg_finfo_add_integers</span>() <span class="punctuation">-&gt;</span> &amp;<span class="string">&#x27;static pg_sys::Pg_finfo_record &#123;</span></span><br><span class="line"><span class="string">    const V1_API: pg_sys::Pg_finfo_record = pg_sys::Pg_finfo_record &#123; api_version: 1 &#125;;</span></span><br><span class="line"><span class="string">    &amp;V1_API</span></span><br><span class="line"><span class="string">&#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">// 2) PostgreSQL 真正會呼叫的 wrapper</span></span><br><span class="line"><span class="string">#[no_mangle]</span></span><br><span class="line"><span class="string">pub extern &quot;C&quot; fn add_integers(fcinfo: pg_sys::FunctionCallInfo) -&gt; pg_sys::Datum &#123;</span></span><br><span class="line"><span class="string">    unsafe &#123;</span></span><br><span class="line"><span class="string">        in_context(CurrentMemoryContext, || &#123;</span></span><br><span class="line"><span class="string">            let fcinfo = &amp;mut *fcinfo;</span></span><br><span class="line"><span class="string">            // 把 catch_unwind、參數解析、回值轉換、錯誤處理全部封裝</span></span><br><span class="line"><span class="string">            // 內部會解析參數、執行 user code、轉成 Datum 回傳</span></span><br><span class="line"><span class="string">        &#125;)</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure><p>實際 macro 內部更複雜,我們稍後拆解。先看核心定義:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[macro_export]</span></span><br><span class="line"><span class="built_in">macro_rules!</span> pg_fn &#123;</span><br><span class="line">    ($(<span class="keyword">pub</span> <span class="keyword">fn</span> $name:<span class="title function_ invoke__">ident</span>($($arg:ident : $typ:ty),*) $(<span class="punctuation">-&gt;</span> $ret:ty)? $body:block)+) =&gt; &#123;</span><br><span class="line">        $(</span><br><span class="line">        $crate::pg_finfo!($name);</span><br><span class="line"></span><br><span class="line">        <span class="meta">#[no_mangle]</span></span><br><span class="line">        <span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> $<span class="title function_ invoke__">name</span>(fcinfo: $crate::pg_sys::FunctionCallInfo)</span><br><span class="line">            <span class="punctuation">-&gt;</span> $crate::pg_sys::Datum</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">unsafe</span> &#123;</span><br><span class="line">                $crate::palloc::<span class="title function_ invoke__">in_context</span>($crate::pg_sys::CurrentMemoryContext, || &#123;</span><br><span class="line">                    <span class="keyword">let</span> <span class="variable">fcinfo</span> = &amp;<span class="keyword">mut</span> *fcinfo;</span><br><span class="line">                    $crate::pg_fn_body!(fcinfo; $<span class="title function_ invoke__">name</span>( $($arg:$typ,)* ) $(<span class="punctuation">-&gt;</span> $ret)? $body);</span><br><span class="line">                &#125;)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;)+</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>這就是 pgrx 的 <code>#[pg_extern]</code> 巨集在做的事情:<strong>把使用者寫的純 Rust 函數,包裝成一個符合 PostgreSQL V1 calling convention 的 <code>extern &quot;C&quot;</code> 函數</strong>。</p><h3 id="對應到-SQL-註冊"><a href="#對應到-SQL-註冊" class="headerlink" title="對應到 SQL 註冊"></a>對應到 SQL 註冊</h3><p>extension 的 <code>.sql</code> 檔案裡會這樣註冊:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> REPLACE <span class="keyword">FUNCTION</span> add_integers(<span class="type">integer</span>, <span class="type">integer</span>)</span><br><span class="line"><span class="keyword">RETURNS</span> <span class="type">integer</span></span><br><span class="line"><span class="keyword">AS</span> <span class="string">&#x27;$libdir/libexample_extension&#x27;</span>, <span class="string">&#x27;add_integers&#x27;</span></span><br><span class="line"><span class="keyword">LANGUAGE</span> C IMMUTABLE STRICT;</span><br></pre></td></tr></table></figure><ul><li><code>LANGUAGE C</code> 告訴 PG「用 V1 慣例呼叫」。</li><li><code>&#39;$libdir/libexample_extension&#39;, &#39;add_integers&#39;</code> 指定 shared library 路徑和符號名 — 對應到我們 <code>#[no_mangle]</code> 出來的那個函數。</li></ul><p>pgrx 更進一步,<strong>自動產生這份 SQL</strong>(透過 <code>cargo pgrx schema</code>),所以你不用手寫。</p><hr><h2 id="五、Datum-轉換-Rust-型別-↔-PostgreSQL-型別"><a href="#五、Datum-轉換-Rust-型別-↔-PostgreSQL-型別" class="headerlink" title="五、Datum 轉換:Rust 型別 ↔ PostgreSQL 型別"></a>五、Datum 轉換:Rust 型別 ↔ PostgreSQL 型別</h2><p>PostgreSQL 內部所有值都用 <strong><code>Datum</code></strong> 表示 — 它本質上是個 <code>uintptr_t</code>(64-bit 平台是 <code>uint64</code>):</p><ul><li>小於等於 64 bit 的 by-value 型別(<code>int</code>, <code>bool</code>, <code>float</code>)直接塞進去。</li><li>大於 64 bit 的 by-reference 型別(<code>text</code>, <code>bytea</code>, <code>array</code>)塞 pointer 進去。</li></ul><p>Rust 是靜態型別語言,要在 SQL 動態型別與 Rust 靜態型別之間搭橋,需要 <strong>trait</strong>。看 <code>timescale-extension-utils/src/datum.rs</code>:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">trait</span> <span class="title class_">FromDatum</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from_datum</span>(datum: Datum) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">trait</span> <span class="title class_">ToDatum</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">to_datum</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> Datum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 處理 NULL:用 Option&lt;T&gt; 包起來</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">trait</span> <span class="title class_">FromOptionalDatum</span>: <span class="built_in">Sized</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">try_from_optional_datum</span>(datum: <span class="type">Option</span>&lt;Datum&gt;) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;<span class="keyword">Self</span>&gt;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">trait</span> <span class="title class_">ToOptionalDatum</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">to_optional_datum</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;Datum&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>整數型別的實作就是直接 cast:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">macro_rules!</span> int_datum_convert &#123;</span><br><span class="line">    ($($typ:ty)*) =&gt; &#123;</span><br><span class="line">        $(</span><br><span class="line">            <span class="keyword">impl</span> <span class="title class_">FromDatum</span> <span class="keyword">for</span> $typ &#123;</span><br><span class="line">                <span class="keyword">fn</span> <span class="title function_">from_datum</span>(datum: Datum) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123; datum <span class="keyword">as</span> <span class="keyword">Self</span> &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">impl</span> <span class="title class_">ToDatum</span> <span class="keyword">for</span> $typ &#123;</span><br><span class="line">                <span class="keyword">fn</span> <span class="title function_">to_datum</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> Datum &#123; <span class="keyword">self</span> <span class="keyword">as</span> Datum &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        )*</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br><span class="line">int_datum_convert!(<span class="type">i8</span> <span class="type">u8</span> <span class="type">i16</span> <span class="type">u16</span> <span class="type">i32</span> <span class="type">u32</span> <span class="type">i64</span> <span class="type">u64</span> <span class="type">isize</span> <span class="type">usize</span>);</span><br></pre></td></tr></table></figure><p>浮點數比較有趣 — 因為 <code>Datum</code> 是整數型別,所以 <code>f64</code> 必須先用 <code>to_bits()</code> &#x2F; <code>from_bits()</code> 轉成位元表示再塞進去:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">impl</span> <span class="title class_">FromDatum</span> <span class="keyword">for</span> <span class="title class_">f64</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from_datum</span>(datum: Datum) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123; <span class="type">f64</span>::<span class="title function_ invoke__">from_bits</span>(datum <span class="keyword">as</span> _) &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">ToDatum</span> <span class="keyword">for</span> <span class="title class_">f64</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">to_datum</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> Datum &#123; <span class="keyword">self</span>.<span class="title function_ invoke__">to_bits</span>() <span class="keyword">as</span> _ &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最關鍵的設計:<strong><code>Option&lt;T&gt;</code> 對應 SQL <code>NULL</code></strong>。這是 Rust 型別系統最漂亮的應用之一 — C 開發者常常忘記檢查 <code>fcinfo-&gt;args[i].isnull</code> 而搞出 crash,Rust 把這件事直接拉到型別層強制處理:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// SQL 的 NULL 自動對應到 Rust 的 None</span></span><br><span class="line"><span class="keyword">impl</span>&lt;T: FromDatum&gt; FromOptionalDatum <span class="keyword">for</span> <span class="title class_">Option</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from_optional_datum</span>(datum: <span class="type">Option</span>&lt;Datum&gt;) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        datum.<span class="title function_ invoke__">map</span>(&lt;T <span class="keyword">as</span> FromDatum&gt;::from_datum)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>用起來就是:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">pg_fn! &#123;</span><br><span class="line">    <span class="comment">// b 可以是 NULL — Rust 端就用 Option&lt;i32&gt;</span></span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">conditional_add</span>(a: <span class="type">i32</span>, b: <span class="type">Option</span>&lt;<span class="type">i32</span>&gt;) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">        <span class="keyword">match</span> b &#123;</span><br><span class="line">            <span class="title function_ invoke__">Some</span>(value) =&gt; a + value,</span><br><span class="line">            <span class="literal">None</span> =&gt; a,</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>pgrx 的型別映射更完整,涵蓋 <code>&amp;str</code> &#x2F; <code>String</code>(text 型別)、<code>Vec&lt;T&gt;</code>(array)、composite type 等,但底層原理完全一樣。</p><hr><h2 id="六、最棘手的核心-Memory-Model-與-Unwinding-Model"><a href="#六、最棘手的核心-Memory-Model-與-Unwinding-Model" class="headerlink" title="六、最棘手的核心:Memory Model 與 Unwinding Model"></a>六、最棘手的核心:Memory Model 與 Unwinding Model</h2><p>這是 pgrx 工程深度最深的兩個議題。我曾在另一篇分析中詳細拆解 — 這裡聚焦在 timescale-extension-utils-rs 的實際實作。</p><h3 id="6-1-Memory-Model-palloc-與全域-allocator"><a href="#6-1-Memory-Model-palloc-與全域-allocator" class="headerlink" title="6.1 Memory Model:palloc 與全域 allocator"></a>6.1 Memory Model:palloc 與全域 allocator</h3><p>PostgreSQL 不用 <code>malloc/free</code>,而是用 <strong>MemoryContext</strong>(階層式 arena allocator)。在 transaction、query、tuple 不同 scope 結束時整批 reset,即使中途 <code>ereport(ERROR)</code> 也能自動回收。</p><p>如果你在 Rust 端用標準 <code>Box&lt;T&gt;</code>、<code>Vec&lt;T&gt;</code>,它們呼叫的是系統 allocator,<strong>Postgres 不知道這些記憶體存在</strong>,error handler 不會幫你清,就會 leak。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[global_allocator]</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">mut</span> GLOBAL: PallocAllocator = PallocAllocator;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">PallocAllocator</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">unsafe</span> <span class="keyword">impl</span> <span class="title class_">GlobalAlloc</span> <span class="keyword">for</span> <span class="title class_">PallocAllocator</span> &#123;</span><br><span class="line">    <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">alloc</span>(&amp;<span class="keyword">self</span>, layout: Layout) <span class="punctuation">-&gt;</span> *<span class="keyword">mut</span> <span class="type">u8</span> &#123;</span><br><span class="line">        <span class="title function_ invoke__">MemoryContextAlloc</span>(CurrentMemoryContext, layout.<span class="title function_ invoke__">size</span>() <span class="keyword">as</span> _) <span class="keyword">as</span> *<span class="keyword">mut</span> _</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">dealloc</span>(&amp;<span class="keyword">self</span>, ptr: *<span class="keyword">mut</span> <span class="type">u8</span>, _layout: Layout) &#123;</span><br><span class="line">        <span class="title function_ invoke__">pfree</span>(ptr <span class="keyword">as</span> *<span class="keyword">mut</span> _)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">alloc_zeroed</span>(&amp;<span class="keyword">self</span>, layout: Layout) <span class="punctuation">-&gt;</span> *<span class="keyword">mut</span> <span class="type">u8</span> &#123;</span><br><span class="line">        <span class="title function_ invoke__">MemoryContextAllocZero</span>(CurrentMemoryContext, layout.<span class="title function_ invoke__">size</span>() <span class="keyword">as</span> _) <span class="keyword">as</span> *<span class="keyword">mut</span> _</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">realloc</span>(&amp;<span class="keyword">self</span>, ptr: *<span class="keyword">mut</span> <span class="type">u8</span>, _layout: Layout, new_size: <span class="type">usize</span>) <span class="punctuation">-&gt;</span> *<span class="keyword">mut</span> <span class="type">u8</span> &#123;</span><br><span class="line">        <span class="title function_ invoke__">repalloc</span>(ptr <span class="keyword">as</span> *<span class="keyword">mut</span> _, new_size <span class="keyword">as</span> _) <span class="keyword">as</span> *<span class="keyword">mut</span> _</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>這意味著 — <strong>整個 Rust extension 內所有的 <code>Box::new</code>、<code>Vec::push</code>、<code>String::from</code> 底層全部都走 PostgreSQL 的 <code>palloc</code></strong>。一旦發生 ereport 觸發的 longjmp,PostgreSQL 自然會在 reset MemoryContext 時把這些記憶體一起清掉,不會 leak。</p><p>這段註解寫得很坦白:</p><blockquote><p>There is an uncomfortable mismatch between rust’s memory allocation and postgres’s; rust tries to clean memory by using stack-based destructors, while postgres does so using arenas. … we use postgres’s MemoryContexts to manage memory, even though this is not strictly speaking safe.</p></blockquote><p>pgrx 的策略類似但更精緻 — 它預設用系統 allocator 配上一系列的 <code>PgBox&lt;T&gt;</code>、<code>PgMemoryContexts</code> 抽象,讓你顯式選擇哪些資料走 palloc、哪些走系統 allocator。</p><h3 id="6-2-切換-MemoryContext-的-RAII-守衛"><a href="#6-2-切換-MemoryContext-的-RAII-守衛" class="headerlink" title="6.2 切換 MemoryContext 的 RAII 守衛"></a>6.2 切換 MemoryContext 的 RAII 守衛</h3><p>PostgreSQL 寫法是手動切換 + 手動恢復:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">MemoryContext old = MemoryContextSwitchTo(target_ctx);</span><br><span class="line"><span class="comment">// ... do work ...</span></span><br><span class="line">MemoryContextSwitchTo(old);   <span class="comment">// 如果中間 ereport 跳走就完蛋</span></span><br></pre></td></tr></table></figure><p>timescale-extension-utils-rs 用 RAII 包成 <code>in_context</code>:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">in_context</span>&lt;T, F&gt;(context: MemoryContext, f: F) <span class="punctuation">-&gt;</span> T</span><br><span class="line"><span class="keyword">where</span> F: <span class="title function_ invoke__">FnOnce</span>() <span class="punctuation">-&gt;</span> T &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">old</span> = <span class="title function_ invoke__">replace</span>(&amp;<span class="keyword">mut</span> CurrentMemoryContext, context);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">_guard</span> = <span class="title function_ invoke__">MemoryContextGuard</span>(old);   <span class="comment">// 在 scope 結束時自動切回</span></span><br><span class="line">    <span class="title function_ invoke__">f</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">MemoryContextGuard</span>(<span class="keyword">pub</span> MemoryContext);</span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Drop</span> <span class="keyword">for</span> <span class="title class_">MemoryContextGuard</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">drop</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>) &#123;</span><br><span class="line">        <span class="keyword">unsafe</span> &#123; <span class="title function_ invoke__">memory_context_switch_to</span>(<span class="keyword">self</span>.<span class="number">0</span>); &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>這就是 pgrx 的 <code>PgMemoryContexts::CurrentMemoryContext.switch_to(|ctx| { ... })</code> 的本質。</p><h3 id="6-3-Pox-—-不會在-Drop-時釋放的-Box"><a href="#6-3-Pox-—-不會在-Drop-時釋放的-Box" class="headerlink" title="6.3 Pox&lt;T&gt; — 不會在 Drop 時釋放的 Box"></a>6.3 <code>Pox&lt;T&gt;</code> — 不會在 Drop 時釋放的 Box</h3><p>對於要 across function call 存活的資料(典型是 aggregate 的 state),你不希望 Rust 的 Drop 提前 free 掉。所以有了 <code>Pox&lt;T&gt;</code>:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">Pox</span>&lt;T: ?<span class="built_in">Sized</span>&gt;(NonNull&lt;T&gt;, PhantomData&lt;T&gt;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;T&gt; Pox&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">new</span>(val: T) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        <span class="keyword">unsafe</span> &#123;</span><br><span class="line">            <span class="title function_ invoke__">Pox</span>(NonNull::<span class="title function_ invoke__">new_unchecked</span>(<span class="type">Box</span>::<span class="title function_ invoke__">into_raw</span>(<span class="type">Box</span>::<span class="title function_ invoke__">new</span>(val))), PhantomData)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">into_raw</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> *<span class="keyword">mut</span> T &#123; <span class="keyword">self</span>.<span class="number">0</span>.<span class="title function_ invoke__">as_ptr</span>() &#125;</span><br><span class="line">    <span class="comment">// 注意:沒有 Drop 實作!</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它擁有 <code>Box&lt;T&gt;</code> 的 API(<code>Deref</code>、<code>DerefMut</code>),但<strong>沒有實作 <code>Drop</code></strong>。釋放時機交給 PostgreSQL 的 aggregate context 自動處理。pgrx 的 <code>PgBox&lt;T, AllocatedByPostgres&gt;</code> 是同個概念。</p><hr><h2 id="七、Unwinding-Model-setjmp-longjmp-與-panic-的雙向轉換"><a href="#七、Unwinding-Model-setjmp-longjmp-與-panic-的雙向轉換" class="headerlink" title="七、Unwinding Model:setjmp&#x2F;longjmp 與 panic 的雙向轉換"></a>七、Unwinding Model:setjmp&#x2F;longjmp 與 panic 的雙向轉換</h2><p>這是整個 pgrx 機制裡最精巧、也最危險的一環。</p><p><strong>問題的本質</strong>:</p><ul><li><strong>Rust</strong> 用 stack unwinding 處理 panic(會跑所有 Drop)。</li><li><strong>PostgreSQL</strong> 用 <code>setjmp/longjmp</code> 處理錯誤(CPU register 瞬移,跳過中間所有 frame)。</li><li>兩種模型互相穿透就是 UB:Rust panic 穿過 C frame → 不可預測;PG longjmp 穿過 Rust frame → Drop 不跑、leak、deadlock。</li></ul><h3 id="7-1-入口處-catch-unwind-防-panic-外洩"><a href="#7-1-入口處-catch-unwind-防-panic-外洩" class="headerlink" title="7.1 入口處:catch_unwind 防 panic 外洩"></a>7.1 入口處:catch_unwind 防 panic 外洩</h3><p>看 <code>pg_fn_body!</code> macro 的核心:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">result</span>: <span class="type">Result</span>&lt;<span class="type">Option</span>&lt;pg_sys::Datum&gt;, _&gt; = <span class="title function_ invoke__">catch_unwind</span>(<span class="title function_ invoke__">AssertUnwindSafe</span>(|| &#123;</span><br><span class="line">    <span class="comment">// 解析參數</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">args</span> = <span class="title function_ invoke__">get_args</span>(&amp;*fc);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">a</span> = &lt;<span class="type">i32</span> <span class="keyword">as</span> FromOptionalDatum&gt;::<span class="title function_ invoke__">try_from_optional_datum</span>(</span><br><span class="line">        args.<span class="title function_ invoke__">next</span>().<span class="title function_ invoke__">unwrap</span>()</span><br><span class="line">    ).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">b</span> = &lt;<span class="type">i32</span> <span class="keyword">as</span> FromOptionalDatum&gt;::<span class="title function_ invoke__">try_from_optional_datum</span>(</span><br><span class="line">        args.<span class="title function_ invoke__">next</span>().<span class="title function_ invoke__">unwrap</span>()</span><br><span class="line">    ).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 執行使用者寫的 Rust code</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">res</span> = (|| &#123; a + b &#125;)();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 轉成 Datum</span></span><br><span class="line">    &lt;<span class="type">i32</span> <span class="keyword">as</span> ToOptionalDatum&gt;::<span class="title function_ invoke__">to_optional_datum</span>(res)</span><br><span class="line">&#125;));</span><br><span class="line"></span><br><span class="line"><span class="keyword">match</span> result &#123;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(<span class="title function_ invoke__">Some</span>(datum)) =&gt; &#123; fc.isnull = <span class="literal">false</span>; <span class="keyword">return</span> datum; &#125;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(<span class="literal">None</span>)        =&gt; &#123; fc.isnull = <span class="literal">true</span>;  <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">    <span class="title function_ invoke__">Err</span>(err)        =&gt; &#123; fc.isnull = <span class="literal">true</span>;  <span class="title function_ invoke__">handle_unwind</span>(err) &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>catch_unwind</code> 攔截 Rust panic,<strong>Rust 所有 frame 在這之前已經正確 unwind、所有 Drop 都跑完</strong>,然後乾淨地呼叫 <code>handle_unwind</code> 把錯誤翻譯成 PostgreSQL 的 <code>ereport</code>。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">handle_unwind</span>(err: <span class="type">Box</span>&lt;<span class="keyword">dyn</span> Any + <span class="built_in">Send</span> + <span class="string">&#x27;static&gt;) -&gt; ! &#123;</span></span><br><span class="line"><span class="string">    if let Some(err) = err.downcast_ref::&lt;PGError&gt;() &#123;</span></span><br><span class="line"><span class="string">        unsafe &#123; err.re_throw() &#125;   // 如果原本就是 PG error,直接重拋</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">    if let Some(msg) = err.downcast_ref::&lt;&amp;&#x27;</span><span class="keyword">static</span> <span class="type">str</span>&gt;() &#123;</span><br><span class="line">        crate::elog!(#unguarded Error, <span class="string">&quot;internal panic: &#123;&#125;&quot;</span>, msg);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    crate::elog!(#unguarded Error, <span class="string">&quot;internal panic&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="7-2-出口處-guard-pg-把-longjmp-轉成-panic"><a href="#7-2-出口處-guard-pg-把-longjmp-轉成-panic" class="headerlink" title="7.2 出口處:guard_pg 把 longjmp 轉成 panic"></a>7.2 出口處:guard_pg 把 longjmp 轉成 panic</h3><p>當 Rust 程式碼要呼叫 PostgreSQL 的 C 函數(例如 <code>palloc</code>、<code>SPI_execute</code>),這些 C 函數內部可能 <code>ereport(ERROR)</code> 觸發 <code>longjmp</code>。如果直接跳走,中間的 Rust frame 的 <code>Drop</code> 就不會跑。</p><p><code>guard_pg</code> 是處理這個的關鍵函數:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">guard_pg</span>&lt;R, F: <span class="title function_ invoke__">FnOnce</span>() <span class="punctuation">-&gt;</span> R&gt;(f: F) <span class="punctuation">-&gt;</span> R &#123;</span><br><span class="line">    <span class="comment">// 1) 保存原本的 exception stack</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">original_exception_stack</span> = pg_sys::PG_exception_stack;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">local_exception_stack</span>: MaybeUninit&lt;sigjmp_buf&gt; = MaybeUninit::<span class="title function_ invoke__">uninit</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2) 安裝我們自己的 setjmp 點</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">jumped</span> = pg_sys::<span class="title function_ invoke__">sigsetjmp</span>(</span><br><span class="line">        local_exception_stack.<span class="title function_ invoke__">as_mut_ptr</span>() <span class="keyword">as</span> *<span class="keyword">mut</span> _,</span><br><span class="line">        <span class="number">1</span>,</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> jumped != <span class="number">0</span> &#123;</span><br><span class="line">        <span class="comment">// 3) longjmp 跳回這裡 — C 那邊發生 ereport 了</span></span><br><span class="line">        pg_sys::PG_exception_stack = original_exception_stack;</span><br><span class="line">        <span class="title function_ invoke__">compiler_fence</span>(Ordering::SeqCst);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4) 把 longjmp 「升級」成 Rust panic</span></span><br><span class="line">        <span class="comment">//    這樣 Rust 端的 catch_unwind 接手,沿途所有 Drop 正常執行</span></span><br><span class="line">        <span class="built_in">panic!</span>(PGError);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 5) 安裝我們的 jmpbuf,執行 C 函數</span></span><br><span class="line">    pg_sys::PG_exception_stack = local_exception_stack.<span class="title function_ invoke__">as_mut_ptr</span>() <span class="keyword">as</span> *<span class="keyword">mut</span> _;</span><br><span class="line">    <span class="title function_ invoke__">compiler_fence</span>(Ordering::SeqCst);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">result</span> = <span class="title function_ invoke__">f</span>();</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">compiler_fence</span>(Ordering::SeqCst);</span><br><span class="line">    pg_sys::PG_exception_stack = original_exception_stack;</span><br><span class="line"></span><br><span class="line">    result</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>整段流程是個漂亮的迴圈:</p><figure class="highlight scss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-attr">[PG C frame]</span></span><br><span class="line">   ↓ 呼叫</span><br><span class="line"><span class="selector-attr">[Rust wrapper (pg_fn_body — catch_unwind 安裝)]</span></span><br><span class="line">   ↓</span><br><span class="line"><span class="selector-attr">[Rust 業務邏輯 (持有 Vec、MutexGuard...)]</span></span><br><span class="line">   ↓ 呼叫</span><br><span class="line"><span class="selector-attr">[guard_pg (sigsetjmp 安裝)]</span></span><br><span class="line">   ↓</span><br><span class="line"><span class="selector-attr">[PG C function (e.g., SPI_execute)]</span></span><br><span class="line">   ↓ 內部</span><br><span class="line"><span class="selector-attr">[ereport(ERROR) → siglongjmp]</span></span><br><span class="line">   ↓ 跳回 sigsetjmp 點</span><br><span class="line"><span class="selector-attr">[guard_pg 攔截,panic!(PGError)]</span></span><br><span class="line">   ↓ Rust unwind 開始</span><br><span class="line"><span class="selector-attr">[Rust 業務邏輯的 Vec, MutexGuard 被 Drop]</span></span><br><span class="line">   ↓ 繼續 unwind</span><br><span class="line"><span class="selector-attr">[Rust wrapper 的 catch_unwind 接住]</span></span><br><span class="line">   ↓ handle_unwind → <span class="built_in">pg_re_throw</span>()</span><br><span class="line"><span class="selector-attr">[PG 的 transaction abort handler]</span></span><br></pre></td></tr></table></figure><p><strong>兩種 unwinding model 互相轉換了兩次</strong>,但每一段都用對應模型的「合法手段」運作,沒有任何一個 frame 被偷偷跳過。pgrx 用屬性 <code>#[pg_guard]</code> 自動把這層包到每個 <code>extern &quot;C&quot;</code> 函數上;timescale-extension-utils-rs 則靠手動 <code>guard_pg</code> 或包進 macro 裡。</p><hr><h2 id="八、Aggregate-函數-狀態跨呼叫的存活"><a href="#八、Aggregate-函數-狀態跨呼叫的存活" class="headerlink" title="八、Aggregate 函數:狀態跨呼叫的存活"></a>八、Aggregate 函數:狀態跨呼叫的存活</h2><p>Aggregate 函數(<code>SUM</code>、<code>AVG</code> 之類)在 PostgreSQL 內部是用兩個函數實作的:</p><ul><li><strong>State function</strong> (<code>sfunc</code>):每處理一個 row 就呼叫一次,更新累積狀態。</li><li><strong>Final function</strong> (<code>finalfunc</code>):所有 row 處理完後呼叫,把狀態轉成最終結果。</li></ul><p>狀態需要在多次呼叫之間活著 — 不能讓 Rust 的 Drop 提前釋放,但又要在 aggregate 結束時被回收。<code>timescale-extension-utils-rs</code> 用 <code>pg_agg!</code> macro + <code>Pox&lt;T&gt;</code> 解決:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[derive(Debug)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">SumState</span> &#123;</span><br><span class="line">    sum: <span class="type">f64</span>,</span><br><span class="line">    count: <span class="type">i64</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">pg_agg! &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">avg_state</span>(state: <span class="type">Option</span>&lt;Pox&lt;SumState&gt;&gt;, value: <span class="type">f64</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;Pox&lt;SumState&gt;&gt; &#123;</span><br><span class="line">        <span class="keyword">match</span> state &#123;</span><br><span class="line">            <span class="title function_ invoke__">Some</span>(<span class="keyword">mut</span> s) =&gt; &#123;</span><br><span class="line">                s.sum += value;</span><br><span class="line">                s.count += <span class="number">1</span>;</span><br><span class="line">                <span class="title function_ invoke__">Some</span>(s)</span><br><span class="line">            &#125;,</span><br><span class="line">            <span class="literal">None</span> =&gt; <span class="title function_ invoke__">Some</span>(Pox::<span class="title function_ invoke__">new</span>(SumState &#123; sum: value, count: <span class="number">1</span> &#125;))</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">pg_agg! &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">avg_final</span>(state: <span class="type">Option</span>&lt;Pox&lt;SumState&gt;&gt;) <span class="punctuation">-&gt;</span> <span class="type">f64</span> &#123;</span><br><span class="line">        state.<span class="title function_ invoke__">map</span>(|s| <span class="keyword">if</span> s.count &gt; <span class="number">0</span> &#123; s.sum / s.count <span class="keyword">as</span> <span class="type">f64</span> &#125; <span class="keyword">else</span> &#123; <span class="number">0.0</span> &#125;)</span><br><span class="line">             .<span class="title function_ invoke__">unwrap_or</span>(<span class="number">0.0</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>macro 展開時做幾件特別的事:</p><ol><li>呼叫 <code>AggCheckCallContext(fcinfo, &amp;mut agg_ctx)</code> 驗證確實是從 aggregate context 呼叫的,並取得 aggregate 的 MemoryContext。</li><li>切換到 <code>agg_ctx</code> 再執行 user code — 確保所有分配都掛在 aggregate 的生命週期上。</li><li>State 用 <code>Pox&lt;T&gt;</code>(無 Drop)— 不會被 Rust 提前釋放,交給 aggregate context 自動清。</li></ol><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">agg_ctx</span>: MemoryContext = std::ptr::<span class="title function_ invoke__">null_mut</span>();</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">unsafe</span> &#123; <span class="title function_ invoke__">AggCheckCallContext</span>(fcinfo, &amp;<span class="keyword">mut</span> agg_ctx) == <span class="number">0</span> &#125; &#123;</span><br><span class="line">    elog!(Error, <span class="built_in">concat!</span>(<span class="string">&quot;must call &quot;</span>, <span class="built_in">stringify!</span>($name), <span class="string">&quot; as an aggregate&quot;</span>))</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">unsafe</span> &#123;</span><br><span class="line">    <span class="title function_ invoke__">in_context</span>(agg_ctx, || &#123;</span><br><span class="line">        <span class="comment">// ... user body ...</span></span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>SQL 端註冊長這樣:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> AGGREGATE custom_avg(<span class="type">double precision</span>) (</span><br><span class="line">    SFUNC <span class="operator">=</span> avg_state,</span><br><span class="line">    STYPE <span class="operator">=</span> internal,</span><br><span class="line">    FINALFUNC <span class="operator">=</span> avg_final</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 使用:</span></span><br><span class="line"><span class="keyword">SELECT</span> custom_avg(score) <span class="keyword">FROM</span> scores;</span><br></pre></td></tr></table></figure><p>pgrx 也支援 aggregate(<code>#[pg_aggregate]</code> 屬性),原理完全一樣 — 只是 API 更簡潔。</p><hr><h2 id="九、把所有東西串起來-從-rs-到-CREATE-EXTENSION"><a href="#九、把所有東西串起來-從-rs-到-CREATE-EXTENSION" class="headerlink" title="九、把所有東西串起來:從 .rs 到 CREATE EXTENSION"></a>九、把所有東西串起來:從 .rs 到 CREATE EXTENSION</h2><p>到這裡為止的所有元件,組合起來就是一條完整的「Rust → PostgreSQL extension」流水線。我用 timescale-extension-utils-rs 的 example-extension 演示一次:</p><h3 id="步驟-1-寫-Rust-程式碼"><a href="#步驟-1-寫-Rust-程式碼" class="headerlink" title="步驟 1:寫 Rust 程式碼"></a>步驟 1:寫 Rust 程式碼</h3><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">use</span> timescale_extension_utils::*;</span><br><span class="line"></span><br><span class="line">pg_module!(<span class="number">1400</span>);   <span class="comment">// 產生 Pg_magic_func()</span></span><br><span class="line"></span><br><span class="line">pg_fn! &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">add_integers</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123; a + b &#125;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">square</span>(x: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123; x * x &#125;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">factorial</span>(n: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i64</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> n &lt;= <span class="number">1</span> &#123; <span class="number">1</span> &#125; <span class="keyword">else</span> &#123; (<span class="number">2</span>..=n <span class="keyword">as</span> <span class="type">i64</span>).<span class="title function_ invoke__">product</span>() &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="步驟-2-編譯成-shared-library"><a href="#步驟-2-編譯成-shared-library" class="headerlink" title="步驟 2:編譯成 shared library"></a>步驟 2:編譯成 shared library</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> example-extension</span><br><span class="line">cargo build --release</span><br><span class="line"><span class="comment"># 產出 target/release/libexample_extension.so</span></span><br></pre></td></tr></table></figure><h3 id="步驟-3-複製到-PostgreSQL-的-lib-目錄"><a href="#步驟-3-複製到-PostgreSQL-的-lib-目錄" class="headerlink" title="步驟 3:複製到 PostgreSQL 的 lib 目錄"></a>步驟 3:複製到 PostgreSQL 的 lib 目錄</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">PG_LIB_DIR=$(pg_config --pkglibdir)</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> ../target/release/libexample_extension.so <span class="variable">$PG_LIB_DIR</span>/</span><br></pre></td></tr></table></figure><h3 id="步驟-4-在-SQL-註冊函數"><a href="#步驟-4-在-SQL-註冊函數" class="headerlink" title="步驟 4:在 SQL 註冊函數"></a>步驟 4:在 SQL 註冊函數</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> REPLACE <span class="keyword">FUNCTION</span> add_integers(<span class="type">integer</span>, <span class="type">integer</span>)</span><br><span class="line"><span class="keyword">RETURNS</span> <span class="type">integer</span></span><br><span class="line"><span class="keyword">AS</span> <span class="string">&#x27;$libdir/libexample_extension&#x27;</span>, <span class="string">&#x27;add_integers&#x27;</span></span><br><span class="line"><span class="keyword">LANGUAGE</span> C IMMUTABLE STRICT;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> REPLACE <span class="keyword">FUNCTION</span> square(<span class="type">integer</span>)</span><br><span class="line"><span class="keyword">RETURNS</span> <span class="type">integer</span></span><br><span class="line"><span class="keyword">AS</span> <span class="string">&#x27;$libdir/libexample_extension&#x27;</span>, <span class="string">&#x27;square&#x27;</span></span><br><span class="line"><span class="keyword">LANGUAGE</span> C IMMUTABLE STRICT;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> REPLACE <span class="keyword">FUNCTION</span> factorial(<span class="type">integer</span>)</span><br><span class="line"><span class="keyword">RETURNS</span> <span class="type">bigint</span></span><br><span class="line"><span class="keyword">AS</span> <span class="string">&#x27;$libdir/libexample_extension&#x27;</span>, <span class="string">&#x27;factorial&#x27;</span></span><br><span class="line"><span class="keyword">LANGUAGE</span> C IMMUTABLE STRICT;</span><br></pre></td></tr></table></figure><h3 id="步驟-5-使用"><a href="#步驟-5-使用" class="headerlink" title="步驟 5:使用"></a>步驟 5:使用</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> add_integers(<span class="number">5</span>, <span class="number">3</span>);   <span class="comment">-- 8</span></span><br><span class="line"><span class="keyword">SELECT</span> square(<span class="number">4</span>);            <span class="comment">-- 16</span></span><br><span class="line"><span class="keyword">SELECT</span> factorial(<span class="number">10</span>);        <span class="comment">-- 3628800</span></span><br></pre></td></tr></table></figure><h3 id="首次呼叫的執行時序"><a href="#首次呼叫的執行時序" class="headerlink" title="首次呼叫的執行時序"></a>首次呼叫的執行時序</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>. psql 送出 SELECT <span class="title function_ invoke__">add_integers</span>(<span class="number">5</span>, <span class="number">3</span>);</span><br><span class="line"><span class="number">2</span>. PG planner 查 pg_proc catalog,知道 add_integers 在</span><br><span class="line">   <span class="variable">$libdir</span>/libexample_extension 的 add_integers 符號</span><br><span class="line"><span class="number">3</span>. 首次需要時 <span class="title function_ invoke__">dlopen</span>() 載入 libexample_extension.so</span><br><span class="line">   ├─ 呼叫 <span class="title function_ invoke__">Pg_magic_func</span>() 驗證 ABI 版本</span><br><span class="line">   ├─ <span class="title function_ invoke__">dlsym</span>(<span class="string">&quot;pg_finfo_add_integers&quot;</span>) → 確認 API v1</span><br><span class="line">   └─ <span class="title function_ invoke__">dlsym</span>(<span class="string">&quot;add_integers&quot;</span>) → 拿到 <span class="function"><span class="keyword">function</span> <span class="title">pointer</span></span></span><br><span class="line"><span class="function">4. <span class="title">PG</span> 建立 <span class="title">FunctionCallInfo</span>,塞入 <span class="title">Datum</span>(<span class="params"><span class="number">5</span></span>) 與 <span class="title">Datum</span>(<span class="params"><span class="number">3</span></span>)</span></span><br><span class="line"><span class="function">5. 透過 <span class="title">function</span> <span class="title">pointer</span> 呼叫 <span class="title">add_integers</span>(<span class="params">fcinfo</span>)</span></span><br><span class="line"><span class="function">   ↓ <span class="title">macro</span> 展開的 <span class="title">wrapper</span> 接手</span></span><br><span class="line"><span class="function">   ↓ <span class="title">in_context</span>(<span class="params">CurrentMemoryContext, ...</span>)</span></span><br><span class="line"><span class="function">   ↓ <span class="title">catch_unwind</span> 安裝</span></span><br><span class="line"><span class="function">   ↓ <span class="title">FromDatum</span>:<span class="title">Datum</span> → <span class="title">i32</span>(<span class="params">對 <span class="number">5</span> 和 <span class="number">3</span></span>)</span></span><br><span class="line"><span class="function">   ↓ 執行純 <span class="title">Rust</span> 的 <span class="title">a</span> + <span class="title">b</span> → 8</span></span><br><span class="line"><span class="function">   ↓ <span class="title">ToDatum</span>:<span class="title">i32</span> → <span class="title">Datum</span>(<span class="params"><span class="number">8</span></span>)</span></span><br><span class="line"><span class="function">6. 回傳 <span class="title">Datum</span>(<span class="params"><span class="number">8</span></span>) 給 <span class="title">executor</span> → 顯示給 <span class="title">user</span></span></span><br></pre></td></tr></table></figure><hr><h2 id="十、初學者的學習建議路徑"><a href="#十、初學者的學習建議路徑" class="headerlink" title="十、初學者的學習建議路徑"></a>十、初學者的學習建議路徑</h2><p>如果你想入門 pgrx,我建議這個順序:</p><ol><li><p><strong>了解 PostgreSQL extension 的 C 開發模式</strong> — 讀官方文件 <a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/extend.html">Extending SQL</a> 跟一個 C 寫的小 extension 的原始碼(例如 <code>contrib/citext</code>)。這會讓你知道 <code>Datum</code>、<code>PG_FUNCTION_INFO_V1</code>、<code>fmgr</code> 是什麼。</p></li><li><p><strong>看 timescale-extension-utils-rs 的原始碼</strong> — 因為它小、純,沒有過多的抽象。讀完 <code>lib.rs</code> 那幾個 macro,你就理解了 pgrx 的精髓。</p></li><li><p><strong>跑 pgrx 的官方 quickstart</strong>:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cargo install cargo-pgrx</span><br><span class="line">cargo pgrx init</span><br><span class="line">cargo pgrx new myext</span><br><span class="line"><span class="built_in">cd</span> myext</span><br><span class="line">cargo pgrx run</span><br></pre></td></tr></table></figure><p>你會在 5 分鐘內有一個能跑的 Rust extension。</p></li><li><p><strong>讀 pgrx 的 <code>examples/</code> 目錄</strong> — 從 <code>aggregate.rs</code>、<code>bgworker.rs</code>、<code>triggers.rs</code> 看不同類型的 extension 怎麼寫。</p></li><li><p><strong>動手寫一個有意義的小專案</strong> — 例如我之前做過的 <a href="https://isdaniel.github.io/pg-where-guard-rust-postgresql-extension/">pg_where_guard</a>(攔截危險 SQL)、<a href="https://isdaniel.github.io/rust-pgrx-extension-fdw/">redis_fdw</a>(把 Redis 當成 PG 表查)。實作的過程會逼你面對 Datum 轉換、MemoryContext、error handling 等真實問題。</p></li></ol><hr><h2 id="小結"><a href="#小結" class="headerlink" title="小結"></a>小結</h2><p>pgrx 之所以能讓 Rust 與 C 寫的 PostgreSQL 「完美合作」,核心是六個對接層:</p><ol><li><strong>bindgen</strong> → 把 C header 自動翻成 Rust FFI 宣告。</li><li><strong><code>#[repr(C)]</code> + <code>extern &quot;C&quot;</code> + <code>#[no_mangle]</code></strong> → 滿足 C ABI 與符號可見性。</li><li><strong><code>Pg_magic_func</code> Magic Block</strong> → 通過 PG 載入時的 ABI 版本握手。</li><li><strong>V1 calling convention + <code>pg_finfo_xxx</code></strong> → 把 Rust 函數包成 PG 可呼叫的形式。</li><li><strong><code>FromDatum</code> &#x2F; <code>ToDatum</code> trait</strong> → Rust 靜態型別 ↔ PG 動態 Datum 的雙向轉換,<code>Option&lt;T&gt;</code> 對應 NULL。</li><li><strong>Allocator 替換 + <code>catch_unwind</code> + <code>sigsetjmp</code> 雙向 guard</strong> → 解決 memory model 與 unwinding model 的衝突,讓 Rust 的 RAII 與 PG 的 longjmp &#x2F; MemoryContext 能各自正確運作。</li></ol><p>透過 <code>timescale-extension-utils-rs</code> 這個輕量版本,你能用最少的程式碼看清楚每一層的設計動機。當你回頭去讀 pgrx 的 source code 時,會發現所有複雜的抽象都是這六層的延伸與強化。</p><p>Rust + pgrx 真正讓 PostgreSQL extension 開發從「危險的系統程式設計」變成「現代化的應用程式設計」</p><hr><h2 id="參考資源"><a href="#參考資源" class="headerlink" title="參考資源"></a>參考資源</h2><ul><li><a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx GitHub</a> — 官方框架</li><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/timescale-extension-utils-rs">timescale-extension-utils-rs (my fork)</a> — 本文使用的迷你實作範例</li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/extend-pgxs.html">PostgreSQL Extension Building Infrastructure</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/xfunc-c.html">PostgreSQL C Language Functions</a></li><li><a target="_blank" rel="noopener" href="https://rust-lang.github.io/rust-bindgen/">bindgen User Guide</a></li><li><a target="_blank" rel="noopener" href="https://doc.rust-lang.org/nomicon/ffi.html">The Rustonomicon — FFI</a></li><li>站內相關文章:<ul><li><a href="https://isdaniel.github.io/rust-pgrx-extension-fdw/">Building a PostgreSQL FDW in Rust with pgrx</a></li><li><a href="https://isdaniel.github.io/pg-where-guard-rust-postgresql-extension/">Building Safe PostgreSQL Extensions with Rust — pg_where_guard</a></li></ul></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pgrx-postgresql-extension-mechanism-deep-dive/">https://isdaniel.github.io/pgrx-postgresql-extension-mechanism-deep-dive/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pgrx-postgresql-extension-mechanism-deep-dive/</id>
    <link href="https://isdaniel.github.io/pgrx-postgresql-extension-mechanism-deep-dive/"/>
    <published>2026-05-17T02:50:42.000Z</published>
    <summary>從零開始拆解 pgrx 如何讓 Rust 與 C 寫的 PostgreSQL 完美互通,並以 timescale-extension-utils-rs 為例,帶初學者了解 pg_fn!、pg_agg!、Datum 轉換、MemoryContext 與 longjmp 處理的完整機制</summary>
    <title>深入 pgrx 機制：用 Rust 撰寫 PostgreSQL Extension 的底層原理與實戰</title>
    <updated>2026-08-05T10:10:26.491Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/logical-replication/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/tags/logical-replication/"/>
    <category term="migration" scheme="https://isdaniel.github.io/tags/migration/"/>
    <category term="CDC" scheme="https://isdaniel.github.io/tags/CDC/"/>
    <content>
      <![CDATA[<h1 id="pg-dbmigrator-Near-Zero-Downtime-PostgreSQL-Migration-with-Online-Mode"><a href="#pg-dbmigrator-Near-Zero-Downtime-PostgreSQL-Migration-with-Online-Mode" class="headerlink" title="pg_dbmigrator: Near-Zero-Downtime PostgreSQL Migration with Online Mode"></a>pg_dbmigrator: Near-Zero-Downtime PostgreSQL Migration with Online Mode</h1><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg_dbmigrator">pg_dbmigrator</a> is a Rust-based CLI and library for moving PostgreSQL databases between two endpoints. It supports two strategies under one tool:</p><ul><li><strong>Offline mode</strong> — a one-shot <code>pg_dump</code> → <code>pg_restore</code>, suitable for maintenance windows.</li><li><strong>Online mode</strong> — a streaming pipeline built on PostgreSQL’s logical replication that copies the initial snapshot, then continuously applies WAL changes until you trigger cutover.</li></ul><p>If you’ve ever had to migrate a busy PostgreSQL database between hosts, regions, or cloud providers (on-prem → RDS, RDS → Aurora, Azure → AWS, major-version upgrades, etc.), the online mode is what makes pg_dbmigrator interesting: the source database stays writable during the entire copy, and downtime is reduced to the few seconds it takes to flip the application’s connection string.</p><p>This post walks through how the tool works, how to operate it end-to-end, and what to watch for in production.</p><h2 id="Why-Another-Migration-Tool"><a href="#Why-Another-Migration-Tool" class="headerlink" title="Why Another Migration Tool?"></a>Why Another Migration Tool?</h2><p>PostgreSQL ships with the building blocks — <code>pg_dump</code>, <code>pg_restore</code>, <code>CREATE PUBLICATION</code>, <code>CREATE SUBSCRIPTION</code>, replication slots — but stitching them together correctly is surprisingly easy to get wrong. Common mistakes:</p><ul><li>Dumping <em>before</em> creating the slot, so you have no consistent starting LSN.</li><li>Creating the slot <em>after</em> the dump, missing changes that occurred during the dump.</li><li>Forgetting to use <code>EXPORT_SNAPSHOT</code> so the dump and the replication start point don’t align.</li><li>Cutting over while the subscriber is still lagging behind, losing writes.</li></ul><p>pg_dbmigrator encodes the correct order as a state machine, surfaces lag metrics, and lets the operator drive the cutover when ready.</p><h2 id="Migration-Modes-at-a-Glance"><a href="#Migration-Modes-at-a-Glance" class="headerlink" title="Migration Modes at a Glance"></a>Migration Modes at a Glance</h2><h3 id="Offline-mode-—-one-shot-with-downtime"><a href="#Offline-mode-—-one-shot-with-downtime" class="headerlink" title="Offline mode — one shot, with downtime"></a>Offline mode — one shot, with downtime</h3><p>The source must be quiesced (writes stopped) before the dump starts. Anything written after the dump begins will be lost. The whole migration is a single linear pipe:</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">┌────────┐   pg_dump    ┌──────┐   pg_restore   ┌────────┐</span><br><span class="line">│ Source │ ───────────▶ │ Dump │ ─────────────▶ │ Target │</span><br><span class="line">└────────┘              └──────┘                └────────┘</span><br><span class="line"> (writes stopped <span class="keyword">for</span> the entire duration)</span><br></pre></td></tr></table></figure><p>Downtime ≈ time to dump + time to restore. Simple, but every minute is user-visible.</p><h3 id="Online-mode-—-copy-replay-cutover-on-demand"><a href="#Online-mode-—-copy-replay-cutover-on-demand" class="headerlink" title="Online mode — copy + replay, cutover on demand"></a>Online mode — copy + replay, cutover on demand</h3><p>Two things run back-to-back on the same timeline. First, an initial snapshot copy (just like offline). Then, while the target catches up, a logical-replication stream keeps replaying every change that happened on the source after the snapshot — including writes that arrived <strong>during</strong> the dump. The source stays writable the whole time.</p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">          Phase <span class="number">1</span>: initial <span class="keyword">copy</span></span><br><span class="line">┌────────┐  snapshot dump   ┌────────┐</span><br><span class="line">│ Source │ ───────────────▶ │ Target │</span><br><span class="line">└────────┘                  └────────┘</span><br><span class="line">     │  (slot created BEFORE dump,</span><br><span class="line">     │   so we know <span class="keyword">the</span> exact LSN</span><br><span class="line">     │   <span class="keyword">the</span> dump corresponds <span class="keyword">to</span>)</span><br><span class="line">     │</span><br><span class="line">     ▼  Phase <span class="number">2</span>: streaming catch-up (runs continuously)</span><br><span class="line">┌────────┐  WAL changes via             ┌────────┐</span><br><span class="line">│ Source │  logical replication ─────▶ │ Target │</span><br><span class="line">└────────┘  (<span class="keyword">from</span> <span class="keyword">the</span> slot&#x27;s LSN)       └────────┘</span><br><span class="line">     │</span><br><span class="line">     │  Source stays writable. Lag shrinks <span class="keyword">over</span> <span class="built_in">time</span>.</span><br><span class="line">     │</span><br><span class="line">     ▼  Phase <span class="number">3</span>: cutover (operator-driven)</span><br><span class="line"> <span class="number">1.</span> Stop app writes <span class="keyword">on</span> <span class="keyword">the</span> source</span><br><span class="line"> <span class="number">2.</span> Wait <span class="keyword">until</span> lag = <span class="number">0</span></span><br><span class="line"> <span class="number">3.</span> Press Ctrl+C  →  pg_dbmigrator stops cleanly</span><br><span class="line"> <span class="number">4.</span> Repoint <span class="keyword">the</span> app <span class="keyword">at</span> <span class="keyword">the</span> target</span><br></pre></td></tr></table></figure><p>The trick that makes this work: the replication slot is created <strong>before</strong> the dump and exports a snapshot. The dump uses that snapshot, and the stream starts at the slot’s LSN — so the stream picks up <em>exactly</em> where the dump left off. No gap, no duplicates.</p><p><strong>Rule of thumb</strong>: pick offline when you can afford a maintenance window the size of (dump + restore). Pick online when you can’t.</p><h2 id="Online-Mode-The-State-Machine"><a href="#Online-Mode-The-State-Machine" class="headerlink" title="Online Mode: The State Machine"></a>Online Mode: The State Machine</h2><p>Internally pg_dbmigrator runs the online migration as a sequence of phases:</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">Validate</span> → SourceVacuum → PrepareSnapshot → Dump → Restore → <span class="keyword">Analyze</span></span><br><span class="line">   → StreamApply → (Lag heartbeat …) → CaughtUp → Cutover</span><br><span class="line">   → SourceCleanup → Complete</span><br></pre></td></tr></table></figure><p>Each phase has a specific job:</p><table><thead><tr><th>Phase</th><th>What happens</th></tr></thead><tbody><tr><td><strong>Validate</strong></td><td>Checks connection strings, <code>wal_level</code>, replication slot&#x2F;sender capacity, target reachability.</td></tr><tr><td><strong>SourceVacuum</strong></td><td>Runs <code>VACUUM ANALYZE</code> on the source to reclaim dead tuples and refresh planner statistics before the dump. Skip with <code>--skip-source-vacuum</code>.</td></tr><tr><td><strong>PrepareSnapshot</strong></td><td>Creates the logical replication slot with <code>EXPORT_SNAPSHOT</code>. Streaming is deferred until after the dump completes; the exported snapshot ID is what makes the dump consistent with the slot’s starting LSN.</td></tr><tr><td><strong>Dump</strong></td><td>Runs <code>pg_dump --snapshot=&lt;exported_id&gt;</code> so the dump reflects the database state <em>exactly</em> at the slot’s starting LSN.</td></tr><tr><td><strong>Restore</strong></td><td><code>pg_restore</code> into the target. Parallelized with <code>--jobs</code> and split-sections (indexes rebuilt in parallel) by default.</td></tr><tr><td><strong>Analyze</strong></td><td>Runs <code>ANALYZE</code> on the target so the first application queries have fresh statistics. Skip with <code>--skip-analyze</code>.</td></tr><tr><td><strong>StreamApply</strong></td><td>Starts <code>START_REPLICATION</code> from the slot’s LSN and applies decoded changes onto the target.</td></tr><tr><td><strong>Lag heartbeat</strong></td><td>Polls <code>pg_current_wal_flush_lsn()</code> on the source on a tunable interval, logs the delta against the receiver&#x2F;applied LSN.</td></tr><tr><td><strong>CaughtUp</strong></td><td>Fires once lag falls below <code>--lag-threshold-bytes</code>, signalling the operator that cutover is safe. The threshold is <strong>advisory only</strong> — it does not auto-trigger cutover.</td></tr><tr><td><strong>Cutover</strong></td><td>Triggered by SIGINT (Ctrl+C). Flushes the final LSN feedback, syncs sequences on the target, then disables and drops the subscription.</td></tr><tr><td><strong>SourceCleanup</strong></td><td>After cutover, drops auto-created publications and the replication slot on the source. Failures are logged as warnings but don’t abort.</td></tr></tbody></table><p>The key invariant: the slot is created <strong>before</strong> the dump, and the dump uses the slot’s exported snapshot. That way the WAL stream picks up exactly where the dump ended — no gap, no overlap.</p><h2 id="Installation"><a href="#Installation" class="headerlink" title="Installation"></a>Installation</h2><p>pg_dbmigrator is published on crates.io:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cargo install pg_dbmigrator</span><br><span class="line">pg_dbmigrator --<span class="built_in">help</span></span><br></pre></td></tr></table></figure><p>Or build from source:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/isdaniel/pg_dbmigrator</span><br><span class="line"><span class="built_in">cd</span> pg_dbmigrator</span><br><span class="line">cargo install --path crates/pg_dbmigrator-cli</span><br></pre></td></tr></table></figure><p>You also need <code>pg_dump</code> and <code>pg_restore</code> on the <code>PATH</code> — the tool shells out to them rather than reimplementing the dump format. Make sure their major version is <strong>&gt;&#x3D;</strong> the source database’s major version, otherwise you’ll hit compatibility errors during dump.</p><h2 id="Source-Prerequisites"><a href="#Source-Prerequisites" class="headerlink" title="Source Prerequisites"></a>Source Prerequisites</h2><p>Online mode requires a few server settings on the <strong>source</strong>:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- postgresql.conf (requires restart)</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">SYSTEM</span> <span class="keyword">SET</span> wal_level <span class="operator">=</span> <span class="string">&#x27;logical&#x27;</span>;</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">SYSTEM</span> <span class="keyword">SET</span> max_replication_slots <span class="operator">=</span> <span class="number">10</span>;</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">SYSTEM</span> <span class="keyword">SET</span> max_wal_senders <span class="operator">=</span> <span class="number">10</span>;</span><br></pre></td></tr></table></figure><p>That’s it for setup — pg_dbmigrator will <strong>auto-create the publication</strong> (<code>FOR ALL TABLES</code>) on first run if it doesn’t already exist, and clean it up after cutover. If you’d rather manage the publication yourself (e.g. to scope it to a subset of tables), pre-create it and pass <code>--no-auto-create-publication</code>:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> PUBLICATION pg_dbmigrator_pub <span class="keyword">FOR</span> <span class="keyword">TABLE</span> schema.t1, schema.t2;</span><br></pre></td></tr></table></figure><p>The connecting role needs <code>REPLICATION</code> and the ability to read every table in the publication. On managed services:</p><ul><li><strong>AWS RDS</strong>: set <code>rds.logical_replication = 1</code> in the parameter group, then reboot.</li><li><strong>Azure Database for PostgreSQL</strong>: enable the <code>logical</code> <code>wal_level</code> server parameter.</li><li><strong>Google Cloud SQL</strong>: enable <code>cloudsql.logical_decoding</code>.</li></ul><p>The <strong>target</strong> doesn’t need <code>wal_level = logical</code> unless you plan to chain replication out of it.</p><h3 id="Resource-Lifecycle"><a href="#Resource-Lifecycle" class="headerlink" title="Resource Lifecycle"></a>Resource Lifecycle</h3><p>By default, pg_dbmigrator owns the resources it creates and cleans them up when the migration finishes. Override the cleanup if you need the resource to outlive the migration:</p><table><thead><tr><th>Resource</th><th>Created by</th><th>Default cleanup</th><th>Override</th></tr></thead><tbody><tr><td>Publication</td><td>Auto-created if missing</td><td>Dropped if auto-created</td><td><code>--no-auto-create-publication</code></td></tr><tr><td>Replication slot</td><td>Always by migrator</td><td>Dropped after cutover</td><td><code>--keep-slot</code></td></tr><tr><td>Subscription</td><td>Always by migrator</td><td>Dropped after cutover</td><td><code>--keep-subscription</code></td></tr></tbody></table><h2 id="Offline-Mode-the-Simple-Case"><a href="#Offline-Mode-the-Simple-Case" class="headerlink" title="Offline Mode: the Simple Case"></a>Offline Mode: the Simple Case</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">pg_dbmigrator \</span><br><span class="line">  --mode offline \</span><br><span class="line">  --<span class="built_in">source</span> <span class="string">&#x27;postgres://user:pw@src.example.com:5432/appdb&#x27;</span> \</span><br><span class="line">  --target <span class="string">&#x27;postgres://user:pw@dst.example.com:5432/appdb&#x27;</span> \</span><br><span class="line">  --<span class="built_in">jobs</span> 4 \</span><br><span class="line">  --drop-target-first</span><br></pre></td></tr></table></figure><p>What this does:</p><ol><li>Drops the target database contents first (<code>--drop-target-first</code>).</li><li>Runs <code>pg_dump</code> against the source.</li><li>Pipes into <code>pg_restore</code> with 4 parallel workers.</li><li>Exits.</li></ol><p>Use it for dev&#x2F;staging clones, small databases, or when an outage window is acceptable.</p><h2 id="Online-Mode-the-Full-Workflow"><a href="#Online-Mode-the-Full-Workflow" class="headerlink" title="Online Mode: the Full Workflow"></a>Online Mode: the Full Workflow</h2><p>A typical online migration command:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">pg_dbmigrator \</span><br><span class="line">  --mode online \</span><br><span class="line">  --<span class="built_in">source</span> <span class="string">&#x27;postgres://user:pw@src.example.com:5432/appdb&#x27;</span> \</span><br><span class="line">  --target <span class="string">&#x27;postgres://user:pw@dst.example.com:5432/appdb&#x27;</span> \</span><br><span class="line">  --slot-name pg_dbmigrator_slot \</span><br><span class="line">  --publication pg_dbmigrator_pub \</span><br><span class="line">  --subscription-name pg_dbmigrator_sub \</span><br><span class="line">  --<span class="built_in">jobs</span> 4 \</span><br><span class="line">  --lag-threshold-bytes 8192 \</span><br><span class="line">  --cutover-poll-secs 5</span><br></pre></td></tr></table></figure><p>Walking through what each flag controls:</p><table><thead><tr><th>Flag</th><th>Purpose</th></tr></thead><tbody><tr><td><code>--mode online</code></td><td>Selects the streaming pipeline.</td></tr><tr><td><code>--source</code> &#x2F; <code>--target</code></td><td>Standard libpq connection strings.</td></tr><tr><td><code>--slot-name</code></td><td>Name of the logical replication slot created on the source. Pick something recognizable so you can clean it up if the migration is aborted.</td></tr><tr><td><code>--publication</code></td><td>Publication name. Defaults to <code>pg_dbmigrator_pub</code>; auto-created <code>FOR ALL TABLES</code> if missing.</td></tr><tr><td><code>--subscription-name</code></td><td>Name pg_dbmigrator gives the subscription it creates on the target.</td></tr><tr><td><code>--jobs</code></td><td>Parallelism for the initial <code>pg_restore</code>. Auto-detected from CPU count and clamped to <code>[1, 8]</code> if omitted.</td></tr><tr><td><code>--lag-threshold-bytes</code></td><td>When the gap between source flush LSN and applied LSN drops below this, the tool emits a “ready to cut over” signal. <strong>Advisory only</strong> — it does not auto-trigger cutover.</td></tr><tr><td><code>--cutover-poll-secs</code></td><td>How often the lag heartbeat polls <code>pg_current_wal_flush_lsn()</code>.</td></tr></tbody></table><p>Useful additional flags:</p><ul><li><code>--drop-target-first</code> — recreate the target schema cleanly. Necessary on most fresh runs.</li><li><code>--no-auto-create-publication</code> — skip auto-creation; use a publication you manage yourself (also skips its cleanup).</li><li><code>--keep-slot</code> &#x2F; <code>--keep-subscription</code> — leave the slot or subscription in place after cutover instead of dropping them. Handy for keeping a rollback window.</li><li><code>--no-sequence-sync</code> — skip the automatic <code>setval()</code> pass at cutover. Use when the target role lacks the privileges; you’ll need to advance sequences manually.</li><li><code>--exclude-schema</code> &#x2F; <code>--exclude-table</code> — repeat to omit specific schemas or tables from the dump.</li><li><code>--skip-source-vacuum</code> &#x2F; <code>--skip-analyze</code> — disable the pre-dump <code>VACUUM ANALYZE</code> on the source or the post-restore <code>ANALYZE</code> on the target. Useful when the source is already well-vacuumed or when you want to control the maintenance window yourself.</li><li><code>--dump-compress</code> — archive compression spec. Defaults to <code>lz4:1</code>; alternatives include <code>zstd:3</code> or <code>none</code>.</li><li><code>--no-split-sections</code> — disable parallel section restore. Split sections are 30–60% faster on index-heavy schemas, so only flip this off if the parallel restore is misbehaving.</li><li><code>--allow-restore-errors</code> — treat <code>pg_restore</code> errors as warnings. Useful when the target is a managed service that reserves extension names (Azure-reserved extensions, missing <code>pg_cron</code>, etc.).</li></ul><h3 id="What-You’ll-See-in-the-Terminal"><a href="#What-You’ll-See-in-the-Terminal" class="headerlink" title="What You’ll See in the Terminal"></a>What You’ll See in the Terminal</h3><p>Once <code>StreamApply</code> starts, the tool prints periodic heartbeats:</p><figure class="highlight dns"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">[INFO] StreamApply: replication lag <span class="number">1,245,184</span> bytes</span><br><span class="line">       (source LSN <span class="number">0</span>/<span class="number">1</span>A2B3C4D, received LSN <span class="number">0</span>/<span class="number">1A1F0000</span>, applied LSN <span class="number">0</span>/<span class="number">1A1E8000</span>)</span><br><span class="line">[INFO] StreamApply: replication lag <span class="number">524,288</span> bytes</span><br><span class="line">       (source LSN <span class="number">0</span>/<span class="number">1A2C1000</span>, received LSN <span class="number">0</span>/<span class="number">1A2B9000</span>, applied LSN <span class="number">0</span>/<span class="number">1A2B8000</span>)</span><br><span class="line">[INFO] StreamApply: replication lag <span class="number">4</span>,<span class="number">096</span> bytes</span><br><span class="line">       (source LSN <span class="number">0</span>/<span class="number">1A2D5000</span>, received LSN <span class="number">0</span>/<span class="number">1A2D4000</span>, applied LSN <span class="number">0</span>/<span class="number">1A2D4000</span>)</span><br><span class="line">[INFO] CaughtUp: lag below <span class="number">8</span>,<span class="number">192</span> bytes — safe to cut over</span><br></pre></td></tr></table></figure><p>The lag is the byte distance between the source’s current flush LSN and the target’s applied LSN. Watch it converge before cutting over.</p><h3 id="Driving-the-Cutover"><a href="#Driving-the-Cutover" class="headerlink" title="Driving the Cutover"></a>Driving the Cutover</h3><p>Cutover is <strong>operator-driven</strong>, not automatic. This is intentional — only you know when the application is in a state where it can be flipped. The flow:</p><ol><li>Wait for <code>CaughtUp</code> to appear (or for the lag to be acceptable).</li><li><strong>Stop application writes to the source</strong> (put the app in maintenance, revoke writes, or fail over the load balancer).</li><li>Wait one more heartbeat to confirm lag is <code>0</code> bytes.</li><li>Press <strong>Ctrl+C</strong> (single SIGINT). pg_dbmigrator:<ul><li>Stops the apply loop at the current LSN and flushes the final feedback message.</li><li>Syncs sequences on the target via <code>setval()</code> (skip with <code>--no-sequence-sync</code>).</li><li>Disables and drops the subscription on the target (unless <code>--keep-subscription</code>).</li><li>Drops the replication slot and any auto-created publication on the source (unless <code>--keep-slot</code> or <code>--no-auto-create-publication</code>).</li><li>Exits cleanly.</li></ul></li><li>Repoint the application’s connection string to the target.</li></ol><p>A second Ctrl+C is an escape hatch — if shutdown is stuck, hitting it again forces termination. Use only as a last resort, since the slot may be left behind on the source (drop manually with <code>SELECT pg_drop_replication_slot(&#39;pg_dbmigrator_slot&#39;);</code>).</p><h2 id="A-Realistic-Online-Migration-Recipe"><a href="#A-Realistic-Online-Migration-Recipe" class="headerlink" title="A Realistic Online Migration Recipe"></a>A Realistic Online Migration Recipe</h2><p>Putting it together, here’s the sequence I’d run for a real migration from on-prem to RDS:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. On the source, one-time setup</span></span><br><span class="line">psql <span class="string">&quot;<span class="variable">$SOURCE</span>&quot;</span> &lt;&lt;<span class="string">&#x27;SQL&#x27;</span></span><br><span class="line">ALTER SYSTEM SET wal_level = <span class="string">&#x27;logical&#x27;</span>;</span><br><span class="line">SQL</span><br><span class="line"><span class="comment"># Restart source PostgreSQL.</span></span><br><span class="line"><span class="comment"># (No need to pre-create the publication — pg_dbmigrator will auto-create</span></span><br><span class="line"><span class="comment">#  pg_dbmigrator_pub FOR ALL TABLES on first run, and clean it up at cutover.)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. Kick off the migration in a tmux/screen session you can leave running.</span></span><br><span class="line">pg_dbmigrator \</span><br><span class="line">  --mode online \</span><br><span class="line">  --<span class="built_in">source</span> <span class="string">&quot;<span class="variable">$SOURCE</span>&quot;</span> \</span><br><span class="line">  --target <span class="string">&quot;<span class="variable">$TARGET</span>&quot;</span> \</span><br><span class="line">  --slot-name pg_dbmigrator_slot \</span><br><span class="line">  --publication pg_dbmigrator_pub \</span><br><span class="line">  --subscription-name pg_dbmigrator_sub \</span><br><span class="line">  --<span class="built_in">jobs</span> 8 \</span><br><span class="line">  --lag-threshold-bytes 16384 \</span><br><span class="line">  --cutover-poll-secs 5 \</span><br><span class="line">  --drop-target-first</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. (In another terminal) sanity-check row counts as the dump completes,</span></span><br><span class="line"><span class="comment">#    and watch lag heartbeats.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. When CaughtUp fires and you&#x27;re ready:</span></span><br><span class="line"><span class="comment">#      a. Put app in maintenance mode (stop writes to source).</span></span><br><span class="line"><span class="comment">#      b. Wait until lag heartbeat shows 0 bytes.</span></span><br><span class="line"><span class="comment">#      c. Ctrl+C the pg_dbmigrator process — it will sync sequences, drop</span></span><br><span class="line"><span class="comment">#         the subscription, and clean up the slot/publication on the source.</span></span><br><span class="line"><span class="comment">#      d. Update DNS / connection string to point at TARGET.</span></span><br><span class="line"><span class="comment">#      e. Bring app out of maintenance.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. Cleanup on the source — by default pg_dbmigrator drops the slot and</span></span><br><span class="line"><span class="comment">#    the auto-created publication itself. Only run these if you used</span></span><br><span class="line"><span class="comment">#    --keep-slot / --no-auto-create-publication, or aborted the run:</span></span><br><span class="line">psql <span class="string">&quot;<span class="variable">$SOURCE</span>&quot;</span> -c <span class="string">&quot;SELECT pg_drop_replication_slot(&#x27;pg_dbmigrator_slot&#x27;);&quot;</span></span><br><span class="line">psql <span class="string">&quot;<span class="variable">$SOURCE</span>&quot;</span> -c <span class="string">&quot;DROP PUBLICATION pg_dbmigrator_pub;&quot;</span></span><br></pre></td></tr></table></figure><p>Total user-visible downtime is steps 4a–4e, which is typically tens of seconds.</p><h2 id="Operational-Gotchas"><a href="#Operational-Gotchas" class="headerlink" title="Operational Gotchas"></a>Operational Gotchas</h2><p>A few things that bite people the first time:</p><p><strong>Replication slots hold WAL</strong>. If you start an online migration and abandon it, the slot will keep the source’s WAL pinned indefinitely, eventually filling the source’s disk. A clean Ctrl+C drops the slot for you, but if the process is killed or crashes, drop the slot manually (<code>pg_drop_replication_slot</code>).</p><p><strong>Sequences are synced automatically at cutover</strong>. pg_dbmigrator runs <code>setval()</code> on every sequence in the included schemas as part of cutover, so you don’t need the old “advance every sequence by hand” ritual. Per-sequence failures (typically a missing privilege on the target role) are logged as warnings but won’t abort cutover. If your target role can’t <code>setval</code>, pass <code>--no-sequence-sync</code> and run the SQL yourself:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> setval(<span class="string">&#x27;public.users_id_seq&#x27;</span>,</span><br><span class="line">              (<span class="keyword">SELECT</span> <span class="built_in">max</span>(id) <span class="keyword">FROM</span> public.users));</span><br></pre></td></tr></table></figure><p><strong>DDL during migration is not replicated.</strong> If someone runs an <code>ALTER TABLE</code> on the source mid-stream, the target won’t see it and apply will eventually fail when a row references the new column. Freeze schema changes for the duration of the migration. If a DDL change really must happen, you’ll need to refresh the publication and restart the migration.</p><p><strong>Restore errors on managed targets.</strong> Azure reserves extension names; <code>pg_cron</code> is unavailable on most managed services; some superuser-only objects can’t be restored by an unprivileged role. <code>--allow-restore-errors</code> lets the migration proceed, but inspect the warnings — anything important needs to be applied manually.</p><p><strong>Custom column transforms aren’t supported.</strong> The streaming apply binds replicated values as text and casts them server-side. If you need to transform data during migration (e.g. re-encrypt a column), do it before or after, not inline.</p><p><strong>Major-version differences.</strong> If source is PG14 and target is PG16, run <code>pg_dump</code>&#x2F;<code>pg_restore</code> from the <strong>higher</strong> version’s binaries. pg_dbmigrator inherits whatever’s on the <code>PATH</code>.</p><h2 id="When-to-Pick-Offline-vs-Online"><a href="#When-to-Pick-Offline-vs-Online" class="headerlink" title="When to Pick Offline vs. Online"></a>When to Pick Offline vs. Online</h2><table><thead><tr><th>Situation</th><th>Mode</th></tr></thead><tbody><tr><td>Database is small (&lt; few GB) and you have a maintenance window</td><td>Offline</td></tr><tr><td>Dev &#x2F; staging clones</td><td>Offline</td></tr><tr><td>Production database, can’t take more than a few seconds of write downtime</td><td>Online</td></tr><tr><td>Cross-cloud or cross-region migrations</td><td>Online</td></tr><tr><td>Major-version upgrade on a live system</td><td>Online</td></tr><tr><td>Source can’t be set to <code>wal_level = logical</code></td><td>Offline (no choice)</td></tr></tbody></table><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>pg_dbmigrator wraps a careful, correct online migration recipe — slot-before-dump, snapshot-consistent copy, streaming apply, operator-driven cutover — into a single Rust CLI. For straightforward jobs the offline mode is enough; for production cutovers where downtime matters, the online mode reduces the outage to the moment you press Ctrl+C.</p><p>The most important habits when operating it:</p><ol><li>Always confirm <code>wal_level = logical</code> on the source before starting.</li><li>Watch the lag heartbeats and only cut over from a <code>CaughtUp</code> state with <code>0</code> bytes lag and writes stopped — the threshold is advisory, not automatic.</li><li>Let the tool exit cleanly via Ctrl+C so it can drop the slot, the auto-created publication, and sync sequences for you. If the process dies hard, drop the slot manually so abandoned WAL doesn’t fill the source’s disk.</li><li>Freeze schema changes for the duration of the migration — DDL isn’t replicated.</li></ol><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg_dbmigrator">pg_dbmigrator GitHub Repository</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/logical-replication.html">PostgreSQL Logical Replication Documentation</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/logicaldecoding-explanation.html#LOGICALDECODING-REPLICATION-SLOTS">PostgreSQL Replication Slots</a></li><li><a href="https://isdaniel.github.io/pg-walstream-rust-postgresql-wal-streaming/">pg-walstream: PostgreSQL WAL Streaming in Rust</a></li><li><a href="https://isdaniel.github.io/postgresql-wal-introduce/">PostgreSQL WAL (Write-Ahead Logging) mechanism</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pg-migrator-online-postgresql-migration/">https://isdaniel.github.io/pg-migrator-online-postgresql-migration/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pg-migrator-online-postgresql-migration/</id>
    <link href="https://isdaniel.github.io/pg-migrator-online-postgresql-migration/"/>
    <published>2026-05-03T16:00:00.000Z</published>
    <summary>A hands-on guide to pg_dbmigrator, a Rust CLI for migrating PostgreSQL databases — covering offline dump/restore and the online logical-replication mode for near-zero-downtime cutovers.</summary>
    <title>pg_dbmigrator: Near-Zero-Downtime PostgreSQL Migration with Online Mode</title>
    <updated>2026-08-05T10:10:26.490Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="Monitoring" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/Monitoring/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Performance-Tuning" scheme="https://isdaniel.github.io/tags/Performance-Tuning/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/tags/pgrx/"/>
    <category term="Monitoring" scheme="https://isdaniel.github.io/tags/Monitoring/"/>
    <content>
      <![CDATA[<h1 id="pg-pidstat-Real-Time-Per-Backend-CPU-Memory-and-I-O-Monitoring-for-PostgreSQL"><a href="#pg-pidstat-Real-Time-Per-Backend-CPU-Memory-and-I-O-Monitoring-for-PostgreSQL" class="headerlink" title="pg_pidstat: Real-Time Per-Backend CPU, Memory, and I&#x2F;O Monitoring for PostgreSQL"></a>pg_pidstat: Real-Time Per-Backend CPU, Memory, and I&#x2F;O Monitoring for PostgreSQL</h1><p>When a PostgreSQL server slows down, the first question is usually: <strong>which backend is consuming resources?</strong> <code>pg_stat_activity</code> tells you what each backend is doing, but not how much CPU, memory, or disk I&#x2F;O it uses. You end up jumping between <code>top</code>, <code>pidstat</code>, and <code>pg_stat_activity</code>, manually correlating PIDs.</p><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg_pidstat">pg_pidstat</a> solves this by embedding OS-level process metrics directly into PostgreSQL. A single SQL view gives you per-backend CPU percentage, memory consumption, I&#x2F;O throughput, and context switch rates — all joined with the connection context you already know from <code>pg_stat_activity</code>.</p><h2 id="The-Problem-Blind-Spots-in-PostgreSQL-Monitoring"><a href="#The-Problem-Blind-Spots-in-PostgreSQL-Monitoring" class="headerlink" title="The Problem: Blind Spots in PostgreSQL Monitoring"></a>The Problem: Blind Spots in PostgreSQL Monitoring</h2><p>PostgreSQL’s built-in statistics views (<code>pg_stat_activity</code>, <code>pg_stat_user_tables</code>, <code>pg_stat_io</code>) provide query-level and table-level insights, but they lack <strong>process-level OS metrics</strong>. When you need to answer questions like:</p><ul><li>Which backend is burning 100% CPU?</li><li>Is that long-running query memory-hungry or I&#x2F;O-bound?</li><li>Are context switches causing latency spikes?</li></ul><p>You typically resort to external tools — <code>pidstat</code>, <code>htop</code>, or custom <code>/proc</code> scrapers — and then manually join their output with <code>pg_stat_activity</code> by PID. This workflow is tedious, error-prone, and impossible to automate in pure SQL.</p><p>pg_pidstat eliminates that gap entirely.</p><h2 id="Key-Features"><a href="#Key-Features" class="headerlink" title="Key Features"></a>Key Features</h2><ul><li><strong>Per-backend CPU usage</strong> as a percentage of total system capacity</li><li><strong>Memory tracking</strong> — both percentage and absolute MB (resident set size)</li><li><strong>I&#x2F;O throughput</strong> — read&#x2F;write bytes per second and IOPS</li><li><strong>I&#x2F;O wait detection</strong> — flags backends blocked on disk</li><li><strong>Context switch rates</strong> — voluntary and non-voluntary, per second</li><li><strong>Full <code>pg_stat_activity</code> integration</strong> — all standard columns (pid, datname, usename, state, query, etc.) included</li><li><strong>Background worker sampling</strong> — metrics updated every 1 second automatically</li><li><strong>Lock-free reads</strong> — minimal contention with brief exclusive writes only during sampling</li></ul><h2 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h2><p>pg_pidstat is built with <a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx</a> (Rust) and consists of four modules:</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────────────────────────────────────────┐</span><br><span class="line">│                    <span class="keyword">SQL</span> Interface                     │</span><br><span class="line">│          pg_pidstat <span class="keyword">view</span> + helper <span class="keyword">functions</span>           │</span><br><span class="line">│                    (activity.rs)                      │</span><br><span class="line">└──────────────────────┬──────────────────────────────┘</span><br><span class="line">                       │  per-second deltas</span><br><span class="line">┌──────────────────────▼──────────────────────────────┐</span><br><span class="line">│               Background Worker                      │</span><br><span class="line">│        Samples <span class="keyword">all</span> backends every <span class="number">1</span> second           │</span><br><span class="line">│                   (bgworker.rs)                      │</span><br><span class="line">└──────────────────────┬──────────────────────────────┘</span><br><span class="line">                       │  raw counters</span><br><span class="line">┌──────────────────────▼──────────────────────────────┐</span><br><span class="line">│           <span class="type">Double</span>-Buffered Shared Memory              │</span><br><span class="line">│        <span class="keyword">Lock</span>-free reads, up <span class="keyword">to</span> <span class="number">1024</span> backends          │</span><br><span class="line">│                    (shmem.rs)                         │</span><br><span class="line">└──────────────────────┬──────────────────────────────┘</span><br><span class="line">                       │  reads /proc/[pid]<span class="comment">/*</span></span><br><span class="line"><span class="comment">┌──────────────────────▼──────────────────────────────┐</span></span><br><span class="line"><span class="comment">│               /proc Filesystem Parser                │</span></span><br><span class="line"><span class="comment">│   /proc/[pid]/stat, statm, io, status, /proc/meminfo│</span></span><br><span class="line"><span class="comment">│                 (proc_stats.rs)                      │</span></span><br><span class="line"><span class="comment">└─────────────────────────────────────────────────────┘</span></span><br></pre></td></tr></table></figure><p><strong>How it works:</strong></p><ol><li>A background worker wakes every second and iterates over all PostgreSQL backend PIDs.</li><li>For each PID, it reads <code>/proc/[pid]/stat</code>, <code>/proc/[pid]/statm</code>, <code>/proc/[pid]/io</code>, and <code>/proc/[pid]/status</code> to collect raw CPU ticks, memory pages, I&#x2F;O byte counters, and context switch counts.</li><li>The raw readings are stored in a double-buffered shared memory region. The double-buffer design lets SQL queries read the previous-second snapshot without blocking the worker’s current write.</li><li>When you query the <code>pg_pidstat</code> view, the <code>activity</code> module computes per-second deltas (rates) from the raw counters and joins them with <code>pg_stat_activity</code> columns.</li></ol><p>This design ensures <strong>zero lock contention on reads</strong> — the only exclusive lock is a brief window when the background worker swaps the active buffer.</p><h2 id="Metrics-Reference"><a href="#Metrics-Reference" class="headerlink" title="Metrics Reference"></a>Metrics Reference</h2><p>The <code>pg_pidstat</code> view includes all standard <code>pg_stat_activity</code> columns plus these monitoring columns:</p><table><thead><tr><th>Column</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>cpu_percent</code></td><td><code>float8</code></td><td>CPU usage as % of total system capacity</td></tr><tr><td><code>memory_percent</code></td><td><code>float8</code></td><td>Resident memory as % of total system RAM</td></tr><tr><td><code>memory_usage_mb</code></td><td><code>float8</code></td><td>Resident memory in megabytes</td></tr><tr><td><code>io_read_bytes_per_sec</code></td><td><code>float8</code></td><td>Disk read throughput (bytes&#x2F;sec)</td></tr><tr><td><code>io_write_bytes_per_sec</code></td><td><code>float8</code></td><td>Disk write throughput (bytes&#x2F;sec)</td></tr><tr><td><code>io_read_ops_per_sec</code></td><td><code>float8</code></td><td>Read IOPS</td></tr><tr><td><code>io_write_ops_per_sec</code></td><td><code>float8</code></td><td>Write IOPS</td></tr><tr><td><code>io_wait</code></td><td><code>bool</code></td><td><code>true</code> if the process is in disk sleep state</td></tr><tr><td><code>voluntary_ctxt_switches_per_sec</code></td><td><code>float8</code></td><td>Voluntary context switches per second</td></tr><tr><td><code>nonvoluntary_ctxt_switches_per_sec</code></td><td><code>float8</code></td><td>Non-voluntary context switches per second</td></tr></tbody></table><h2 id="Usage-Examples"><a href="#Usage-Examples" class="headerlink" title="Usage Examples"></a>Usage Examples</h2><h3 id="Find-CPU-Heavy-Backends"><a href="#Find-CPU-Heavy-Backends" class="headerlink" title="Find CPU-Heavy Backends"></a>Find CPU-Heavy Backends</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid, datname, usename, state, query,</span><br><span class="line">       cpu_percent, memory_percent, memory_usage_mb</span><br><span class="line"><span class="keyword">FROM</span> pg_pidstat</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> cpu_percent <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><h3 id="Identify-I-O-Intensive-Queries"><a href="#Identify-I-O-Intensive-Queries" class="headerlink" title="Identify I&#x2F;O-Intensive Queries"></a>Identify I&#x2F;O-Intensive Queries</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid, usename, state, query,</span><br><span class="line">       io_read_bytes_per_sec,</span><br><span class="line">       io_write_bytes_per_sec,</span><br><span class="line">       io_read_ops_per_sec,</span><br><span class="line">       io_write_ops_per_sec,</span><br><span class="line">       io_wait</span><br><span class="line"><span class="keyword">FROM</span> pg_pidstat</span><br><span class="line"><span class="keyword">WHERE</span> state <span class="operator">=</span> <span class="string">&#x27;active&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> io_read_bytes_per_sec <span class="operator">+</span> io_write_bytes_per_sec <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><h3 id="Detect-Context-Switch-Hotspots"><a href="#Detect-Context-Switch-Hotspots" class="headerlink" title="Detect Context Switch Hotspots"></a>Detect Context Switch Hotspots</h3><p>High non-voluntary context switches indicate CPU contention — too many backends fighting for CPU time:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid, usename, query,</span><br><span class="line">       voluntary_ctxt_switches_per_sec,</span><br><span class="line">       nonvoluntary_ctxt_switches_per_sec</span><br><span class="line"><span class="keyword">FROM</span> pg_pidstat</span><br><span class="line"><span class="keyword">WHERE</span> state <span class="operator">=</span> <span class="string">&#x27;active&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> nonvoluntary_ctxt_switches_per_sec <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><h3 id="Full-Troubleshooting-Dashboard"><a href="#Full-Troubleshooting-Dashboard" class="headerlink" title="Full Troubleshooting Dashboard"></a>Full Troubleshooting Dashboard</h3><p>Combine all metrics for a comprehensive view of backend resource usage:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pid, datname, usename, state,</span><br><span class="line">       <span class="keyword">left</span>(query, <span class="number">80</span>) <span class="keyword">AS</span> query_preview,</span><br><span class="line">       round(cpu_percent::<span class="type">numeric</span>, <span class="number">2</span>) <span class="keyword">AS</span> cpu_pct,</span><br><span class="line">       round(memory_usage_mb::<span class="type">numeric</span>, <span class="number">1</span>) <span class="keyword">AS</span> mem_mb,</span><br><span class="line">       round((io_read_bytes_per_sec <span class="operator">/</span> <span class="number">1024</span> <span class="operator">/</span> <span class="number">1024</span>)::<span class="type">numeric</span>, <span class="number">2</span>) <span class="keyword">AS</span> read_mb_s,</span><br><span class="line">       round((io_write_bytes_per_sec <span class="operator">/</span> <span class="number">1024</span> <span class="operator">/</span> <span class="number">1024</span>)::<span class="type">numeric</span>, <span class="number">2</span>) <span class="keyword">AS</span> write_mb_s,</span><br><span class="line">       io_wait,</span><br><span class="line">       round(voluntary_ctxt_switches_per_sec::<span class="type">numeric</span>, <span class="number">0</span>) <span class="keyword">AS</span> vol_cs,</span><br><span class="line">       round(nonvoluntary_ctxt_switches_per_sec::<span class="type">numeric</span>, <span class="number">0</span>) <span class="keyword">AS</span> nvol_cs</span><br><span class="line"><span class="keyword">FROM</span> pg_pidstat</span><br><span class="line"><span class="keyword">WHERE</span> state <span class="operator">=</span> <span class="string">&#x27;active&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> cpu_percent <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure><h3 id="Monitoring-Over-Time-with-pg-cron"><a href="#Monitoring-Over-Time-with-pg-cron" class="headerlink" title="Monitoring Over Time with pg_cron"></a>Monitoring Over Time with pg_cron</h3><p>Pair pg_pidstat with <a target="_blank" rel="noopener" href="https://github.com/citusdata/pg_cron">pg_cron</a> to build a historical resource usage table:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> backend_metrics_history (</span><br><span class="line">    captured_at timestamptz <span class="keyword">DEFAULT</span> now(),</span><br><span class="line">    pid <span class="type">int</span>,</span><br><span class="line">    datname name,</span><br><span class="line">    usename name,</span><br><span class="line">    state text,</span><br><span class="line">    cpu_percent float8,</span><br><span class="line">    memory_usage_mb float8,</span><br><span class="line">    io_read_bytes_per_sec float8,</span><br><span class="line">    io_write_bytes_per_sec float8</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Sample every 10 seconds via pg_cron</span></span><br><span class="line"><span class="keyword">SELECT</span> cron.schedule(<span class="string">&#x27;backend-metrics&#x27;</span>, <span class="string">&#x27;10 seconds&#x27;</span>,</span><br><span class="line">$$</span><br><span class="line"><span class="keyword">INSERT INTO</span> backend_metrics_history</span><br><span class="line">    (pid, datname, usename, state, cpu_percent, memory_usage_mb,</span><br><span class="line">     io_read_bytes_per_sec, io_write_bytes_per_sec)</span><br><span class="line"><span class="keyword">SELECT</span> pid, datname, usename, state, cpu_percent, memory_usage_mb,</span><br><span class="line">       io_read_bytes_per_sec, io_write_bytes_per_sec</span><br><span class="line"><span class="keyword">FROM</span> pg_pidstat <span class="keyword">WHERE</span> state <span class="operator">=</span> <span class="string">&#x27;active&#x27;</span>;</span><br><span class="line">$$);</span><br></pre></td></tr></table></figure><h2 id="Why-pg-pidstat-Over-External-Monitoring"><a href="#Why-pg-pidstat-Over-External-Monitoring" class="headerlink" title="Why pg_pidstat Over External Monitoring?"></a>Why pg_pidstat Over External Monitoring?</h2><table><thead><tr><th>Aspect</th><th>External tools (pidstat, top)</th><th>pg_pidstat</th></tr></thead><tbody><tr><td>PID-to-query mapping</td><td>Manual correlation</td><td>Automatic (joined with <code>pg_stat_activity</code>)</td></tr><tr><td>Query interface</td><td>Shell parsing &#x2F; custom scripts</td><td>Standard SQL</td></tr><tr><td>Alerting integration</td><td>Requires glue code</td><td>Use any SQL-based alerting</td></tr><tr><td>Historical storage</td><td>External time-series DB</td><td><code>INSERT INTO ... SELECT</code> from the view</td></tr><tr><td>Granularity</td><td>Per-process only</td><td>Per-backend with database context</td></tr><tr><td>Deployment</td><td>Agent on every host</td><td>PostgreSQL extension — no extra process</td></tr></tbody></table><p>The key advantage is <strong>context</strong>: knowing that PID 12345 uses 90% CPU is useful, but knowing it’s the <code>analytics</code> user running a sequential scan on the <code>orders</code> table in the <code>production</code> database is actionable.</p><h2 id="Performance-Design"><a href="#Performance-Design" class="headerlink" title="Performance Design"></a>Performance Design</h2><p>pg_pidstat is designed for minimal overhead in production:</p><ul><li><strong>Release-mode optimization</strong>: <code>opt-level = 3</code>, fat LTO, single codegen unit</li><li><strong>Lock-free reads</strong>: Double-buffered shared memory means <code>SELECT</code> from the view never blocks</li><li><strong>Brief exclusive writes</strong>: Only during the buffer swap (microseconds)</li><li><strong>Bounded memory</strong>: Fixed-size shared memory for up to 1024 backends</li><li><strong>1-second sampling</strong>: Background worker reads <code>/proc</code> once per second — negligible system load</li></ul><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>pg_pidstat brings OS-level process monitoring into PostgreSQL itself. Instead of correlating PIDs across <code>top</code>, <code>pidstat</code>, and <code>pg_stat_activity</code>, you get CPU, memory, I&#x2F;O, and context switch metrics in a single SQL view — with full connection context attached.</p><p>For anyone running PostgreSQL on Linux and needing to quickly identify resource-heavy backends, pg_pidstat turns a multi-tool investigation into one query.</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg_pidstat">pg_pidstat GitHub Repository</a></li><li><a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx — Rust Framework for PostgreSQL Extensions</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/monitoring-stats.html#MONITORING-PG-STAT-ACTIVITY-VIEW">PostgreSQL pg_stat_activity Documentation</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pg-pidstat-real-time-postgresql-backend-monitoring/">https://isdaniel.github.io/pg-pidstat-real-time-postgresql-backend-monitoring/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pg-pidstat-real-time-postgresql-backend-monitoring/</id>
    <link href="https://isdaniel.github.io/pg-pidstat-real-time-postgresql-backend-monitoring/"/>
    <published>2026-04-27T02:00:00.000Z</published>
    <summary>pg_pidstat is a Rust-based PostgreSQL extension that adds real-time per-backend CPU, memory, I/O, and context switch monitoring via a single SQL view.</summary>
    <title>pg_pidstat: Real-Time Per-Backend CPU, Memory, and I/O Monitoring for PostgreSQL</title>
    <updated>2026-08-05T10:10:26.491Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/logical-replication/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/tags/logical-replication/"/>
    <category term="CDC" scheme="https://isdaniel.github.io/tags/CDC/"/>
    <category term="WAL" scheme="https://isdaniel.github.io/tags/WAL/"/>
    <content>
      <![CDATA[<h1 id="pg-walstream-High-Performance-PostgreSQL-WAL-Streaming-in-Rust"><a href="#pg-walstream-High-Performance-PostgreSQL-WAL-Streaming-in-Rust" class="headerlink" title="pg-walstream: High-Performance PostgreSQL WAL Streaming in Rust"></a>pg-walstream: High-Performance PostgreSQL WAL Streaming in Rust</h1><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg-walstream">pg-walstream</a> is a Rust library for parsing and streaming PostgreSQL Write-Ahead Log (WAL) messages through logical and physical replication protocols. It provides a type-safe, async-first interface for building real-time Change Data Capture (CDC) pipelines.</p><p>If you need to react to database changes in real-time — event-driven architectures, data pipelines, audit logging, cache invalidation, or search index syncing — pg-walstream abstracts the complex PostgreSQL replication protocol into a clean Rust API.</p><p><a target="_blank" rel="noopener" href="https://crates.io/crates/pg_walstream">crates.io</a> | <a target="_blank" rel="noopener" href="https://docs.rs/pg-walstream">API Docs</a></p><h2 id="Background-PostgreSQL-WAL-and-Replication"><a href="#Background-PostgreSQL-WAL-and-Replication" class="headerlink" title="Background: PostgreSQL WAL and Replication"></a>Background: PostgreSQL WAL and Replication</h2><p>Before diving into pg-walstream, it helps to understand how PostgreSQL replication works at a fundamental level.</p><h3 id="What-is-WAL-Write-Ahead-Logging"><a href="#What-is-WAL-Write-Ahead-Logging" class="headerlink" title="What is WAL (Write-Ahead Logging)?"></a>What is WAL (Write-Ahead Logging)?</h3><p>WAL is PostgreSQL’s mechanism for ensuring data durability. The core idea: <strong>write the transaction log first, then write the actual data</strong>. Every INSERT, UPDATE, DELETE, and DDL change is recorded sequentially in WAL files before the corresponding data pages are flushed to disk.</p><p>This design provides two key benefits:</p><ol><li><strong>Crash recovery</strong> — if the system crashes, PostgreSQL can replay (REDO) the WAL to recover committed transactions that weren’t yet flushed to disk.</li><li><strong>I&#x2F;O efficiency</strong> — sequential WAL writes are much faster than random page writes, so PostgreSQL doesn’t need to flush dirty pages on every commit.</li></ol><p>Each WAL record is identified by a <strong>Log Sequence Number (LSN)</strong> — a monotonically increasing pointer into the WAL stream. LSN is the backbone of replication: it tells both the sender and receiver exactly where they are in the change history.</p><figure class="highlight n1ql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">               WAL Stream</span><br><span class="line">┌───────┬───────┬───────┬───────┬───────┐</span><br><span class="line">│ BEGIN │<span class="keyword">INSERT</span> │<span class="keyword">UPDATE</span> │<span class="keyword">DELETE</span> │<span class="keyword">COMMIT</span> │  ...</span><br><span class="line">│LSN: <span class="number">1</span> │LSN: <span class="number">2</span> │LSN: <span class="number">3</span> │LSN: <span class="number">4</span> │LSN: <span class="number">5</span> │</span><br><span class="line">└───────┴───────┴───────┴───────┴───────┘</span><br><span class="line">                      │</span><br><span class="line">           Replayed <span class="keyword">by</span> standby / consumed <span class="keyword">by</span> CDC</span><br></pre></td></tr></table></figure><p>For a deeper dive into WAL internals, see my earlier post: <a href="https://isdaniel.github.io/postgresql-wal-introduce/">PostgreSQL WAL (Write-Ahead Logging) mechanism</a>.</p><h3 id="Physical-vs-Logical-Replication"><a href="#Physical-vs-Logical-Replication" class="headerlink" title="Physical vs. Logical Replication"></a>Physical vs. Logical Replication</h3><p>PostgreSQL supports two replication modes, each serving different use cases:</p><p><strong>Physical Replication</strong> streams raw WAL bytes — the exact disk-level changes — to a standby server. The standby replays these byte-for-byte, producing an identical copy of the primary. This is what powers read replicas and high-availability setups.</p><p><strong>Logical Replication</strong> decodes WAL into higher-level change events (INSERT, UPDATE, DELETE) using an output plugin (e.g., <code>pgoutput</code>). Instead of raw disk blocks, consumers receive structured messages like “row X was inserted into table Y with these column values.” This is what powers CDC pipelines.</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Physical Replication:</span><br><span class="line">  <span class="keyword">Primary</span> ──[raw WAL bytes]──▶ Standby (byte<span class="operator">-</span>identical <span class="keyword">copy</span>)</span><br><span class="line"></span><br><span class="line">Logical Replication:</span><br><span class="line">  <span class="keyword">Primary</span> ──[pgoutput plugin]──▶ Decoded Change Events ──▶ CDC Consumer</span><br><span class="line">                                  (<span class="keyword">INSERT</span><span class="operator">/</span><span class="keyword">UPDATE</span><span class="operator">/</span><span class="keyword">DELETE</span>)</span><br></pre></td></tr></table></figure><p>pg-walstream supports <strong>both</strong> modes — physical replication for standby&#x2F;backup scenarios and logical replication for CDC.</p><h3 id="The-Logical-Replication-Protocol"><a href="#The-Logical-Replication-Protocol" class="headerlink" title="The Logical Replication Protocol"></a>The Logical Replication Protocol</h3><p>When a client connects to PostgreSQL in replication mode, they communicate via the <strong>Streaming Replication Protocol</strong>. For logical replication, the flow works as follows:</p><ol><li><strong>Client creates a replication slot</strong> — this tells PostgreSQL to retain WAL segments needed by this consumer, preventing them from being recycled.</li><li><strong>Client starts replication</strong> from a slot, specifying the output plugin (<code>pgoutput</code>) and which publication to subscribe to.</li><li><strong>PostgreSQL streams messages</strong> — the server continuously sends WAL data messages (<code>XLogData</code>) containing decoded change events.</li><li><strong>Client sends feedback</strong> — periodically, the client reports its progress (flushed LSN, applied LSN) back to the server. This lets PostgreSQL know which WAL segments can be safely recycled.</li></ol><p>The decoded messages follow the <strong>logical replication message format</strong>, which has evolved across four protocol versions:</p><table><thead><tr><th align="center">Protocol Version</th><th align="center">PostgreSQL</th><th>Key Additions</th></tr></thead><tbody><tr><td align="center">v1</td><td align="center">14+</td><td>Core messages: BEGIN, COMMIT, INSERT, UPDATE, DELETE, TRUNCATE, RELATION, TYPE, ORIGIN</td></tr><tr><td align="center">v2</td><td align="center">14+</td><td><strong>Streaming transactions</strong>: STREAM_START, STREAM_STOP, STREAM_COMMIT, STREAM_ABORT — allows consuming large, in-progress transactions before COMMIT</td></tr><tr><td align="center">v3</td><td align="center">14+</td><td><strong>Two-phase commit</strong>: BEGIN_PREPARE, PREPARE, COMMIT_PREPARED, ROLLBACK_PREPARED, STREAM_PREPARE</td></tr><tr><td align="center">v4</td><td align="center">17+</td><td><strong>Parallel streaming</strong>, <code>abort_lsn</code> field for more precise abort handling</td></tr></tbody></table><p>Each message type carries specific data. For example, an INSERT message contains:</p><ul><li><strong>Relation ID</strong> — which table the row belongs to</li><li><strong>Tuple data</strong> — the column values of the new row, typed by OID</li></ul><p>The RELATION message (sent once per table, or when a schema changes) maps the relation ID to a table name, namespace, and column definitions — so the consumer can interpret the tuple data.</p><h3 id="Why-Build-a-Library-for-This"><a href="#Why-Build-a-Library-for-This" class="headerlink" title="Why Build a Library for This?"></a>Why Build a Library for This?</h3><p>Implementing the replication protocol from scratch involves:</p><ul><li>Managing the PostgreSQL wire protocol and authentication (cleartext, MD5, SCRAM-SHA-256)</li><li>Parsing binary WAL messages with protocol-version-specific formats</li><li>Tracking LSN positions and sending periodic feedback to avoid WAL bloat</li><li>Handling connection drops, retries, and replication slot lifecycle</li><li>Dealing with streaming transactions that may arrive interleaved</li></ul><p>pg-walstream encapsulates all of this complexity into a type-safe Rust API, so you can focus on what to <strong>do</strong> with the change events rather than how to <strong>receive</strong> them.</p><h2 id="Key-Features"><a href="#Key-Features" class="headerlink" title="Key Features"></a>Key Features</h2><ul><li><strong>Protocol v1–v4 support</strong> including streaming transactions (v2), two-phase commit (v3), and parallel streaming (v4)</li><li><strong>Two connection backends</strong>: <code>libpq</code> (C FFI, default) and <code>rustls-tls</code> (pure Rust, no runtime C deps)</li><li><strong>Zero-copy buffers</strong> via the <code>bytes</code> crate — no unnecessary data cloning</li><li><strong>Serde-based deserialization</strong> — map WAL events directly to Rust structs</li><li><strong>Automatic retry</strong> with exponential backoff for transient failures</li><li><strong>Async&#x2F;await</strong> with <code>tokio</code> and <code>futures::Stream</code> integration</li><li><strong>Memory efficient</strong> — all configurations stay under 18 MB RSS</li></ul><h2 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h2><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────────┐</span><br><span class="line">│          Application Layer               │</span><br><span class="line">│  (Your CDC / <span class="keyword">Replication</span> Logic)          │</span><br><span class="line">└──────────────┬───────────────────────────┘</span><br><span class="line">               │</span><br><span class="line">┌──────────────▼───────────────────────────┐</span><br><span class="line">│    LogicalReplicationStream              │</span><br><span class="line">│  - <span class="keyword">Connection</span> management &amp; retry         │</span><br><span class="line">│  - Event processing &amp; LSN feedback       │</span><br><span class="line">│  - <span class="keyword">Snapshot</span> export support               │</span><br><span class="line">└──────────────┬───────────────────────────┘</span><br><span class="line">               │</span><br><span class="line">┌──────────────▼───────────────────────────┐</span><br><span class="line">│  LogicalReplicationParser                │</span><br><span class="line">│  - Protocol v1-v4 parsing                │</span><br><span class="line">│  - Zero-<span class="keyword">copy</span> message deserialization     │</span><br><span class="line">│  - Streaming <span class="keyword">transaction</span> support         │</span><br><span class="line">└──────────────┬───────────────────────────┘</span><br><span class="line">               │</span><br><span class="line">┌──────────────▼───────────────────────────┐</span><br><span class="line">│     PgReplicationConnection              │</span><br><span class="line">│  ┌─────────────────┬──────────────────┐  │</span><br><span class="line">│  │  libpq backend  │ rustls-tls       │  │</span><br><span class="line">│  │  (C FFI)        │ (pure Rust)      │  │</span><br><span class="line">│  └─────────────────┴──────────────────┘  │</span><br><span class="line">│  Compile-<span class="type">time</span> feature flag selection     │</span><br><span class="line">└──────────────┬───────────────────────────┘</span><br><span class="line">               │</span><br><span class="line">┌──────────────▼───────────────────────────┐</span><br><span class="line">│     BufferReader / BufferWriter          │</span><br><span class="line">│  - Zero-<span class="keyword">copy</span> operations (bytes crate)    │</span><br><span class="line">│  - Binary protocol handling              │</span><br><span class="line">└──────────────────────────────────────────┘</span><br></pre></td></tr></table></figure><p>The library has two connection backends selected at compile time:</p><table><thead><tr><th>Backend</th><th>Feature Flag</th><th>Dependencies</th><th>Description</th></tr></thead><tbody><tr><td>libpq (default)</td><td><code>libpq</code></td><td><code>libpq-dev</code>, <code>libclang-dev</code></td><td>FFI wrapper around PostgreSQL’s C client library</td></tr><tr><td>rustls-tls</td><td><code>rustls-tls</code></td><td><code>cmake</code> (build-time only)</td><td>Pure-Rust TLS via rustls + aws-lc-rs (hardware-accelerated)</td></tr></tbody></table><p>When both features are enabled, <code>rustls-tls</code> takes priority automatically.</p><h2 id="Getting-Started"><a href="#Getting-Started" class="headerlink" title="Getting Started"></a>Getting Started</h2><h3 id="Installation"><a href="#Installation" class="headerlink" title="Installation"></a>Installation</h3><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Default (libpq backend)</span></span><br><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">pg_walstream</span> = <span class="string">&quot;0.6.2&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Pure-Rust backend (no C runtime deps)</span></span><br><span class="line"><span class="attr">pg_walstream</span> = &#123; version = <span class="string">&quot;0.6.2&quot;</span>, default-features = <span class="literal">false</span>, features = [<span class="string">&quot;rustls-tls&quot;</span>] &#125;</span><br></pre></td></tr></table></figure><h3 id="PostgreSQL-Setup"><a href="#PostgreSQL-Setup" class="headerlink" title="PostgreSQL Setup"></a>PostgreSQL Setup</h3><p>Enable logical replication in <code>postgresql.conf</code>:</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">wal_level</span> = logical</span><br><span class="line"><span class="attr">max_replication_slots</span> = <span class="number">4</span></span><br><span class="line"><span class="attr">max_wal_senders</span> = <span class="number">4</span></span><br></pre></td></tr></table></figure><p>Create a publication and replication user:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> PUBLICATION my_publication <span class="keyword">FOR</span> <span class="keyword">TABLE</span> users, orders;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> replication_user <span class="keyword">WITH</span> REPLICATION PASSWORD <span class="string">&#x27;secure_password&#x27;</span>;</span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">SELECT</span> <span class="keyword">ON</span> <span class="keyword">ALL</span> TABLES <span class="keyword">IN</span> SCHEMA public <span class="keyword">TO</span> replication_user;</span><br></pre></td></tr></table></figure><h3 id="Basic-Streaming-Example"><a href="#Basic-Streaming-Example" class="headerlink" title="Basic Streaming Example"></a>Basic Streaming Example</h3><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">use</span> pg_walstream::&#123;</span><br><span class="line">    LogicalReplicationStream, ReplicationStreamConfig, RetryConfig,</span><br><span class="line">    StreamingMode, CancellationToken,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">use</span> std::time::Duration;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[tokio::main]</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">fn</span> <span class="title function_">main</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;(), <span class="type">Box</span>&lt;<span class="keyword">dyn</span> std::error::Error&gt;&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">config</span> = ReplicationStreamConfig::<span class="title function_ invoke__">new</span>(</span><br><span class="line">        <span class="string">&quot;my_slot&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        <span class="string">&quot;my_publication&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        <span class="number">2</span>,                              <span class="comment">// Protocol version</span></span><br><span class="line">        StreamingMode::On,</span><br><span class="line">        Duration::<span class="title function_ invoke__">from_secs</span>(<span class="number">10</span>),        <span class="comment">// Feedback interval</span></span><br><span class="line">        Duration::<span class="title function_ invoke__">from_secs</span>(<span class="number">30</span>),        <span class="comment">// Connection timeout</span></span><br><span class="line">        Duration::<span class="title function_ invoke__">from_secs</span>(<span class="number">60</span>),        <span class="comment">// Health check interval</span></span><br><span class="line">        RetryConfig::<span class="title function_ invoke__">default</span>(),</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">stream</span> = LogicalReplicationStream::<span class="title function_ invoke__">new</span>(</span><br><span class="line">        <span class="string">&quot;postgresql://postgres:password@localhost:5432/mydb?replication=database&quot;</span>,</span><br><span class="line">        config,</span><br><span class="line">    ).<span class="keyword">await</span>?;</span><br><span class="line"></span><br><span class="line">    stream.<span class="title function_ invoke__">start</span>(<span class="literal">None</span>).<span class="keyword">await</span>?;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">cancel_token</span> = CancellationToken::<span class="title function_ invoke__">new</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">loop</span> &#123;</span><br><span class="line">        <span class="keyword">match</span> stream.<span class="title function_ invoke__">next_event_with_retry</span>(&amp;cancel_token).<span class="keyword">await</span> &#123;</span><br><span class="line">            <span class="title function_ invoke__">Ok</span>(event) =&gt; &#123;</span><br><span class="line">                <span class="built_in">println!</span>(<span class="string">&quot;Received: &#123;:?&#125;&quot;</span>, event);</span><br><span class="line">                stream.shared_lsn_feedback.<span class="title function_ invoke__">update_applied_lsn</span>(event.lsn.<span class="title function_ invoke__">value</span>());</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="title function_ invoke__">Err</span>(e) <span class="keyword">if</span> matches!(e, pg_walstream::ReplicationError::<span class="title function_ invoke__">Cancelled</span>(_)) =&gt; &#123;</span><br><span class="line">                <span class="built_in">println!</span>(<span class="string">&quot;Shutting down gracefully&quot;</span>);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="title function_ invoke__">Err</span>(e) =&gt; &#123;</span><br><span class="line">                <span class="built_in">eprintln!</span>(<span class="string">&quot;Error: &#123;&#125;&quot;</span>, e);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Typed-Deserialization"><a href="#Typed-Deserialization" class="headerlink" title="Typed Deserialization"></a>Typed Deserialization</h3><p>pg-walstream supports mapping WAL events directly to Rust structs via Serde:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">use</span> serde::Deserialize;</span><br><span class="line"><span class="keyword">use</span> pg_walstream::EventType;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Debug, Deserialize)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    id: <span class="type">i64</span>,</span><br><span class="line">    username: <span class="type">String</span>,</span><br><span class="line">    email: <span class="type">Option</span>&lt;<span class="type">String</span>&gt;,</span><br><span class="line">    score: <span class="type">f64</span>,</span><br><span class="line">    active: <span class="type">bool</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Inside your event loop:</span></span><br><span class="line"><span class="keyword">match</span> &amp;event.event_type &#123;</span><br><span class="line">    EventType::Insert &#123; .. &#125; =&gt; &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">user</span>: User = event.<span class="title function_ invoke__">deserialize_insert</span>()?;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;New user: &#123;:?&#125;&quot;</span>, user);</span><br><span class="line">    &#125;</span><br><span class="line">    EventType::Update &#123; .. &#125; =&gt; &#123;</span><br><span class="line">        <span class="keyword">let</span> (old, new): (<span class="type">Option</span>&lt;User&gt;, User) = event.<span class="title function_ invoke__">deserialize_update</span>()?;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;Updated: &#123;:?&#125; -&gt; &#123;:?&#125;&quot;</span>, old, new);</span><br><span class="line">    &#125;</span><br><span class="line">    EventType::Delete &#123; .. &#125; =&gt; &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">user</span>: User = event.<span class="title function_ invoke__">deserialize_delete</span>()?;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;Deleted: &#123;:?&#125;&quot;</span>, user);</span><br><span class="line">    &#125;</span><br><span class="line">    _ =&gt; &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Producer-Consumer-Pattern-with-tokio-spawn"><a href="#Producer-Consumer-Pattern-with-tokio-spawn" class="headerlink" title="Producer&#x2F;Consumer Pattern with tokio::spawn"></a>Producer&#x2F;Consumer Pattern with tokio::spawn</h3><p>For high-throughput scenarios, decouple WAL reading from event processing using channels:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::sync::mpsc;</span><br><span class="line"><span class="keyword">use</span> pg_walstream::&#123;ChangeEvent, ReplicationError&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">fn</span> <span class="title function_">run_producer</span>(</span><br><span class="line">    <span class="keyword">mut</span> stream: LogicalReplicationStream,</span><br><span class="line">    cancel_token: CancellationToken,</span><br><span class="line">    tx: mpsc::Sender&lt;ChangeEvent&gt;,</span><br><span class="line">) &#123;</span><br><span class="line">    stream.<span class="title function_ invoke__">start</span>(<span class="literal">None</span>).<span class="keyword">await</span>.<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">    <span class="keyword">loop</span> &#123;</span><br><span class="line">        <span class="keyword">match</span> stream.<span class="title function_ invoke__">next_event_with_retry</span>(&amp;cancel_token).<span class="keyword">await</span> &#123;</span><br><span class="line">            <span class="title function_ invoke__">Ok</span>(event) =&gt; &#123;</span><br><span class="line">                stream.shared_lsn_feedback.<span class="title function_ invoke__">update_applied_lsn</span>(event.lsn.<span class="title function_ invoke__">value</span>());</span><br><span class="line">                <span class="keyword">if</span> tx.<span class="title function_ invoke__">send</span>(event).<span class="keyword">await</span>.<span class="title function_ invoke__">is_err</span>() &#123; <span class="keyword">break</span>; &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="title function_ invoke__">Err</span>(ReplicationError::<span class="title function_ invoke__">Cancelled</span>(_)) =&gt; <span class="keyword">break</span>,</span><br><span class="line">            <span class="title function_ invoke__">Err</span>(e) =&gt; &#123;</span><br><span class="line">                <span class="built_in">eprintln!</span>(<span class="string">&quot;Fatal: &#123;e&#125;&quot;</span>);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    stream.<span class="title function_ invoke__">stop</span>().<span class="keyword">await</span>.<span class="title function_ invoke__">ok</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Load-Testing-Results"><a href="#Load-Testing-Results" class="headerlink" title="Load Testing Results"></a>Load Testing Results</h2><p>The library was benchmarked on an 8-core Intel Xeon Platinum 8370C (16 GB RAM, Ubuntu 22.04) across three PostgreSQL configurations:</p><ul><li><strong>PG16</strong> with protocol v4 + parallel streaming (rustls-tls backend)</li><li><strong>PG18</strong> with binary mode + direct TLS negotiation</li><li><strong>PG18 + COPY</strong> with COPY generator optimization</li></ul><h3 id="DML-Throughput-events-sec"><a href="#DML-Throughput-events-sec" class="headerlink" title="DML Throughput (events&#x2F;sec)"></a>DML Throughput (events&#x2F;sec)</h3><table><thead><tr><th>Scenario</th><th align="right">PG16</th><th align="right">PG18 Binary+DirectTLS</th><th align="right">PG18 +COPY</th></tr></thead><tbody><tr><td>Baseline</td><td align="right">148,533</td><td align="right">168,205</td><td align="right"><strong>209,193</strong></td></tr><tr><td>Batch-5000</td><td align="right">132,623</td><td align="right">151,820</td><td align="right"><strong>190,687</strong></td></tr><tr><td>4-Writers</td><td align="right">135,036</td><td align="right">159,233</td><td align="right"><strong>193,354</strong></td></tr><tr><td>Mixed-DML</td><td align="right">42,198</td><td align="right">176,580</td><td align="right"><strong>186,019</strong></td></tr><tr><td>Batch-100</td><td align="right">22,270</td><td align="right">141,597</td><td align="right"><strong>199,780</strong></td></tr><tr><td>Wide-20col</td><td align="right">18,917</td><td align="right">172,772</td><td align="right"><strong>173,283</strong></td></tr><tr><td>Payload-2KB</td><td align="right">14,017</td><td align="right">114,884</td><td align="right"><strong>134,323</strong></td></tr></tbody></table><p>Peak throughput: <strong>209,193 events&#x2F;sec</strong> (PG18 + COPY, Baseline scenario).</p><h3 id="Data-Throughput-MB-s"><a href="#Data-Throughput-MB-s" class="headerlink" title="Data Throughput (MB&#x2F;s)"></a>Data Throughput (MB&#x2F;s)</h3><table><thead><tr><th>Scenario</th><th align="right">PG16</th><th align="right">PG18 Binary+DirectTLS</th><th align="right">PG18 +COPY</th></tr></thead><tbody><tr><td>Baseline</td><td align="right">30.4</td><td align="right">31.1</td><td align="right"><strong>38.7</strong></td></tr><tr><td>Wide-20col</td><td align="right">21.3</td><td align="right">50.7</td><td align="right"><strong>51.5</strong></td></tr><tr><td>Payload-2KB</td><td align="right">28.3</td><td align="right">43.9</td><td align="right"><strong>57.1</strong></td></tr><tr><td>Mixed-DML</td><td align="right">7.9</td><td align="right">32.1</td><td align="right"><strong>33.8</strong></td></tr><tr><td>Batch-100</td><td align="right">4.6</td><td align="right">26.2</td><td align="right"><strong>37.0</strong></td></tr></tbody></table><p>Best data throughput: <strong>57.1 MB&#x2F;s</strong> (PG18 + COPY, Payload-2KB scenario).</p><h3 id="Stress-Scaling-16-to-192-Concurrent-Writers"><a href="#Stress-Scaling-16-to-192-Concurrent-Writers" class="headerlink" title="Stress Scaling: 16 to 192 Concurrent Writers"></a>Stress Scaling: 16 to 192 Concurrent Writers</h3><table><thead><tr><th align="right">Writers</th><th align="right">PG16</th><th align="right">PG18 Binary+DirectTLS</th><th align="right">PG18 +COPY</th></tr></thead><tbody><tr><td align="right">16</td><td align="right">125,657</td><td align="right">130,625</td><td align="right"><strong>185,044</strong></td></tr><tr><td align="right">32</td><td align="right">111,970</td><td align="right">133,880</td><td align="right"><strong>184,718</strong></td></tr><tr><td align="right">64</td><td align="right">103,937</td><td align="right">125,082</td><td align="right"><strong>182,349</strong></td></tr><tr><td align="right">128</td><td align="right">87,352</td><td align="right">109,594</td><td align="right"><strong>160,293</strong></td></tr><tr><td align="right">192</td><td align="right">71,316</td><td align="right">98,482</td><td align="right"><strong>171,585</strong></td></tr></tbody></table><p>Under high concurrency (16 to 192 writers), PG16 degrades by <strong>43%</strong> while PG18 + COPY only degrades by <strong>~7%</strong>, demonstrating significantly better scalability.</p><h3 id="CPU-Efficiency-events-sec-per-1-CPU"><a href="#CPU-Efficiency-events-sec-per-1-CPU" class="headerlink" title="CPU Efficiency (events&#x2F;sec per 1% CPU)"></a>CPU Efficiency (events&#x2F;sec per 1% CPU)</h3><table><thead><tr><th>Scenario</th><th align="right">PG16</th><th align="right">PG18 Binary+DirectTLS</th><th align="right">PG18 +COPY</th></tr></thead><tbody><tr><td>Baseline</td><td align="right">5,689</td><td align="right">5,637</td><td align="right"><strong>5,920</strong></td></tr><tr><td>Batch-5000</td><td align="right">5,379</td><td align="right"><strong>5,733</strong></td><td align="right">5,440</td></tr><tr><td>Wide-20col</td><td align="right">2,369</td><td align="right">5,059</td><td align="right"><strong>5,517</strong></td></tr><tr><td>Batch-100</td><td align="right">3,966</td><td align="right">5,572</td><td align="right"><strong>5,693</strong></td></tr></tbody></table><p>PG18 variants deliver consistently higher CPU efficiency, averaging <strong>5,200+ events&#x2F;sec per 1% CPU</strong> compared to PG16’s <strong>~4,700</strong>.</p><h3 id="Memory-Usage"><a href="#Memory-Usage" class="headerlink" title="Memory Usage"></a>Memory Usage</h3><p>All configurations remain extremely lightweight — between <strong>15–18 MB RSS</strong> regardless of load. Memory stays flat even under 192 concurrent writers, demonstrating the zero-copy buffer design pays off.</p><h3 id="Key-Takeaways-from-Load-Tests"><a href="#Key-Takeaways-from-Load-Tests" class="headerlink" title="Key Takeaways from Load Tests"></a>Key Takeaways from Load Tests</h3><ol><li><strong>PG18 + COPY + binary mode</strong> is the clear winner, peaking at <strong>209K events&#x2F;sec</strong></li><li><strong>Stress resilience</strong> — PG18 + COPY maintains throughput under heavy concurrency where PG16 degrades sharply</li><li><strong>CPU efficient</strong> — the rustls-tls backend uses ~3x less CPU than libpq in prior benchmarks (4,252 vs 1,628 events&#x2F;sec per 1% CPU)</li><li><strong>Memory stable</strong> — sub-18 MB footprint under all tested conditions</li><li><strong>Binary mode + direct TLS</strong> provide significant improvements even without COPY optimization</li></ol><h2 id="Performance-Design-Decisions"><a href="#Performance-Design-Decisions" class="headerlink" title="Performance Design Decisions"></a>Performance Design Decisions</h2><p>Several design choices contribute to pg-walstream’s performance:</p><ul><li><strong>SmallVec</strong> for tuple data — up to 16 columns stored inline on the stack, avoiding heap allocation for common cases</li><li><strong>Custom OidHasher</strong> — eliminates SipHash overhead for 32-bit OID integer keys</li><li><strong>Arc&lt;str&gt;</strong> for column&#x2F;namespace names — shared immutable strings across events</li><li><strong>CachePadded atomics</strong> for LSN feedback — avoids false sharing in concurrent scenarios</li><li><strong>Feedback throttling</strong> — time checks only every 128 events via bitmask (<code>count &amp; 0x7F == 0</code>)</li></ul><h2 id="TCP-Tuning-for-Production"><a href="#TCP-Tuning-for-Production" class="headerlink" title="TCP Tuning for Production"></a>TCP Tuning for Production</h2><p>For high-throughput deployments, the following Linux kernel parameters are recommended:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">net.core.rmem_max = 67108864</span><br><span class="line">net.core.wmem_max = 67108864</span><br><span class="line">net.ipv4.tcp_rmem = 4096 262144 67108864</span><br><span class="line">net.ipv4.tcp_wmem = 4096 262144 67108864</span><br><span class="line">net.ipv4.tcp_congestion_control = bbr</span><br><span class="line">net.core.netdev_max_backlog = 5000</span><br></pre></td></tr></table></figure><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>pg-walstream fills a gap in the Rust ecosystem for a production-grade PostgreSQL WAL streaming library. With protocol v1–v4 support, dual connection backends, zero-copy parsing, and throughput exceeding 200K events&#x2F;sec, it provides a solid foundation for building CDC pipelines, event-driven systems, and real-time data synchronization.</p><p>The load testing results demonstrate that pairing pg-walstream with PostgreSQL 18’s binary mode and COPY optimization delivers exceptional performance and scalability — maintaining high throughput even under 192 concurrent writers while keeping memory usage under 18 MB.</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg-walstream">pg-walstream GitHub Repository</a></li><li><a target="_blank" rel="noopener" href="https://crates.io/crates/pg_walstream">pg-walstream on crates.io</a></li><li><a target="_blank" rel="noopener" href="https://docs.rs/pg-walstream">API Documentation on docs.rs</a></li><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg-walstream/blob/main/LOAD_TEST_COMPARISON.md">Load Test Comparison Report</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/logical-replication.html">PostgreSQL Logical Replication Documentation</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pg-walstream-rust-postgresql-wal-streaming/">https://isdaniel.github.io/pg-walstream-rust-postgresql-wal-streaming/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pg-walstream-rust-postgresql-wal-streaming/</id>
    <link href="https://isdaniel.github.io/pg-walstream-rust-postgresql-wal-streaming/"/>
    <published>2026-04-21T14:00:00.000Z</published>
    <summary>A high-performance Rust library for parsing and streaming PostgreSQL WAL messages via logical replication — with load testing results showing 200K+ events/sec.</summary>
    <title>pg-walstream: High-Performance PostgreSQL WAL Streaming in Rust</title>
    <updated>2026-08-05T10:10:26.491Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/logical-replication/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/tags/logical-replication/"/>
    <category term="CDC" scheme="https://isdaniel.github.io/tags/CDC/"/>
    <content>
      <![CDATA[<h2 id="What-is-pg2any"><a href="#What-is-pg2any" class="headerlink" title="What is pg2any?"></a>What is pg2any?</h2><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg2any">pg2any</a> is a Rust library (published as <code>pg2any_lib</code> on <a target="_blank" rel="noopener" href="https://crates.io/crates/pg2any_lib">crates.io</a>) that builds production-ready Change Data Capture (CDC) pipelines. It reads PostgreSQL’s Write-Ahead Log (WAL) through logical replication and replays changes — inserts, updates, deletes, and truncates — to destination databases in real time.</p><p>Supported destinations:</p><ul><li><strong>MySQL</strong> (via SQLx)</li><li><strong>SQL Server</strong> (via Tiberius)</li><li><strong>SQLite</strong> (via SQLx)</li></ul><p>Each destination is behind a Cargo feature flag (<code>mysql</code>, <code>sqlserver</code>, <code>sqlite</code>), so you compile only the drivers you need.</p><p>A ready-to-run example application lives at <a target="_blank" rel="noopener" href="https://github.com/isdaniel/rust_playground/tree/main/pg2any-example">pg2any-example</a>, and the underlying PostgreSQL streaming replication protocol is handled by the companion crate <a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg-walstream">pg_walstream</a>.</p><h2 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h2><p>pg2any follows a <strong>producer–consumer</strong> pattern with <strong>file-based transaction persistence</strong> as the intermediary. This design gives crash safety: if the process dies mid-stream, committed-but-unexecuted transactions survive on disk and are replayed on restart.</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────┐        ┌──────────────────────────────────────┐       ┌──────────────────┐</span><br><span class="line">│   PostgreSQL    │        │          pg2any CDC Engine            │       │   Destination    │</span><br><span class="line">│                 │  WAL   │                                      │  SQL  │  MySQL <span class="symbol">/</span> MSSQL   │</span><br><span class="line">│  Logical Repli- │───────▶│  ┌──────────┐     ┌───────────────┐ │──────▶│  <span class="symbol">/</span> SQLite        │</span><br><span class="line">│  cation Stream  │        │  │ Producer  │────▶│ File Storage  │ │       │                  │</span><br><span class="line">│                 │        │  └──────────┘     └───────┬───────┘ │       └──────────────────┘</span><br><span class="line">└─────────────────┘        │                          │         │</span><br><span class="line">                           │                   ┌──────▼───────┐ │       ┌──────────────────┐</span><br><span class="line">                           │                   │   Consumer   │ │       │   Prometheus     │</span><br><span class="line">                           │                   │ (Priority Q) │ │       │   <span class="symbol">/metrics</span>       │</span><br><span class="line">                           │                   └──────────────┘ │       │   <span class="symbol">/health</span>        │</span><br><span class="line">                           └──────────────────────────────────────┘       └──────────────────┘</span><br></pre></td></tr></table></figure><h3 id="Three-Directory-Transaction-Lifecycle"><a href="#Three-Directory-Transaction-Lifecycle" class="headerlink" title="Three-Directory Transaction Lifecycle"></a>Three-Directory Transaction Lifecycle</h3><p>Transactions flow through three directories during their lifetime:</p><table><thead><tr><th>Directory</th><th>Purpose</th></tr></thead><tbody><tr><td><code>sql_data_tx/</code></td><td>Stores actual SQL content. Files are append-only and rotate at 64 MB segments for large transactions.</td></tr><tr><td><code>sql_received_tx/</code></td><td>Metadata for <strong>in-progress</strong> transactions (created at <code>BEGIN</code>).</td></tr><tr><td><code>sql_pending_tx/</code></td><td>Metadata for <strong>committed</strong> transactions ready for the consumer (atomically moved from <code>sql_received_tx/</code> on <code>COMMIT</code>).</td></tr></tbody></table><p>This three-phase approach means that only fully committed transactions ever reach the consumer, and incomplete transactions are cleaned up on restart.</p><h3 id="Producer"><a href="#Producer" class="headerlink" title="Producer"></a>Producer</h3><p>The producer reads the logical replication stream event by event:</p><ol><li>On <strong>BEGIN</strong> — creates a metadata file in <code>sql_received_tx/</code> and a data file in <code>sql_data_tx/</code>.</li><li>On <strong>INSERT &#x2F; UPDATE &#x2F; DELETE &#x2F; TRUNCATE</strong> — converts each event to destination-dialect SQL and appends it to the data file via a <code>BufferedEventWriter</code>.</li><li>On <strong>COMMIT</strong> — atomically moves metadata from <code>sql_received_tx/</code> to <code>sql_pending_tx/</code>, making the transaction visible to the consumer.</li></ol><p>For protocol version 2+, the producer also handles <strong>streaming transactions</strong> (<code>StreamStart</code> &#x2F; <code>StreamStop</code> &#x2F; <code>StreamCommit</code>), which allow PostgreSQL to send chunks of large in-progress transactions before the final commit.</p><h3 id="Consumer"><a href="#Consumer" class="headerlink" title="Consumer"></a>Consumer</h3><p>The consumer maintains a <strong>priority queue ordered by commit LSN</strong> to guarantee correct replay order:</p><ol><li>Reads pending transaction metadata from <code>sql_pending_tx/</code>.</li><li>Parses SQL from <code>sql_data_tx/</code> using a streaming SQL parser (constant memory regardless of transaction size).</li><li>Executes statements atomically in a destination-side database transaction.</li><li>Invokes a <strong>PreCommitHook</strong> — a callback that runs inside the destination transaction before <code>COMMIT</code>, used to atomically persist the LSN checkpoint alongside the data. This eliminates the window where data is committed but the checkpoint is not (or vice versa).</li><li>Commits, then deletes processed files.</li></ol><h3 id="Crash-Recovery"><a href="#Crash-Recovery" class="headerlink" title="Crash Recovery"></a>Crash Recovery</h3><p>On startup, pg2any scans:</p><ul><li><code>sql_received_tx/</code> for incomplete transactions → <strong>aborts</strong> them.</li><li><code>sql_pending_tx/</code> for committed-but-unexecuted transactions → <strong>replays</strong> them.</li></ul><p>The <code>LsnTracker</code> persists the last successfully applied LSN, so replication resumes exactly where it left off.</p><h2 id="Key-Features"><a href="#Key-Features" class="headerlink" title="Key Features"></a>Key Features</h2><h3 id="DML-Coalescing"><a href="#DML-Coalescing" class="headerlink" title="DML Coalescing"></a>DML Coalescing</h3><p>One of pg2any’s most impactful optimizations. Instead of executing individual DML statements one by one, the coalescing engine merges consecutive same-table operations:</p><ul><li>Multiple <code>INSERT</code>s → multi-value <code>INSERT INTO ... VALUES (...), (...), (...)</code>.</li><li>Multiple <code>UPDATE</code>s → <code>CASE</code>-<code>WHEN</code> batch updates.</li><li>Multiple <code>DELETE</code>s → combined <code>WHERE</code> clauses with <code>OR</code>.</li></ul><p>This is applied across all three destination types, with dialect-aware identifier quoting (backticks for MySQL, brackets for SQL Server, double quotes for SQLite) and respects <code>max_allowed_packet</code> limits (MySQL) with an 80% safety margin.</p><h3 id="Compressed-Storage"><a href="#Compressed-Storage" class="headerlink" title="Compressed Storage"></a>Compressed Storage</h3><p>When enabled via <code>PG2ANY_ENABLE_COMPRESSION=true</code>, transaction files are stored as <code>.sql.gz</code> with accompanying <code>.sql.gz.idx</code> index files. Sync points are created every 1,000 statements, enabling O(1) seeking to arbitrary positions without decompressing the entire file — critical for efficient crash recovery of large transactions.</p><h3 id="Monitoring"><a href="#Monitoring" class="headerlink" title="Monitoring"></a>Monitoring</h3><p>With the <code>metrics</code> feature enabled, pg2any exposes a Prometheus-compatible HTTP server (default port 8080):</p><ul><li><code>GET /metrics</code> — Prometheus text format with event counters, LSN progress, processing rates, error counts, and transaction statistics.</li><li><code>GET /health</code> — JSON health status.</li></ul><p>Metrics use <code>AtomicU64</code> counters (lock-free) to minimize overhead on the hot path. When compiled without the <code>metrics</code> feature, all metric calls become zero-cost no-ops.</p><h3 id="Protocol-Version-Support"><a href="#Protocol-Version-Support" class="headerlink" title="Protocol Version Support"></a>Protocol Version Support</h3><table><thead><tr><th>Version</th><th>Capabilities</th></tr></thead><tbody><tr><td>v1</td><td>Basic logical replication (BEGIN, INSERT, UPDATE, DELETE, TRUNCATE, COMMIT)</td></tr><tr><td>v2</td><td>Adds streaming transactions for large in-progress transactions</td></tr><tr><td>v3</td><td>Adds two-phase commit support</td></tr><tr><td>v4</td><td>Additional protocol capabilities</td></tr></tbody></table><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Prerequisites"><a href="#Prerequisites" class="headerlink" title="Prerequisites"></a>Prerequisites</h3><ul><li>PostgreSQL 10+ with <code>wal_level = logical</code></li><li>A destination database (MySQL 8.0+, SQL Server, or SQLite)</li></ul><h3 id="PostgreSQL-Setup"><a href="#PostgreSQL-Setup" class="headerlink" title="PostgreSQL Setup"></a>PostgreSQL Setup</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Verify logical replication is enabled</span></span><br><span class="line"><span class="keyword">SHOW</span> wal_level;           <span class="comment">-- must be &#x27;logical&#x27;</span></span><br><span class="line"><span class="keyword">SHOW</span> max_replication_slots;</span><br><span class="line"><span class="keyword">SHOW</span> max_wal_senders;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Create a publication for the tables you want to replicate</span></span><br><span class="line"><span class="keyword">CREATE</span> PUBLICATION cdc_pub <span class="keyword">FOR</span> <span class="keyword">ALL</span> TABLES;</span><br><span class="line"><span class="comment">-- Or for specific tables:</span></span><br><span class="line"><span class="comment">-- CREATE PUBLICATION cdc_pub FOR TABLE orders, customers;</span></span><br></pre></td></tr></table></figure><h3 id="Using-pg2any-as-a-Library"><a href="#Using-pg2any-as-a-Library" class="headerlink" title="Using pg2any as a Library"></a>Using pg2any as a Library</h3><p>Add <code>pg2any_lib</code> to your <code>Cargo.toml</code> with the destination features you need:</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">pg2any_lib</span> = &#123; version = <span class="string">&quot;0.9&quot;</span>, features = [<span class="string">&quot;mysql&quot;</span>, <span class="string">&quot;metrics&quot;</span>] &#125;</span><br><span class="line"><span class="attr">tokio</span> = &#123; version = <span class="string">&quot;1&quot;</span>, features = [<span class="string">&quot;full&quot;</span>] &#125;</span><br></pre></td></tr></table></figure><h3 id="Configuration-via-Environment-Variables"><a href="#Configuration-via-Environment-Variables" class="headerlink" title="Configuration via Environment Variables"></a>Configuration via Environment Variables</h3><table><thead><tr><th>Variable</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>CDC_SOURCE_CONNECTION_STRING</code></td><td>PostgreSQL URI with <code>?replication=database</code></td><td>Required</td></tr><tr><td><code>CDC_DEST_TYPE</code></td><td><code>MySQL</code>, <code>SqlServer</code>, or <code>SQLite</code></td><td>Required</td></tr><tr><td><code>CDC_DEST_URI</code></td><td>Destination connection string</td><td>Required</td></tr><tr><td><code>CDC_REPLICATION_SLOT</code></td><td>Replication slot name</td><td>Required</td></tr><tr><td><code>CDC_PUBLICATION</code></td><td>Publication name</td><td>Required</td></tr><tr><td><code>CDC_SCHEMA_MAPPING</code></td><td>Comma-separated <code>source:dest</code> pairs (e.g., <code>public:cdc_db</code>)</td><td>None</td></tr><tr><td><code>CDC_PROTOCOL_VERSION</code></td><td>Protocol version (1–4)</td><td><code>1</code></td></tr><tr><td><code>CDC_STREAMING_MODE</code></td><td>Enable streaming transactions (requires v2+)</td><td><code>false</code></td></tr><tr><td><code>CDC_BINARY_MODE</code></td><td>Binary format for protocol</td><td><code>false</code></td></tr><tr><td><code>CDC_CONNECTION_TIMEOUT</code></td><td>Connection timeout (seconds)</td><td><code>30</code></td></tr><tr><td><code>CDC_QUERY_TIMEOUT</code></td><td>Query timeout (seconds)</td><td><code>60</code></td></tr><tr><td><code>CDC_BUFFER_SIZE</code></td><td>Transaction channel queue capacity</td><td><code>1000</code></td></tr><tr><td><code>CDC_BATCH_SIZE</code></td><td>Batch size for destination inserts</td><td><code>1000</code></td></tr><tr><td><code>PG2ANY_ENABLE_COMPRESSION</code></td><td>Enable gzip compression for SQL files</td><td><code>false</code></td></tr><tr><td><code>PG2ANY_METRICS_PORT</code></td><td>Prometheus HTTP port</td><td><code>8080</code></td></tr></tbody></table><h3 id="Run-with-Docker-Compose"><a href="#Run-with-Docker-Compose" class="headerlink" title="Run with Docker Compose"></a>Run with Docker Compose</h3><p>The <a target="_blank" rel="noopener" href="https://github.com/isdaniel/rust_playground/tree/main/pg2any-example">example project</a> includes a full Docker Compose stack with PostgreSQL, MySQL, the CDC application, and Prometheus:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/isdaniel/rust_playground.git</span><br><span class="line"><span class="built_in">cd</span> rust_playground/pg2any-example</span><br><span class="line"></span><br><span class="line"><span class="comment"># Start all services</span></span><br><span class="line">docker-compose up -d</span><br><span class="line"></span><br><span class="line"><span class="comment"># Watch CDC logs</span></span><br><span class="line">docker-compose logs -f cdc_app</span><br></pre></td></tr></table></figure><h2 id="Design-Decisions-Worth-Noting"><a href="#Design-Decisions-Worth-Noting" class="headerlink" title="Design Decisions Worth Noting"></a>Design Decisions Worth Noting</h2><p><strong>File-based persistence over in-memory queues</strong> — Using the filesystem as the intermediary between producer and consumer trades some latency for crash safety. If the process is killed, no committed transaction data is lost.</p><p><strong>PreCommitHook for atomic checkpoints</strong> — Executing the LSN checkpoint update inside the same destination transaction as the data changes eliminates an entire class of consistency bugs where the checkpoint and data can diverge.</p><p><strong>Feature-gated compilation</strong> — Database drivers and monitoring are behind Cargo features, so the binary only includes what you actually use. This reduces compile time, binary size, and attack surface.</p><p><strong>Transaction segmentation at 64 MB</strong> — Large transactions (e.g., bulk imports) are split across multiple files to prevent unbounded memory and disk usage.</p><h2 id="Testing"><a href="#Testing" class="headerlink" title="Testing"></a>Testing</h2><p>pg2any has 104+ tests across 16 test files, covering:</p><ul><li>Integration tests for all three destination types</li><li>Streaming transaction correctness</li><li>Compression and large file handling</li><li>WHERE clause generation for UPDATE&#x2F;DELETE with various replica identity configurations</li><li>Position tracking for crash recovery</li><li>Metrics logic</li></ul><p>Beyond unit and integration tests, the project runs <strong>chaos testing</strong> in CI — randomly restarting the CDC application during pgbench workloads to validate graceful shutdown and recovery under real conditions.</p><h2 id="Links"><a href="#Links" class="headerlink" title="Links"></a>Links</h2><ul><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg2any">pg2any source code (GitHub)</a></li><li><a target="_blank" rel="noopener" href="https://crates.io/crates/pg2any_lib">pg2any_lib on crates.io</a></li><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/rust_playground/tree/main/pg2any-example">Example application</a></li><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg-walstream">pg_walstream — PostgreSQL replication protocol crate</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pg2any-rust-introduce/">https://isdaniel.github.io/pg2any-rust-introduce/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pg2any-rust-introduce/</id>
    <link href="https://isdaniel.github.io/pg2any-rust-introduce/"/>
    <published>2025-09-09T13:10:43.000Z</published>
    <summary>pg2any is a Rust CDC library that streams PostgreSQL changes via logical replication to MySQL, SQL Server, or SQLite with crash-safe persistence</summary>
    <title>pg2any: A Rust CDC Library for Streaming PostgreSQL Changes to Any Database</title>
    <updated>2026-08-05T10:10:26.491Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="Redis" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/Redis/"/>
    <category term="FDW" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/Redis/FDW/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/tags/pgrx/"/>
    <category term="Redis" scheme="https://isdaniel.github.io/tags/Redis/"/>
    <category term="FDW" scheme="https://isdaniel.github.io/tags/FDW/"/>
    <content>
      <![CDATA[<p>大家好，今天要和大家介紹我近期開發的一個開源專案 —— <a target="_blank" rel="noopener" href="https://github.com/isdaniel/redis_fdw_rs"><code>redis_fdw_rs</code></a>，這是一個使用 Rust 語言與 <a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx</a> 框架實作的 <strong>Redis Foreign Data Wrapper (FDW)</strong>，讓你能夠在 PostgreSQL 中直接查詢 Redis 資料，就像操作一般的資料表一樣。</p><h2 id="為什麼需要-Redis-FDW？"><a href="#為什麼需要-Redis-FDW？" class="headerlink" title="為什麼需要 Redis FDW？"></a>為什麼需要 Redis FDW？</h2><p>Redis 是高效的快取資料庫，常被用於儲存 session、排行榜、事件流等資料。但當你想從 PostgreSQL 中同步存取 Redis 資料，就必須透過額外程式碼或 ETL 工具，相對麻煩。</p><p><code>redis_fdw_rs</code> 就是為了解決這個痛點而生：<strong>透過 FDW 介面，讓 PostgreSQL 能用 SQL 查 Redis！</strong></p><hr><h2 id="🚀-專案特色與支援功能"><a href="#🚀-專案特色與支援功能" class="headerlink" title="🚀 專案特色與支援功能"></a>🚀 專案特色與支援功能</h2><p>這個 FDW 專案目前已經支援以下功能，適合實際部署與使用：</p><ul><li>✅ <strong>支援 Redis Cluster</strong></li><li>✅ <strong>WHERE 條件下推</strong>（Pushdown）：減少資料搬移量，提升查詢效率</li><li>✅ <strong>連線池管理</strong>：避免反覆連線 Redis 的開銷</li><li>✅ <strong>Stream 大量資料支援</strong>：批次查詢、分頁等場景皆可處理</li><li>✅ <strong>支援 PostgreSQL 14~17</strong></li><li>✅ <strong>Unit Test &amp; Integration Test</strong>：專案有測試覆蓋，確保穩定性</li></ul><hr><h2 id="使用範例（超簡單）"><a href="#使用範例（超簡單）" class="headerlink" title="使用範例（超簡單）"></a>使用範例（超簡單）</h2><p>只需要幾行 SQL，就能連結 Redis 並開始查詢：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 建立 Redis 伺服器連線</span></span><br><span class="line"><span class="keyword">CREATE</span> SERVER redis_server</span><br><span class="line"><span class="keyword">FOREIGN</span> DATA WRAPPER redis_wrapper</span><br><span class="line">OPTIONS (host_port <span class="string">&#x27;127.0.0.1:6379&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 宣告一個 Redis hash 的外部表格</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">FOREIGN</span> <span class="keyword">TABLE</span> user_profiles (</span><br><span class="line">  field text,</span><br><span class="line">  <span class="keyword">value</span> text</span><br><span class="line">)</span><br><span class="line">SERVER redis_server</span><br><span class="line">OPTIONS (table_type <span class="string">&#x27;hash&#x27;</span>, table_key_prefix <span class="string">&#x27;user:profiles&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 開始使用 SQL 操作 Redis！</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_profiles <span class="keyword">VALUES</span> (<span class="string">&#x27;name&#x27;</span>, <span class="string">&#x27;John&#x27;</span>);</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> user_profiles <span class="keyword">WHERE</span> field <span class="operator">=</span> <span class="string">&#x27;email&#x27;</span>;</span><br></pre></td></tr></table></figure><h2 id="Redis-Cluster-模式支援"><a href="#Redis-Cluster-模式支援" class="headerlink" title="Redis Cluster 模式支援"></a>Redis Cluster 模式支援</h2><p><code>redis_fdw_rs</code> 也完全支援 Redis Cluster 架構。你只需指定多個節點的 <code>host_port</code>，即可享有以下好處：</p><h3 id="Cluster-優勢"><a href="#Cluster-優勢" class="headerlink" title="Cluster 優勢"></a>Cluster 優勢</h3><ul><li><strong>自動故障轉移</strong>：節點失效時自動轉移到健康節點</li><li><strong>自動 sharding</strong>：資料分散在多節點，自動分片</li><li><strong>節點自動探索</strong>：只需指定一個節點，驅動程式會自動發現整個叢集</li><li><strong>高可用性</strong>：節點損壞仍可正常讀寫</li></ul><h3 id="範例設定："><a href="#範例設定：" class="headerlink" title="範例設定："></a>範例設定：</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 建立 cluster foreign server</span></span><br><span class="line"><span class="keyword">CREATE</span> SERVER redis_cluster_server</span><br><span class="line"><span class="keyword">FOREIGN</span> DATA WRAPPER redis_wrapper</span><br><span class="line">OPTIONS (</span><br><span class="line">    host_port <span class="string">&#x27;127.0.0.1:7000,127.0.0.1:7001,127.0.0.1:7002&#x27;</span>,</span><br><span class="line">    password <span class="string">&#x27;your_redis_password&#x27;</span>  <span class="comment">-- 可選</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 建立 cluster 對應的外部表格</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">FOREIGN</span> <span class="keyword">TABLE</span> user_sessions (</span><br><span class="line">    field TEXT,</span><br><span class="line">    <span class="keyword">value</span> TEXT</span><br><span class="line">)</span><br><span class="line">SERVER redis_cluster_server</span><br><span class="line">OPTIONS (</span><br><span class="line">    database <span class="string">&#x27;0&#x27;</span>,</span><br><span class="line">    table_type <span class="string">&#x27;hash&#x27;</span>,</span><br><span class="line">    table_key_prefix <span class="string">&#x27;session:active&#x27;</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 與單節點操作無異</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> user_sessions <span class="keyword">VALUES</span> (<span class="string">&#x27;user123&#x27;</span>, <span class="string">&#x27;session_token_abc&#x27;</span>);</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> user_sessions <span class="keyword">WHERE</span> field <span class="operator">=</span> <span class="string">&#x27;user123&#x27;</span>;</span><br></pre></td></tr></table></figure><h3 id="範例結果"><a href="#範例結果" class="headerlink" title="範例結果"></a>範例結果</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><span class="line">redis_fdw_rs<span class="operator">=</span># <span class="keyword">INSERT INTO</span> user_profiles (key, <span class="keyword">value</span>)</span><br><span class="line"><span class="keyword">SELECT</span> i, <span class="string">&#x27;value_&#x27;</span> <span class="operator">||</span> i</span><br><span class="line"><span class="keyword">FROM</span> generate_series(<span class="number">1</span>,<span class="number">100000</span>) i;</span><br><span class="line"><span class="keyword">INSERT</span> <span class="number">0</span> <span class="number">100000</span></span><br><span class="line"><span class="type">Time</span>: <span class="number">12911.183</span> ms (<span class="number">00</span>:<span class="number">12.911</span>)</span><br><span class="line">redis_fdw_rs<span class="operator">=</span># <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> user_profiles <span class="keyword">where</span> key <span class="operator">=</span> <span class="string">&#x27;5&#x27;</span>;</span><br><span class="line"> key <span class="operator">|</span>  <span class="keyword">value</span></span><br><span class="line"><span class="comment">-----+---------</span></span><br><span class="line"> <span class="number">5</span>   <span class="operator">|</span> value_5</span><br><span class="line">(<span class="number">1</span> <span class="type">row</span>)</span><br><span class="line"></span><br><span class="line"><span class="type">Time</span>: <span class="number">15.380</span> ms</span><br><span class="line">redis_fdw_rs<span class="operator">=</span># <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> user_profiles <span class="keyword">where</span> key <span class="keyword">in</span> (<span class="string">&#x27;10&#x27;</span>, <span class="string">&#x27;15&#x27;</span>, <span class="string">&#x27;20&#x27;</span>);</span><br><span class="line"> key <span class="operator">|</span>  <span class="keyword">value</span></span><br><span class="line"><span class="comment">-----+----------</span></span><br><span class="line"> <span class="number">10</span>  <span class="operator">|</span> value_10</span><br><span class="line"> <span class="number">15</span>  <span class="operator">|</span> value_15</span><br><span class="line"> <span class="number">20</span>  <span class="operator">|</span> value_20</span><br><span class="line">(<span class="number">3</span> <span class="keyword">rows</span>)</span><br><span class="line"></span><br><span class="line">redis_fdw_rs<span class="operator">=</span>#  <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> user_profiles <span class="keyword">where</span> key <span class="keyword">like</span> <span class="string">&#x27;555%&#x27;</span>;</span><br><span class="line">  key  <span class="operator">|</span>    <span class="keyword">value</span></span><br><span class="line"><span class="comment">-------+-------------</span></span><br><span class="line"> <span class="number">55556</span> <span class="operator">|</span> value_55556</span><br><span class="line"> <span class="number">55581</span> <span class="operator">|</span> value_55581</span><br><span class="line"> <span class="number">55569</span> <span class="operator">|</span> value_55569</span><br><span class="line"> <span class="number">55561</span> <span class="operator">|</span> value_55561</span><br><span class="line"> <span class="number">55516</span> <span class="operator">|</span> value_55516</span><br><span class="line"> <span class="number">55538</span> <span class="operator">|</span> value_55538</span><br><span class="line"> <span class="number">55549</span> <span class="operator">|</span> value_55549</span><br><span class="line"> <span class="number">55539</span> <span class="operator">|</span> value_55539</span><br><span class="line"> <span class="number">55531</span> <span class="operator">|</span> value_55531</span><br><span class="line"> <span class="number">55545</span> <span class="operator">|</span> value_55545</span><br><span class="line"> <span class="number">55590</span> <span class="operator">|</span> value_55590</span><br><span class="line"> <span class="number">55512</span> <span class="operator">|</span> value_55512</span><br><span class="line"> <span class="number">55523</span> <span class="operator">|</span> value_55523</span><br><span class="line"> <span class="number">55534</span> <span class="operator">|</span> value_55534</span><br><span class="line"> <span class="number">55518</span> <span class="operator">|</span> value_55518</span><br><span class="line"> <span class="number">55560</span> <span class="operator">|</span> value_55560</span><br><span class="line"> <span class="number">55564</span> <span class="operator">|</span> value_55564</span><br><span class="line"> <span class="number">55592</span> <span class="operator">|</span> value_55592</span><br><span class="line"> <span class="number">55572</span> <span class="operator">|</span> value_55572</span><br><span class="line"> <span class="number">55519</span> <span class="operator">|</span> value_55519</span><br><span class="line"> <span class="number">55526</span> <span class="operator">|</span> value_55526</span><br><span class="line"> <span class="number">5559</span>  <span class="operator">|</span> value_5559</span><br><span class="line"> <span class="number">55530</span> <span class="operator">|</span> value_55530</span><br><span class="line"> <span class="number">55511</span> <span class="operator">|</span> value_55511</span><br><span class="line"> <span class="number">55562</span> <span class="operator">|</span> value_55562</span><br><span class="line"> <span class="number">55542</span> <span class="operator">|</span> value_55542</span><br><span class="line"> <span class="number">55582</span> <span class="operator">|</span> value_55582</span><br><span class="line"> <span class="number">55580</span> <span class="operator">|</span> value_55580</span><br><span class="line"> <span class="number">55501</span> <span class="operator">|</span> value_55501</span><br><span class="line"> <span class="number">55540</span> <span class="operator">|</span> value_55540</span><br><span class="line"> <span class="number">55554</span> <span class="operator">|</span> value_55554</span><br><span class="line"> <span class="number">55546</span> <span class="operator">|</span> value_55546</span><br><span class="line"> <span class="number">55513</span> <span class="operator">|</span> value_55513</span><br><span class="line"> <span class="number">55548</span> <span class="operator">|</span> value_55548</span><br><span class="line"><span class="comment">--More--</span></span><br></pre></td></tr></table></figure><hr><h2 id="支援的-Redis-資料型態"><a href="#支援的-Redis-資料型態" class="headerlink" title="支援的 Redis 資料型態"></a>支援的 Redis 資料型態</h2><p>目前支援以下 Redis 資料型別，並標示已實作的操作：</p><table><thead><tr><th>Redis Type</th><th>SELECT</th><th>INSERT</th><th>UPDATE</th><th>DELETE</th><th>Status</th></tr></thead><tbody><tr><td>Hash</td><td>✅</td><td>✅</td><td>❌</td><td>✅</td><td><strong>Partial</strong> (UPDATE not supported)</td></tr><tr><td>List</td><td>✅</td><td>✅</td><td>❌</td><td>✅</td><td><strong>Partial</strong> (UPDATE not supported)</td></tr><tr><td>Set</td><td>✅</td><td>✅</td><td>❌</td><td>✅</td><td><strong>Partial</strong> (UPDATE not supported)</td></tr><tr><td>ZSet</td><td>✅</td><td>✅</td><td>❌</td><td>✅</td><td><strong>Partial</strong> (UPDATE not supported)</td></tr><tr><td>String</td><td>✅</td><td>✅</td><td>❌</td><td>✅</td><td><strong>Partial</strong> (UPDATE not supported)</td></tr><tr><td>Stream</td><td>✅</td><td>✅</td><td>❌</td><td>✅</td><td><strong>Full</strong> (Large data set support with pagination)</td></tr></tbody></table><p>可透過 <code>table_type</code> 和 <code>table_key_prefix</code> 指定資料類型與 key 前綴，也支援選擇 Redis 的 <code>database</code>（預設為 0）。</p><hr><h2 id="專案資源"><a href="#專案資源" class="headerlink" title="專案資源"></a>專案資源</h2><ul><li>GitHub 倉庫：<a target="_blank" rel="noopener" href="https://github.com/isdaniel/redis_fdw_rs">https://github.com/isdaniel/redis_fdw_rs</a></li><li>文件與安裝說明完整、範例齊全</li><li>歡迎 Star、開 issue、PR 一起改進專案！</li></ul><hr><h2 id="結語：一起打造更強的資料存取能力！"><a href="#結語：一起打造更強的資料存取能力！" class="headerlink" title="結語：一起打造更強的資料存取能力！"></a>結語：一起打造更強的資料存取能力！</h2><p>這個專案仍持續演進中，如果你有 Redis &#x2F; PostgreSQL 混合架構的需求，或是對 Rust、FDW 開發有興趣，非常歡迎你一起參與改進。</p><p>有任何回饋，歡迎透過 GitHub 討論，我會持續更新與優化這個實用的工具！</p><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/redis-fdw-rs/">https://isdaniel.github.io/redis-fdw-rs/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/redis-fdw-rs/</id>
    <link href="https://isdaniel.github.io/redis-fdw-rs/"/>
    <published>2025-08-16T03:12:43.000Z</published>
    <summary>介紹開源專案 redis_fdw_rs，使用 Rust 與 pgrx 實作的 Redis FDW，讓 PostgreSQL 直接查詢 Redis 資料如同操作一般資料表</summary>
    <title>【開源介紹】redis_fdw_rs：讓 PostgreSQL 直接查 Redis 的 FDW 擴充套件（Rust 編寫）</title>
    <updated>2026-08-05T10:10:26.493Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/logical-replication/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="logical-replication" scheme="https://isdaniel.github.io/tags/logical-replication/"/>
    <content>
      <![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>大家好，今天要和大家介紹我近期開發的一個開源專案 <a target="_blank" rel="noopener" href="https://github.com/isdaniel/replication_checker_rs" title="GitHub - isdaniel&#x2F;replication_checker_rs">replication_checker_rs</a> 如果你想用一個輕量、可讀性高的工具實時觀察 PostgreSQL 邏輯複寫（logical replication）流，或想把複寫協議的學習變成可執行的實驗場，<code>replication_checker_rs</code> 是一個不錯的起點：它是 Rust 實現 PostgreSQL logical replication protocol 使用 <code>libpq-sys</code> 的實作，能連上資料庫、建立 replication slot，並將 INSERT &#x2F; UPDATE &#x2F; DELETE &#x2F; TRUNCATE 等變更以可讀格式顯示出來。(<a target="_blank" rel="noopener" href="https://github.com/isdaniel/replication_checker_rs">https://github.com/isdaniel/replication_checker_rs</a>)</p><hr><h2 id="為什麼會想用它？"><a href="#為什麼會想用它？" class="headerlink" title="為什麼會想用它？"></a>為什麼會想用它？</h2><ul><li><strong>學習角度</strong>：它直接實作 PostgreSQL 的 logical replication protocol（WAL message parsing、relation&#x2F;tuple 格式）以可讀、可改造的 Rust 程式碼呈現，適合希望理解底層協議的人。</li><li><strong>快速驗證</strong>：想知道 publication 有沒有正確產生事件、或某些 schema 變更會如何呈現時，可以直接跑這個工具觀察實際輸出。</li><li><strong>Rust + libpq 真實範例</strong>：展示如何用 <code>libpq-sys</code> 與 Tokio 做低階連線管理與 parser 實作</li><li><strong>延伸空間大</strong>：可以把它當作 PoC（proof of concept），加上 JSON 化、推到 Kafka&#x2F;Redis、做到可重啟的 consumer。</li></ul><hr><h2 id="功能與限制（快速掃描）"><a href="#功能與限制（快速掃描）" class="headerlink" title="功能與限制（快速掃描）"></a>功能與限制（快速掃描）</h2><p><strong>主要功能</strong></p><ul><li>用作邏輯複寫客戶端（logical replication client），可建立 replication slot 並接收變更。</li><li>支援顯示 <code>BEGIN</code>、<code>COMMIT</code>、<code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code>、<code>TRUNCATE</code> 以及 relation／tuple 資訊，並能處理 streaming（大型）交易。</li></ul><p><strong>目前限制</strong></p><ul><li>目前只把變更<strong>顯示為人類可讀格式</strong>，沒有把事件推到 Kafka&#x2F;Redis 等下游處理器。</li><li>只對文字型別（text types）有良好處理；binary 類型會以 raw 形式顯示。</li><li>slot 管理、錯誤復原邏輯較簡單（遇到大部分錯誤會結束程式）</li></ul><hr><h2 id="實作前準備（PostgreSQL-與系統）"><a href="#實作前準備（PostgreSQL-與系統）" class="headerlink" title="實作前準備（PostgreSQL 與系統）"></a>實作前準備（PostgreSQL 與系統）</h2><ol><li><strong>PostgreSQL 必須開啟 logical WAL</strong>：<code>wal_level = logical</code>（修改後需重啟 PostgreSQL）。這是 logical replication 的必要條件。</li><li><strong>建立 publication</strong>（只複寫你想要的 table，或 <code>FOR ALL TABLES</code>）：<code>CREATE PUBLICATION my_publication FOR TABLE table1, table2;</code>。</li><li><strong>建立有 replication 權限的 user</strong>：例如 <code>CREATE USER replicator WITH REPLICATION LOGIN PASSWORD &#39;password&#39;;</code> 並給予必要的 SELECT 權限。</li><li><strong>系統相依套件</strong>：需要 <code>libpq</code> 開發檔（例如 Debian&#x2F;Ubuntu 的 <code>libpq-dev</code>、macOS 用 Homebrew 安裝 <code>postgresql</code>），並使用 Rust 1.70+。</li></ol><blockquote><p>請注意：PostgreSQL DB 版本必須等於或高於版本 14，更多資訊請參閱以下連結。</p></blockquote><p><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/14/protocol-replication.html">https://www.postgresql.org/docs/14/protocol-replication.html</a><br><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/protocol-logical-replication.html#PROTOCOL-LOGICAL-REPLICATION-PARAMS">https://www.postgresql.org/docs/current/protocol-logical-replication.html#PROTOCOL-LOGICAL-REPLICATION-PARAMS</a></p><h3 id="PostgreSQL-設定（必要）"><a href="#PostgreSQL-設定（必要）" class="headerlink" title="PostgreSQL 設定（必要）"></a>PostgreSQL 設定（必要）</h3><ol><li><code>postgresql.conf</code>：</li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wal_level = logical</span><br><span class="line">max_replication_slots = 10    # 視需求調整</span><br><span class="line">max_wal_senders = 10</span><br></pre></td></tr></table></figure><p>修改後重啟 PostgreSQL。</p><ol start="2"><li>建 publication 與 replication user（在 psql 下執行）：</li></ol><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 建 replication user</span></span><br><span class="line"><span class="keyword">CREATE</span> ROLE replicator <span class="keyword">WITH</span> REPLICATION LOGIN PASSWORD <span class="string">&#x27;replicator_pw&#x27;</span>;</span><br><span class="line"><span class="comment">-- 建 publication（只複寫特定 table 或使用 FOR ALL TABLES）</span></span><br><span class="line"><span class="keyword">CREATE</span> PUBLICATION my_publication <span class="keyword">FOR</span> <span class="keyword">TABLE</span> public.my_table;</span><br></pre></td></tr></table></figure><blockquote><p>注意：不要無限制地建立 replication slot（未刪除會造成 WAL 快速累積），測試時留意 WAL 使用量。</p></blockquote><hr><h2 id="快速上手（編譯-執行）"><a href="#快速上手（編譯-執行）" class="headerlink" title="快速上手（編譯 &amp; 執行）"></a>快速上手（編譯 &amp; 執行）</h2><p><strong>從原始碼編譯</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/isdaniel/replication_checker_rs.git</span><br><span class="line"><span class="built_in">cd</span> replication_checker_rs</span><br><span class="line">cargo build --release</span><br></pre></td></tr></table></figure><p>（注意系統需先安裝 libpq 開發庫）。</p><p><strong>範例執行方式</strong>（README 範例改寫，實務上把 db 參數用空格分開的 key-value 傳入）</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 設定 slot / publication 名稱（可用環境變數）</span></span><br><span class="line"><span class="built_in">export</span> slot_name=<span class="string">&quot;my_slot&quot;</span></span><br><span class="line"><span class="built_in">export</span> pub_name=<span class="string">&quot;my_publication&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 執行（參數順序為 key value key value ...）</span></span><br><span class="line">./target/release/pg_replica_rs user azureuser **replication database** host 127.0.0.1 dbname redis_fdw_rs port 5432</span><br></pre></td></tr></table></figure><blockquote><p>連接字串需要使用 replication database 代表是 replication 操作<br>你也可以用 <code>RUST_LOG</code> 控制日誌等級（例：<code>RUST_LOG=debug</code>）。</p></blockquote><p><strong>Docker 方式</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">docker build -t pg_replica_rs .</span><br><span class="line">docker run -e slot_name=my_slot -e pub_name=my_pub \</span><br><span class="line">  pg_replica_rs user postgres password secret host host.docker.internal port 5432 dbname mydb</span><br></pre></td></tr></table></figure><p>方便在隔離環境做測試。</p><h2 id="實戰：建立-publication、產生測試資料、觀察輸出"><a href="#實戰：建立-publication、產生測試資料、觀察輸出" class="headerlink" title="實戰：建立 publication、產生測試資料、觀察輸出"></a>實戰：建立 publication、產生測試資料、觀察輸出</h2><h3 id="在資料庫上建立測試-table-與-publication"><a href="#在資料庫上建立測試-table-與-publication" class="headerlink" title="在資料庫上建立測試 table 與 publication"></a>在資料庫上建立測試 table 與 publication</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> public.my_table (</span><br><span class="line">  id serial <span class="keyword">primary key</span>,</span><br><span class="line">  msg text</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> PUBLICATION my_publication <span class="keyword">FOR</span> <span class="keyword">TABLE</span> public.my_table;</span><br></pre></td></tr></table></figure><h3 id="在另一個-psql-session-產生變更"><a href="#在另一個-psql-session-產生變更" class="headerlink" title="在另一個 psql session 產生變更"></a>在另一個 psql session 產生變更</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> public.my_table (msg) <span class="keyword">VALUES</span> (<span class="string">&#x27;hello replication&#x27;</span>);</span><br><span class="line"><span class="keyword">UPDATE</span> public.my_table <span class="keyword">SET</span> msg <span class="operator">=</span> <span class="string">&#x27;altered&#x27;</span> <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> public.my_table <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><h3 id="觀察-replication-checker-rs-的輸出（示例）"><a href="#觀察-replication-checker-rs-的輸出（示例）" class="headerlink" title="觀察 replication_checker_rs 的輸出（示例）"></a>觀察 <code>replication_checker_rs</code> 的輸出（示例）</h3><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">57</span>:<span class="number">54</span>.<span class="number">417412</span>Z  INFO Started receiving data from database server</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">01</span>.<span class="number">488499</span>Z  INFO BEGIN: Xid <span class="number">1522</span></span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">01</span>.<span class="number">489158</span>Z  INFO Received relation info for public.t1</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">01</span>.<span class="number">489235</span>Z  INFO TRUNCATE</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">01</span>.<span class="number">489255</span>Z  INFO public.t1</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">01</span>.<span class="number">489318</span>Z  INFO COMMIT: flags: <span class="number">0</span>, lsn: <span class="number">43614824</span>, end_lsn: <span class="number">43614944</span>, commit_time: <span class="number">2025</span>-<span class="number">08</span>-<span class="number">10</span> <span class="number">02</span>:<span class="number">58</span>:<span class="number">01</span>.<span class="number">484</span> UTC</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">07</span>.<span class="number">583760</span>Z  INFO BEGIN: Xid <span class="number">1523</span></span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">07</span>.<span class="number">583925</span>Z  INFO Received relation info for public.t1</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">07</span>.<span class="number">584000</span>Z  INFO table public.t1: INSERT:</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">07</span>.<span class="number">584012</span>Z  INFO a: <span class="number">1</span></span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">07</span>.<span class="number">584040</span>Z  INFO table public.t1: INSERT:</span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">07</span>.<span class="number">584062</span>Z  INFO a: <span class="number">2</span></span><br><span class="line"><span class="attribute">2025</span>-<span class="number">08</span>-<span class="number">10</span>T02:<span class="number">58</span>:<span class="number">07</span>.<span class="number">584104</span>Z  INFO COMMIT: flags: <span class="number">0</span>, lsn: <span class="number">43615128</span>, end_lsn: <span class="number">43615176</span>, commit_time: <span class="number">2025</span>-<span class="number">08</span>-<span class="number">10</span> <span class="number">02</span>:<span class="number">58</span>:<span class="number">07</span>.<span class="number">580</span> UTC</span><br><span class="line"></span><br></pre></td></tr></table></figure><hr><h2 id="程式碼結構與關鍵模組導覽"><a href="#程式碼結構與關鍵模組導覽" class="headerlink" title="程式碼結構與關鍵模組導覽"></a>程式碼結構與關鍵模組導覽</h2><p>（以 repo 常見的檔案分佈為範例）</p><ul><li><code>main.rs</code>：啟動、參數解析、log 設定</li><li><code>server.rs</code> &#x2F; <code>connection.rs</code>：與 Postgres 建立 replication 連線、處理 libpq loop</li><li><code>parser.rs</code>：負責把 raw WAL message 解析成內部事件（BEGIN&#x2F;RELATION&#x2F;INSERT&#x2F;UPDATE&#x2F;DELETE&#x2F;TRUNCATE&#x2F;COMMIT）</li><li><code>types.rs</code>：定義 relation、tuple 與欄位型別</li><li><code>utils.rs</code>：byte 解析、LSN 處理、輔助 function</li></ul><hr><h2 id="深入：實踐中的概念與操作建議"><a href="#深入：實踐中的概念與操作建議" class="headerlink" title="深入：實踐中的概念與操作建議"></a>深入：實踐中的概念與操作建議</h2><p>下面幾點是把工具從「觀察器」進化到「可實際應用」時會用到的概念與工程建議。</p><ol><li><p><strong>Replication Slot</strong></p><ul><li>Slot 決定了你從哪個 WAL LSN 開始讀，並讓 PostgreSQL 保留 WAL（直到被確認消費）。測試時注意不要無限建立未刪除的 slot（會造成 WAL 累積）。<code>replication_checker_rs</code> 可建立 slot，但目前在程式中 slot cleanup 還是簡單處理，所以測試環境中你要管理好。</li></ul></li><li><p><strong>Publication 與 Schema 一致性</strong></p><ul><li>Publication 決定哪些 table 的變更會被發送。上線前請確認 schema（尤其 REPLICA IDENTITY、nullable、type 改動）在 source 與 downstream 處理端的一致性，否則解析或重放會有問題。<code>replication_checker_rs</code> 會顯示 relation 資訊，能幫你驗證。</li></ul></li><li><p><strong>Streaming 交易</strong></p><ul><li>對大交易（very large transactions），Postgres 可能以 streaming 模式傳送。此專案已處理 streaming 交易，這讓它在面對大批量資料變更時不會輕易崩潰。(</li></ul></li><li><p><strong>Feedback（ack）機制</strong></p><ul><li>Logical replication protocol 支援回報已處理的 WAL 位置（可用於讓 primary safe remove WAL）。專案實作有 feedback 機制，但在 production 要確保回報策略（多久 ack、持久化 LSN 等）與你下游同步策略一致。</li><li></li></ul></li><li><p><strong>從「顯示」到「處理」：把事件送到下游</strong></p><ul><li>如果你要把變更送到 Kafka、Redis 或寫入另一個 DB，建議把 parser 與 message handling 拆成兩層：<strong>（1）可靠地接收並 ack WAL（LSN）</strong>、<strong>（2）異步或批次地把事件轉送到下游並重試</strong>。目前 <code>replication_checker_rs</code> 主要做第（1）與可視化，延伸第（2）需要你加上連線池、backpressure 與錯誤重試。</li></ul></li></ol><hr><h2 id="實務上常見問題（與排解）"><a href="#實務上常見問題（與排解）" class="headerlink" title="實務上常見問題（與排解）"></a>實務上常見問題（與排解）</h2><ul><li><code>libpq not found</code>：請安裝系統的 PostgreSQL 開發套件（如 <code>libpq-dev</code>、<code>postgresql-devel</code> 或 Homebrew 的 <code>postgresql</code>）。</li><li>權限錯誤：Replication user 需要 <code>REPLICATION</code> 權限，且 publication 應包含你想觀察的 tables。</li><li>Slot 已存在：若 slot 名稱衝突，請手動刪除舊 slot 或指定不同名稱再試。</li></ul><hr><h2 id="延伸建議（如果你想把它用到更真實的場景）"><a href="#延伸建議（如果你想把它用到更真實的場景）" class="headerlink" title="延伸建議（如果你想把它用到更真實的場景）"></a>延伸建議（如果你想把它用到更真實的場景）</h2><ol><li>將輸出轉成 <strong>structured JSON</strong> 並發到 Kafka 或 Event Hub（便於 downstream consumer 處理）。</li><li>增加 <strong>slot cleanup &amp; resume 機制</strong>：遇錯不要直接停，保存最後 ack 的 LSN，重啟時從該位置恢復。</li><li>支援 binary 類型與更完整的 type mapping（目前以 raw 顯示 binary）。</li><li>把 <code>replication_checker_rs</code> 包成一個可監控的 service，加上 metrics（Prometheus）與 health-check endpoint。<br>這些方向都屬於從 PoC → production 的典型演進路線。</li></ol><hr><h2 id="結語"><a href="#結語" class="headerlink" title="結語"></a>結語</h2><p><code>replication_checker_rs</code> 是一個很實用的學習與測試工具：它把 PostgreSQL logical replication protocol 的各個重要面向（slot、publication、BEGIN&#x2F;COMMIT、tuple parsing、streaming 交易、feedback）用 Rust + libpq 呈現出來，適合用來做教學、驗證複寫行為或作為你自製複寫處理器的起點。想進一步把它變成 production-ready，需要在錯誤復原、slot 管理、下游整合跟 binary type 處理上補強。(<a target="_blank" rel="noopener" href="https://github.com/isdaniel/replication_checker_rs" title="GitHub - isdaniel&#x2F;replication_checker_rs">GitHub</a>)</p><ul><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/replication_checker_rs" title="GitHub - isdaniel&#x2F;replication_checker_rs">replication_checker_rs</a></li><li>歡迎 Star、開 issue、PR 一起改進專案！</li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/logical-replication-checker-replication_checker_rs/">https://isdaniel.github.io/logical-replication-checker-replication_checker_rs&#x2F;</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/logical-replication-checker-replication_checker_rs/</id>
    <link href="https://isdaniel.github.io/logical-replication-checker-replication_checker_rs/"/>
    <published>2025-08-10T02:30:11.000Z</published>
    <summary>Rust 實作的 PostgreSQL logical replication 協議解析工具，可建立 replication slot 並即時顯示資料變更，適合學習與驗證複寫設定</summary>
    <title>Rust 實作的 PostgreSQL logical replication checker (replication_checker_rs)</title>
    <updated>2026-08-05T10:10:26.489Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="MySQL" scheme="https://isdaniel.github.io/categories/MySQL/"/>
    <category term="Database" scheme="https://isdaniel.github.io/categories/MySQL/Database/"/>
    <category term="Database" scheme="https://isdaniel.github.io/tags/Database/"/>
    <category term="Charset" scheme="https://isdaniel.github.io/tags/Charset/"/>
    <category term="MySQL" scheme="https://isdaniel.github.io/tags/MySQL/"/>
    <content>
      <![CDATA[<h1 id="為什麼修改-MySQL-的-character-set-server-後仍需重啟？從-mysql-connector-net-探討字元集的陷阱"><a href="#為什麼修改-MySQL-的-character-set-server-後仍需重啟？從-mysql-connector-net-探討字元集的陷阱" class="headerlink" title="為什麼修改 MySQL 的 character_set_server 後仍需重啟？從 mysql-connector-net 探討字元集的陷阱"></a>為什麼修改 MySQL 的 <code>character_set_server</code> 後仍需重啟？從 mysql-connector-net 探討字元集的陷阱</h1><p>在近期處理一個與 MySQL 字元集相關的問題時，我深入研究了 MySQL Server 的 Handshake 機制以及 <code>mysql-connector-net</code> 原始碼，發現了一個容易被忽略但可能會造成重大錯誤的細節——<strong>即使 <code>character_set_server</code> 是動態參數，但實際上修改後仍需要重啟 MySQL Server，否則會造成驅動端的解碼錯誤。</strong></p><h2 id="問題背景：為什麼驅動程式仍使用舊的字元集？"><a href="#問題背景：為什麼驅動程式仍使用舊的字元集？" class="headerlink" title="問題背景：為什麼驅動程式仍使用舊的字元集？"></a>問題背景：為什麼驅動程式仍使用舊的字元集？</h2><p>根據 MySQL Server 的設計，當 client 端連線時，Server 會在 Handshake Initial Packet 中回傳一些基本資訊，其中就包括伺服器的預設字元集（<code>character_set_server</code>）。這段資訊是透過以下的程式碼取得：</p><p>🔗 <a target="_blank" rel="noopener" href="https://github.com/mysql/mysql-server/blob/61a3a1d8ef15512396b4c2af46e922a19bf2b174/sql/auth/sql_authentication.cc#L1872">MySQL Source Code 參考連結</a></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">packet-&gt;<span class="built_in">append_int1</span>(default_charset_info-&gt;number);</span><br></pre></td></tr></table></figure><p>根據 MySQL 官方文件，這段資訊會被封裝在 Handshake v10 Protocol 中傳送（<a target="_blank" rel="noopener" href="https://dev.mysql.com/doc/dev/mysql-server/latest/page_protocol_connection_phase_packets_protocol_handshake_v10.html">官方文件</a>）。</p><p>然而，這個值其實是在 MySQL Server 啟動時就被載入的，<strong>即使你在執行中動態修改 <code>character_set_server</code>，重新連線後 greeting packet 裡的字元集值仍然不會更新。</strong></p><p><img data-src="/../images/2025-07-10_12h42_13.png" alt="img"></p><h2 id="驅動程式的行為：根據-Greeting-決定後續欄位解碼"><a href="#驅動程式的行為：根據-Greeting-決定後續欄位解碼" class="headerlink" title="驅動程式的行為：根據 Greeting 決定後續欄位解碼"></a>驅動程式的行為：根據 Greeting 決定後續欄位解碼</h2><p><code>mysql-connector-net</code> 在收到 greeting packet 時，會將字元集儲存在 <code>ConnectionCharSetIndex</code>：</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* New protocol with 16 bytes to describe server characteristics */</span></span><br><span class="line">owner.ConnectionCharSetIndex = (<span class="built_in">int</span>)packet.ReadByte(); <span class="comment">// e.g. 54 = UTF16</span></span><br></pre></td></tr></table></figure><p>🔗 <a target="_blank" rel="noopener" href="https://github.com/mysql/mysql-connector-net/blob/9.1.0/MySQL.Data/src/NativeDriver.cs#L241">連結程式碼位置</a></p><p>接下來，在處理每個欄位的資料時，若欄位的 charset 為 binary (63)，會 fallback 回 connection 的預設值：</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (CharacterSetIndex == <span class="number">63</span>)</span><br><span class="line">    CharacterSetIndex = driver.ConnectionCharSetIndex;</span><br></pre></td></tr></table></figure><p>🔗 <a target="_blank" rel="noopener" href="https://github.com/mysql/mysql-connector-net/blob/9.1.0/MySQL.Data/src/Field.cs#L250">欄位處理程式碼位置</a></p><p>這裡的問題就產生了：在 MySQL protocol 中，即使是整數、時間戳等 binary 類型欄位，也會設定 charset 為 binary (63)。這意味著實際 decode 資料時會 fallback 到 UTF-16（預設 greeting 的字元集），導致錯誤解碼，產生亂碼或例外。</p><h2 id="實驗觀察與錯誤範例"><a href="#實驗觀察與錯誤範例" class="headerlink" title="實驗觀察與錯誤範例"></a>實驗觀察與錯誤範例</h2><p>以下是我透過封包擷取與實際執行觀察到的問題：</p><h3 id="Greeting-Packet："><a href="#Greeting-Packet：" class="headerlink" title="Greeting Packet："></a>Greeting Packet：</h3><p>在連線時，我們觀察到 greeting charset 設為 54（UTF-16）：</p><figure class="highlight node-repl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Server Greeting</span><br><span class="line"><span class="meta prompt_">...</span></span><br><span class="line">    Language: utf16 COLLATE utf16_general_ci (54)</span><br></pre></td></tr></table></figure><p><img data-src="/../images/2025-07-10_12h42_49.png" alt="img"></p><h3 id="整數欄位錯誤解析："><a href="#整數欄位錯誤解析：" class="headerlink" title="整數欄位錯誤解析："></a>整數欄位錯誤解析：</h3><p>欄位的 <code>characterSet</code> 是 63 (binary)，fallback 到 54（UTF-16）解碼，造成如下的 <code>System.FormatException</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">System.FormatException: Input string was not in a correct format.</span><br><span class="line">   at System.Int32.Parse(String s, IFormatProvider provider)</span><br></pre></td></tr></table></figure><p><img data-src="/../images/2025-07-10_12h43_10.png" alt="img"></p><p>因為目前 mysql-connector-net client library 尚未完全支援 UTF16, 未來可能會處理此問題</p><h2 id="修正建議與討論"><a href="#修正建議與討論" class="headerlink" title="修正建議與討論"></a>修正建議與討論</h2><h3 id="為什麼需要重啟？"><a href="#為什麼需要重啟？" class="headerlink" title="為什麼需要重啟？"></a>為什麼需要重啟？</h3><p>雖然 <code>character_set_server</code> 是 dynamic parameter，但 greeting 中的字元集是在啟動時就定義的（由 <code>default_charset_info</code> 載入）。因此如果你修改這個參數 <strong>但未重啟 MySQL Server</strong>，那麼新的連線仍然會收到舊的 greeting charset 資訊。</p><p>在使用 <code>mysql-connector-net</code> 這種驅動時，由於 greeting charset 會被 fallback 作為 binary 欄位的 decode 基準，這會導致我們解析數值型資料時發生錯誤或亂碼。</p><h2 id="建議改善方向"><a href="#建議改善方向" class="headerlink" title="建議改善方向"></a>建議改善方向</h2><ol><li><p><strong>驅動程式修正建議</strong>：</p><ul><li>應該判斷 field type 是否為 binary 前，再決定是否 fallback 到 greeting charset。</li><li>例如：對於 Int32、Int64、Timestamp 等欄位，可跳過 charset fallback。</li></ul></li><li><p><strong>伺服器端操作建議</strong>：</p><ul><li>若需更改 <code>character_set_server</code>，務必搭配重新啟動 MySQL server，確保 greeting packet 同步更新。</li><li>如果您是使用 mysql-connector-net library 請使用 utf8mb4 取代 UTF16</li></ul></li></ol><h2 id="總結"><a href="#總結" class="headerlink" title="總結"></a>總結</h2><p>這次調查突顯了 MySQL greeting packet 與驅動解碼邏輯間的一個不易察覺的錯誤來源。特別是在使用 <code>mysql-connector-net</code> 驅動、並處理 binary 或整數類型資料時，若伺服器端未重新啟動而 greeting charset 未更新，就可能引發解碼錯誤與系統例外。</p><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/mysql-connector-net-charset-issue/">https://isdaniel.github.io/mysql-connector-net-charset-issue/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/mysql-connector-net-charset-issue/</id>
    <link href="https://isdaniel.github.io/mysql-connector-net-charset-issue/"/>
    <published>2025-07-09T15:10:43.000Z</published>
    <summary>深入分析 MySQL character_set_server 修改後為何需要重啟：從 mysql-connector-net 原始碼與 MySQL Handshake 機制解析字元集設定的陷阱，避免驅動端解碼錯誤</summary>
    <title>為什麼修改 MySQL 的 character_set_server 後仍需重啟？從 mysql-connector-net 探討字元集的陷阱</title>
    <updated>2026-08-05T10:10:26.490Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/categories/Rust/pgrx/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/pgrx/PostgreSQL/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/tags/pgrx/"/>
    <content>
      <![CDATA[<p>Here’s a well-structured draft for your technical blog post based on the provided Rust + pgrx Foreign Data Wrapper (FDW) code.</p><h1 id="🚀-Building-a-Simple-PostgreSQL-FDW-with-Rust-and-pgrx"><a href="#🚀-Building-a-Simple-PostgreSQL-FDW-with-Rust-and-pgrx" class="headerlink" title="🚀 Building a Simple PostgreSQL FDW with Rust and pgrx"></a>🚀 Building a Simple PostgreSQL FDW with Rust and pgrx</h1><p>PostgreSQL Foreign Data Wrappers (FDW) enable PostgreSQL to query external data sources as if they were regular tables. Traditionally, FDWs are written in C, but with <a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx"><code>pgrx</code></a>, we can now build PostgreSQL extensions — including FDWs — in <strong>Rust</strong>, unlocking safety and modern tooling.</p><p>In this post, we’ll walk through creating a simple FDW using Rust and <code>pgrx</code> that simulates reading rows from an external source (e.g., Redis or API). While it’s a stub, it demonstrates how to implement the core FDW lifecycle.</p><h2 id="🛠️-What-Can-You-Build-with-pgrx"><a href="#🛠️-What-Can-You-Build-with-pgrx" class="headerlink" title="🛠️ What Can You Build with pgrx?"></a>🛠️ What Can You Build with <code>pgrx</code>?</h2><ul><li>✅ <strong>SQL Functions:</strong> Scalar, aggregate, and set-returning functions.</li><li>✅ <strong>Custom Types:</strong> Define composite types or enums in Rust.</li><li>✅ <strong>Foreign Data Wrappers (FDWs):</strong> Like the one in your example — connect PostgreSQL to external systems (Redis, APIs, file systems, etc.).</li><li>✅ <strong>Index Access Methods:</strong> Implement new index types.</li><li>✅ <strong>Background Workers:</strong> Run tasks in the background inside PostgreSQL.</li><li>✅ <strong>Hooks:</strong> Intercept or modify PostgreSQL internal behavior (like planner or executor hooks).</li></ul><h3 id="🌐-Under-the-hood"><a href="#🌐-Under-the-hood" class="headerlink" title="🌐 Under the hood:"></a>🌐 Under the hood:</h3><ul><li>PostgreSQL communicates via C APIs.</li><li><code>pgrx</code> provides Rust-safe bindings to these APIs.</li><li>Memory management is handled carefully via <code>PgMemoryContexts</code>, matching PostgreSQL’s memory context model.</li><li>Rust functions are exposed to PostgreSQL as SQL-callable functions with the <code>#[pg_extern]</code> macro.</li></ul><h2 id="🏗️-Key-Components-of-an-default-fdw"><a href="#🏗️-Key-Components-of-an-default-fdw" class="headerlink" title="🏗️ Key Components of an default_fdw"></a>🏗️ Key Components of an default_fdw</h2><p>PostgreSQL FDWs consist of several callback functions that handle different phases of query planning and execution:</p><p>Example extension <a target="_blank" rel="noopener" href="https://github.com/isdaniel/rust_pg_extensions/blob/main/src/default_fdw.rs">default_fdw</a></p><ul><li><p><strong>Planning Phase:</strong></p><ul><li><code>GetForeignRelSize</code>: Estimate rows.</li><li><code>GetForeignPaths</code>: Generate access paths.</li><li><code>GetForeignPlan</code>: Create the scan plan.</li></ul></li><li><p><strong>Execution Phase:</strong></p><ul><li><code>BeginForeignScan</code>: Initialize the scan.</li><li><code>IterateForeignScan</code>: Produce each row.</li><li><code>ReScanForeignScan</code>: Restart the scan if needed.</li><li><code>EndForeignScan</code>: Cleanup.</li></ul></li></ul><h3 id="How-to-use"><a href="#How-to-use" class="headerlink" title="How to use"></a>How to use</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">foreign</span> data wrapper default_wrapper</span><br><span class="line">  handler default_fdw_handler;</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> server my_default_server</span><br><span class="line">  <span class="keyword">foreign</span> data wrapper default_wrapper</span><br><span class="line">  options (</span><br><span class="line">    foo <span class="string">&#x27;bar&#x27;</span></span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">foreign</span> <span class="keyword">table</span> hello (</span><br><span class="line">  id <span class="type">bigint</span>,</span><br><span class="line">  col text</span><br><span class="line">)</span><br><span class="line">server my_default_server options (</span><br><span class="line">foo <span class="string">&#x27;bar&#x27;</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>Then we can select hello table.</p><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> hello;</span><br></pre></td></tr></table></figure><h2 id="🔧-Setting-Up-the-FDW-Handler"><a href="#🔧-Setting-Up-the-FDW-Handler" class="headerlink" title="🔧 Setting Up the FDW Handler"></a>🔧 Setting Up the FDW Handler</h2><p>The entry point is the FDW handler function, which PostgreSQL calls to retrieve a set of function pointers.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_extern(create_or_replace)]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">default_fdw_handler</span>() <span class="punctuation">-&gt;</span> PgBox&lt;pg_sys::FdwRoutine&gt; &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; default_fdw_handler&quot;</span>);</span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">fdw_routine</span> = PgBox::&lt;pg_sys::FdwRoutine, AllocatedByRust&gt;::<span class="title function_ invoke__">alloc_node</span>(pg_sys::NodeTag::T_FdwRoutine);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Planning callbacks</span></span><br><span class="line">        fdw_routine.GetForeignRelSize = <span class="title function_ invoke__">Some</span>(get_foreign_rel_size);</span><br><span class="line">        fdw_routine.GetForeignPaths = <span class="title function_ invoke__">Some</span>(get_foreign_paths);</span><br><span class="line">        fdw_routine.GetForeignPlan = <span class="title function_ invoke__">Some</span>(get_foreign_plan);</span><br><span class="line">        fdw_routine.ExplainForeignScan = <span class="title function_ invoke__">Some</span>(explain_foreign_scan);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Execution callbacks</span></span><br><span class="line">        fdw_routine.BeginForeignScan = <span class="title function_ invoke__">Some</span>(begin_foreign_scan);</span><br><span class="line">        fdw_routine.IterateForeignScan = <span class="title function_ invoke__">Some</span>(iterate_foreign_scan);</span><br><span class="line">        fdw_routine.ReScanForeignScan = <span class="title function_ invoke__">Some</span>(re_scan_foreign_scan);</span><br><span class="line">        fdw_routine.EndForeignScan = <span class="title function_ invoke__">Some</span>(end_foreign_scan);</span><br><span class="line"></span><br><span class="line">        fdw_routine.<span class="title function_ invoke__">into_pg_boxed</span>()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="📦-Extracting-Foreign-Table-Options"><a href="#📦-Extracting-Foreign-Table-Options" class="headerlink" title="📦 Extracting Foreign Table Options"></a>📦 Extracting Foreign Table Options</h2><p>PostgreSQL allows specifying options like hostnames or credentials when creating a foreign table. This function retrieves those options:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">get_foreign_table_options</span>(relid: pg_sys::Oid) <span class="punctuation">-&gt;</span> HashMap&lt;<span class="type">String</span>, <span class="type">String</span>&gt; &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>This is crucial when your FDW needs to connect to external systems like Redis, REST APIs, or filesystems.</p><h2 id="📊-Planner-Callbacks"><a href="#📊-Planner-Callbacks" class="headerlink" title="📊 Planner Callbacks"></a>📊 Planner Callbacks</h2><h3 id="1️⃣-GetForeignRelSize"><a href="#1️⃣-GetForeignRelSize" class="headerlink" title="1️⃣ GetForeignRelSize"></a>1️⃣ <strong>GetForeignRelSize</strong></h3><p>Estimates the number of rows in the foreign table.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_guard]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">get_foreign_rel_size</span>(..., baserel: *<span class="keyword">mut</span> pg_sys::RelOptInfo, ...) &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; get_foreign_rel_size&quot;</span>);</span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        (*baserel).rows = <span class="number">1000.0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2️⃣-GetForeignPaths"><a href="#2️⃣-GetForeignPaths" class="headerlink" title="2️⃣ GetForeignPaths"></a>2️⃣ <strong>GetForeignPaths</strong></h3><p>Defines possible access paths for the planner.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_guard]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">get_foreign_paths</span>(..., baserel: *<span class="keyword">mut</span> pg_sys::RelOptInfo, ...) &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; get_foreign_paths&quot;</span>);</span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">path</span> = pg_sys::<span class="title function_ invoke__">create_foreignscan_path</span>(</span><br><span class="line">            ...,</span><br><span class="line">            (*baserel).rows,</span><br><span class="line">            <span class="number">10.0</span>,   <span class="comment">// startup cost</span></span><br><span class="line">            <span class="number">100.0</span>,  <span class="comment">// total cost</span></span><br><span class="line">            ...</span><br><span class="line">        );</span><br><span class="line">        pg_sys::<span class="title function_ invoke__">add_path</span>(baserel, path <span class="keyword">as</span> *<span class="keyword">mut</span> pg_sys::Path);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3️⃣-GetForeignPlan"><a href="#3️⃣-GetForeignPlan" class="headerlink" title="3️⃣ GetForeignPlan"></a>3️⃣ <strong>GetForeignPlan</strong></h3><p>Generates the actual execution plan.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_guard]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">get_foreign_plan</span>(...) <span class="punctuation">-&gt;</span> *<span class="keyword">mut</span> pg_sys::ForeignScan &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; get_foreign_plan&quot;</span>);</span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        pg_sys::<span class="title function_ invoke__">make_foreignscan</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="▶️-Execution-Callbacks"><a href="#▶️-Execution-Callbacks" class="headerlink" title="▶️ Execution Callbacks"></a>▶️ Execution Callbacks</h2><h3 id="🏁-BeginForeignScan"><a href="#🏁-BeginForeignScan" class="headerlink" title="🏁 BeginForeignScan"></a>🏁 <strong>BeginForeignScan</strong></h3><p>Initializes the scan.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_guard]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">begin_foreign_scan</span>(node: *<span class="keyword">mut</span> pg_sys::ForeignScanState, ...) &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; begin_foreign_scan&quot;</span>);</span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">relid</span> = (*(*node).ss.ss_currentRelation).rd_id;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">options</span> = <span class="title function_ invoke__">get_foreign_table_options</span>(relid);</span><br><span class="line">        log!(<span class="string">&quot;Foreign table options: &#123;:?&#125;&quot;</span>, options);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">let</span> <span class="variable">state</span> = PgMemoryContexts::CurrentMemoryContext</span><br><span class="line">            .<span class="title function_ invoke__">leak_and_drop_on_delete</span>(RedisFdwState &#123; row: <span class="number">0</span> &#125;);</span><br><span class="line"></span><br><span class="line">        (*node).fdw_state = state <span class="keyword">as</span> *<span class="keyword">mut</span> std::ffi::c_void;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="🔁-IterateForeignScan"><a href="#🔁-IterateForeignScan" class="headerlink" title="🔁 IterateForeignScan"></a>🔁 <strong>IterateForeignScan</strong></h3><p>Produces rows one at a time.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_guard]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">iterate_foreign_scan</span>(node: *<span class="keyword">mut</span> pg_sys::ForeignScanState) <span class="punctuation">-&gt;</span> *<span class="keyword">mut</span> pg_sys::TupleTableSlot &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; iterate_foreign_scan&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">state</span> = &amp;<span class="keyword">mut</span> *((*node).fdw_state <span class="keyword">as</span> *<span class="keyword">mut</span> RedisFdwState);</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">slot</span> = (*node).ss.ss_ScanTupleSlot;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">tupdesc</span> = (*slot).tts_tupleDescriptor;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">natts</span> = (*tupdesc).natts <span class="keyword">as</span> <span class="type">usize</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> state.row &gt;= <span class="number">5</span> &#123;</span><br><span class="line">            <span class="title function_ invoke__">exec_clear_tuple</span>(slot);</span><br><span class="line">            <span class="keyword">return</span> slot;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="title function_ invoke__">exec_clear_tuple</span>(slot);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">let</span> <span class="variable">values_ptr</span> = PgMemoryContexts::<span class="title function_ invoke__">For</span>((*slot).tts_mcxt)</span><br><span class="line">            .<span class="title function_ invoke__">palloc</span>(std::mem::size_of::&lt;pg_sys::Datum&gt;() * natts) <span class="keyword">as</span> *<span class="keyword">mut</span> pg_sys::Datum;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">let</span> <span class="variable">nulls_ptr</span> = PgMemoryContexts::<span class="title function_ invoke__">For</span>((*slot).tts_mcxt)</span><br><span class="line">            .<span class="title function_ invoke__">palloc</span>(std::mem::size_of::&lt;<span class="type">bool</span>&gt;() * natts) <span class="keyword">as</span> *<span class="keyword">mut</span> <span class="type">bool</span>;</span><br><span class="line"></span><br><span class="line">        *values_ptr.<span class="title function_ invoke__">add</span>(<span class="number">0</span>) = (state.row + <span class="number">1</span>).<span class="title function_ invoke__">into</span>();</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">name</span> = <span class="built_in">format!</span>(<span class="string">&quot;hello_&#123;&#125;&quot;</span>, state.row + <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">cstring</span> = CString::<span class="title function_ invoke__">new</span>(name).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">        *values_ptr.<span class="title function_ invoke__">add</span>(<span class="number">1</span>) = Datum::<span class="title function_ invoke__">from</span>(pg_sys::<span class="title function_ invoke__">cstring_to_text</span>(cstring.<span class="title function_ invoke__">as_ptr</span>()));</span><br><span class="line"></span><br><span class="line">        *nulls_ptr.<span class="title function_ invoke__">add</span>(<span class="number">0</span>) = <span class="literal">false</span>;</span><br><span class="line">        *nulls_ptr.<span class="title function_ invoke__">add</span>(<span class="number">1</span>) = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">        (*slot).tts_values = values_ptr;</span><br><span class="line">        (*slot).tts_isnull = nulls_ptr;</span><br><span class="line"></span><br><span class="line">        pg_sys::<span class="title function_ invoke__">ExecStoreVirtualTuple</span>(slot);</span><br><span class="line"></span><br><span class="line">        state.row += <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">        slot</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>This example emits five rows with <code>(id, name)</code> pairs like <code>(1, hello_1)</code>.</p><h3 id="🔄-ReScanForeignScan"><a href="#🔄-ReScanForeignScan" class="headerlink" title="🔄 ReScanForeignScan"></a>🔄 <strong>ReScanForeignScan</strong></h3><p>Handles rescan requests.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_guard]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">re_scan_foreign_scan</span>(_node: *<span class="keyword">mut</span> pg_sys::ForeignScanState) &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; re_scan_foreign_scan&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="🛑-EndForeignScan"><a href="#🛑-EndForeignScan" class="headerlink" title="🛑 EndForeignScan"></a>🛑 <strong>EndForeignScan</strong></h3><p>Frees resources.</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[pg_guard]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">end_foreign_scan</span>(node: *<span class="keyword">mut</span> pg_sys::ForeignScanState) &#123;</span><br><span class="line">    log!(<span class="string">&quot;&gt; end_foreign_scan&quot;</span>);</span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> !(*node).fdw_state.<span class="title function_ invoke__">is_null</span>() &#123;</span><br><span class="line">            (*node).fdw_state = std::ptr::<span class="title function_ invoke__">null_mut</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="🗒️-Utilities"><a href="#🗒️-Utilities" class="headerlink" title="🗒️ Utilities"></a>🗒️ Utilities</h2><p>Tuple clearing is handled by this helper:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">unsafe</span> <span class="keyword">fn</span> <span class="title function_">exec_clear_tuple</span>(slot: *<span class="keyword">mut</span> pg_sys::TupleTableSlot) &#123;</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(clear) = (*(*slot).tts_ops).clear &#123;</span><br><span class="line">        <span class="title function_ invoke__">clear</span>(slot);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="🏁-Conclusion"><a href="#🏁-Conclusion" class="headerlink" title="🏁 Conclusion"></a>🏁 Conclusion</h2><p>This post walked you through the basics of building a PostgreSQL FDW using Rust and <code>pgrx</code>. While this example generates dummy data, the same structure can be extended to connect with real-world systems like Redis, REST APIs, or message queues.</p><h2 id="🚀-Next-Steps"><a href="#🚀-Next-Steps" class="headerlink" title="🚀 Next Steps"></a>🚀 Next Steps</h2><ul><li>Add connection logic to Redis or any backend.</li><li>Support <code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code> by implementing the modification callbacks.</li><li>Package and distribute as a PostgreSQL extension.</li></ul><h2 id="📚-References"><a href="#📚-References" class="headerlink" title="📚 References"></a>📚 References</h2><ul><li><a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx GitHub</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/fdw-callbacks.html">PostgreSQL FDW API Documentation</a></li></ul><p>If you’d like, I can help refine this post further, format it for Medium&#x2F;Dev.to, or extend it with Redis connection examples. Would you like that?</p><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/rust-pgrx-extension-fdw/">https://isdaniel.github.io/rust-pgrx-extension-fdw/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/rust-pgrx-extension-fdw/</id>
    <link href="https://isdaniel.github.io/rust-pgrx-extension-fdw/"/>
    <published>2025-06-30T14:30:11.000Z</published>
    <summary>Learn how to build a PostgreSQL Foreign Data Wrapper (FDW) in Rust using pgrx — query external data sources as regular PostgreSQL tables</summary>
    <title>Building a PostgreSQL Foreign Data Wrapper (FDW) in Rust with pgrx</title>
    <updated>2026-08-05T10:10:26.493Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Python" scheme="https://isdaniel.github.io/categories/Python/"/>
    <category term="Python" scheme="https://isdaniel.github.io/tags/Python/"/>
    <category term="AI" scheme="https://isdaniel.github.io/tags/AI/"/>
    <category term="MCP" scheme="https://isdaniel.github.io/tags/MCP/"/>
    <content>
      <![CDATA[<h2 id="什麼是-MCP"><a href="#什麼是-MCP" class="headerlink" title="什麼是 MCP?"></a>什麼是 MCP?</h2><p>MCP(Model Context Protocol)是一種協定，用於在工具之間進行通訊與協作。透過 MCP，可以讓各種獨立的工具(如模型、插件、服務)以一致的格式互相交換資料與指令。MCP Server 是提供特定功能的伺服器端程式，能與支援 MCP 的前端進行互動。</p><h2 id="Weather-MCP-Server-是什麼？"><a href="#Weather-MCP-Server-是什麼？" class="headerlink" title="Weather MCP Server 是什麼？"></a>Weather MCP Server 是什麼？</h2><p>Weather MCP Server 是一個基於 MCP 協定開發的天氣資訊伺服器，利用 <a target="_blank" rel="noopener" href="https://open-meteo.com/">Open-Meteo API</a> 提供免費的天氣資料。透過這個伺服器，你可以查詢：</p><ul><li>某城市的即時天氣資訊</li><li>某城市在特定時間範圍內的天氣預報</li><li>指定時區的目前時間</li></ul><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/mcp_weather_server">mcp_weather_server source code</a><br><a target="_blank" rel="noopener" href="https://smithery.ai/server/@isdaniel/mcp_weather_server">smithery AI</a></p><p>使用此 MCP Server 搭配 AI Model 可以輕易搭建出即時天氣小助手, 如下我的 AI Bot</p><p><img data-src="/../images/20096630aZiRf1dO1g.png"><br><img data-src="/../images/20096630ecDVbg8JZL.png"></p><h2 id="功能特色"><a href="#功能特色" class="headerlink" title="功能特色"></a>功能特色</h2><ul><li>查詢指定城市的即時天氣</li><li>查詢指定日期區間的天氣預測</li><li>查詢目前時間(支援指定時區)</li></ul><h2 id="安裝方式"><a href="#安裝方式" class="headerlink" title="安裝方式"></a>安裝方式</h2><p>使用 pip 安裝：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install mcp_weather_server</span><br></pre></td></tr></table></figure><p>接著，需要在 MCP 設定檔中手動加入 Weather Server 的啟動設定。</p><h3 id="設定-cline-mcp-settings-json"><a href="#設定-cline-mcp-settings-json" class="headerlink" title="設定 cline_mcp_settings.json"></a>設定 <code>cline_mcp_settings.json</code></h3><p>請將以下內容新增到 <code>cline_mcp_settings.json</code> 檔案中的 <code>mcpServers</code> 區塊：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;mcpServers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;weather&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;python&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="string">&quot;-m&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="string">&quot;mcp_weather_server&quot;</span></span><br><span class="line">      <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;disabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;autoApprove&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>儲存後，即可在 MCP 架構中啟動並使用 Weather Server。</p><h2 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h2><p>Weather MCP Server 提供以下三個工具：</p><h3 id="1-get-weather：查詢指定城市目前天氣"><a href="#1-get-weather：查詢指定城市目前天氣" class="headerlink" title="1. get_weather：查詢指定城市目前天氣"></a>1. <code>get_weather</code>：查詢指定城市目前天氣</h3><p><strong>參數說明：</strong></p><ul><li><code>city</code>(字串，必填)：城市名稱，例如 “Taipei”</li></ul><p><strong>範例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">use_mcp_tool</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">server_name</span>&gt;</span>weather<span class="tag">&lt;/<span class="name">server_name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tool_name</span>&gt;</span>get_weather<span class="tag">&lt;/<span class="name">tool_name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">arguments</span>&gt;</span></span><br><span class="line">&#123;</span><br><span class="line">  &quot;city&quot;: &quot;Taipei&quot;</span><br><span class="line">&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">arguments</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">use_mcp_tool</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="2-get-weather-by-datetime-range：查詢日期區間的天氣預報"><a href="#2-get-weather-by-datetime-range：查詢日期區間的天氣預報" class="headerlink" title="2. get_weather_by_datetime_range：查詢日期區間的天氣預報"></a>2. <code>get_weather_by_datetime_range</code>：查詢日期區間的天氣預報</h3><p><strong>參數說明：</strong></p><ul><li><code>city</code>(字串，必填)：城市名稱</li><li><code>start_date</code>(字串，必填)：開始日期，格式為 YYYY-MM-DD</li><li><code>end_date</code>(字串，必填)：結束日期，格式為 YYYY-MM-DD</li></ul><p><strong>範例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">use_mcp_tool</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">server_name</span>&gt;</span>weather<span class="tag">&lt;/<span class="name">server_name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tool_name</span>&gt;</span>get_weather_by_datetime_range<span class="tag">&lt;/<span class="name">tool_name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">arguments</span>&gt;</span></span><br><span class="line">&#123;</span><br><span class="line">  &quot;city&quot;: &quot;London&quot;,</span><br><span class="line">  &quot;start_date&quot;: &quot;2024-01-01&quot;,</span><br><span class="line">  &quot;end_date&quot;: &quot;2024-01-07&quot;</span><br><span class="line">&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">arguments</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">use_mcp_tool</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="3-get-current-datetime：查詢指定時區目前時間"><a href="#3-get-current-datetime：查詢指定時區目前時間" class="headerlink" title="3. get_current_datetime：查詢指定時區目前時間"></a>3. <code>get_current_datetime</code>：查詢指定時區目前時間</h3><p><strong>參數說明：</strong></p><ul><li><code>timezone_name</code>(字串，必填)：IANA 時區名稱，例如 “America&#x2F;New_York”、”Europe&#x2F;London”。若未指定，預設為 UTC。</li></ul><p><strong>範例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">use_mcp_tool</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">server_name</span>&gt;</span>weather<span class="tag">&lt;/<span class="name">server_name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tool_name</span>&gt;</span>get_current_datetime<span class="tag">&lt;/<span class="name">tool_name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">arguments</span>&gt;</span></span><br><span class="line">&#123;</span><br><span class="line">  &quot;timezone_name&quot;: &quot;America/New_York&quot;</span><br><span class="line">&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">arguments</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">use_mcp_tool</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="開發者注意事項"><a href="#開發者注意事項" class="headerlink" title="開發者注意事項"></a>開發者注意事項</h2><p>如需在開發或除錯時手動執行 Weather MCP Server，可以直接執行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python -m mcp_weather_server</span><br></pre></td></tr></table></figure><h2 id="結語"><a href="#結語" class="headerlink" title="結語"></a>結語</h2><p>Weather MCP Server 是一個輕量、無需 API 金鑰的天氣資訊服務，適合用於教育、研究或原型開發。透過 MCP 的整合能力，可以輕鬆地將天氣查詢功能加入到各種自動化或智慧應用中。</p><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/mcp-server-weather/">https://isdaniel.github.io/mcp-server-weather/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/mcp-server-weather/</id>
    <link href="https://isdaniel.github.io/mcp-server-weather/"/>
    <published>2025-06-22T12:42:00.000Z</published>
    <summary>以 Weather MCP Server 為例，介紹 Model Context Protocol (MCP) 協定如何讓 AI 模型、插件與服務以統一格式交換資料與指令，從零建立一個 MCP Server</summary>
    <title>使用 Model Context Protocol 以 Weather MCP Server 為例</title>
    <updated>2026-08-05T10:10:26.489Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="C#" scheme="https://isdaniel.github.io/categories/C/"/>
    <category term="OOP" scheme="https://isdaniel.github.io/categories/C/OOP/"/>
    <category term="framework" scheme="https://isdaniel.github.io/categories/C/OOP/framework/"/>
    <category term="C#" scheme="https://isdaniel.github.io/tags/C/"/>
    <category term="OOP" scheme="https://isdaniel.github.io/tags/OOP/"/>
    <category term="Process" scheme="https://isdaniel.github.io/tags/Process/"/>
    <category term="workerpool" scheme="https://isdaniel.github.io/tags/workerpool/"/>
    <category term="message-queue" scheme="https://isdaniel.github.io/tags/message-queue/"/>
    <category term="open-source" scheme="https://isdaniel.github.io/tags/open-source/"/>
    <content>
      <![CDATA[<h2 id="簡介"><a href="#簡介" class="headerlink" title="簡介"></a>簡介</h2><p>最近我開發了 <code>MessageWorkerPool</code> 專案。其主要概念是提供一個平台框架，使使用者能夠快速且輕鬆地在 <code>Worker</code> 內實作邏輯。該設計高度靈活，允許基於我創建的 Worker 通訊協議，以多種程式語言實作 <code>Worker</code>。目前，我已提供使用 C#、Rust 和 Python 編寫的 Worker 範例。</p><p>這個函式庫在多進程環境中處理任務表現優異。此外，它還支援優雅關閉 (graceful shutdown)，確保在隨時 consumer worker 能順利終止處理程序。</p><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/MessageWorkerPool">MessageWorkerPool GitHub</a></p><h2 id="為什麼選擇-ProcessPool-而非-ThreadPool"><a href="#為什麼選擇-ProcessPool-而非-ThreadPool" class="headerlink" title="為什麼選擇 ProcessPool 而非 ThreadPool?"></a>為什麼選擇 ProcessPool 而非 ThreadPool?</h2><p>當你需要強大的隔離性，以防止某個任務影響其他任務時，應該選擇 ProcessPool，特別是針對關鍵操作或容易崩潰的任務。雖然 ThreadPool 較為輕量（因為執行緒共用記憶體並且具有較低的上下文切換開銷），但 ProcessPool 能夠提供更靈活的解決方案，允許使用不同的程式語言來實作 Worker。</p><h2 id="安裝"><a href="#安裝" class="headerlink" title="安裝"></a>安裝</h2><p>要安裝 <code>MessageWorkerPool</code> 套件，請使用以下 NuGet 指令：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">PM &gt; Install-Package MessageWorkerPool</span><br></pre></td></tr></table></figure><p>若要手動安裝此函式庫，可克隆儲存庫並建置專案：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/isdaniel/MessageWorkerPool.git</span><br><span class="line"><span class="built_in">cd</span> MessageWorkerPool</span><br><span class="line">dotnet build</span><br></pre></td></tr></table></figure><h2 id="架構概覽"><a href="#架構概覽" class="headerlink" title="架構概覽"></a>架構概覽</h2><p><img data-src="https://raw.githubusercontent.com/isdaniel/MessageWorkerPool/refs/heads/main/images/arhc-overview.png"></p><h2 id="快速開始"><a href="#快速開始" class="headerlink" title="快速開始"></a>快速開始</h2><p>這是部署 RabbitMQ 和相關服務的快速開始指南，使用提供的 docker-compose.yml 檔案和 .env 中的環境變數。</p><figure class="highlight mel"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker-compose --<span class="keyword">env</span>-<span class="keyword">file</span> .\<span class="keyword">env</span>\.<span class="keyword">env</span> up --build -d</span><br></pre></td></tr></table></figure><ol><li>檢查 RabbitMQ 健康狀態：在瀏覽器中開啟 <a target="_blank" rel="noopener" href="http://localhost:8888/">http://localhost:8888</a> 以訪問 RabbitMQ 管理面板。</li></ol><ul><li>使用者名稱: guest</li><li>密碼: guest</li></ul><ol start="2"><li>檢查 OrleansDashboard <a target="_blank" rel="noopener" href="http://localhost:8899/">http://localhost:8899</a></li></ol><ul><li>使用者名稱: admin</li><li>密碼: test.123</li></ul><h2 id="程式結構"><a href="#程式結構" class="headerlink" title="程式結構"></a>程式結構</h2><p>以下是創建並配置與 RabbitMQ 互動的 workerpool 的範例程式碼。以下是其功能的解析：workerpool 將根據您的 RabbitMqSetting 設定從 RabbitMQ 伺服器獲取訊息，並通過 Process.StandardInput 將訊息傳遞給用戶創建的真實 worker node</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">async</span> Task <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        CreateHostBuilder(<span class="keyword">args</span>).Build().Run();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> IHostBuilder <span class="title">CreateHostBuilder</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span> =&gt;</span><br><span class="line">        Host.CreateDefaultBuilder(<span class="keyword">args</span>)</span><br><span class="line">            .ConfigureLogging(logging =&gt;</span><br><span class="line">            &#123;</span><br><span class="line">                logging.ClearProviders();</span><br><span class="line">                logging.AddConsole(options =&gt; &#123;</span><br><span class="line">                    options.FormatterName = ConsoleFormatterNames.Simple;</span><br><span class="line">                &#125;);</span><br><span class="line">                logging.Services.Configure&lt;SimpleConsoleFormatterOptions&gt;(options =&gt; &#123;</span><br><span class="line">                    options.IncludeScopes = <span class="literal">true</span>;</span><br><span class="line">                    options.TimestampFormat = <span class="string">&quot; yyyy-MM-dd HH:mm:ss &quot;</span>;</span><br><span class="line">                &#125;);</span><br><span class="line">            &#125;).AddRabbitMqWorkerPool(<span class="keyword">new</span> RabbitMqSetting</span><br><span class="line">            &#123;</span><br><span class="line">                UserName = Environment.GetEnvironmentVariable(<span class="string">&quot;USERNAME&quot;</span>) ?? <span class="string">&quot;guest&quot;</span>,</span><br><span class="line">                Password = Environment.GetEnvironmentVariable(<span class="string">&quot;PASSWORD&quot;</span>) ?? <span class="string">&quot;guest&quot;</span>,</span><br><span class="line">                HostName = Environment.GetEnvironmentVariable(<span class="string">&quot;RABBITMQ_HOSTNAME&quot;</span>),</span><br><span class="line">                Port = <span class="built_in">ushort</span>.TryParse(Environment.GetEnvironmentVariable(<span class="string">&quot;RABBITMQ_PORT&quot;</span>), <span class="keyword">out</span> <span class="built_in">ushort</span> p) ? p : (<span class="built_in">ushort</span>) <span class="number">5672</span>,</span><br><span class="line">                PrefetchTaskCount = <span class="number">3</span></span><br><span class="line">            &#125;, <span class="keyword">new</span> WorkerPoolSetting() &#123; WorkerUnitCount = <span class="number">9</span>, CommandLine = <span class="string">&quot;dotnet&quot;</span>, Arguments = <span class="string">@&quot;./ProcessBin/WorkerProcessSample.dll&quot;</span>, QueueName = Environment.GetEnvironmentVariable(<span class="string">&quot;QUEUENAME&quot;</span>), &#125;</span><br><span class="line">            );</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="worker-process-與-workerPool-之間的協議"><a href="#worker-process-與-workerPool-之間的協議" class="headerlink" title="worker process 與 workerPool 之間的協議"></a>worker process 與 workerPool 之間的協議</h2><p>worker node 與任務進程之間的協議使用 MessagePack 二進制格式來進行更快且更小的資料傳輸，標準輸入將發送信號來控制 worker process。</p><p>一開始 workerPool 將通過標準輸入傳遞 NamedPipe 名稱，因此 worker node 需要接收該名稱並建立 worker process 和 workerPool 之間的 NamedPipe。</p><h3 id="workerPool-發送的操作指令"><a href="#workerPool-發送的操作指令" class="headerlink" title="workerPool 發送的操作指令"></a>workerPool 發送的操作指令</h3><p>目前，workerPool將通過標準輸入向 worker process 發送操作信號或指令。</p><ul><li>CLOSED_SIGNAL (<code>__quit__</code>): 代表 workerPool 發送關閉或關機信號給 worker node，worker process 應盡快執行優雅關機。<br>通過 (Data Named Pipe Stream) 進行資料傳輸<br>命名管道是一種強大的進程間通信 (IPC) 機制，它允許兩個或更多的進程之間進行通信，即使它們運行在不同的機器上（例如 Windows 等支持的平台）。我們的 worker 使用此方式在 worker node 與 workerPool 之間傳輸資料。</li></ul><p>msgpack 協議支持的資料類型如下類別與 byte[] 格式。</p><p>對應的 byte[] 資料是：</p><figure class="highlight dns"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">[<span class="number">132,161,48,179</span>,<span class="number">78,101,119,32</span>,<span class="number">79,117,116,80</span>,<span class="number">117,116,32,77</span>,<span class="number">101,115,115,97</span>,<span class="number">103,101,33,161</span>,<span class="number">49,204,200,161</span>,<span class="number">50,129,164,116</span>,<span class="number">101,115,116,167</span>,<span class="number">116,101,115,116</span>,<span class="number">118,97,108,161</span>,<span class="number">51,169,116,101</span>,<span class="number">115,116,81,117</span>,<span class="number">101,117,101</span>]</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>要將提供的偽 JSON 結構表示為 <code>MsgPack</code> 格式（byte[]），我們可以分解過程如下：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Edit</span><br><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;0&quot;</span><span class="punctuation">:</span> <span class="string">&quot;New OutPut Message!&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;1&quot;</span><span class="punctuation">:</span> <span class="number">200</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;2&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;test&quot;</span><span class="punctuation">:</span> <span class="string">&quot;testval&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;3&quot;</span><span class="punctuation">:</span> <span class="string">&quot;testQueue&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>更多資訊，您可以使用 <a target="_blank" rel="noopener" href="https://ref45638.github.io/msgpack-converter/">msgpack-converter</a> 來解碼和編碼。</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"> <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;summary&gt;</span></span></span><br><span class="line"><span class="comment"><span class="doctag">///</span> 封裝來自 MQ 服務的訊息</span></span><br><span class="line"><span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;/summary&gt;</span></span></span><br><span class="line">[<span class="meta">MessagePackObject</span>]</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">MessageOutputTask</span></span><br><span class="line">&#123;</span><br><span class="line">   <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;summary&gt;</span></span></span><br><span class="line">   <span class="comment"><span class="doctag">///</span> 來自進程的輸出訊息</span></span><br><span class="line">   <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;/summary&gt;</span></span></span><br><span class="line">   [<span class="meta">Key(<span class="string">&quot;0&quot;</span>)</span>]</span><br><span class="line">   <span class="keyword">public</span> <span class="built_in">string</span> Message &#123; <span class="keyword">get</span>; <span class="keyword">set</span>; &#125;</span><br><span class="line">   [<span class="meta">Key(<span class="string">&quot;1&quot;</span>)</span>]</span><br><span class="line">   <span class="keyword">public</span> MessageStatus Status &#123; <span class="keyword">get</span>; <span class="keyword">set</span>; &#125;</span><br><span class="line">   <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;summary&gt;</span></span></span><br><span class="line">   <span class="comment"><span class="doctag">///</span> 我們希望儲存的回應資訊以便繼續執行訊息。</span></span><br><span class="line">   <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;/summary&gt;</span></span></span><br><span class="line">   [<span class="meta">Key(<span class="string">&quot;2&quot;</span>)</span>]</span><br><span class="line">   [<span class="meta">MessagePackFormatter(typeof(PrimitiveObjectResolver))</span>]</span><br><span class="line">   <span class="keyword">public</span> IDictionary&lt;<span class="built_in">string</span>, <span class="built_in">object</span>&gt; Headers &#123; <span class="keyword">get</span>; <span class="keyword">set</span>; &#125;</span><br><span class="line">   <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;summary&gt;</span></span></span><br><span class="line">   <span class="comment"><span class="doctag">///</span> 預設使用 BasicProperties.Reply To 隊列名稱，任務處理器可以覆寫回應隊列名稱。</span></span><br><span class="line">   <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;/summary&gt;</span></span></span><br><span class="line">   <span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;value&gt;</span>預設使用 BasicProperties.Reply<span class="doctag">&lt;/value&gt;</span></span></span><br><span class="line">   [<span class="meta">Key(<span class="string">&quot;3&quot;</span>)</span>]</span><br><span class="line">   <span class="keyword">public</span> <span class="built_in">string</span> ReplyQueueName &#123; <span class="keyword">get</span>; <span class="keyword">set</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我將在此介紹 MessageStatus 的含義。</p><ul><li>IGNORE_MESSAGE (-1) : 將訊息附加到資料流管道中，而不進行進一步處理。<ul><li>Status &#x3D; -1: 任務處理告訴 worker process 這不是回應或確認訊息，只是回饋到資料流管道。</li></ul></li><li>MESSAGE_DONE (200) : 通知 worker process 該案件可以由訊息隊列服務進行確認。<ul><li>Status &#x3D; 200 任務處理告訴 worker process 該任務已完成並且可以確認。</li></ul></li><li>MESSAGE_DONE_WITH_REPLY (201) : 請確保我們滿足以下步驟以支援 RPC。<ul><li>客戶端代碼必須提供 ReplyTo 資訊。</li><li>任務處理將使用 JSON 負載中的 Message 欄位來回應隊列資訊。</li><li>例如：當 Status &#x3D; 201 透過資料流管道發送時，任務處理指示 worker process 輸出，例如 1010，該數據必須然後發送到回應隊列。</li></ul></li></ul><p>我們可以通過不同的程式語言來編寫自己的 worker node （我已經在此 github 提供了 Python, .NET, rust example code）。</p><h2 id="如何處理長時間運行的任務或涉及處理大量數據行的任務？"><a href="#如何處理長時間運行的任務或涉及處理大量數據行的任務？" class="headerlink" title="如何處理長時間運行的任務或涉及處理大量數據行的任務？"></a>如何處理長時間運行的任務或涉及處理大量數據行的任務？</h2><p>類似於操作系統中的進程，發生上下文切換（中斷等）。</p><p>客戶端可以通過 Header 發送一個 <code>TimeoutMilliseconds</code> 值：在取消之前等待的時間（毫秒）。如果任務執行超過該值，worker process 可以使用該值來設置中斷，例如 CancellationToken。</p><p>例如，<code>MessageOutputTask</code> 的 JSON 可以如下所示，<code>status=201</code> 代表此訊息將重新入隊以便下次處理，並且訊息將攜帶 <code>Headers</code> 資訊再次重新入隊。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;Message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;This is Mock Json Data&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;Status&quot;</span><span class="punctuation">:</span> <span class="number">201</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;Headers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;CreateTimestamp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2025-01-01T14:35:00Z&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PreviousProcessingTimestamp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2025-01-01T14:40:00Z&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;Source&quot;</span><span class="punctuation">:</span> <span class="string">&quot;OrderProcessingService&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;PreviousExecutedRows&quot;</span><span class="punctuation">:</span> <span class="string">&quot;123&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;RequeueTimes&quot;</span><span class="punctuation">:</span> <span class="string">&quot;3&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>此專案還包括 integration、unit test 和 github action pipeline。雖然 API 文件（專案仍在 beta 階段），但我計劃在未來逐步添加。如果您對此專案有任何想法或建議，請隨時創建問題或發送 PR。</p><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/mq-workerpool-introduction/">https://isdaniel.github.io/mq-workerpool-introduction/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/mq-workerpool-introduction/</id>
    <link href="https://isdaniel.github.io/mq-workerpool-introduction/"/>
    <published>2025-06-02T08:00:00.000Z</published>
    <summary>最近我開發了 MessageWorkerPool 專案。其主要概念是提供一個平台框架，使使用者能夠快速且輕鬆地在 Worker 內實作邏輯。該設計高度靈活，允許基於我創建的 Worker 通訊協議，以多種程式語言實作 Worker。目前，我已提供使用 C#、Rust 和 Python 編寫的 Worker 範例。</summary>
    <title>MessageWorkerPool framework introduction</title>
    <updated>2026-08-05T10:10:26.490Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="Linux" scheme="https://isdaniel.github.io/categories/Rust/Linux/"/>
    <category term="Docker" scheme="https://isdaniel.github.io/tags/Docker/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="Linux" scheme="https://isdaniel.github.io/tags/Linux/"/>
    <content>
      <![CDATA[<h1 id="RustBox"><a href="#RustBox" class="headerlink" title="RustBox"></a>RustBox</h1><blockquote><p>A Docker-like container runtime written in Rust with daemon architecture, supporting multi-container orchestration, persistent state management, and comprehensive CLI commands.</p></blockquote><h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p><strong>RustBox</strong> is a container runtime that isn’t competing with (Docker or Kubernetes), we return to the core and build a simplest “Sandbox&#x2F;isolated runtime environment” from the lowest level Linux kernel mechanisms (namespaces, cgroups, OverlayFS, etc.), provides Docker-like functionality using:</p><ul><li><strong>Daemon Architecture</strong> with Unix domain socket communication</li><li><strong>Multi-container Management</strong> with persistent state</li><li><strong>OverlayFS</strong> for isolated container filesystems</li><li><strong>Cgroups v2</strong> for resource limits (memory, CPU)</li><li><strong>Linux namespaces</strong> for complete process isolation</li><li><strong>Comprehensive CLI</strong> with run, stop, list, inspect, remove, logs, and attach commands</li></ul><p>This tool is designed for container orchestration, testing environments, and secure code execution.</p><h2 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h2><h3 id="Daemon-Client-Model"><a href="#Daemon-Client-Model" class="headerlink" title="Daemon-Client Model"></a>Daemon-Client Model</h3><figure class="highlight axapta"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br></pre></td><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────────────────────────────────┐</span><br><span class="line">│                                RustBox Architecture                          │</span><br><span class="line">└──────────────────────────────────────────────────────────────────────────────┘</span><br><span class="line"></span><br><span class="line">[rustbox CLI]                                 [rustboxd Daemon]</span><br><span class="line">     │                                               │</span><br><span class="line">     │  Unix Socket                                  │</span><br><span class="line">     │  /tmp/rustbox-daemon.sock                     │</span><br><span class="line">     │                                               │</span><br><span class="line">     │  IPC Protocol (JSON messages)                 │</span><br><span class="line">     │  ───────────────────────────────────────────▶ │</span><br><span class="line">     │  Commands:                                   │</span><br><span class="line">     │   • run                                      │</span><br><span class="line">     │   • stop                                     │</span><br><span class="line">     │   • list                                     │</span><br><span class="line">     │   • inspect                                  │</span><br><span class="line">     │   • remove                                   │</span><br><span class="line">     │   • logs                                     │</span><br><span class="line">     │   • attach                                   │</span><br><span class="line">     │                                               ▼</span><br><span class="line">     │                                    ┌────────────────────────────┐</span><br><span class="line">     │                                    │  Container Manager         │</span><br><span class="line">     │                                    │  ───────────────────────── │</span><br><span class="line">     │                                    │  • Controls lifecycle      │</span><br><span class="line">     │                                    │  • Creates sandbox env     │</span><br><span class="line">     │                                    │  • Manages PTY + process   │</span><br><span class="line">     │                                    └────────────────────────────┘</span><br><span class="line">     │                                               │</span><br><span class="line">     │                                               ▼</span><br><span class="line">     │                                    ┌────────────────────────────┐</span><br><span class="line">     │                                    │  Registry (HashMap&lt;ID, Container&gt;)│</span><br><span class="line">     │                                    └────────────────────────────┘</span><br><span class="line">     │                                               │</span><br><span class="line">     │                     ┌────────────────────────────────────────────┐</span><br><span class="line">     │                     │           Container Instances              │</span><br><span class="line">     │                     └────────────────────────────────────────────┘</span><br><span class="line">     │                         │              │              │</span><br><span class="line">     │                         ▼              ▼              ▼</span><br><span class="line">     │                    [Container <span class="number">1</span>]  [Container <span class="number">2</span>]  [Container N]</span><br><span class="line">     │                         │              │              │</span><br><span class="line">     │                  ┌──────────────────────────────────────────────┐</span><br><span class="line">     │                  │              Sandbox Components               │</span><br><span class="line">     │                  │  overlayfs + cgroups + namespaces             │</span><br><span class="line">     │                  └──────────────────────────────────────────────┘</span><br><span class="line">     │                         │</span><br><span class="line">     │                         │</span><br><span class="line">     │  (When attaching)       │</span><br><span class="line">     │  ───────────────────────────────────────────────────────────────────────────</span><br><span class="line">    ┌─────────────────────────────────────────────────────────────────────────────┐</span><br><span class="line">    │                            Container Attach Flow                            │</span><br><span class="line">    └─────────────────────────────────────────────────────────────────────────────┘</span><br><span class="line"></span><br><span class="line">    Client (e.g. docker attach, CLI, web terminal)</span><br><span class="line">    │</span><br><span class="line">    │  <span class="number">1.</span> Send/receive stdin/stdout over Unix socket</span><br><span class="line">    ▼</span><br><span class="line">    ┌───────────────────────────────────────────────────────────┐</span><br><span class="line">    │ Daemon Process                                            │</span><br><span class="line">    │ ───────────────────────────────────────────────────────── │</span><br><span class="line">    │  • Manages <span class="built_in">container</span> lifecycle                            │</span><br><span class="line">    │  • Holds PTY master side                                  │</span><br><span class="line">    │  • Forwards data between <span class="keyword">client</span> and <span class="built_in">container</span>             │</span><br><span class="line">    │                                                           │</span><br><span class="line">    │  ┌──────────────────────────────────────────────────────┐ │</span><br><span class="line">    │  │ Unix Socket (Client ↔ Daemon)                        │ │</span><br><span class="line">    │  │  - AttachStdin  (<span class="keyword">client</span> → daemon)                    │ │</span><br><span class="line">    │  │  - AttachStdout (daemon → <span class="keyword">client</span>)                    │ │</span><br><span class="line">    │  └──────────────────────────────────────────────────────┘ │</span><br><span class="line">    │                              │</span><br><span class="line">    │                              │ (I/O forwarding loop)</span><br><span class="line">    │                              ▼</span><br><span class="line">    │  ┌──────────────────────────────────────────────────────┐ │</span><br><span class="line">    │  │ PTY Master                                           │ │</span><br><span class="line">    │  │  - Pseudo terminal device endpoint controlled <span class="keyword">by</span>     │ │</span><br><span class="line">    │  │    the daemon                                        │ │</span><br><span class="line">    │  │  - Reads <span class="built_in">container</span> output                            │ │</span><br><span class="line">    │  │  - Writes <span class="keyword">client</span> input                               │ │</span><br><span class="line">    │  └──────────────────────────────────────────────────────┘ │</span><br><span class="line">    │                              │</span><br><span class="line">    │                              │ (kernel-level link)</span><br><span class="line">    │                              ▼</span><br><span class="line">    │  ┌──────────────────────────────────────────────────────┐ │</span><br><span class="line">    │  │ PTY Slave                                            │ │</span><br><span class="line">    │  │  - Exposed inside the <span class="built_in">container</span> <span class="keyword">as</span> /dev/tty or stdin │ │</span><br><span class="line">    │  │  - Attached to the <span class="built_in">container</span>’s process (e.g. /bin/bash)││</span><br><span class="line">    │  │  - Container writes stdout/stderr → goes to Master   │ │</span><br><span class="line">    │  │  - Container reads stdin ← comes <span class="keyword">from</span> Master         │ │</span><br><span class="line">    │  └──────────────────────────────────────────────────────┘ │</span><br><span class="line">    └───────────────────────────────────────────────────────────┘</span><br><span class="line">    │</span><br><span class="line">    ▼</span><br><span class="line">    Container Process (e.g. /bin/bash, sh)</span><br><span class="line">    • Reads <span class="keyword">from</span> stdin (/dev/tty)</span><br><span class="line">    • Writes to stdout/stderr (/dev/tty)</span><br><span class="line"></span><br><span class="line">    ───────────────────────────────────────────────────────────────</span><br><span class="line">    Summary:</span><br><span class="line">    - PTY Master: controlled <span class="keyword">by</span> the daemon, mediates all I/O</span><br><span class="line">    - PTY Slave : presented to the <span class="built_in">container</span> process <span class="keyword">as</span> its terminal</span><br><span class="line">    - Unix Socket: transports attach stream between <span class="keyword">client</span> ↔ daemon</span><br><span class="line">    ───────────────────────────────────────────────────────────────</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="Container-Isolation"><a href="#Container-Isolation" class="headerlink" title="Container Isolation"></a>Container Isolation</h3><p>RustBox employs a <strong>double fork</strong> pattern for each container to ensure proper isolation:</p><h3 id="Process-Hierarchy-Per-Container"><a href="#Process-Hierarchy-Per-Container" class="headerlink" title="Process Hierarchy (Per Container)"></a>Process Hierarchy (Per Container)</h3><figure class="highlight tcl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">[Daemon Process]</span><br><span class="line">    └─&gt; spawn_blocking()</span><br><span class="line">        └─&gt; [Container Task]</span><br><span class="line">            └─&gt; fork() #<span class="number">1</span></span><br><span class="line">                ├─&gt; [Namespaced Parent Process]</span><br><span class="line">                │   ├─&gt; unshare() - Creates new namespaces</span><br><span class="line">                │   ├─&gt; setup cgroups and overlay</span><br><span class="line">                │   └─&gt; fork() #<span class="number">2</span></span><br><span class="line">                │       ├─&gt; [Inner Child Process]</span><br><span class="line">                │       │   ├─&gt; Mount /<span class="keyword">proc</span><span class="title"> and</span> /dev</span><br><span class="line">                │       │   ├─&gt;<span class="title"> chroot()</span> to<span class="title"> merged</span> overlay</span><br><span class="line">                │       │   ├─&gt;<span class="title"> chdir()</span> to<span class="title"> working</span> directory</span><br><span class="line">                │       │   └─&gt;<span class="title"> execv()</span> -<span class="title"> Execute</span> command</span><br><span class="line">                │       └─&gt; [Namespaced<span class="title"> Parent]</span> waits<span class="title"> for</span> inner<span class="title"> child</span></span><br><span class="line">                │           └─&gt;<span class="title"> Unmounts</span> /<span class="keyword">proc</span><span class="title"> and</span> /dev<span class="title"> inside</span> namespace</span><br><span class="line">                └─&gt; [Container<span class="title"> Task]</span> waits<span class="title"> for</span> namespaced<span class="title"> parent</span></span><br><span class="line">                    ├─&gt;<span class="title"> Unmounts</span> overlay<span class="title"> filesystem</span></span><br><span class="line">                    ├─&gt;<span class="title"> Cleans</span> up<span class="title"> cgroups</span></span><br><span class="line">                    └─&gt;<span class="title"> Updates</span> container<span class="title"> state</span> in<span class="title"> registry</span></span><br></pre></td></tr></table></figure><h3 id="Container-Lifecycle-States"><a href="#Container-Lifecycle-States" class="headerlink" title="Container Lifecycle States"></a>Container Lifecycle States</h3><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Created ──(start)──&gt; Running ──(<span class="keyword">exit</span>)──────&gt; Exited</span><br><span class="line">                       │</span><br><span class="line">                       └──(stop)──&gt; Stopped ──(<span class="keyword">exit</span>)──&gt; Exited</span><br></pre></td></tr></table></figure><h3 id="Persistent-State-Management"><a href="#Persistent-State-Management" class="headerlink" title="Persistent State Management"></a>Persistent State Management</h3><ul><li><strong>Container metadata</strong>: <code>/var/lib/rustbox/containers/&lt;container-id&gt;.json</code></li><li><strong>Container logs</strong>: <code>/var/lib/rustbox/logs/&lt;container-id&gt;/</code></li><li><strong>Overlay filesystems</strong>: <code>/var/lib/rustbox/overlay/&lt;container-id&gt;/</code></li><li><strong>State recovery</strong>: Daemon recovers container state on restart</li></ul><h2 id="Features"><a href="#Features" class="headerlink" title="Features"></a>Features</h2><ul><li><strong>Daemon Architecture</strong> with background process and client-server communication</li><li><strong>Multi-container Management</strong> supporting concurrent container execution</li><li><strong>Persistent State Management</strong> with automatic recovery across daemon restarts</li><li><strong>Complete Container Lifecycle</strong> (create, start, stop, remove, inspect)</li><li><strong>Interactive Attach Support</strong> with TTY allocation and real-time I&#x2F;O streaming</li><li><strong>Real-time Logging</strong> with per-container stdout&#x2F;stderr files</li><li><strong>Resource Isolation</strong> using cgroups v2 (memory, CPU limits)</li><li><strong>Filesystem Isolation</strong> using overlayfs with automatic cleanup</li><li><strong>Full Namespace Isolation</strong> (PID, UTS, NET, USER, IPC)</li><li><strong>Docker-like CLI</strong> with familiar commands (run, ps, logs, inspect, rm, attach)</li><li><strong>Graceful Shutdown</strong> with proper signal handling and resource cleanup</li><li><strong>Security</strong> with proper privilege separation and input validation</li></ul><h2 id="Requirements"><a href="#Requirements" class="headerlink" title="Requirements"></a>Requirements</h2><ul><li>Linux kernel 5.x or higher (with overlayfs and cgroups v2 support)</li><li>Rust (1.70+ recommended)</li><li>Root privileges (for daemon operations, mounting, and namespace creation)</li></ul><h2 id="Installation"><a href="#Installation" class="headerlink" title="Installation"></a>Installation</h2><h3 id="Build-from-Source"><a href="#Build-from-Source" class="headerlink" title="Build from Source"></a>Build from Source</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/isdaniel/RustBox.git</span><br><span class="line"><span class="built_in">cd</span> RustBox</span><br><span class="line">cargo build --release</span><br></pre></td></tr></table></figure><h3 id="Binaries"><a href="#Binaries" class="headerlink" title="Binaries"></a>Binaries</h3><p>After building, you’ll have two binaries:</p><ul><li><code>rustbox</code> - Client CLI tool</li><li><code>daemon_rs</code> - Background daemon process</li></ul><h2 id="Usage"><a href="#Usage" class="headerlink" title="Usage"></a>Usage</h2><h3 id="Start-the-Daemon"><a href="#Start-the-Daemon" class="headerlink" title="Start the Daemon"></a>Start the Daemon</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Start the daemon in background (requires root)</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/daemon_rs 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure><p>The daemon will:</p><ul><li>Listen on Unix socket <code>/tmp/rustbox-daemon.sock</code></li><li>Create system directories under <code>/var/lib/rustbox/</code></li><li>Recover existing container state from disk</li><li>Handle graceful shutdown on SIGTERM&#x2F;SIGINT</li></ul><h3 id="Container-Management"><a href="#Container-Management" class="headerlink" title="Container Management"></a>Container Management</h3><h4 id="Create-and-Run-Containers"><a href="#Create-and-Run-Containers" class="headerlink" title="Create and Run Containers"></a>Create and Run Containers</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Run a container in background with TTY support (allows interactive attach)</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox run --<span class="built_in">tty</span> --memory 256M --cpu 0.5 /bin/bash</span><br><span class="line"></span><br><span class="line"><span class="comment"># Run a container with custom name</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox run --<span class="built_in">tty</span> --memory 256M --cpu 0.5 /bin/bash 2&gt;&amp;1</span><br><span class="line"></span><br><span class="line"><span class="comment"># Run a non-interactive container</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox run --memory 256M /usr/bin/python3 script.py</span><br></pre></td></tr></table></figure><p><strong>Note</strong>: The <code>--tty</code> flag is required if you want to attach to the container later.</p><h4 id="List-Containers"><a href="#List-Containers" class="headerlink" title="List Containers"></a>List Containers</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># List running containers</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox list</span><br><span class="line"></span><br><span class="line"><span class="comment"># List all containers (including stopped)</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox list -a</span><br><span class="line"><span class="comment"># or</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox list --all</span><br></pre></td></tr></table></figure><h4 id="Attach-to-Running-Containers"><a href="#Attach-to-Running-Containers" class="headerlink" title="Attach to Running Containers"></a>Attach to Running Containers</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Attach to a running container (container must have been created with --tty flag)</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox attach &lt;container-id&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># Example:</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox attach f1a5f84880a1</span><br></pre></td></tr></table></figure><p><strong>Interactive Controls</strong>:</p><ul><li>Press <code>Ctrl+P</code> followed by <code>Ctrl+Q</code> to detach from container (leaves it running)</li><li>Press <code>Ctrl+C</code> to send interrupt signal and exit</li></ul><p><strong>Requirements</strong>:</p><ul><li>Container must have been started with <code>--tty</code> flag</li><li>Container must be in <code>Running</code> state</li></ul><h4 id="Stop-Remove-and-Inspect-Containers"><a href="#Stop-Remove-and-Inspect-Containers" class="headerlink" title="Stop, Remove, and Inspect Containers"></a>Stop, Remove, and Inspect Containers</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Stop a running container</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox stop &lt;container-id&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># View container logs</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox logs &lt;container-id&gt;</span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox logs --<span class="built_in">tail</span> 50 &lt;container-id&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># Inspect container details</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox inspect &lt;container-id&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># Remove a stopped container</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox remove &lt;container-id&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># Force remove a running container</span></span><br><span class="line"><span class="built_in">sudo</span> ./target/release/rustbox remove --force &lt;container-id&gt;</span><br></pre></td></tr></table></figure><p><strong>Available Options</strong>:</p><ul><li><code>--name</code> - Custom container name (auto-generated if not provided)</li><li><code>--memory</code> - Memory limit (e.g., “256M”, “1G”, “512000”)</li><li><code>--cpu</code> - CPU limit as fraction of one core (e.g., “0.5”, “1.0”)</li><li><code>--workdir</code> - Working directory inside container (default: “&#x2F;“)</li><li><code>--rootfs</code> - Path to rootfs directory (default: “.&#x2F;rootfs”)</li><li><code>--tty</code> - Allocate a pseudo-TTY for interactive use (required for attach)</li></ul><h2 id="Directory-Structure"><a href="#Directory-Structure" class="headerlink" title="Directory Structure"></a>Directory Structure</h2><h3 id="Runtime-Directories-created-by-daemon"><a href="#Runtime-Directories-created-by-daemon" class="headerlink" title="Runtime Directories (created by daemon)"></a>Runtime Directories (created by daemon)</h3><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="operator">/</span>var<span class="operator">/</span>lib<span class="operator">/</span>rustbox<span class="symbol">/</span></span><br><span class="line">├── containers<span class="symbol">/</span>           <span class="comment"># Container metadata (JSON files)</span></span><br><span class="line">│   ├── a1b2c3d4e5f6.json</span><br><span class="line">│   └── f6e5d4c3b2a1.json</span><br><span class="line">├── logs<span class="symbol">/</span>                 <span class="comment"># Container logs</span></span><br><span class="line">│   ├── a1b2c3d4e5f6<span class="symbol">/</span></span><br><span class="line">│   │   ├── stdout.log</span><br><span class="line">│   │   └── stderr.log</span><br><span class="line">│   └── f6e5d4c3b2a1<span class="symbol">/</span></span><br><span class="line">│       ├── stdout.log</span><br><span class="line">│       └── stderr.log</span><br><span class="line">└── overlay<span class="symbol">/</span>              <span class="comment"># Overlay filesystem layers</span></span><br><span class="line">    ├── a1b2c3d4e5f6<span class="symbol">/</span></span><br><span class="line">    │   ├── lowerdir<span class="symbol">/</span>     <span class="comment"># Read-only base layer</span></span><br><span class="line">    │   ├── upperdir<span class="symbol">/</span>     <span class="comment"># Container changes</span></span><br><span class="line">    │   ├── workdir<span class="symbol">/</span>      <span class="comment"># Overlay work directory</span></span><br><span class="line">    │   └── merged<span class="symbol">/</span>       <span class="comment"># Final mounted filesystem</span></span><br><span class="line">    └── f6e5d4c3b2a1<span class="symbol">/</span></span><br><span class="line">        ├── lowerdir<span class="symbol">/</span></span><br><span class="line">        ├── upperdir<span class="symbol">/</span></span><br><span class="line">        ├── workdir<span class="symbol">/</span></span><br><span class="line">        └── merged<span class="operator">/</span></span><br></pre></td></tr></table></figure><h3 id="Source-Code-Structure"><a href="#Source-Code-Structure" class="headerlink" title="Source Code Structure"></a>Source Code Structure</h3><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">src<span class="symbol">/</span></span><br><span class="line">├── lib.rs                <span class="comment"># Public API exports</span></span><br><span class="line">├── main.rs               <span class="comment"># Client CLI entry point</span></span><br><span class="line">├── daemon<span class="symbol">/</span>               <span class="comment"># Daemon implementation</span></span><br><span class="line">│   ├── main.rs          <span class="comment"># Daemon entry point</span></span><br><span class="line">│   ├── server.rs        <span class="comment"># Unix socket server</span></span><br><span class="line">│   ├── container_manager.rs <span class="comment"># Container lifecycle management</span></span><br><span class="line">│   └── signal_handler.rs <span class="comment"># Graceful shutdown handling</span></span><br><span class="line">├── ipc<span class="symbol">/</span>                  <span class="comment"># Inter-process communication</span></span><br><span class="line">│   ├── protocol.rs      <span class="comment"># Message types and framing</span></span><br><span class="line">│   └── client.rs        <span class="comment"># Client-side socket communication</span></span><br><span class="line">├── container<span class="symbol">/</span>            <span class="comment"># Container abstractions</span></span><br><span class="line">│   ├── mod.rs           <span class="comment"># Container data structures</span></span><br><span class="line">│   ├── config.rs        <span class="comment"># Configuration and validation</span></span><br><span class="line">│   ├── sandbox.rs       <span class="comment"># Core isolation logic</span></span><br><span class="line">│   ├── state_machine.rs <span class="comment"># Container state transitions</span></span><br><span class="line">│   └── id.rs            <span class="comment"># ID generation and validation</span></span><br><span class="line">├── storage<span class="symbol">/</span>              <span class="comment"># Persistent storage</span></span><br><span class="line">│   ├── metadata.rs      <span class="comment"># Container metadata management</span></span><br><span class="line">│   └── logs.rs          <span class="comment"># Log file management</span></span><br><span class="line">├── cli<span class="symbol">/</span>                  <span class="comment"># CLI command implementations</span></span><br><span class="line">│   ├── run.rs           <span class="comment"># Create and start containers</span></span><br><span class="line">│   ├── stop.rs          <span class="comment"># Stop containers</span></span><br><span class="line">│   ├── list.rs          <span class="comment"># List containers</span></span><br><span class="line">│   ├── inspect.rs       <span class="comment"># Container details</span></span><br><span class="line">│   ├── remove.rs        <span class="comment"># Remove containers</span></span><br><span class="line">│   ├── logs.rs          <span class="comment"># View container logs</span></span><br><span class="line">│   └── attach.rs        <span class="comment"># Attach to containers</span></span><br><span class="line">└── error.rs             <span class="comment"># Error handling</span></span><br></pre></td></tr></table></figure><h2 id="Technical-Details"><a href="#Technical-Details" class="headerlink" title="Technical Details"></a>Technical Details</h2><h3 id="IPC-Protocol"><a href="#IPC-Protocol" class="headerlink" title="IPC Protocol"></a>IPC Protocol</h3><p>Communication between client and daemon uses length-prefixed JSON messages over Unix domain sockets:</p><figure class="highlight scheme"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[<span class="name">4-byte</span> length (<span class="name">u32</span>, big-endian)][<span class="name">JSON</span> payload]</span><br></pre></td></tr></table></figure><p>Example:</p><figure class="highlight puppet"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0<span class="keyword">x0000001E</span>  &#123;<span class="string">&quot;type&quot;</span>:<span class="string">&quot;ListRequest&quot;</span>,<span class="string">&quot;all&quot;</span>:<span class="keyword">true</span>&#125;</span><br></pre></td></tr></table></figure><h3 id="Container-ID-Format"><a href="#Container-ID-Format" class="headerlink" title="Container ID Format"></a>Container ID Format</h3><ul><li>12-character hexadecimal identifiers (e.g., <code>a1b2c3d4e5f6</code>)</li><li>Auto-generated names follow <code>adjective-noun</code> pattern (e.g., <code>happy-elephant</code>)</li><li>CLI commands accept either ID or name</li></ul><h3 id="Resource-Limits"><a href="#Resource-Limits" class="headerlink" title="Resource Limits"></a>Resource Limits</h3><ul><li><strong>Memory</strong>: Supports units like <code>100M</code>, <code>1G</code>, <code>512000</code> (bytes)</li><li><strong>CPU</strong>: Fraction of one core, e.g., <code>0.5</code> for 50% CPU limit</li><li>Enforced via cgroups v2 at <code>/sys/fs/cgroup/rustbox/&lt;container-id&gt;/</code></li></ul><h3 id="Security-Model"><a href="#Security-Model" class="headerlink" title="Security Model"></a>Security Model</h3><ul><li>Daemon runs as root for privileged operations</li><li>Client commands run as user, connect via Unix socket</li><li>Containers run in isolated namespaces (PID, NET, UTS, IPC, USER)</li><li>Input validation prevents directory traversal and injection attack</li></ul><h2 id="Final-Thoughts"><a href="#Final-Thoughts" class="headerlink" title="Final Thoughts"></a>Final Thoughts</h2><p><strong>RustBox</strong> is not a full container system, and that’s by design — it’s <strong>transparent</strong>, <strong>hackable</strong>, and <strong>educational</strong>. Whether you’re looking to secure untrusted code, explore low-level Linux features, or just love writing systems code in Rust, RustBox is a fantastic playground.</p><blockquote><p>Give it a ⭐ on <a target="_blank" rel="noopener" href="https://github.com/isdaniel/RustBox">GitHub</a> and explore the source!</p></blockquote><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/rustbox-introduce/">https://isdaniel.github.io/rustbox-introduce/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/rustbox-introduce/</id>
    <link href="https://isdaniel.github.io/rustbox-introduce/"/>
    <published>2025-06-01T15:30:11.000Z</published>
    <summary>RustBox 是用 Rust 打造的類 Docker 容器 runtime，基於 Linux namespaces、cgroups、OverlayFS，支援多容器編排與完整 CLI</summary>
    <title>RustBox - Docker-Lite Sandbox for Hackers and Learners</title>
    <updated>2026-08-05T10:10:26.494Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="C#" scheme="https://isdaniel.github.io/categories/Rust/C/"/>
    <category term="OpenTelemetry" scheme="https://isdaniel.github.io/categories/Rust/C/OpenTelemetry/"/>
    <category term="C#" scheme="https://isdaniel.github.io/tags/C/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="OpenTelemetry" scheme="https://isdaniel.github.io/tags/OpenTelemetry/"/>
    <category term="Sidecar" scheme="https://isdaniel.github.io/tags/Sidecar/"/>
    <category term="Observability" scheme="https://isdaniel.github.io/tags/Observability/"/>
    <content>
      <![CDATA[<h2 id="introduce"><a href="#introduce" class="headerlink" title="introduce"></a>introduce</h2><p>If you’ve ever struggled to instrument legacy apps or non-standard services for observability, <code>OpenTelemetry_SideCar</code> is here to help. This project offers a <strong>non-intrusive way to export metrics and traces</strong> via <strong>OpenTelemetry</strong> using a <strong>sidecar approach</strong> — no SDK required in your main app.</p><hr><h2 id="🌐-Project-Overview"><a href="#🌐-Project-Overview" class="headerlink" title="🌐 Project Overview"></a>🌐 Project Overview</h2><p>📦 <strong>Repo:</strong> <a target="_blank" rel="noopener" href="https://github.com/isdaniel/OpenTelemetry_SideCar">isdaniel&#x2F;OpenTelemetry_SideCar</a></p><p><code>OpenTelemetry_SideCar</code> is a standalone proxy service that collects metrics and traces <strong>outside of your application</strong> and forwards them to a telemetry backend (e.g., Prometheus, Jaeger, or Azure Monitor).</p><h3 id="💡-Why-a-Sidecar"><a href="#💡-Why-a-Sidecar" class="headerlink" title="💡 Why a Sidecar?"></a>💡 Why a Sidecar?</h3><p>In cloud-native systems, a <em>sidecar</em> is a helper container or process that runs alongside your main application. It can observe, extend, or enhance app behavior <strong>without changing application code</strong>. This pattern is ideal for adding <strong>observability</strong> when:</p><ul><li>You can’t modify the original code (e.g., closed-source, legacy binaries).</li><li>You want to centralize telemetry logic.</li><li>You’re aiming for a unified instrumentation strategy.</li></ul><hr><h2 id="🧠-Key-Concepts"><a href="#🧠-Key-Concepts" class="headerlink" title="🧠 Key Concepts"></a>🧠 Key Concepts</h2><h3 id="📊-OpenTelemetry"><a href="#📊-OpenTelemetry" class="headerlink" title="📊 OpenTelemetry"></a>📊 OpenTelemetry</h3><p>OpenTelemetry is the CNCF-backed observability framework offering a vendor-neutral standard to collect metrics, logs, and traces.</p><p>This project leverages:</p><ul><li><strong>OTLP (OpenTelemetry Protocol)</strong> for data transport</li><li><strong>Push-based metrics</strong> collection via HTTP endpoints</li><li><strong>Custom trace generation</strong> from event messages</li></ul><h3 id="🧱-Sidecar-Design-Pattern"><a href="#🧱-Sidecar-Design-Pattern" class="headerlink" title="🧱 Sidecar Design Pattern"></a>🧱 Sidecar Design Pattern</h3><p>This service runs in parallel with your main app and exposes lightweight endpoints for:</p><ul><li>Sending <strong>metrics</strong> via <code>/metrics</code></li><li>Sending <strong>traces</strong> via <code>/trace</code></li></ul><p>Apps interact with the sidecar using simple HTTP POST requests.</p><hr><h2 id="⚙️-How-It-Works"><a href="#⚙️-How-It-Works" class="headerlink" title="⚙️ How It Works"></a>⚙️ How It Works</h2><h2 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h2><p>The project consists of the following components:</p><ul><li><strong>.NET Web Application</strong>: A simple web service with custom metrics and tracing- <strong>OpenTelemetry Collector</strong>: Receives telemetry data and exports it to backends</li><li><strong>Prometheus</strong>: Time-series database for storing and querying metrics- <strong>Jaeger</strong>: Distributed tracing system for monitoring and troubleshooting</li><li><strong>Grafana</strong>: Visualization and dashboarding platform</li></ul><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐</span><br><span class="line">│  Rust We        │     │                 │     │                 │</span><br><span class="line">│  .<span class="keyword">NET</span> <span class="keyword">App</span>       │────▶│  OTel Collector │────▶│  Prometheus     │</span><br><span class="line">│                 │     │                 │     │                 │</span><br><span class="line">└─────────────────┘     └────────┬────────┘     └─────────────────┘</span><br><span class="line">                                 │                        ▲</span><br><span class="line">                                 │                        │</span><br><span class="line">                                 ▼                        │</span><br><span class="line">                        ┌─────────────────┐      ┌────────┴────────┐</span><br><span class="line">                        │                 │      │                 │</span><br><span class="line">                        │  Jaeger         │      │  Grafana        │</span><br><span class="line">                        │                 │      │                 │</span><br><span class="line">                        └─────────────────┘      └─────────────────┘</span><br></pre></td></tr></table></figure><h2 id="Features"><a href="#Features" class="headerlink" title="Features"></a>Features</h2><ul><li>Custom metrics using OpenTelemetry Metrics API- Distributed tracing with OpenTelemetry Tracing API</li><li>Nested HTTP calls with context propagation- Prometheus metrics collection</li><li>Jaeger trace visualization- Grafana dashboards for metrics visualization</li></ul><h2 id="Prerequisites"><a href="#Prerequisites" class="headerlink" title="Prerequisites"></a>Prerequisites</h2><ul><li>Docker and Docker Compose</li><li>.NET 9.0 SDK (for local development)</li></ul><h2 id="Getting-Started"><a href="#Getting-Started" class="headerlink" title="Getting Started"></a>Getting Started</h2><h3 id="Running-the-Application"><a href="#Running-the-Application" class="headerlink" title="Running the Application"></a>Running the Application</h3><p>Clone the repository:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/yourusername/OpenTelemetry_SideCar.git</span><br><span class="line"><span class="built_in">cd</span> OpenTelemetry_SideCar</span><br></pre></td></tr></table></figure><p>Start the services using Docker Compose:<br> <figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">docker-compose up -d</span></span><br></pre></td></tr></table></figure></p><ol><li>Access the application: Web Application: <a target="_blank" rel="noopener" href="http://localhost:8080/">http://localhost:8080</a>   - Nested Greeting: <a target="_blank" rel="noopener" href="http://localhost:8080/NestedGreeting?nestlevel=3">http://localhost:8080/NestedGreeting?nestlevel=3</a></li></ol><h3 id="Accessing-Observability-Tools"><a href="#Accessing-Observability-Tools" class="headerlink" title="Accessing Observability Tools"></a>Accessing Observability Tools</h3><ul><li><strong>Prometheus</strong>: <a target="_blank" rel="noopener" href="http://localhost:9090/">http://localhost:9090</a><ul><li>Query metrics with PromQL  - Example: <code>greetings_count_total</code></li></ul></li><li><strong>Jaeger UI</strong>: <a target="_blank" rel="noopener" href="http://localhost:16686/">http://localhost:16686</a><ul><li>View distributed traces  - Filter by service: <code>telemetry_example</code></li></ul></li><li><strong>Grafana</strong>: <a target="_blank" rel="noopener" href="http://localhost:3000/">http://localhost:3000</a><ul><li>Default credentials: admin&#x2F;admin  - Pre-configured dashboards for application metrics</li></ul></li></ul><h2 id="Application-Endpoints"><a href="#Application-Endpoints" class="headerlink" title="Application Endpoints"></a>Application Endpoints</h2><ul><li><strong>&#x2F;</strong> - Returns a simple greeting and increments the greeting counter</li><li><strong>&#x2F;NestedGreeting?nestlevel&#x3D;N</strong> - Creates a chain of N nested HTTP calls, demonstrating trace context propagation</li></ul><h2 id="Configuration-Files"><a href="#Configuration-Files" class="headerlink" title="Configuration Files"></a>Configuration Files</h2><ul><li><strong>docker-compose.yml</strong>: Defines all services and their connections- <strong>otel-collector-config.yaml</strong>: Configures the OpenTelemetry Collector</li><li><strong>prometheus.yml</strong>: Prometheus scraping configuration- <strong>Dockerfile</strong>: Builds the .NET application</li></ul><h2 id="Troubleshooting"><a href="#Troubleshooting" class="headerlink" title="Troubleshooting"></a>Troubleshooting</h2><h3 id="Common-Issues"><a href="#Common-Issues" class="headerlink" title="Common Issues"></a>Common Issues</h3><ol><li><strong>No metrics in Prometheus</strong>:<ul><li>Verify the OpenTelemetry Collector is running: <code>docker-compose ps</code>   - Check collector logs: <code>docker-compose logs otel-collector</code></li><li>Ensure Prometheus is scraping the collector: <a target="_blank" rel="noopener" href="http://localhost:9090/targets">http://localhost:9090/targets</a></li></ul></li><li><strong>No traces in Jaeger</strong>:   - Verify Jaeger is running: <code>docker-compose ps jaeger</code><ul><li>Check that OTLP is enabled in Jaeger   - Generate some traces by accessing the application endpoints</li></ul></li><li><strong>Application errors</strong>:<ul><li>Check application logs: <code>docker-compose logs app</code></li></ul></li></ol><h2 id="Development"><a href="#Development" class="headerlink" title="Development"></a>Development</h2><h3 id="Local-Development"><a href="#Local-Development" class="headerlink" title="Local Development"></a>Local Development</h3><p>To run the application locally:</p><ol><li>Navigate to the src directory2. Run <code>dotnet run</code><br>Note: When running locally, you’ll need to update the OTLP endpoint in Program.cs to point to your local OpenTelemetry Collector.</li></ol><h3 id="Adding-Custom-Metrics"><a href="#Adding-Custom-Metrics" class="headerlink" title="Adding Custom Metrics"></a>Adding Custom Metrics</h3><ol><li>Create a new meter:<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> myMeter = <span class="keyword">new</span> Meter(<span class="string">&quot;MyApp.Metrics&quot;</span>, <span class="string">&quot;1.0.0&quot;</span>);</span><br></pre></td></tr></table></figure></li><li>Create metrics instruments:<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> myCounter = myMeter.CreateCounter&lt;<span class="built_in">int</span>&gt;(<span class="string">&quot;my.counter&quot;</span>, <span class="string">&quot;Count of operations&quot;</span>);</span><br></pre></td></tr></table></figure></li><li>Record measurements:<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">myCounter.Add(<span class="number">1</span>);</span><br></pre></td></tr></table></figure></li><li>Register the meter in the OpenTelemetry configuration:<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">metricsProviderBuilder.AddMeter(<span class="string">&quot;MyApp.Metrics&quot;</span>);</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="📥-Example-Sending-Metrics"><a href="#📥-Example-Sending-Metrics" class="headerlink" title="📥 Example: Sending Metrics"></a>📥 Example: Sending Metrics</h2><p>Suppose your app wants to record a counter metric for user logins. All it needs to do is POST to the sidecar:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">curl -X POST http://localhost:8080/metrics \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    &quot;name&quot;: &quot;user_login_total&quot;,</span></span><br><span class="line"><span class="string">    &quot;kind&quot;: &quot;counter&quot;,</span></span><br><span class="line"><span class="string">    &quot;value&quot;: 1,</span></span><br><span class="line"><span class="string">    &quot;attributes&quot;: &#123;</span></span><br><span class="line"><span class="string">      &quot;service&quot;: &quot;auth-service&quot;,</span></span><br><span class="line"><span class="string">      &quot;status&quot;: &quot;success&quot;</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span></span><br></pre></td></tr></table></figure><p>📌 This will be transformed into an OpenTelemetry metric and pushed to your configured OTLP collector.</p><hr><h2 id="📡-Example-Sending-Traces"><a href="#📡-Example-Sending-Traces" class="headerlink" title="📡 Example: Sending Traces"></a>📡 Example: Sending Traces</h2><p>To record a trace span (e.g., for a request to <code>/api/data</code>), POST to <code>/trace</code>:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">curl -X POST http://localhost:8080/trace \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    &quot;trace_id&quot;: &quot;abc123&quot;,</span></span><br><span class="line"><span class="string">    &quot;span_id&quot;: &quot;def456&quot;,</span></span><br><span class="line"><span class="string">    &quot;name&quot;: &quot;GET /api/data&quot;,</span></span><br><span class="line"><span class="string">    &quot;kind&quot;: &quot;server&quot;,</span></span><br><span class="line"><span class="string">    &quot;start_time&quot;: &quot;2024-01-01T00:00:00Z&quot;,</span></span><br><span class="line"><span class="string">    &quot;end_time&quot;: &quot;2024-01-01T00:00:01Z&quot;,</span></span><br><span class="line"><span class="string">    &quot;attributes&quot;: &#123;</span></span><br><span class="line"><span class="string">      &quot;http.method&quot;: &quot;GET&quot;,</span></span><br><span class="line"><span class="string">      &quot;http.status_code&quot;: 200</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span></span><br></pre></td></tr></table></figure><p>The sidecar will:</p><ul><li>Create the span</li><li>Set its metadata and timing</li><li>Export it via OTLP to your backend (e.g., Jaeger or Zipkin)</li></ul><hr><h2 id="🛠️-Configuration"><a href="#🛠️-Configuration" class="headerlink" title="🛠️ Configuration"></a>🛠️ Configuration</h2><p>Set the following environment variables:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">OTEL_EXPORTER_OTLP_ENDPOINT=http://collector:4317</span><br><span class="line">OTEL_SERVICE_NAME=my-sidecar</span><br><span class="line">OTEL_METRICS_EXPORT_INTERVAL=1000</span><br></pre></td></tr></table></figure><p>These control how frequently data is flushed and where it’s sent.</p><hr><h2 id="🔒-No-SDK-No-Problem"><a href="#🔒-No-SDK-No-Problem" class="headerlink" title="🔒 No SDK, No Problem"></a>🔒 No SDK, No Problem</h2><p>One of the biggest benefits of <code>OpenTelemetry_SideCar</code> is that your main app doesn’t need to:</p><ul><li>Link or compile with any OpenTelemetry SDK</li><li>Maintain exporter or collector logic</li><li>Handle telemetry lifecycle</li></ul><p>Your app stays clean — just send HTTP!</p><hr><h2 id="🚀-Get-Started"><a href="#🚀-Get-Started" class="headerlink" title="🚀 Get Started"></a>🚀 Get Started</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/isdaniel/OpenTelemetry_SideCar.git</span><br><span class="line"><span class="built_in">cd</span> OpenTelemetry_SideCar</span><br><span class="line">cargo run</span><br></pre></td></tr></table></figure><p>Then, start POSTing traces and metrics from your apps.</p><hr><h2 id="🙌-Final-Thoughts"><a href="#🙌-Final-Thoughts" class="headerlink" title="🙌 Final Thoughts"></a>🙌 Final Thoughts</h2><p><code>OpenTelemetry_SideCar</code> empowers teams to <strong>add observability with zero code changes</strong> to their applications. It’s perfect for teams looking to modernize telemetry practices without touching production binaries.</p><p>If you’re working with mixed environments or maintaining legacy services, give it a try!</p><blockquote><p>⭐️ Star the repo: <a target="_blank" rel="noopener" href="https://github.com/isdaniel/OpenTelemetry_SideCar">isdaniel&#x2F;OpenTelemetry_SideCar</a></p></blockquote><p>&lt;! Above information summaries from AI. &#x2F;&gt;</p><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/opentelemetry-sidecar/">https://isdaniel.github.io/opentelemetry-sidecar/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/opentelemetry-sidecar/</id>
    <link href="https://isdaniel.github.io/opentelemetry-sidecar/"/>
    <published>2025-06-01T14:30:11.000Z</published>
    <summary>OpenTelemetry SideCar 是獨立的 sidecar proxy，無需修改程式碼即可匯出 metrics 與 traces，適合監控 legacy 或封閉原始碼應用</summary>
    <title>Instrument Any App Instantly Using OpenTelemetry_SideCar</title>
    <updated>2026-08-05T10:10:26.490Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Python" scheme="https://isdaniel.github.io/categories/Python/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Python/PostgreSQL/"/>
    <category term="AI" scheme="https://isdaniel.github.io/categories/Python/PostgreSQL/AI/"/>
    <category term="Performance" scheme="https://isdaniel.github.io/categories/Python/PostgreSQL/AI/Performance/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Performance-Tuning" scheme="https://isdaniel.github.io/tags/Performance-Tuning/"/>
    <category term="Python" scheme="https://isdaniel.github.io/tags/Python/"/>
    <category term="AI" scheme="https://isdaniel.github.io/tags/AI/"/>
    <category term="MCP" scheme="https://isdaniel.github.io/tags/MCP/"/>
    <category term="Database-Optimization" scheme="https://isdaniel.github.io/tags/Database-Optimization/"/>
    <content>
      <![CDATA[<h1 id="AI-Powered-PostgreSQL-Performance-Tuning-with-MCP-Introducing-pgtuner-mcp"><a href="#AI-Powered-PostgreSQL-Performance-Tuning-with-MCP-Introducing-pgtuner-mcp" class="headerlink" title="AI-Powered PostgreSQL Performance Tuning with MCP - Introducing pgtuner_mcp"></a>AI-Powered PostgreSQL Performance Tuning with MCP - Introducing pgtuner_mcp</h1><p>Database performance optimization is one of the most critical yet challenging aspects of maintaining production systems. Identifying slow queries, optimizing indexes, and monitoring database health requires deep expertise and constant vigilance. Today, I’m excited to introduce <strong>pgtuner_mcp</strong>, a Model Context Protocol (MCP) server that brings AI-powered PostgreSQL performance tuning capabilities directly into your development workflow.</p><h2 id="What-is-pgtuner-mcp"><a href="#What-is-pgtuner-mcp" class="headerlink" title="What is pgtuner_mcp?"></a>What is pgtuner_mcp?</h2><p><strong>pgtuner_mcp</strong> is an intelligent PostgreSQL performance analysis server built on the Model Context Protocol (MCP). It bridges the gap between AI assistants (like Claude) and your PostgreSQL database, enabling natural language interactions for complex database optimization tasks.</p><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pgtuner_mcp">GitHub Repository</a></p><h3 id="Key-Capabilities"><a href="#Key-Capabilities" class="headerlink" title="Key Capabilities"></a>Key Capabilities</h3><ul><li><strong>Intelligent Query Analysis</strong>: Identify slow queries with detailed statistics from <code>pg_stat_statements</code></li><li><strong>AI-Powered Index Recommendations</strong>: Get smart indexing suggestions based on actual workload patterns</li><li><strong>Hypothetical Index Testing</strong>: Test indexes without creating them using HypoPG</li><li><strong>Comprehensive Health Checks</strong>: Monitor connections, cache efficiency, locks, and replication</li><li><strong>Bloat Detection</strong>: Identify and quantify table&#x2F;index bloat for maintenance</li><li><strong>Vacuum Monitoring</strong>: Track vacuum operations and autovacuum effectiveness</li><li><strong>I&#x2F;O Analysis</strong>: Analyze disk read&#x2F;write patterns and identify bottlenecks</li><li><strong>Configuration Review</strong>: Get recommendations for memory, checkpoint, and connection settings</li></ul><h2 id="Architecture-Overview"><a href="#Architecture-Overview" class="headerlink" title="Architecture Overview"></a>Architecture Overview</h2><p>pgtuner_mcp leverages several PostgreSQL extensions and Python libraries to provide comprehensive analysi</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────────┐</span><br><span class="line">│  AI Assistant   │</span><br><span class="line">│  (Claude, etc)  │</span><br><span class="line">└────────┬────────┘</span><br><span class="line">         │ MCP Protocol</span><br><span class="line">         ▼</span><br><span class="line">┌─────────────────┐    ┌──────────────────┐</span><br><span class="line">│  pgtuner_mcp    │───▶│   PostgreSQL     │</span><br><span class="line">│  (Python MCP    │    │   + Extensions   │</span><br><span class="line">│   <span class="keyword">Server</span>)       │    │  - pg_stat_      │</span><br><span class="line">└─────────────────┘    │    statements    │</span><br><span class="line">         │              │  - hypopg        │</span><br><span class="line">         │              │  - pgstattuple   │</span><br><span class="line">         ▼              └──────────────────┘</span><br><span class="line">┌─────────────────┐</span><br><span class="line">│  Analysis &amp;     │</span><br><span class="line">│  Recommendations│</span><br><span class="line">└─────────────────┘</span><br></pre></td></tr></table></figure><h3 id="Core-Components"><a href="#Core-Components" class="headerlink" title="Core Components"></a>Core Components</h3><ol><li><strong>MCP Server</strong>: Provides tools, prompts, and resources via Model Context Protocol</li><li><strong>Query Analyzer</strong>: Parses and analyzes SQL using <code>pglast</code> library</li><li><strong>Performance Metrics</strong>: Collects statistics from PostgreSQL system views</li><li><strong>AI Recommendations</strong>: Generates intelligent suggestions based on workload patterns</li><li><strong>Multiple Transport Modes</strong>: Supports stdio, SSE, and streamable HTTP</li></ol><h2 id="Installation-and-Setup"><a href="#Installation-and-Setup" class="headerlink" title="Installation and Setup"></a>Installation and Setup</h2><h3 id="Prerequisites"><a href="#Prerequisites" class="headerlink" title="Prerequisites"></a>Prerequisites</h3><p>Before installing pgtuner_mcp, ensure you have:</p><ul><li>Python 3.10+</li><li>PostgreSQL 12+ (recommended: 14+)</li><li>Access to install PostgreSQL extensions</li></ul><h3 id="Quick-Installation"><a href="#Quick-Installation" class="headerlink" title="Quick Installation"></a>Quick Installation</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Install via pip</span></span><br><span class="line">pip install pgtuner_mcp</span><br><span class="line"></span><br><span class="line"><span class="comment"># Or using uv (faster)</span></span><br><span class="line">uv pip install pgtuner_mcp</span><br></pre></td></tr></table></figure><h3 id="PostgreSQL-Extensions-Setup"><a href="#PostgreSQL-Extensions-Setup" class="headerlink" title="PostgreSQL Extensions Setup"></a>PostgreSQL Extensions Setup</h3><p>pgtuner_mcp requires specific PostgreSQL extensions for full functionality:</p><h4 id="1-pg-stat-statements-Required"><a href="#1-pg-stat-statements-Required" class="headerlink" title="1. pg_stat_statements (Required)"></a>1. pg_stat_statements (Required)</h4><p>This extension tracks query execution statistics:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Add to postgresql.conf</span></span><br><span class="line">shared_preload_libraries <span class="operator">=</span> <span class="string">&#x27;pg_stat_statements&#x27;</span></span><br><span class="line">compute_query_id <span class="operator">=</span> o</span><br><span class="line">pg_stat_statements.max <span class="operator">=</span> <span class="number">10000</span></span><br><span class="line">pg_stat_statements.track <span class="operator">=</span> top</span><br><span class="line">pg_stat_statements.track_utility <span class="operator">=</span> <span class="keyword">on</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- Restart PostgreSQL, then:</span></span><br><span class="line"><span class="keyword">CREATE</span> EXTENSION IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> pg_stat_statements;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Verify installation</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> pg_stat_statements LIMIT <span class="number">1</span>;</span><br></pre></td></tr></table></figure><h4 id="2-HypoPG-Optional-Recommended"><a href="#2-HypoPG-Optional-Recommended" class="headerlink" title="2. HypoPG (Optional, Recommended)"></a>2. HypoPG (Optional, Recommended)</h4><p>Enables hypothetical index testing without disk usage:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> EXTENSION IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> hypopg;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Verify installation</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> hypopg_list_indexes();</span><br></pre></td></tr></table></figure><h4 id="3-pgstattuple-Optional-for-Bloat-Detection"><a href="#3-pgstattuple-Optional-for-Bloat-Detection" class="headerlink" title="3. pgstattuple (Optional, for Bloat Detection)"></a>3. pgstattuple (Optional, for Bloat Detection)</h4><p>Provides tuple-level statistics for bloat analysis:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> EXTENSION IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> pgstattuple;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Verify installation</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> pgstattuple(<span class="string">&#x27;pg_class&#x27;</span>) LIMIT <span class="number">1</span>;</span><br></pre></td></tr></table></figure><h3 id="User-Permissions"><a href="#User-Permissions" class="headerlink" title="User Permissions"></a>User Permissions</h3><p>Create a dedicated monitoring user with minimal required permissions:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Create monitoring user</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> pgtuner_monitor <span class="keyword">WITH</span> PASSWORD <span class="string">&#x27;secure_password&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Grant connection and schema access</span></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">CONNECT</span> <span class="keyword">ON</span> DATABASE your_database <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"><span class="keyword">GRANT</span> USAGE <span class="keyword">ON</span> SCHEMA public <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Grant read access to user tables</span></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">SELECT</span> <span class="keyword">ON</span> <span class="keyword">ALL</span> TABLES <span class="keyword">IN</span> SCHEMA public <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">DEFAULT</span> PRIVILEGES <span class="keyword">IN</span> SCHEMA public</span><br><span class="line">  <span class="keyword">GRANT</span> <span class="keyword">SELECT</span> <span class="keyword">ON</span> TABLES <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Grant system statistics access (PostgreSQL 10+)</span></span><br><span class="line"><span class="keyword">GRANT</span> pg_read_all_stats <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Grant access to pg_stat_statements</span></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">SELECT</span> <span class="keyword">ON</span> pg_stat_statements <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">SELECT</span> <span class="keyword">ON</span> pg_stat_statements_info <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- For bloat detection (PostgreSQL 14+)</span></span><br><span class="line"><span class="keyword">GRANT</span> pg_stat_scan_tables <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- For HypoPG functions</span></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">SELECT</span> <span class="keyword">ON</span> hypopg_list_indexes <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">EXECUTE</span> <span class="keyword">ON</span> <span class="keyword">FUNCTION</span> hypopg_create_index(text) <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">EXECUTE</span> <span class="keyword">ON</span> <span class="keyword">FUNCTION</span> hypopg_drop_index(oid) <span class="keyword">TO</span> pgtuner_monitor;</span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">EXECUTE</span> <span class="keyword">ON</span> <span class="keyword">FUNCTION</span> hypopg_reset() <span class="keyword">TO</span> pgtuner_monitor;</span><br></pre></td></tr></table></figure><h2 id="Configuration"><a href="#Configuration" class="headerlink" title="Configuration"></a>Configuration</h2><h3 id="Server-Modes"><a href="#Server-Modes" class="headerlink" title="Server Modes"></a>Server Modes</h3><p>pgtuner_mcp supports three deployment modes:</p><h4 id="1-Standard-MCP-Mode-stdio"><a href="#1-Standard-MCP-Mode-stdio" class="headerlink" title="1. Standard MCP Mode (stdio)"></a>1. Standard MCP Mode (stdio)</h4><p>Best for MCP clients like Claude Desktop or Cline:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Default mode</span></span><br><span class="line">python -m pgtuner_mcp</span><br><span class="line"></span><br><span class="line"><span class="comment"># Explicit stdio mode</span></span><br><span class="line">python -m pgtuner_mcp --mode stdio</span><br></pre></td></tr></table></figure><p><strong>Configuration for Claude Desktop</strong> (<code>cline_mcp_settings.json</code>):</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;mcpServers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;pgtuner_mcp&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;python&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;-m&quot;</span><span class="punctuation">,</span> <span class="string">&quot;pgtuner_mcp&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;DATABASE_URI&quot;</span><span class="punctuation">:</span> <span class="string">&quot;postgresql://pgtuner_monitor:password@localhost:5432/mydb&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;disabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;autoApprove&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h4 id="2-HTTP-SSE-Mode-Legacy-Web-Applications"><a href="#2-HTTP-SSE-Mode-Legacy-Web-Applications" class="headerlink" title="2. HTTP SSE Mode (Legacy Web Applications)"></a>2. HTTP SSE Mode (Legacy Web Applications)</h4><p>Server-Sent Events for web-based MCP communication:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Start SSE server</span></span><br><span class="line">python -m pgtuner_mcp --mode sse --host 0.0.0.0 --port 8080</span><br><span class="line"></span><br><span class="line"><span class="comment"># With debug mode</span></span><br><span class="line">python -m pgtuner_mcp --mode sse --debug</span><br></pre></td></tr></table></figure><p><strong>Endpoints</strong>:</p><ul><li><code>GET /sse</code> - SSE connection endpoint</li><li><code>POST /messages</code> - Send messages&#x2F;requests</li></ul><h4 id="3-Streamable-HTTP-Mode-Recommended-for-Web"><a href="#3-Streamable-HTTP-Mode-Recommended-for-Web" class="headerlink" title="3. Streamable HTTP Mode (Recommended for Web)"></a>3. Streamable HTTP Mode (Recommended for Web)</h4><p>Modern MCP protocol with single <code>/mcp</code> endpoint:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Stateful mode (maintains session state)</span></span><br><span class="line">python -m pgtuner_mcp --mode streamable-http</span><br><span class="line"></span><br><span class="line"><span class="comment"># Stateless mode (serverless-friendly)</span></span><br><span class="line">python -m pgtuner_mcp --mode streamable-http --stateless</span><br><span class="line"></span><br><span class="line"><span class="comment"># Custom host/port</span></span><br><span class="line">python -m pgtuner_mcp --mode streamable-http --host localhost --port 8080</span><br></pre></td></tr></table></figure><p><strong>Configuration</strong>:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;mcpServers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;pgtuner_mcp&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;url&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://localhost:8080/mcp&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h3 id="Environment-Variables"><a href="#Environment-Variables" class="headerlink" title="Environment Variables"></a>Environment Variables</h3><table><thead><tr><th>Variable</th><th>Description</th><th>Required</th></tr></thead><tbody><tr><td><code>DATABASE_URI</code></td><td>PostgreSQL connection string</td><td>Yes</td></tr><tr><td><code>PGTUNER_EXCLUDE_USERIDS</code></td><td>Comma-separated user OIDs to exclude</td><td>No</td></tr></tbody></table><p><strong>Connection String Format</strong>:</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">postgresql:</span>//<span class="symbol">user:</span>password<span class="variable">@host</span><span class="symbol">:port/database</span></span><br></pre></td></tr></table></figure><h2 id="Available-Tools"><a href="#Available-Tools" class="headerlink" title="Available Tools"></a>Available Tools</h2><p>pgtuner_mcp provides 15+ specialized tools organized into categories:</p><h3 id="Performance-Analysis-Tools"><a href="#Performance-Analysis-Tools" class="headerlink" title="Performance Analysis Tools"></a>Performance Analysis Tools</h3><table><thead><tr><th>Tool</th><th>Description</th></tr></thead><tbody><tr><td><code>get_slow_queries</code></td><td>Retrieve slow queries with detailed statistics (time, calls, cache hit ratio)</td></tr><tr><td><code>analyze_query</code></td><td>Analyze execution plans with EXPLAIN ANALYZE and automated issue detection</td></tr><tr><td><code>get_table_stats</code></td><td>Get table statistics: size, row counts, dead tuples, access patterns</td></tr><tr><td><code>analyze_disk_io_patterns</code></td><td>Analyze I&#x2F;O patterns, identify hot tables and bottlenecks</td></tr></tbody></table><h3 id="Index-Tuning-Tools"><a href="#Index-Tuning-Tools" class="headerlink" title="Index Tuning Tools"></a>Index Tuning Tools</h3><table><thead><tr><th>Tool</th><th>Description</th></tr></thead><tbody><tr><td><code>get_index_recommendations</code></td><td>AI-powered index recommendations based on workload analysis</td></tr><tr><td><code>explain_with_indexes</code></td><td>Test hypothetical indexes without creating them</td></tr><tr><td><code>manage_hypothetical_indexes</code></td><td>Create, list, drop, or reset HypoPG hypothetical indexes</td></tr><tr><td><code>find_unused_indexes</code></td><td>Find unused and duplicate indexes for cleanup</td></tr></tbody></table><h3 id="Database-Health-Tools"><a href="#Database-Health-Tools" class="headerlink" title="Database Health Tools"></a>Database Health Tools</h3><table><thead><tr><th>Tool</th><th>Description</th></tr></thead><tbody><tr><td><code>check_database_health</code></td><td>Comprehensive health check with scoring</td></tr><tr><td><code>get_active_queries</code></td><td>Monitor active queries and find long-running transactions</td></tr><tr><td><code>analyze_wait_events</code></td><td>Identify I&#x2F;O, lock, or CPU bottlenecks</td></tr><tr><td><code>review_settings</code></td><td>Review PostgreSQL configuration with recommendations</td></tr></tbody></table><h3 id="Bloat-Detection-Tools"><a href="#Bloat-Detection-Tools" class="headerlink" title="Bloat Detection Tools"></a>Bloat Detection Tools</h3><table><thead><tr><th>Tool</th><th>Description</th></tr></thead><tbody><tr><td><code>analyze_table_bloat</code></td><td>Analyze table bloat using pgstattuple extension</td></tr><tr><td><code>analyze_index_bloat</code></td><td>Analyze B-tree index bloat (also supports GIN&#x2F;Hash)</td></tr><tr><td><code>get_bloat_summary</code></td><td>Comprehensive bloat overview with maintenance priorities</td></tr></tbody></table><h3 id="Vacuum-Monitoring-Tools"><a href="#Vacuum-Monitoring-Tools" class="headerlink" title="Vacuum Monitoring Tools"></a>Vacuum Monitoring Tools</h3><table><thead><tr><th>Tool</th><th>Description</th></tr></thead><tbody><tr><td><code>monitor_vacuum_progress</code></td><td>Track VACUUM, VACUUM FULL, and autovacuum operations</td></tr></tbody></table><h2 id="Docker-Deployment"><a href="#Docker-Deployment" class="headerlink" title="Docker Deployment"></a>Docker Deployment</h2><p>pgtuner_mcp is available as a Docker image for easy deployment:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Pull the image</span></span><br><span class="line">docker pull dog830228/pgtuner_mcp</span><br><span class="line"></span><br><span class="line"><span class="comment"># Run in streamable HTTP mode (recommended)</span></span><br><span class="line">docker run -p 8080:8080 \</span><br><span class="line">  -e DATABASE_URI=postgresql://user:pass@host:5432/db \</span><br><span class="line">  dog830228/pgtuner_mcp --mode streamable-http</span><br><span class="line"></span><br><span class="line"><span class="comment"># Run in stateless mode (serverless-friendly)</span></span><br><span class="line">docker run -p 8080:8080 \</span><br><span class="line">  -e DATABASE_URI=postgresql://user:pass@host:5432/db \</span><br><span class="line">  dog830228/pgtuner_mcp --mode streamable-http --stateless</span><br><span class="line"></span><br><span class="line"><span class="comment"># Run in stdio mode for MCP clients</span></span><br><span class="line">docker run -i \</span><br><span class="line">  -e DATABASE_URI=postgresql://user:pass@host:5432/db \</span><br><span class="line">  dog830228/pgtuner_mcp --mode stdio</span><br></pre></td></tr></table></figure><h2 id="Real-World-Use-Cases"><a href="#Real-World-Use-Cases" class="headerlink" title="Real-World Use Cases"></a>Real-World Use Cases</h2><h3 id="1-Slow-Query-Investigation"><a href="#1-Slow-Query-Investigation" class="headerlink" title="1. Slow Query Investigation"></a>1. Slow Query Investigation</h3><p><strong>Scenario</strong>: Application experiencing slow response times.</p><p><strong>Workflow</strong>:</p><figure class="highlight n1ql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">User: &quot;Find the slowest queries in my database&quot;</span><br><span class="line">AI + pgtuner_mcp:</span><br><span class="line">  1. Calls get_slow_queries(limit=10, order_by=<span class="string">&quot;total_time&quot;</span>)</span><br><span class="line">  <span class="number">2.</span> Identifies top <span class="number">3</span> problematic queries</span><br><span class="line">  <span class="number">3.</span> Calls analyze_query() <span class="keyword">for</span> <span class="keyword">each</span></span><br><span class="line">  <span class="number">4.</span> Detects sequential scans <span class="keyword">and</span> <span class="literal">missing</span> indexes</span><br><span class="line">  <span class="number">5.</span> Calls get_index_recommendations()</span><br><span class="line">  <span class="number">6.</span> Provides <span class="keyword">CREATE</span> <span class="keyword">INDEX</span> statements <span class="keyword">with</span> impact estimates</span><br></pre></td></tr></table></figure><h3 id="2-Index-Optimization"><a href="#2-Index-Optimization" class="headerlink" title="2. Index Optimization"></a>2. Index Optimization</h3><p><strong>Scenario</strong>: Database growing, need to optimize indexes.</p><p><strong>Workflow</strong>:</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">User: <span class="string">&quot;Help me optimize my database indexes&quot;</span></span><br><span class="line">AI + pgtuner_mcp:</span><br><span class="line">  <span class="number">1.</span> <span class="function">Calls <span class="title">find_unused_indexes</span>() to identify cleanup candidates</span></span><br><span class="line"><span class="function">  2. Calls <span class="title">get_index_recommendations</span>() <span class="keyword">for</span> <span class="keyword">new</span> index suggestions</span></span><br><span class="line"><span class="function">  3. Calls <span class="title">explain_with_indexes</span>() to test hypothetical indexes</span></span><br><span class="line"><span class="function">  4. Estimates storage savings <span class="keyword">and</span> performance improvements</span></span><br><span class="line"><span class="function">  5. Provides prioritized action plan</span></span><br></pre></td></tr></table></figure><h3 id="3-Health-Check-Before-Production-Deploy"><a href="#3-Health-Check-Before-Production-Deploy" class="headerlink" title="3. Health Check Before Production Deploy"></a>3. Health Check Before Production Deploy</h3><p><strong>Scenario</strong>: Pre-deployment database health validation.</p><p><strong>Workflow</strong>:</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">User: <span class="string">&quot;Is my database ready for production traffic?&quot;</span></span><br><span class="line">AI + pgtuner_mcp:</span><br><span class="line">  1. Calls check_database_health(<span class="attribute">verbose</span>=<span class="literal">True</span>)</span><br><span class="line">  2. Analyzes<span class="built_in"> connection pool </span>capacity</span><br><span class="line">  3. Checks cache hit ratios</span><br><span class="line">  4. Reviews vacuum <span class="keyword">and</span> autovacuum status</span><br><span class="line">  5. Analyzes wait events</span><br><span class="line">  6. Reviews configuration<span class="built_in"> settings</span></span><br><span class="line"><span class="built_in"></span>  7. Provides comprehensive<span class="built_in"> health </span>report with recommendations</span><br></pre></td></tr></table></figure><h3 id="4-Performance-Regression-Investigation"><a href="#4-Performance-Regression-Investigation" class="headerlink" title="4. Performance Regression Investigation"></a>4. Performance Regression Investigation</h3><p><strong>Scenario</strong>: Performance degraded after recent changes.</p><p><strong>Workflow</strong>:</p><figure class="highlight smali"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">User: <span class="string">&quot;Why is my database slower than last week?&quot;</span></span><br><span class="line">AI + pgtuner_mcp:</span><br><span class="line">  1. Calls get_table_stats() to identify growth patterns</span><br><span class="line">  2. Calls analyze_disk_io_patterns() for I/O bottlenecks</span><br><span class="line">  3. Calls get_bloat_summary() to detect table/index bloat</span><br><span class="line">  4. Calls monitor_vacuum_progress() to<span class="built_in"> check </span>maintenance</span><br><span class="line">  5. Calls analyze_wait_events() to find resource contention</span><br><span class="line">  6. Identifies root causes<span class="built_in"> and </span>provides remediation steps</span><br></pre></td></tr></table></figure><h2 id="Performance-Considerations"><a href="#Performance-Considerations" class="headerlink" title="Performance Considerations"></a>Performance Considerations</h2><h3 id="Extension-Overhead"><a href="#Extension-Overhead" class="headerlink" title="Extension Overhead"></a>Extension Overhead</h3><table><thead><tr><th>Extension</th><th>Performance Impact</th><th>Recommendation</th></tr></thead><tbody><tr><td><code>pg_stat_statements</code></td><td>Low (~1-2%)</td><td>Always enable</td></tr><tr><td><code>track_io_timing</code></td><td>Low-Medium (~2-5%)</td><td>Enable in production, test first</td></tr><tr><td><code>track_functions = all</code></td><td>Low</td><td>Enable for function-heavy workloads</td></tr><tr><td><code>pgstattuple</code> functions</td><td>Varies by table size</td><td>Use <code>_approx</code> for large tables</td></tr><tr><td><code>HypoPG</code></td><td>Zero (in-memory only)</td><td>Safe for all environments</td></tr></tbody></table><p><strong>Tip</strong>: Use <code>pg_test_timing</code> to measure timing overhead on your specific hardware:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> pg_test_timing();</span><br></pre></td></tr></table></figure><h3 id="Best-Practices"><a href="#Best-Practices" class="headerlink" title="Best Practices"></a>Best Practices</h3><ol><li><strong>Use Approximate Analysis</strong>: For large tables (&gt;5GB), use <code>pgstattuple_approx</code> instead of <code>pgstattuple</code></li><li><strong>Filter System Users</strong>: Exclude monitoring&#x2F;replication users using <code>PGTUNER_EXCLUDE_USERIDS</code></li><li><strong>Limit Query History</strong>: Configure <code>pg_stat_statements.max</code> based on your workload</li><li><strong>Regular Maintenance</strong>: Use vacuum monitoring tools to ensure optimal performance</li><li><strong>Test Hypothetical Indexes</strong>: Always test with HypoPG before creating real indexes</li></ol><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>pgtuner_mcp represents a paradigm shift in database performance optimization. By combining the power of AI assistants with deep PostgreSQL expertise through the Model Context Protocol, it makes advanced database tuning accessible to developers at all skill levels.</p><p>The tool doesn’t replace database administrators—it augments their capabilities and democratizes access to expert-level analysis. Whether you’re debugging a slow query, planning index strategies, or conducting pre-deployment health checks, pgtuner_mcp provides intelligent, context-aware assistance.</p><h3 id="Key-Takeaways"><a href="#Key-Takeaways" class="headerlink" title="Key Takeaways"></a>Key Takeaways</h3><ol><li><strong>AI-Native Performance Tuning</strong>: Natural language interface to complex database operations</li><li><strong>Risk-Free Testing</strong>: HypoPG enables index testing without disk usage</li><li><strong>Comprehensive Analysis</strong>: 15+ tools covering queries, indexes, health, bloat, vacuum, and I&#x2F;O</li><li><strong>Flexible Deployment</strong>: stdio, HTTP SSE, or streamable HTTP modes</li><li><strong>Production-Ready</strong>: Minimal overhead, proper permissions, comprehensive monitoring</li></ol><p>Whether you’re a seasoned DBA looking to leverage AI for faster workflows or a developer seeking to understand and optimize database performance, pgtuner_mcp offers a powerful, modern approach to PostgreSQL tuning.</p><h3 id="Resources"><a href="#Resources" class="headerlink" title="Resources"></a>Resources</h3><ul><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pgtuner_mcp">pgtuner_mcp GitHub Repository</a></li><li><a target="_blank" rel="noopener" href="https://modelcontextprotocol.io/">Model Context Protocol Documentation</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/performance-tips.html">PostgreSQL Performance Tuning Guide</a></li><li><a target="_blank" rel="noopener" href="https://github.com/HypoPG/hypopg">HypoPG Extension</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/pgstatstatements.html">pg_stat_statements Documentation</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pgtuner-mcp-ai-powered-postgresql-performance/">https://isdaniel.github.io/pgtuner-mcp-ai-powered-postgresql-performance/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pgtuner-mcp-ai-powered-postgresql-performance/</id>
    <link href="https://isdaniel.github.io/pgtuner-mcp-ai-powered-postgresql-performance/"/>
    <published>2025-01-13T02:30:00.000Z</published>
    <summary>pgtuner_mcp is an MCP server for AI-driven PostgreSQL performance analysis, tuning advice, and health checks from your development workflow</summary>
    <title>AI-Powered PostgreSQL Performance Tuning with MCP - Introducing pgtuner_mcp</title>
    <updated>2026-08-05T10:10:26.491Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="Rust" scheme="https://isdaniel.github.io/categories/Rust/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/"/>
    <category term="Database-Safety" scheme="https://isdaniel.github.io/categories/Rust/PostgreSQL/Database-Safety/"/>
    <category term="PostgreSQL" scheme="https://isdaniel.github.io/tags/PostgreSQL/"/>
    <category term="Rust" scheme="https://isdaniel.github.io/tags/Rust/"/>
    <category term="pgrx" scheme="https://isdaniel.github.io/tags/pgrx/"/>
    <category term="Database-Safety" scheme="https://isdaniel.github.io/tags/Database-Safety/"/>
    <category term="Extension" scheme="https://isdaniel.github.io/tags/Extension/"/>
    <content>
      <![CDATA[<h1 id="Building-Safe-PostgreSQL-Extensions-with-Rust-Introducing-pg-where-guard"><a href="#Building-Safe-PostgreSQL-Extensions-with-Rust-Introducing-pg-where-guard" class="headerlink" title="Building Safe PostgreSQL Extensions with Rust - Introducing pg_where_guard"></a>Building Safe PostgreSQL Extensions with Rust - Introducing pg_where_guard</h1><p>Database safety is a critical concern for any production system. Accidental data loss from <code>DELETE</code> or <code>UPDATE</code> statements without <code>WHERE</code> clauses can be catastrophic. Today, I’ll introduce <strong>pg_where_guard</strong>, a PostgreSQL extension built with Rust and the pgrx framework that prevents these dangerous operations.</p><h2 id="What-is-pg-where-guard"><a href="#What-is-pg-where-guard" class="headerlink" title="What is pg_where_guard?"></a>What is pg_where_guard?</h2><p><strong>pg_where_guard</strong> is a PostgreSQL extension that acts as a safety net for your database by intercepting and blocking potentially dangerous SQL operations:</p><ul><li><strong>DELETE Protection</strong>: Prevents <code>DELETE FROM table</code> without WHERE clause</li><li><strong>UPDATE Protection</strong>: Prevents <code>UPDATE table SET ...</code> without WHERE clause</li><li><strong>CTE Support</strong>: Recursively checks Common Table Expressions</li><li><strong>Hook Integration</strong>: Uses PostgreSQL’s <code>post_parse_analyze_hook</code> for query interception</li><li><strong>Memory Safe</strong>: Written in Rust with pgrx for safety and performance</li></ul><p><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg_where_guard">GitHub Repository</a></p><h2 id="Why-Rust-for-PostgreSQL-Extensions"><a href="#Why-Rust-for-PostgreSQL-Extensions" class="headerlink" title="Why Rust for PostgreSQL Extensions?"></a>Why Rust for PostgreSQL Extensions?</h2><p>Building PostgreSQL extensions traditionally meant working with C and dealing with manual memory management, potential segmentation faults, and complex debugging. Rust changes this paradigm by offering:</p><h3 id="Performance"><a href="#Performance" class="headerlink" title="Performance"></a>Performance</h3><p>Zero-cost abstractions mean Rust code performs as well as equivalent C code while being much safer.</p><h3 id="pgrx-Framework"><a href="#pgrx-Framework" class="headerlink" title="pgrx Framework"></a>pgrx Framework</h3><p>The <a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx framework</a> provides:</p><ul><li>Type-safe PostgreSQL API bindings</li><li>Automatic SQL schema generation</li><li>Comprehensive testing support</li><li>Easy development workflow</li></ul><h2 id="Technical-Architecture"><a href="#Technical-Architecture" class="headerlink" title="Technical Architecture"></a>Technical Architecture</h2><h3 id="Hook-Based-Implementation"><a href="#Hook-Based-Implementation" class="headerlink" title="Hook-Based Implementation"></a>Hook-Based Implementation</h3><p>pg_where_guard leverages PostgreSQL’s hook system to intercept queries after parsing:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Hook registration in _PG_init</span></span><br><span class="line">PREV_POST_PARSE_ANALYZE_HOOK = pg_sys::post_parse_analyze_hook;</span><br><span class="line">pg_sys::post_parse_analyze_hook = <span class="title function_ invoke__">Some</span>(delete_needs_where_check);</span><br></pre></td></tr></table></figure><h3 id="Query-Analysis-Engine"><a href="#Query-Analysis-Engine" class="headerlink" title="Query Analysis Engine"></a>Query Analysis Engine</h3><p>The extension examines the parsed query tree to detect dangerous operations:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Query checking logic</span></span><br><span class="line"><span class="keyword">match</span> query.commandType &#123;</span><br><span class="line">    pg_sys::CmdType::CMD_DELETE =&gt; &#123;</span><br><span class="line">        <span class="keyword">if</span> !query.jointree.<span class="title function_ invoke__">is_null</span>() &#123;</span><br><span class="line">            <span class="keyword">let</span> <span class="variable">jointree</span> = &amp;*query.jointree;</span><br><span class="line">            <span class="keyword">if</span> jointree.quals.<span class="title function_ invoke__">is_null</span>() &#123;</span><br><span class="line">                error!(<span class="string">&quot;DELETE requires a WHERE clause&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    pg_sys::CmdType::CMD_UPDATE =&gt; &#123;</span><br><span class="line">        <span class="keyword">if</span> !query.jointree.<span class="title function_ invoke__">is_null</span>() &#123;</span><br><span class="line">            <span class="keyword">let</span> <span class="variable">jointree</span> = &amp;*query.jointree;</span><br><span class="line">            <span class="keyword">if</span> jointree.quals.<span class="title function_ invoke__">is_null</span>() &#123;</span><br><span class="line">                error!(<span class="string">&quot;UPDATE requires a WHERE clause&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    _ =&gt; &#123;</span><br><span class="line">        <span class="comment">// Other command types are allowed</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Key-Components"><a href="#Key-Components" class="headerlink" title="Key Components"></a>Key Components</h3><ol><li><p><strong>Hook Function</strong> (<code>delete_needs_where_check</code>):</p><ul><li>Intercepts queries via <code>post_parse_analyze_hook</code></li><li>Checks command types (DELETE&#x2F;UPDATE)</li><li>Validates presence of WHERE clauses</li><li>Handles Common Table Expressions recursively</li></ul></li><li><p><strong>Query Analysis</strong> (<code>check_query_for_where_clause</code>):</p><ul><li>Examines the query’s <code>jointree</code> structure</li><li>Looks for <code>quals</code> (qualification&#x2F;WHERE conditions)</li><li>Throws errors for unqualified modifications</li></ul></li><li><p><strong>Extension Functions</strong>:</p><ul><li><code>pg_where_guard_is_enabled()</code>: Check if protection is active</li><li><code>pg_where_guard_enable()</code>: Enable protection</li></ul></li></ol><h2 id="Installation-and-Setup"><a href="#Installation-and-Setup" class="headerlink" title="Installation and Setup"></a>Installation and Setup</h2><h3 id="Prerequisites"><a href="#Prerequisites" class="headerlink" title="Prerequisites"></a>Prerequisites</h3><p>Before installing pg_where_guard, ensure you have:</p><ul><li>Rust toolchain (1.70+)</li><li>pgrx framework</li><li>PostgreSQL development headers</li><li>cargo-pgrx</li></ul><h3 id="Build-and-Install"><a href="#Build-and-Install" class="headerlink" title="Build and Install"></a>Build and Install</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Clone the repository</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/isdaniel/pg_where_guard.git</span><br><span class="line"><span class="built_in">cd</span> pg_where_guard</span><br><span class="line"></span><br><span class="line"><span class="comment"># Install cargo-pgrx if not already installed</span></span><br><span class="line">cargo install cargo-pgrx</span><br><span class="line"></span><br><span class="line"><span class="comment"># Initialize pgrx for your PostgreSQL version</span></span><br><span class="line">cargo pgrx init</span><br><span class="line"></span><br><span class="line"><span class="comment"># Install the extension</span></span><br><span class="line">cargo pgrx install</span><br></pre></td></tr></table></figure><h3 id="Database-Setup"><a href="#Database-Setup" class="headerlink" title="Database Setup"></a>Database Setup</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Create the extension</span></span><br><span class="line"><span class="keyword">CREATE</span> EXTENSION pg_where_guard;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Verify installation</span></span><br><span class="line"><span class="keyword">SELECT</span> pg_where_guard_is_enabled();  <span class="comment">-- Returns: true</span></span><br></pre></td></tr></table></figure><h2 id="Usage-Examples"><a href="#Usage-Examples" class="headerlink" title="Usage Examples"></a>Usage Examples</h2><h3 id="Safe-Operations-Allowed"><a href="#Safe-Operations-Allowed" class="headerlink" title="Safe Operations (Allowed)"></a>Safe Operations (Allowed)</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Create a test table</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> employees (</span><br><span class="line">    id SERIAL <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">    name TEXT <span class="keyword">NOT NULL</span>,</span><br><span class="line">    department TEXT,</span><br><span class="line">    salary <span class="type">INTEGER</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- Insert test data</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> employees (name, department, salary) <span class="keyword">VALUES</span></span><br><span class="line">    (<span class="string">&#x27;Alice Johnson&#x27;</span>, <span class="string">&#x27;Engineering&#x27;</span>, <span class="number">75000</span>),</span><br><span class="line">    (<span class="string">&#x27;Bob Smith&#x27;</span>, <span class="string">&#x27;Marketing&#x27;</span>, <span class="number">65000</span>),</span><br><span class="line">    (<span class="string">&#x27;Charlie Brown&#x27;</span>, <span class="string">&#x27;Engineering&#x27;</span>, <span class="number">80000</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- These operations work fine (have WHERE clauses)</span></span><br><span class="line"><span class="keyword">UPDATE</span> employees <span class="keyword">SET</span> salary <span class="operator">=</span> <span class="number">78000</span> <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;Alice Johnson&#x27;</span>;</span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> employees <span class="keyword">WHERE</span> department <span class="operator">=</span> <span class="string">&#x27;Marketing&#x27;</span>;</span><br></pre></td></tr></table></figure><h3 id="Dangerous-Operations-Blocked"><a href="#Dangerous-Operations-Blocked" class="headerlink" title="Dangerous Operations (Blocked)"></a>Dangerous Operations (Blocked)</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- These commands will FAIL due to pg_where_guard protection:</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- This will fail: UPDATE without WHERE clause</span></span><br><span class="line"><span class="keyword">UPDATE</span> employees <span class="keyword">SET</span> salary <span class="operator">=</span> <span class="number">100000</span>;</span><br><span class="line"><span class="comment">-- ERROR: UPDATE requires a WHERE clause</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- This will fail: DELETE without WHERE clause</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> employees;</span><br><span class="line"><span class="comment">-- ERROR: DELETE requires a WHERE clause</span></span><br></pre></td></tr></table></figure><h3 id="Common-Table-Expression-Support"><a href="#Common-Table-Expression-Support" class="headerlink" title="Common Table Expression Support"></a>Common Table Expression Support</h3><p>The extension also protects CTEs:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- This would also be blocked</span></span><br><span class="line"><span class="keyword">WITH</span> department_update <span class="keyword">AS</span> (</span><br><span class="line">    <span class="keyword">UPDATE</span> employees <span class="keyword">SET</span> salary <span class="operator">=</span> salary <span class="operator">*</span> <span class="number">1.1</span>  <span class="comment">-- No WHERE clause!</span></span><br><span class="line">    RETURNING <span class="operator">*</span></span><br><span class="line">)</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> department_update;</span><br></pre></td></tr></table></figure><h2 id="Performance-Considerations"><a href="#Performance-Considerations" class="headerlink" title="Performance Considerations"></a>Performance Considerations</h2><h3 id="Minimal-Overhead"><a href="#Minimal-Overhead" class="headerlink" title="Minimal Overhead"></a>Minimal Overhead</h3><p>pg_where_guard adds minimal performance overhead because it:</p><ul><li>Only analyzes DELETE and UPDATE statements</li><li>Performs lightweight checks on the parsed query tree</li><li>Uses efficient Rust code with zero-cost abstractions</li><li>Operates at parse time, not execution time</li></ul><h3 id="Production-Readiness"><a href="#Production-Readiness" class="headerlink" title="Production Readiness"></a>Production Readiness</h3><p>The extension is designed for production use with:</p><ul><li>Comprehensive error handling</li><li>Memory-safe implementation</li><li>Minimal system resource usage</li><li>Support for PostgreSQL 12-16</li></ul><h2 id="Development-and-Testing"><a href="#Development-and-Testing" class="headerlink" title="Development and Testing"></a>Development and Testing</h2><h3 id="Project-Structure"><a href="#Project-Structure" class="headerlink" title="Project Structure"></a>Project Structure</h3><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">pg_where_guard/</span><br><span class="line">├── Cargo.toml              # Rust project <span class="keyword">configuration</span></span><br><span class="line">├── pg_where_guard.control  # PostgreSQL <span class="keyword">extension</span> control file</span><br><span class="line">├── src/</span><br><span class="line">│   ├── lib.rs             # Main <span class="keyword">extension</span> code</span><br><span class="line">│   └── bin/</span><br><span class="line">│       └── pgrx_embed.rs  # pgrx <span class="keyword">schema</span> generation</span><br><span class="line">├── <span class="keyword">sql</span>/                   # <span class="keyword">SQL</span> test scripts</span><br><span class="line">└── tests/                 # Test files</span><br></pre></td></tr></table></figure><h3 id="Running-Tests"><a href="#Running-Tests" class="headerlink" title="Running Tests"></a>Running Tests</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Run the test suite</span></span><br><span class="line">cargo pgrx <span class="built_in">test</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Test with specific PostgreSQL version</span></span><br><span class="line">cargo pgrx <span class="built_in">test</span> pg15</span><br></pre></td></tr></table></figure><h3 id="Development-Workflow"><a href="#Development-Workflow" class="headerlink" title="Development Workflow"></a>Development Workflow</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Start a development PostgreSQL instance</span></span><br><span class="line">cargo pgrx run</span><br><span class="line"></span><br><span class="line"><span class="comment"># Install the extension in development</span></span><br><span class="line">cargo pgrx install --debug</span><br></pre></td></tr></table></figure><h2 id="Benefits-of-the-Rust-pgrx-Approach"><a href="#Benefits-of-the-Rust-pgrx-Approach" class="headerlink" title="Benefits of the Rust + pgrx Approach"></a>Benefits of the Rust + pgrx Approach</h2><h3 id="Developer-Experience"><a href="#Developer-Experience" class="headerlink" title="Developer Experience"></a>Developer Experience</h3><ol><li><strong>Type Safety</strong>: Compile-time guarantees prevent runtime errors</li><li><strong>Modern Tooling</strong>: Cargo ecosystem and excellent IDE support</li><li><strong>Testing</strong>: Built-in unit testing and integration testing</li><li><strong>Documentation</strong>: Automatic documentation generation</li></ol><h3 id="Safety-Guarantees"><a href="#Safety-Guarantees" class="headerlink" title="Safety Guarantees"></a>Safety Guarantees</h3><ol><li><strong>Memory Safety</strong>: No buffer overflows or memory leaks</li><li><strong>Thread Safety</strong>: Rust’s ownership model prevents data races</li><li><strong>Error Handling</strong>: Explicit error handling with Result types</li><li><strong>Null Safety</strong>: No null pointer dereferences</li></ol><h3 id="Performance-Benefits"><a href="#Performance-Benefits" class="headerlink" title="Performance Benefits"></a>Performance Benefits</h3><ol><li><strong>Zero-Cost Abstractions</strong>: High-level code without runtime overhead</li><li><strong>Optimized Compilation</strong>: LLVM backend generates efficient machine code</li><li><strong>Minimal Dependencies</strong>: Small runtime footprint</li><li><strong>Efficient Resource Usage</strong>: Predictable memory usage patterns</li></ol><h2 id="Comparison-with-Traditional-C-Extensions"><a href="#Comparison-with-Traditional-C-Extensions" class="headerlink" title="Comparison with Traditional C Extensions"></a>Comparison with Traditional C Extensions</h2><table><thead><tr><th>Aspect</th><th>C Extension</th><th>Rust + pgrx Extension</th></tr></thead><tbody><tr><td>Memory Safety</td><td>Manual management</td><td>Automatic, compile-time guaranteed</td></tr><tr><td>Development Speed</td><td>Slow, error-prone</td><td>Fast, safe development</td></tr><tr><td>Debugging</td><td>GDB, complex</td><td>Standard Rust tooling</td></tr><tr><td>Testing</td><td>Manual, limited</td><td>Built-in unit&#x2F;integration tests</td></tr><tr><td>Maintenance</td><td>High overhead</td><td>Low overhead</td></tr><tr><td>Performance</td><td>Optimal</td><td>Near-optimal with safety</td></tr></tbody></table><h3 id="Integration-Possibilities"><a href="#Integration-Possibilities" class="headerlink" title="Integration Possibilities"></a>Integration Possibilities</h3><p>pg_where_guard can be integrated with:</p><ul><li><strong>Database Migration Tools</strong>: Validate migrations before execution</li><li><strong>ORM Frameworks</strong>: Add safety checks to generated queries</li><li><strong>Monitoring Systems</strong>: Alert on attempted dangerous operations</li><li><strong>Audit Systems</strong>: Log blocked operations for compliance</li></ul><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>pg_where_guard demonstrates the power of modern Rust tooling for PostgreSQL extension development. By combining Rust’s safety guarantees with pgrx’s ease of use, we can build robust database tools that protect against common but dangerous operations.</p><p>The extension serves as both a practical safety tool and an example of how Rust is revolutionizing systems programming beyond traditional applications. As the PostgreSQL ecosystem continues to evolve, Rust-based extensions like pg_where_guard pave the way for safer, more maintainable database tools.</p><h3 id="Key-Takeaways"><a href="#Key-Takeaways" class="headerlink" title="Key Takeaways"></a>Key Takeaways</h3><ol><li><strong>Safety First</strong>: Rust eliminates entire classes of bugs that plague C extensions</li><li><strong>Developer Productivity</strong>: pgrx makes PostgreSQL extension development accessible</li><li><strong>Performance</strong>: Memory safety doesn’t require sacrificing performance</li><li><strong>Future-Proof</strong>: Rust’s growing ecosystem ensures long-term maintainability</li></ol><p>Whether you’re looking to protect your database from accidental data loss or explore modern PostgreSQL extension development, pg_where_guard offers a compelling example of what’s possible with Rust and pgrx.</p><h3 id="Resources"><a href="#Resources" class="headerlink" title="Resources"></a>Resources</h3><ul><li><a target="_blank" rel="noopener" href="https://github.com/isdaniel/pg_where_guard">pg_where_guard GitHub Repository</a></li><li><a target="_blank" rel="noopener" href="https://github.com/pgcentralfoundation/pgrx">pgrx Framework Documentation</a></li><li><a target="_blank" rel="noopener" href="https://www.postgresql.org/docs/current/extend.html">PostgreSQL Extension Development Guide</a></li></ul><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/pg-where-guard-rust-postgresql-extension/">https://isdaniel.github.io/pg-where-guard-rust-postgresql-extension/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/pg-where-guard-rust-postgresql-extension/</id>
    <link href="https://isdaniel.github.io/pg-where-guard-rust-postgresql-extension/"/>
    <published>2025-01-02T23:20:00.000Z</published>
    <summary>pg_where_guard is a Rust/pgrx PostgreSQL extension that blocks unsafe UPDATE and DELETE without WHERE clauses, preventing accidental data loss</summary>
    <title>Building Safe PostgreSQL Extensions with Rust - Introducing pg_where_guard</title>
    <updated>2026-08-05T10:10:26.491Z</updated>
  </entry>
  <entry>
    <author>
      <name>Daniel Shih</name>
    </author>
    <category term="ssl" scheme="https://isdaniel.github.io/categories/ssl/"/>
    <category term="tls" scheme="https://isdaniel.github.io/categories/ssl/tls/"/>
    <category term="ssl" scheme="https://isdaniel.github.io/tags/ssl/"/>
    <category term="tls" scheme="https://isdaniel.github.io/tags/tls/"/>
    <content>
      <![CDATA[<h2 id="Foreword"><a href="#Foreword" class="headerlink" title="Foreword"></a>Foreword</h2><p>TLS (Transport Layer Security) is a cryptographic protocol that provides secure communication over a network, commonly used to secure HTTP traffic (i.e., HTTPS). Here’s a high-level overview of the TLS workflow, which includes handshake and data transfer phases.</p><p>After TCP handshake, it will execute TLS handshake if client require.</p><p>Below image is my experiment TLS (TLS 1.2) workflow from PostgreSQL server, Red-frame represent TCP 3 handshake, and yellow-frame represent TLS handshake.</p><p><img data-src="/images/tls-ssl/2024-11-11_15h17_11.png" alt="img"></p><p>In the beginning, client will send a request to require sslmode connection (SSL&#x2F;TLS), if server support it will reply (‘S’).</p><p><img data-src="/images/tls-ssl/2024-11-11_17h48_32.png" alt="img"></p><p>Eventually, processing below steps to do TLS handshake.</p><ol><li>ClientHello → 2. ServerHello → 3. Server Certificate → 4. ServerHelloDone → 5. Client Key Exchange</li></ol><h3 id="TLS-work-flow"><a href="#TLS-work-flow" class="headerlink" title="TLS work-flow"></a>TLS work-flow</h3><ol><li>Client Hello:<ul><li>The TLS version the client supports.</li><li>A list of cipher suites (encryption algorithms) it supports.</li><li>A random number (used later for generating encryption keys).</li></ul></li><li>Server Hello:<ul><li>The TLS version and cipher suite that it chose based on the client’s list.</li><li>A random number (used later for generating encryption keys).</li></ul></li><li>Server Certificate and Optional Server Key Exchange: The server sends its digital certificate to the client to prove its identity. This certificate includes the server’s public key and is typically signed by a trusted Certificate Authority (CA).<ul><li>Checking its expiration date.</li><li>Ensuring that the certificate is signed by a CA trusted by the client’s operating system or browser.</li></ul></li><li>Server Hello Done : The server sends a ServerHelloDone message, indicating it has finished its part of the handshake.</li><li>Client Key Exchange<ul><li>The client generates a pre-master secret (a random value used for encryption key generation) and encrypts it with the server’s public key (from the server’s certificate).</li><li>This encrypted pre-master secret is sent to the server. Only the server, with its private key, can decrypt this secret.<br><img data-src="/images/tls-ssl/2024-11-11_18h53_01.png" alt="img"></li></ul></li><li>Generating Session Keys: Both the client and server now have enough information (random numbers and the pre-master secret) to generate session keys.<ul><li>Symmetric encryption of the data sent over the connection.</li><li>Message integrity to ensure data is not tampered with.</li></ul></li></ol><p>Above items, I pointed out in below snapshot workflow.</p><p><img data-src="/images/tls-ssl/2024-11-11_18h47_35.png" alt="img"></p><h2 id="Certificate-File"><a href="#Certificate-File" class="headerlink" title="Certificate File"></a>Certificate File</h2><p>A certificate file is an important part within TLS workflow, that for doing Client Key Exchange, and client will encrypt data via public key (server side can decrypt by private key).</p><p>Certificate file contains several important pieces of information:</p><ul><li>Public Key: The certificate includes the public key of the entity being certified (e.g., a server or individual). This key can be used to encrypt data or verify signatures.</li><li>Subject Information: Identifying information about the certificate holder, such as:<ul><li>Common Name (CN) – often the domain name for websites.</li><li>Organization (O), Organizational Unit (OU).</li><li>Country (C).</li></ul></li><li>Issuer Information: Identifying information about the Certificate Authority (CA) that issued the certificate.</li><li>Validity Period: The start and end dates defining the period during which the certificate is valid.</li><li>Digital Signature: A cryptographic signature from the CA, which verifies the certificate’s authenticity.</li><li>Certificate Serial Number: A unique identifier for the certificate issued by the CA.</li></ul><h3 id="Certificate-Root-Certificate-file"><a href="#Certificate-Root-Certificate-file" class="headerlink" title="Certificate &amp; Root Certificate file"></a>Certificate &amp; Root Certificate file</h3><ul><li>Root Certificate file: A root certificate is the top-level certificate in a certificate chain and serves as the foundation of trust for all other certificates within the hierarchy.<ul><li>Lifespan: Root certificates are used to issue intermediate certificates, which in turn issue end-entity certificates. This hierarchy enhances security by limiting direct exposure of the root certificate.</li></ul></li><li>Certificate file: This type of certificate is used to verify the identity of an entity, like a website, individual, or organization, and is typically issued by an intermediate certificate, not directly by the root.<ul><li>Lifespan:They usually have shorter lifespans (often 1-2 years) to enhance security through periodic renewal and revocation if compromised.</li></ul></li></ul><p>Certificate file would be verified whether valid by Root certificate, and OS machine would install Root certificate file when we setup our machine.</p><blockquote><p>The root certificate is the trust anchor, while end-entity certificates rely on this anchor for trust. This hierarchy allows a scalable, secure infrastructure where trust flows from the root certificate down to the individual end-entity certificates.</p></blockquote><p><strong>此文作者</strong>：Daniel Shih(石頭)<br /><strong>此文地址</strong>： <a href="https://isdaniel.github.io/tls-ssl/">https://isdaniel.github.io/tls-ssl/</a> <br /><strong>版權聲明</strong>：本博客所有文章除特別聲明外，均採用 <a target="_blank" rel="noopener" href="https://creativecommons.org/licenses/by-nc-sa/3.0/tw/">CC BY-NC-SA 3.0 TW</a> 許可協議。轉載請註明出處！</p>]]>
    </content>
    <id>https://isdaniel.github.io/tls-ssl/</id>
    <link href="https://isdaniel.github.io/tls-ssl/"/>
    <published>2024-11-11T14:30:11.000Z</published>
    <summary>圖解 TLS/SSL 握手流程：從 ClientHello 到 Session Key 生成，完整解析 TLS 1.2 憑證驗證與加密建立過程</summary>
    <title>Understand TLS/SSL networking flow</title>
    <updated>2026-08-05T10:10:26.495Z</updated>
  </entry>
</feed>
